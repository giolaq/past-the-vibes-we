// GENERATED by scripts/build-site.mjs from workshop/lessons/*.md — do not edit by hand.
window.WORKSHOP_MODULES = [{"id":"welcome","number":"00","nav":"Start here","time":"20 minutes","title":"Set up the workshop and understand the runtime","lead":"Complete this page before lesson 1. Stop troubleshooting after 10 minutes and use replay. A live model or Vega device must never block the workshop.","objective":"Choose a reliable workshop path and explain where Strands, ADBT, the harness, and Git each fit.","evidence":"A successful replay run, one chosen execution path, and a completed readiness checklist.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Read this first if you have never built an agent harness</h2><p>You are a React Native developer. You may have never touched an \"agent,\" an \"LLM tool,\" or \"MCP.\" That's fine — nothing here assumes you have. We will port a small RN app to Vega (Amazon's TV OS) without doing it by hand and without just asking an AI to \"please port my app.\" Instead we build a <strong>harness</strong> with AWS Strands Agents SDK: a plain TypeScript program that runs a fixed pipeline, lets an AI model <em>propose</em> code inside tight walls, and keeps every dangerous action — writing files, running checks, committing to Git, spending money, talking to the device — for itself. That structure is why you're here rather than just prompting a coding agent: the harness gives you control (your code owns every write, check, and dollar) and observability (every model turn, document read, cost, and commit is recorded). And it's reusable — swap the skills, the MCP server, or the executor, and the same pipeline works on your own project.</p></section><aside class=\"note \"><strong>The one sentence to remember</strong><br>The model is a contractor with read-only access. The harness is the foreman: it checks the work and decides what gets in.</aside><h2>Vocabulary, translated for a React Native dev</h2>\n      <p>You already know these ideas under other names. Here's the dictionary; the rest of the workshop uses these words.</p><table><thead><tr><th>Term</th><th>What it actually is</th><th>Your mental model</th></tr></thead><tbody><tr><td><strong>LLM / model</strong></td><td>A program that turns text in into text out. Given a prompt, it returns a guess. Claude is one.</td><td>A well-read intern who writes plausible code but never runs it.</td></tr><tr><td><strong>Prompt</strong></td><td>The text you send the model.</td><td>The Jira ticket plus all the context you paste in.</td></tr><tr><td><strong>Agent</strong></td><td>A model wired to a loop where it can call functions (\"tools\"), see the result, and decide what to do next.</td><td>An intern who can grep your repo before answering, several times.</td></tr><tr><td><strong>Tool</strong></td><td>A named function you expose to the model, with a typed signature. The model <em>requests</em> a call; your code runs it and returns the result.</td><td>A locked-down CLI you hand the intern: <code>read_file</code>, <code>list_files</code>, nothing else.</td></tr><tr><td><strong>Structured output</strong></td><td>Forcing the model to answer as JSON matching a schema, not free prose.</td><td>A required PR template the intern cannot deviate from.</td></tr><tr><td><strong>MCP</strong></td><td><a href=\"https://modelcontextprotocol.io\" target=\"_blank\" rel=\"noopener\">Model Context Protocol</a> — a standard so one program can start another as a \"server\" and call its tools.</td><td>Starting a language server (LSP) and querying it, but for arbitrary tools.</td></tr><tr><td><strong>ADBT</strong></td><td><a href=\"https://www.npmjs.com/package/@amazon-devices/amazon-devices-buildertools-mcp\" target=\"_blank\" rel=\"noopener\">Amazon Devices Builder Tools</a>, exposed here as an MCP server serving Vega migration docs.</td><td>An internal wiki you can query programmatically.</td></tr><tr><td><strong>Skill</strong></td><td>A block of domain instructions (\"how to do TV focus\") kept separate from code.</td><td>A runbook you paste into the ticket for one task.</td></tr><tr><td><strong>Harness</strong></td><td>The deterministic TypeScript program orchestrating all of the above.</td><td>Your CI pipeline, if CI could also call an intern mid-step.</td></tr><tr><td><strong>Replay</strong></td><td>Running the pipeline against <em>recorded</em> model answers instead of a live model.</td><td>Fixtures / VCR cassettes for network calls.</td></tr><tr><td><strong>VDA</strong></td><td>Vega Virtual Device — an emulator for the TV OS.</td><td>Android emulator, but for Vega.</td></tr></tbody></table><aside class=\"note \"><strong>The single most important idea</strong><br>An LLM generates <em>plausible</em> text, and plausible is not the same as correct: <code>plausible &ne; verified</code>. Everything the harness does closes that gap. Every phase ends in a mechanical check, not a vibe.</aside><h2>Who is allowed to do what</h2>\n      <p>This is the security model:</p><div class=\"flow\"><div><b>ADBT MCP</b><span>Approved Vega context</span></div><i>→</i><div><b>Strands</b><span>Read and propose</span></div><i>→</i><div><b>Harness</b><span>Write and check</span></div><i>→</i><div><b>Git</b><span>Commit evidence</span></div></div><figure class=\"snippet\"><figcaption>The boundary, in one diagram</figcaption><pre><code>ADBT (MCP server) &lt;--- model calls list_documents / read_document itself ---+\n                                                                            |\nguarded app copy  &lt;--- read-only tools (list/read/search) -----------------+--&gt; Strands Agent --&gt; typed patch {summary, files}\n                                                                            |\n                                                                            v\n     HARNESS: reconstruct + hash ADBT reads -&gt; validate paths -&gt; write files -&gt; run checks -&gt; retry -&gt; git commit -&gt; report</code></pre><p class=\"look\"><strong>Where to look:</strong> The model (via Strands) can <em>list, read, and search</em> the guarded app, and the harness hands the whole ADBT <code>McpClient</code> to the agent so Strands discovers the ADBT tools dynamically and the model calls them itself. It still has no write tool and no shell. Afterward the harness reconstructs which ADBT docs it read and hashes them. Everything with consequences stays in <code>packages/workshop-harness/src/port-pipeline.ts</code>.</p></figure><p>The strictness has a reason: a model with a write tool or a shell can corrupt your repo on one confident wrong guess. Keep irreversible actions in deterministic code, and the worst a bad answer can do is <em>fail a check and get rejected</em>.</p><aside class=\"note \"><strong>What is Strands Agents SDK?</strong><br><a href=\"https://github.com/strands-agents/harness-sdk\" target=\"_blank\" rel=\"noopener\">Strands</a> is AWS's open-source agent runtime (TypeScript and Python), used here as the live remote path. Both the complete workshop harness and the staged mini-harness pin 1.10.0. It fits this workshop for two reasons: the plumbing (provider adapters, typed tools, structured output, limits, cancellation, usage metrics) is built in, and it's a library, not a framework — it doesn't try to own writes or orchestration, so the harness boundary stays where we put it.</aside><table><thead><tr><th>Strands supplies</th><th>The harness owns</th></tr></thead><tbody><tr><td>Agent loop and model providers</td><td>Phase order and approval</td></tr><tr><td>Read-only typed tools</td><td>Protected file writes</td></tr><tr><td>Validated patch output</td><td>Checks, retry, and Git commits</td></tr><tr><td>MCP client and metrics</td><td>Cost cap, replay, and report</td></tr></tbody></table><p>The port agent can list, read, and search the guarded app. During <code>analyze</code> and <code>plan</code> the harness also hands it the ADBT <code>McpClient</code>, and Strands discovers ADBT's own tools (<code>list_documents</code>, <code>read_document</code>, <code>search_documentation</code>) dynamically — the harness does not pre-pick which workflows to read; the model does. It still gets no shell or write tool. After the phase, the harness walks the agent's tool calls and records each ADBT read with a SHA-256 hash, so the run stays auditable. Replay uses recorded model turns and recorded ADBT context, so it needs no live model or MCP server.</p><h2>1. Check the basics</h2><ol class=\"tasks\"><li>Install Node.js 20 or newer and Git. <a href=\"https://nodejs.org/api/corepack.html\" target=\"_blank\" rel=\"noopener\">Corepack</a> supplies Yarn 4.12.</li><li>Clone this repository, run <code>cd past-the-vibes-we</code>, and keep the terminal at that root.</li><li>Choose Pocket Cinema unless your own React Native app already runs.</li></ol><div class=\"grid\"><article><h3>Pocket Cinema</h3><p>Recommended. Every exercise, recording, and checkpoint supports this app.</p><code>apps/pocket-cinema</code></article><article><h3>Your app</h3><p>Use one working flow: launch → screen → action → back. Switch to Pocket Cinema if discovery takes more than 10 minutes.</p><code>launch → screen → action → back</code></article></div><h3>Bring-your-own-app safety check</h3><div class=\"checklist\"><label><input type=\"checkbox\">The app runs before the workshop</label><label><input type=\"checkbox\">Git status is clean</label><label><input type=\"checkbox\">It contains no production secrets or private data</label><label><input type=\"checkbox\">It contains no protected media</label><label><input type=\"checkbox\">It can be shared with the chosen model provider</label></div><h2>2. Install the workshop workspace</h2><div class=\"command\"><header><span>Install all workshop packages</span><button class=\"copy\">Copy command</button></header><pre><code>unset NODE_TLS_REJECT_UNAUTHORIZED\ncorepack enable\nyarn setup</code></pre></div><h2>3. Run the setup check</h2><div class=\"command\"><header><span>Check the key-free replay path</span><button class=\"copy\">Copy command</button></header><pre><code>yarn doctor</code></pre></div><p>You are ready when the command reports <code>state: ready</code>. Model and Vega checks are optional in replay mode.</p><div class=\"command\"><header><span>Optional: check live ADBT with everything else replayed</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --adbt-live --json</code></pre></div><h2>4. Choose one execution path</h2><table><thead><tr><th>Path</th><th>Use it when</th><th>Needs</th></tr></thead><tbody><tr><td>Your CLI coding agent</td><td>You already run <a href=\"https://code.claude.com/docs\" target=\"_blank\" rel=\"noopener\">Claude Code</a>, Codex, or a similar CLI agent. The workshop ships a Claude Code executor; the <code>Executor</code> interface in <code>port-executor.ts</code> is where another CLI plugs in.</td><td>That agent installed and authenticated; ADBT via init-context</td></tr><tr><td>Strands + Bedrock</td><td>Run the model in-process through the SDK the harness is built with.</td><td>AWS credentials and <a href=\"https://docs.aws.amazon.com/bedrock/\" target=\"_blank\" rel=\"noopener\">Bedrock</a> model access</td></tr><tr><td>Replay</td><td>Fallback when a live model, ADBT, or device is unavailable.</td><td>Nothing beyond the installed packages</td></tr></tbody></table><p>Both live paths run the same harness — pick whichever you can authenticate today. The recorded replay path stays available as a fallback in every lesson. Confirm your live path is ready:</p><div class=\"command\"><header><span>Claude Code: check the local executor</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --executor claude-cli --json</code></pre></div><div class=\"command\"><header><span>Strands: check Bedrock credentials</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --executor strands --provider bedrock --json</code></pre></div><div class=\"command\"><header><span>Fallback: check the key-free replay path</span><button class=\"copy\">Copy command</button></header><pre><code>yarn doctor</code></pre></div><aside class=\"note \"><strong>Pick one live executor</strong><br>Choose your CLI agent or Strands + Bedrock as your primary path — you don't need both. If your chosen live path fails mid-workshop, save the error and use the replay fallback shown in that lesson.</aside><h2>5. Optional Vega and VDA setup</h2>\n      <p>Skip this section when you plan to use lifecycle replay. For the live path, install the <a href=\"https://developer.amazon.com/docs/vega/0.22/install-vega-sdk.html\" target=\"_blank\" rel=\"noopener\">Vega SDK</a> <code>0.22.5875</code> and create a Vega Virtual Device.</p>\n      <p>How ADBT reaches the model depends on your executor:</p>\n      <ul>\n        <li><strong>Replay (default)</strong>: recorded context, no <code>init-context</code>, no install.</li>\n        <li><strong>Strands</strong>: the harness owns the ADBT <code>McpClient</code> and hands it to the agent so the model calls ADBT's tools itself. Still run <code>init-context</code> once — lesson 4's live runs load the <code>amazon-devices-vega-*</code> skills it installs.</li>\n        <li><strong>Claude Code CLI</strong>: the CLI has its own MCP client, so the same <code>init-context</code> run registers the ADBT server with it and installs the skills. Run this in a real system terminal, then reopen your agent:</li>\n      </ul><div class=\"command\"><header><span>Set up ADBT MCP for the Claude Code CLI (one time)</span><button class=\"copy\">Copy command</button></header><pre><code># Run in a system terminal (not inside the agent). Sets up the ADBT MCP server\n# and installs the amazon-devices-vega-* skills that lesson 4 loads.\n# --force skips the confirmation prompts.\nnpx -y @amazon-devices/amazon-devices-buildertools-mcp@latest init-context --agent claude-code-cli --force</code></pre></div><div class=\"command\"><header><span>Verify the ADBT MCP setup</span><button class=\"copy\">Copy command</button></header><pre><code>npx -y @amazon-devices/amazon-devices-buildertools-mcp@latest check-status --agent claude-code-cli</code></pre></div><aside class=\"note \"><strong>Where these come from</strong><br>Amazon Devices Builder Tools ships the ADBT MCP server, skills, and steering docs as one pinned npm package: over 400 current Vega documents — migration workflows, knowledge-base pages, prompts — plus ten agent skills, served from a local process. <code>init-context</code> writes the MCP config into your agent (it supports Claude Code, Cursor, Kiro, Cline, and Copilot); <code>check-status</code> confirms it. With it, the model reads the vendor's current guidance instead of guessing Vega APIs from memory. See the <a href=\"https://developer.amazon.com/docs/vega/0.22/mcp-server.html\" target=\"_blank\" rel=\"noopener\">Vega ADBT setup docs</a>.</aside><p>When the harness runs the CLI executor, it invokes it with <code>--allowedTools \"*\"</code> so whatever ADBT tools <code>init-context</code> configured are permitted without stalling on a permission prompt in non-interactive mode. The CLI runs against the guarded copy, and the harness applies only the returned typed patch.</p><div class=\"command\"><header><span>Check ADBT and start VDA in a system terminal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --adbt-live --json\nvega --version\nvega virtual-device start --gui</code></pre></div><p>Keep that terminal open. In a second system terminal, run:</p><div class=\"command\"><header><span>Confirm the SDK and attached device</span><button class=\"copy\">Copy command</button></header><pre><code># Run this in a second system terminal.\nvega --version\nvega virtual-device status\nvega exec vda devices -l</code></pre></div><aside class=\"note success\"><strong>Live Vega is ready only when</strong><br>The SDK prints 0.22.5875, virtual-device status reports running: true, and devices -l lists an attached device.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Try one repair for no more than 10 minutes. Then use replay. Do not spend workshop time repairing a model account or device.</aside><h2>Setup complete</h2><div class=\"checklist\"><label><input type=\"checkbox\">Node 20+, Git, and Corepack are available</label><label><input type=\"checkbox\">All three workspace packages are installed</label><label><input type=\"checkbox\">One replay run completed</label><label><input type=\"checkbox\">I chose replay, Claude Code, or Strands</label><label><input type=\"checkbox\">I chose Pocket Cinema or checked my own app</label><label><input type=\"checkbox\">I understand that Vega device work is optional</label><label><input type=\"checkbox\">I can explain what Strands supplies and what the harness owns</label></div><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the most important boundary in this workshop?</summary><p>The model can inspect and propose, but the harness controls approval, protected writes, verification, retries, cost, commits, and reports. ADBT supplies selected platform knowledge; it does not take over the run.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>The workspace is installed, one replay succeeds, one execution path is chosen, and you know which app you will use.</aside>"},{"id":"single-agent","number":"01","nav":"One model call","time":"15 minutes","title":"Start with one model call","lead":"Run the smallest example against a React Native app and identify what it cannot prove.","objective":"Locate the model boundary and distinguish generated output from verified output.","evidence":"Three concrete claims that the one-call script cannot prove by itself.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Why this step exists</h2><p>A model can produce plausible files, but plausibility is not evidence. Start with the smallest possible agent so its missing guarantees are easy to see.</p></section><aside class=\"note \"><strong>One app from the first minute</strong><br>Every mini-harness step begins with a reduced Pocket Cinema React Native app and runs the same three phases: analyze → plan → build_test. The later port changes platform concerns, but phase → skill → executor → check stays the same.</aside><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>Before you run it, name one bug that could hide inside the model's analysis that looks complete.</p></aside><h2>Run it against a live model</h2><p>Pick the executor you set up in lesson 0. It calls a real model and writes files into <code>out/</code>.</p><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI\nyarn --cwd packages/mini-harness tsx steps/01-single-agent/index.ts run \\\n  steps/01-single-agent/fixtures/phases.json \\\n  --executor claude-cli --model sonnet</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock\nyarn --cwd packages/mini-harness tsx steps/01-single-agent/index.ts run \\\n  steps/01-single-agent/fixtures/phases.json \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2</code></pre></div><h2>Trace the model boundary</h2><ol class=\"tasks\"><li>Open <code>steps/01-single-agent/index.ts</code>.</li><li>Find where it copies the starter app, builds the prompt, reads the model response, and writes files.</li><li>Open the generated <code>out/ANALYSIS.md</code> (the analyze phase output).</li><li>Write down three claims that need an independent check: the analysis is accurate, a component it named really exists, a part it called portable actually runs on TV.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is this an agent script, but not yet a reliable harness?</summary><p>It has a model call and side effects, but no independent verification, bounded retry, checkpoint, approval gate, or durable evidence.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>You can point to the model boundary and name three missing React Native checks.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>If the live model is blocked, run the committed recording instead — same exercise, no account:</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback if the live model is blocked\nyarn --cwd packages/mini-harness tsx steps/01-single-agent/index.ts run \\\n  steps/01-single-agent/fixtures/phases.json \\\n  --replay steps/01-single-agent/fixtures/demo-recording.json</code></pre></div>"},{"id":"verify","number":"02","nav":"Check and retry","time":"20 minutes","title":"Turn a failure into a useful retry","lead":"Run a mechanical check and send its exact failure into one bounded retry.","objective":"Trace a requirement through a failed check, a contextual retry, and a passing result.","evidence":"The failed grep message appears in the retry request and the second attempt passes.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Why this step exists</h2><p>A retry is useful only when it carries new information. The check turns a vague failure into precise context the next attempt can act on.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The plan phase must document the remote control. Predict what the check greps for, and what happens if the model's first plan omits it.</p></aside><h2>Run it against a live model</h2><p>A live model may pass every check on the first try. To see the retry loop deterministically, use the committed recording — its first plan attempt is missing the required section, so the harness feeds the exact failure back and the second attempt fixes it.</p><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI\nyarn --cwd packages/mini-harness tsx steps/02-verify-loop/index.ts run \\\n  steps/02-verify-loop/fixtures/phases.json \\\n  --executor claude-cli --model sonnet</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock\nyarn --cwd packages/mini-harness tsx steps/02-verify-loop/index.ts run \\\n  steps/02-verify-loop/fixtures/phases.json \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2</code></pre></div><h2>See the retry loop (recording)</h2><div class=\"command\"><header><span>Replay the failed check and repair</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback: the committed recording forces the check to fail, then pass\nyarn --cwd packages/mini-harness tsx steps/02-verify-loop/index.ts run \\\n  steps/02-verify-loop/fixtures/phases.json \\\n  --replay steps/02-verify-loop/fixtures/retry-recording.json</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>Pattern \"## Remote Control\" not found in out/TV_PORT_PLAN.md</code></pre></div><h2>Follow the evidence</h2><ol class=\"tasks\"><li>Find the failed <code>grep</code> check in the output.</li><li>Open <code>steps/02-verify-loop/verify.ts</code> and locate that check.</li><li>Find the same failure text in the retry request.</li><li>Open <code>out/TV_PORT_PLAN.md</code> and confirm the second attempt adds the remote control section.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why pass the exact failure into the retry instead of saying try again?</summary><p>The exact failure narrows the problem, preserves the original requirement, and makes the retry explainable. A generic retry buys another guess.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>You can trace requirement → failed check → retry → passing output.</aside>"},{"id":"phases","number":"03","nav":"Phases and resume","time":"25 minutes","title":"Split the work and resume it","lead":"Use phases for small changes, commits for verified code, and checkpoints for run progress.","objective":"Explain the different jobs of a phase, a checkpoint, and a Git commit.","evidence":"A paused run resumes at focus, while the Git log keeps one commit per completed phase.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Why this step exists</h2><p>Long agent runs fail. Small phases limit the damage, checkpoints remember orchestration progress, and commits preserve code that already passed its checks.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The run stops after plan. Which phase should resume next, and which phases must not run again?</p></aside><h2>Run it against a live model, pausing after plan</h2><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI. Stop after the plan phase to inspect the checkpoint.\nyarn --cwd packages/mini-harness tsx steps/03-phases/index.ts run \\\n  steps/03-phases/fixtures/phases.json \\\n  --executor claude-cli --model sonnet \\\n  --stop-after plan</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock\nyarn --cwd packages/mini-harness tsx steps/03-phases/index.ts run \\\n  steps/03-phases/fixtures/phases.json \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2 \\\n  --stop-after plan</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>Paused after plan.\ncheckpoint.json: { \"nextPhase\": 2 }</code></pre></div><h2>Inspect before resuming</h2><ol class=\"tasks\"><li>Open <code>out/checkpoint.json</code>.</li><li>Use <code>phases.json</code> to confirm index 2 is <code>build_test</code>.</li><li>Open the Git log and find commits for analyze and plan.</li></ol><div class=\"command\"><header><span>Resume the same run</span><button class=\"copy\">Copy command</button></header><pre><code># Resume the same run; only build_test runs.\nyarn --cwd packages/mini-harness tsx steps/03-phases/index.ts run \\\n  steps/03-phases/fixtures/phases.json \\\n  --executor claude-cli --model sonnet \\\n  --resume</code></pre></div><h2>Compare the result</h2><ol class=\"tasks\"><li>Confirm only build_test ran after resume.</li><li>Open the final checkpoint and Git log.</li><li>Explain what progress belongs in the checkpoint and what evidence belongs in Git.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why keep both a checkpoint and per-phase commits?</summary><p>The checkpoint tells the engine where to continue. Git records the exact verified code state for each completed phase. They answer different recovery questions.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>After resume, only build_test runs — analyze and plan are not repeated.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>If the live model is blocked, use the recording (then resume with --replay ... --resume):</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback, then resume with --replay ... --resume\nyarn --cwd packages/mini-harness tsx steps/03-phases/index.ts run \\\n  steps/03-phases/fixtures/phases.json \\\n  --replay steps/03-phases/fixtures/demo-recording.json \\\n  --stop-after plan</code></pre></div>"},{"id":"skills","number":"04","nav":"Skills and executors","time":"20 minutes","title":"Separate knowledge from model access","lead":"A skill supplies domain instructions. An executor calls the model. The pipeline should not depend on one provider.","objective":"Separate domain knowledge, model execution, tools, and deterministic pipeline control.","evidence":"You can point to the file that owns each responsibility in both the mini and complete workshop harnesses.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Four responsibilities</h2><p>Skills teach domain knowledge. Phase context assembles the task. An executor talks to a model. Tools expose narrow capabilities. The pipeline decides when side effects are allowed.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>Where should a D-pad focus rule live: the executor, a skill, a read tool, or a verification check?</p></aside><h2>Run it against a live model</h2><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI\nyarn --cwd packages/mini-harness tsx steps/04-skills/index.ts run \\\n  steps/04-skills/fixtures/phases.json \\\n  --executor claude-cli --model sonnet</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock\nyarn --cwd packages/mini-harness tsx steps/04-skills/index.ts run \\\n  steps/04-skills/fixtures/phases.json \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 \\\n  --region us-west-2</code></pre></div><h2>Map the teaching harness</h2><ol class=\"tasks\"><li>Open <code>phases.json</code> and find the ADBT skill each phase names: <code>amazon-devices-vega-best-practices</code> (analyze), <code>amazon-devices-vega-focus-management</code> (plan), <code>amazon-devices-vega-build-and-run</code> (build_test).</li><li>Open one of them in <code>~/.claude/skills</code> — a folder with a <code>SKILL.md</code>, written and versioned by Amazon, installed by lesson 0's <code>init-context</code>.</li><li>Follow the names through <code>skills.ts</code>, <code>pipeline-engine.ts</code>, and <code>executor.ts</code>.</li><li>In <code>model-runtime.ts</code>, compare <code>injectSkillText()</code> with <code>createSkillsPlugin()</code>.</li><li>Compare every module with <code>packages/mini-harness/ISOMORPHISM.md</code>.</li></ol><aside class=\"note \"><strong>The skills are Amazon's, not ours</strong><br>ADBT ships nine <code>amazon-devices-vega-*</code> skills — manifest configuration, focus management, navigation, media playback, performance, build-and-run, and more — versioned and updated by Amazon. The harness consumes three of them without owning their content: real TV domain expertise reaches the agent without you writing or maintaining a word of it, and swapping a phase's expertise is a one-line change in <code>phases.json</code>. The executor is the same kind of swap point: the workshop ships a Claude Code executor, and the <code>Executor</code> interface is where another CLI agent would plug in. If they are not installed, <code>workshop/fixtures/adbt-skills.json</code> records their names, hashes, and excerpts.</aside><h2>One skill, two delivery paths</h2>\n    <table><thead><tr><th>Executor</th><th>How the selected skill arrives</th></tr></thead><tbody><tr><td>Claude CLI</td><td><code>injectSkillText()</code> appends the complete skill body to the subprocess prompt.</td></tr><tr><td>Strands</td><td><code>Skill</code> objects enter an <code>AgentSkills</code> plugin. Metadata appears first; the agent activates instructions with the <code>skills</code> tool.</td></tr><tr><td>Replay</td><td>No model runs. A recorded response replaces the live executor while the same phase plan remains visible.</td></tr></tbody></table>\n    <aside class=\"note \"><strong>Why Strands gets three turns</strong><br>With a selected skill, the mini agent needs room to discover the skill, activate it, and return JSON. Without skills it keeps the one-turn limit.</aside><h2>The whole model interaction, in one code block</h2>\n      <p>In <code>src/port-executor.ts</code>, the entire live model interaction for one phase is essentially this (the <code>systemPrompt</code> is shortened here — the real one also tells the agent to use the ADBT tools):</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-executor.ts (simplified)</figcaption><pre><code>const agent = new Agent({\n  name: `workshop-${phase}`,\n  model: createModel(config),                 // Bedrock / OpenAI / OpenRouter behind one interface\n  tools: createProjectReadTools(appDir),      // list/read/search only — no write, no shell\n  structuredOutputSchema: PortOutputSchema,   // must return { summary, files }\n  systemPrompt: \"Inspect with read-only tools. Return a complete patch. Never claim a file or API exists without reading evidence.\",\n  printer: false,                             // keep stdout clean for JSON\n});\nconst result = await agent.invoke(prompt, {\n  cancelSignal: AbortSignal.timeout(10 * 60_000),  // 10-min hard stop\n  limits: { turns: 8, totalTokens: 40_000 },       // bounded loop\n});</code></pre><p class=\"look\"><strong>Where to look:</strong> Strands supplies the model-and-tool loop, provider adapters, schema-validated output, turn/token limits, cancellation, and usage metrics. Writing files, verification, Git, cost policy, and ADBT selection stay in the harness.</p></figure><aside class=\"note \"><strong>The tools themselves are guarded hard</strong><br>In <code>src/port-tools.ts</code>, the read tools reject absolute paths, <code>..</code> traversal, symlinks, <code>.git</code>, <code>.env</code>, <code>node_modules</code>, binaries, and files over 100&nbsp;KB. Even the read side of the model's authority has walls.</aside><h2>Strands constructs used here</h2>\n    <p>Read this table from setup to result. These are the Strands APIs the workshop uses — and everything in it is SDK-supplied: one dependency provides the loop, providers, validated tools, enforced schemas, limits, and metrics, while writes, checks, retries, cost, and commits stay in workshop code.</p>\n    <table><thead><tr><th>Construct</th><th>What it does here</th><th>Where to find it</th></tr></thead><tbody><tr><td><code>Agent</code></td><td>Runs the model-and-tool loop for one phase. <code>name</code> and <code>description</code> identify its job.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>Model</code>, <code>BedrockModel</code>, <code>OpenAIModel</code></td><td>Hide provider-specific model calls. OpenRouter uses <code>OpenAIModel</code> with an OpenAI-compatible base URL.</td><td><code>model-factory.ts</code></td></tr><tr><td><code>systemPrompt</code></td><td>Sets durable operating rules: inspect first, use read-only evidence, and return a complete patch.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>tool()</code></td><td>Turns a named callback into a model-callable capability. The description tells the model when to use it.</td><td><code>port-tools.ts</code></td></tr><tr><td><code>inputSchema</code></td><td>Uses Zod to validate tool arguments and type the callback input before project code runs.</td><td><code>port-tools.ts</code></td></tr><tr><td><code>tools</code></td><td>Registers only list, read, and literal search. No write or shell capability enters the agent loop.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>Skill</code></td><td>Represents one selected mini-harness instruction with a name, description, and full body.</td><td><code>model-runtime.ts</code></td></tr><tr><td><code>AgentSkills</code></td><td>Adds progressive skill disclosure and the model-callable <code>skills</code> activation tool.</td><td><code>model-runtime.ts</code></td></tr><tr><td><code>plugins</code></td><td>Registers <code>AgentSkills</code> on the Strands agent. Claude CLI does not use this field.</td><td><code>model-runtime.ts</code></td></tr><tr><td><code>structuredOutputSchema</code></td><td>Requires the final answer to match <code>{ summary, files }</code>. Strands validates it and can feed schema failures back to the model.</td><td><code>port-contract.ts</code></td></tr><tr><td><code>printer: false</code></td><td>Disables Strands' automatic console renderer so the CLI keeps stdout reserved for versioned JSON events.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>agent.invoke()</code></td><td>Starts one bounded run with the assembled phase prompt.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>limits.turns</code> / <code>limits.totalTokens</code></td><td>Bound the loop. The mini-harness allows three turns with skills and one without; the port allows 8 turns and 40,000 tokens.</td><td><code>port-executor.ts</code>, <code>model-runtime.ts</code></td></tr><tr><td><code>cancelSignal</code></td><td>Lets a native ten-minute <code>AbortSignal</code> cancel the invocation at Strands cancellation points.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>AgentResult</code></td><td>Carries validated <code>structuredOutput</code>, messages, stop state, and metrics after invocation.</td><td>returned by <code>invoke()</code></td></tr><tr><td><code>StructuredOutputError</code></td><td>Makes a missing structured patch an explicit executor failure.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>metrics.accumulatedUsage</code></td><td>Reports input and output tokens. The harness records them and applies its own cost rates.</td><td><code>port-executor.ts</code></td></tr></tbody></table>\n    <aside class=\"note \"><strong>Keep the boundary visible</strong><br>Zod supplies schemas, native <code>AbortSignal</code> supplies cancellation, and the harness supplies cost policy and verification. Strands consumes those inputs and runs the bounded agent loop.</aside><h2>Why the workshop uses invoke</h2>\n    <p>The workshop uses <code>agent.invoke()</code> so one phase has one visible request, one typed result, and one metrics object. A larger CLI can use <code>agent.stream()</code> and <code>AgentStreamEvent</code> for progress without changing the pipeline boundary.</p>\n    <aside class=\"note \"><strong>Features still outside the design</strong><br>The repository does not use Strands hooks, Graph, Swarm, agent-as-tool, SDK session or memory managers, custom conversation managers, or SDK-provided write and shell tools.</aside><h2>Inspect the complete Strands boundary</h2><ol class=\"tasks\"><li>Open <code>packages/workshop-harness/src/port-tools.ts</code> and match each <code>tool()</code> field to the first table.</li><li>Open <code>port-contract.ts</code> and find the Zod schema passed as <code>structuredOutputSchema</code>.</li><li>Open <code>port-executor.ts</code> and trace <code>new Agent()</code> → <code>invoke()</code> → <code>AgentResult</code>.</li><li>Follow the result into usage accounting and <code>port-recorder.ts</code>.</li><li>Confirm the workshop port agent has no write or shell tool. Its pipeline owns both.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why use AgentSkills with Strands but prompt injection with Claude CLI?</summary><p>Strands can expose skill metadata and let the agent progressively activate instructions through a plugin. The CLI subprocess has no shared in-process plugin, so the executor sends the selected instructions directly in its prompt.</p></details></section><div class=\"links\"><a href=\"strands-constructs.md\">Open the Strands reference</a></div><aside class=\"note success\"><strong>You are done when</strong><br>You can trace one React Native phase skill through Claude prompt injection or Strands AgentSkills, then separate both from pipeline controls.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>If the live model is blocked, replay shows the same module boundaries without credentials:</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback if the live model is blocked\nyarn --cwd packages/mini-harness tsx steps/04-skills/index.ts run \\\n  steps/04-skills/fixtures/phases.json \\\n  --replay steps/04-skills/fixtures/demo-recording.json</code></pre></div>"},{"id":"memory","number":"05","nav":"Project memory","time":"15 minutes","title":"Review facts before saving them","lead":"Use a disposable input copy. Proposed context becomes project memory only after you review it.","objective":"Turn selected source material into small, reviewable project facts with provenance.","evidence":"The saved PROJECT_CONTEXT.md separates decisions, facts, questions, and source ids.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Why this step exists</h2><p>Project context should stay small, and every entry needs a source and a human sign-off. A raw transcript has none of that: it's noisy, private, and easy for an agent to misread as instruction.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>What could go wrong if the harness imported every remembered sentence automatically?</p></aside><div class=\"command\"><header><span>Create a disposable input copy</span><button class=\"copy\">Copy command</button></header><pre><code>WORKSHOP_INPUTS=\"/tmp/past-the-vibes-pocket-cinema-inputs\"\nrm -rf \"$WORKSHOP_INPUTS\"\ncp -R workshop/fixtures/pocket-cinema-inputs \\\n  \"$WORKSHOP_INPUTS\"</code></pre></div><div class=\"command\"><header><span>Show the current project memory (empty at first)</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts memory show /tmp/past-the-vibes-pocket-cinema-inputs --json</code></pre></div><div class=\"command\"><header><span>Create a memory proposal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts memory propose /tmp/past-the-vibes-pocket-cinema-inputs \\\n  --from ../../workshop/fixtures/bee-context/snapshot.json \\\n  --json</code></pre></div><h2>Review before applying</h2><ol class=\"tasks\"><li>Read every proposed fact.</li><li>Check that each fact names its source.</li><li>Keep open questions separate from decisions.</li><li>Reject anything private, temporary, or ambiguous.</li></ol><div class=\"command\"><header><span>Apply the reviewed proposal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts memory apply /tmp/past-the-vibes-pocket-cinema-inputs \\\n  --from ../../workshop/fixtures/bee-context/snapshot.json \\\n  --yes --json</code></pre></div><h2>Inspect the saved context</h2><ol class=\"tasks\"><li>Open <code>/tmp/past-the-vibes-pocket-cinema-inputs/PROJECT_CONTEXT.md</code>.</li><li>Confirm the source ids survived the transformation.</li><li>Confirm the committed fixture in the repository is unchanged.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is the proposal step an approval gate rather than another model phase?</summary><p>The human owns what becomes durable project truth. The model may summarize candidate facts, but it cannot silently promote them into trusted context.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>Every saved fact has a source and the repository fixture is unchanged.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Use the synthetic Bee snapshot. Live Bee access is not required.</aside>"},{"id":"plan","number":"06","nav":"Plan and port","time":"35 minutes","title":"Inspect first, then change a guarded copy","lead":"Review scope, checks, ADBT context, seed, and cost before approving a port. The source app stays untouched.","objective":"Follow the complete port boundary from plan approval to a checked, committed app copy.","evidence":"The report links approved ADBT context, a typed patch, check results, cost, and Git commits.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The production loop</h2><p>The plan is the human approval boundary. During analyze and plan the model calls ADBT over MCP itself to gather Vega knowledge. Strands proposes a typed patch from read-only project tools. The harness records every ADBT read, applies the patch, checks it, retries once, and commits only verified work.</p></section><div class=\"flow\"><div><b>ADBT MCP</b><span>Model reads workflows</span></div><i>→</i><div><b>Record</b><span>Hash each read</span></div><i>→</i><div><b>Model</b><span>Propose a typed patch</span></div><i>→</i><div><b>Checks</b><span>Write, commit, or retry</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Your source is never touched</h2><p>Before anything runs, the harness copies your app into <code>out/&lt;runId&gt;/app/</code> and runs <code>git init</code> inside that copy. Everything the model proposes is written <em>there</em>. Your real <code>apps/pocket-cinema/</code> is read once and never modified. The <code>&lt;runId&gt;</code> is a fresh directory per run, so runs never clobber each other — and the harness makes a Git commit per passing phase, so if phase 3 explodes, phases 1 and 2 are already committed and safe.</p></section><h2>The three phases of a port</h2>\n      <p>The port is a fixed sequence of three phases that mirror the mini-harness exactly: <strong>analyze → plan → build_test</strong>. Each is a model phase that ends in a mechanical check; two of them draw on ADBT. Source of truth: <code>src/index.ts</code> (the <code>phases: [...]</code> array) and <code>src/port-pipeline.ts</code> (the <code>phases()</code> function).</p><figure class=\"snippet\"><figcaption>The phase order — packages/workshop-harness/src/port-pipeline.ts</figcaption><pre><code>analyze  -&gt;  plan  -&gt;  build_test\n(model +      (model +    (model + executable focus test\n feasibility   live ADBT)  + mandatory device screenshot)\n via ADBT)</code></pre><p class=\"look\"><strong>Where to look:</strong> Open <code>src/port-pipeline.ts</code> and read <code>phases()</code>. Each name below maps to one entry.</p></figure><section class=\"phase\"><h3><span class=\"num\">1</span><code>analyze</code><span class=\"tag model\">model</span><span class=\"tag adbt\">ADBT feasibility</span></h3><dl><dt>Does</dt><dd>Reads the guarded app and writes <code>ANALYSIS.md</code> describing its screens, components, data, and what is portable to Vega TV. Alongside it, during the <code>plan</code> command — before any phase runs — a deterministic dependency inventory (<code>auditSource</code>) plus a bounded model+ADBT step judge whether the port is even possible.</dd><dt>Check</dt><dd><code>ANALYSIS.md</code> must contain <code>## Portable</code>.</dd><dt>Feasibility gate</dt><dd>The model returns a verdict — <code>feasible</code>, <code>feasible-with-adapters</code>, or <code>blocked</code>. A <code>blocked</code> verdict stops the run with exit code 5 <em>before</em> any build budget is spent.</dd><dt>Inspect</dt><dd><code>out/&lt;runId&gt;/app/ANALYSIS.md</code>, <code>feasibility-report.json</code>, and the deterministic inventory in <code>portability-report.json</code>.</dd><dt>Code</dt><dd><code>src/port-pipeline.ts</code>, <code>src/feasibility.ts</code>, <code>src/portability-audit.ts</code></dd></dl></section><section class=\"phase\"><h3><span class=\"num\">2</span><code>plan</code><span class=\"tag model\">model</span><span class=\"tag adbt\">live ADBT</span></h3><dl><dt>Goal</dt><dd>Plan the Vega TV port: preserved product behavior, Vega replacements, and the exact remote flow.</dd><dt>ADBT step</dt><dd>The harness hands the ADBT <code>McpClient</code> to the Strands agent; Strands lists ADBT's tools dynamically and the model calls them itself — <code>list_documents</code> to discover Vega workflows, then <code>read_document</code> to read the ones it judges relevant. The harness pre-picks nothing; after the phase it reconstructs the reads from the agent's messages and hashes them into <code>adbt-port-context.json</code>. (The claude-cli executor reaches ADBT through its own MCP config set up by <code>init-context</code>.)</dd><dt>Skill injected</dt><dd>Use the ADBT tools to discover and read the Vega migration workflows you need. Keep facts and assumptions separate, port one vertical slice, record gaps instead of inventing APIs.</dd><dt>Checks</dt><dd><code>VEGA_PORT.md</code> contains <code>## TV Flow</code>; <code>NextSteps.md</code> contains <code>ADBT</code> (names its sources).</dd><dt>Inspect</dt><dd><code>out/&lt;runId&gt;/app/VEGA_PORT.md</code>, <code>NextSteps.md</code>, <code>adbt-port-context.json</code>, and the commit <code>workshop(plan): ...</code>.</dd></dl></section><section class=\"phase\"><h3><span class=\"num\">3</span><code>build_test</code><span class=\"tag model\">model</span><span class=\"tag \">executable test</span><span class=\"tag adbt\">device screenshot</span></h3><dl><dt>Goal</dt><dd>Build the <code>apps/vega</code> package from the SDK shape, wire the remote-only home→details flow, and prove it.</dd><dt>Build checks</dt><dd>manifest <code>schema-version = 1</code> and <code>[[components.interactive]]</code>; <code>package.json</code> has <code>build-vega</code>; <code>app.json</code> and <code>metro.config.js</code> exist; root <code>package.json</code> has <code>vega:build</code>; <code>src/tv/focus-state.ts</code> exists; <code>src/App.tsx</code> imports <code>./tv/focus-state</code>.</dd><dt>Test checks</dt><dd>A real command runs — <code>node --import tsx tests/verify-tv-focus.ts</code> must exit 0. Back must return focus to the <em>originating card</em>, verified by a script, not a human eyeball. <code>tv-focus-result.json</code> shows <code>\"passed\": true</code>; <code>TV_VERIFICATION.md</code> contains <code>originating card</code>.</dd><dt>Screenshot (mandatory)</dt><dd>The phase then runs the Vega device lifecycle: two pre-gates (SDK version, device status), then build → install → launch → logs → capture → pull — the eight gates lesson 8 inspects. <strong>The run fails unless a launch screenshot is produced.</strong> Replay uses <code>--platform-replay</code> to stay key-free; live runs against a real VDA.</dd><dt>Inspect</dt><dd><code>out/&lt;runId&gt;/01-launch.png</code>, <code>vega-platform-result.json</code>, <code>tv-focus-result.json</code>, and the commit <code>workshop(build_test): ...</code>.</dd><dt>Code</dt><dd><code>src/port-pipeline.ts</code>, <code>src/platform/vega.ts</code></dd></dl></section><aside class=\"note warning\"><strong>Screenshot is now a mandatory gate</strong><br>The device screenshot is a required pass criterion of <code>build_test</code>. Two consequences: a device (or its <code>--platform-replay</code> fixture) is now mandatory for a green run, and on the current VDA image the live screenshot tool segfaults, so the <em>live</em> screenshot can't be produced until that tooling is fixed. The key-free replay path stays green via <code>--platform-replay ../../workshop/fixtures/vega-lifecycle.json</code>.</aside><h2>Every phase is built from the same prompt template</h2>\n      <p>You don't need to guess what the model sees. Every phase prompt is assembled by one function — <code>prompt()</code> in <code>src/port-pipeline.ts</code> — from the same slots. The model is told the exact checks it will be graded against, a failed attempt gets the verbatim failure text fed back in, and the output contract is strict JSON.</p><figure class=\"snippet\"><figcaption>The universal prompt template — src/port-pipeline.ts, prompt()</figcaption><pre><code>You are porting the CURRENT guarded React Native app to Vega SDK 0.22.5875.\nRead existing files before proposing edits. Preserve unrelated work.\n\nPhase: &lt;name&gt;\nGoal: &lt;one sentence&gt;\nSkill: &lt;domain instruction for this phase&gt;\nCreative seed: workshop-v1\n\nApproved context:\n&lt;project memory, or \"No approved project context.\"&gt;\n\nPortability findings:\n&lt;the JSON from phase 2's audit&gt;\n\n[ ONLY on plan: the ADBT guidance block, with SHA-256 hashes ]\n\nRequired checks:\n&lt;each check listed verbatim, so the model knows the bar it must clear&gt;\n\n[ ONLY on a retry: \"Previous attempt failed:\\n&lt;exact failure lines&gt;\\nFix these.\" ]\n\nReturn ONLY JSON: {\"summary\":\"...\",\"files\":{\"relative/path\":\"complete contents\"}}.</code></pre><p class=\"look\"><strong>Where to look:</strong> This exact string is built in <code>src/port-pipeline.ts</code>. Every live model turn — the prompt sent and the raw text returned — is recorded to <code>out/&lt;runId&gt;/port-recording.json</code>. That file is your audit trail: <code>request.messages[0].content</code> is the prompt, <code>response[].result</code> is the model's answer.</p></figure><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The model now calls ADBT over MCP itself. What has to be recorded for the run to stay reproducible and auditable?</p></aside><div class=\"command\"><header><span>Plan the Pocket Cinema port</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts plan ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --seed workshop-v1 --max-cost 3 --json</code></pre></div><h2>Review the plan before approval</h2><ol class=\"tasks\"><li>Confirm the source app and target flow.</li><li>Read the deterministic portability findings and the model's feasibility verdict.</li><li>Confirm the feasibility verdict is not <code>blocked</code> — a blocked verdict stops the run at exit code 5 before any build budget.</li><li>Check that ADBT is assigned to <code>analyze</code> (feasibility) and <code>plan</code>.</li><li>Check the three-phase plan (analyze → plan → build_test), fixed seed, and $3 cap.</li><li>Notice that build_test folds in the device screenshot lifecycle from lesson 8.</li></ol><h2>Run the port against a live model</h2><p>Approve and run with a real model. On the Claude CLI path the model reaches ADBT through the MCP config you set up in lesson 0; on the Strands path the harness hands it the ADBT client. <code>build_test</code> needs an attached VDA to capture the launch screenshot.</p><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI. The model drives ADBT via the CLI's MCP config (init-context).\n# build_test needs an attached VDA to capture the launch screenshot.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --yes --seed workshop-v1 --max-cost 3 --json</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock. The harness hands the ADBT McpClient to the agent.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2 \\\n  --yes --seed workshop-v1 --max-cost 3 --json</code></pre></div><h2>Build an evidence chain</h2><ol class=\"tasks\"><li>Copy the <code>runId</code> from the output.</li><li>Open <code>out/&lt;runId&gt;/adbt-port-context.json</code> and find the workflow names and hashes the model read.</li><li>Open <code>port-result.json</code> and confirm <code>adbt.mode: live</code>.</li><li>Open <code>app/NextSteps.md</code> and find ADBT sources and unsupported mappings.</li><li>Inspect the guarded app, report, and Git log.</li><li>Confirm <code>apps/pocket-cinema</code> is unchanged.</li></ol><p>This chain is the observability you built the harness for: the prompts, the document reads, the cost, and the commits of the whole run are on disk, inspectable by anyone, without asking the model what it did.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>The model fetches ADBT docs itself now. How does the run stay auditable and reproducible?</summary><p>The harness reconstructs every ADBT read from the agent's messages after the phase — document name, excerpt, and a SHA-256 hash into <code>adbt-port-context.json</code>. So even though the model chose what to read, there is an exact, hashed record of the knowledge it used — and replay can rerun from that recorded context with no live MCP server.</p></details></section><aside class=\"note warning\"><strong>If blocked</strong><br>If a live model, ADBT, or VDA is unavailable, run the fully recorded path — same phases and evidence contract, no credentials:</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback if a live model, ADBT, or VDA is unavailable.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --platform-replay ../../workshop/fixtures/vega-lifecycle.json \\\n  --yes --seed workshop-v1 --max-cost 3 --json</code></pre></div><h2>Worked example: real prompt in, real output out</h2>\n      <p>This is captured from an actual live run (<code>c9fc9e58</code>, real Claude model, live ADBT over MCP). It shows the ADBT-driven planning and the generated Vega package. (This capture predates the model-driven-MCP change: it used harness injection. Today the model calls ADBT's <code>read_document</code> tool itself, but the recorded provenance below — document names and SHA-256 hashes — is exactly what the harness now reconstructs from the model's tool calls.)</p>\n      <h3>What the model read from ADBT</h3>\n      <p>The harness records every document the model fetched, with a hash, into <code>adbt-port-context.json</code>. Recorded provenance from this run:</p><figure class=\"snippet\"><figcaption>adbt-port-context.json (recorded reads, excerpt)</figcaption><pre><code>## ADBT Vega Port Guidance\n\nMode: live\nSources:\n- port_tv_app_to_vega.md (sha256: 5dcf0e6f8a5b6a62d688562c46a9f22f414715c1b792ecdaf92bc0e8016214ea)\n- port_tv_app_to_vega_fos_rn_app.md (sha256: 2f67d9dc1133a52e9873513c3d66a2c0a2ca090a0d90284e1bb54e3f825f5607)\n\n### port_tv_app_to_vega.md\n## Purpose\nEntry point for all FOS-to-Vega app migrations. This workflow determines what the\nuser wants to convert, runs shared prerequisites (SDK check, device detection), then\ndispatches to the appropriate conversion-specific orchestrator.\n...\n\nUse these ADBT sources for Vega-specific decisions. Do not invent Vega APIs.\nWrite unsupported or uncertain mappings to NextSteps.md and name the ADBT documents consulted.</code></pre><p class=\"look\"><strong>Where to look:</strong> Why hashes? So the exact knowledge the model was given is provable and reproducible later. Find it in <code>out/&lt;runId&gt;/adbt-port-context.json</code>.</p></figure><h3>Out of <code>build_test</code> — what came out</h3>\n      <p>The model returned a multi-file patch. Several build_test checks <code>grep</code> the generated manifest — and it contains exactly the strings they look for:</p><figure class=\"snippet\"><figcaption>Generated apps/vega/manifest.toml</figcaption><pre><code>schema-version = 1\n\n[package]\ntitle = \"Pocket Cinema\"\nid = \"com.pocketcinema.app\"\n\n[[components.interactive]]\nid = \"com.pocketcinema.app.main\"\nruntime-module = \"@pocket-cinema/rn\"\nlaunch-type = \"singleton\"</code></pre></figure><p>Also worth reading: the <code>NextSteps.md</code> the model wrote back in the <em>plan</em> phase. It didn't have full MCP doc access in that session, and instead of bluffing, it said so:</p><figure class=\"snippet\"><figcaption>Generated NextSteps.md (excerpt) — the model admitting uncertainty</figcaption><pre><code>## Unverified against SDK docs (MCP doc access not granted)\n\nThe buildertools MCP read_document / list_documents calls were denied in this\nsession, so the items below rely on the Vega skill summaries and the ADBT\nworkflows above. They MUST be confirmed against the named KB documents before\nrelying on them — they are not invented APIs presented as fact.\n\n1. Manifest schema — verify field names and the [[components.interactive]] shape.\n2. App icon asset — vega_app_manifest.md requires a 512x512 PNG.\n3. Build CLI — confirm the exact Kepler/Vega build invocation.</code></pre><p class=\"look\"><strong>Where to look:</strong> The skill told the model to record gaps instead of inventing APIs, and this is what that looks like in output. The plan-phase check <code>NextSteps.md contains \"ADBT\"</code> passed, so the plan committed; the later build_test phase then had to clear all 11 of its own checks before it committed too.</p></figure><aside class=\"note \"><strong>What the worked example shows</strong><br>The model produces large, well-structured artifacts — and it also wraps JSON in prose and admits uncertainty. The harness doesn't read any of that as truth: it extracts the JSON, writes it to the guarded copy, and runs mechanical grep/file_exists checks. Only passing work is committed.</aside><h2>How ADBT connects during the port</h2><div class=\"command\"><header><span>Check the native ADBT MCP path</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --adbt-live --json</code></pre></div><h2>Strands MCP constructs used here</h2>\n    <table><thead><tr><th>Construct</th><th>Role in the ADBT path</th></tr></thead><tbody><tr><td><code>McpClient</code></td><td>Strands client wrapper that connects lazily, exposes MCP tools, calls them, and cleans up the connection.</td></tr><tr><td><code>applicationName</code> / <code>applicationVersion</code></td><td>Identify Past the Vibes Workshop to the MCP server during connection setup.</td></tr><tr><td><code>listTools()</code></td><td>Strands calls this for the passed <code>McpClient</code> to discover ADBT's tools dynamically, then exposes them to the model. The harness does not require or pre-pick tool names.</td></tr><tr><td><code>Agent({ tools: [...projectTools, adbtClient] })</code></td><td>The <code>McpClient</code> is passed into the agent's tools. Strands invokes ADBT's tools for the model during the agent loop; the harness does not call them.</td></tr><tr><td><code>agent.messages</code></td><td>After the phase, the harness walks the tool-use and tool-result blocks to reconstruct which ADBT docs the model read, then hashes them into <code>adbt-port-context.json</code>.</td></tr><tr><td><code>disconnect()</code></td><td>Closes the child server and transport in <code>finally</code>, including failure paths.</td></tr></tbody></table>\n    <aside class=\"note \"><strong>The transport is a separate layer</strong><br><code>StdioClientTransport</code> comes from the official Model Context Protocol SDK, not Strands. It starts pinned ADBT as a child process. The harness passes the <code>McpClient</code> into the agent's tools so the model can call ADBT itself; provenance is reconstructed from the message history afterward.</aside><aside class=\"note \"><strong>Two executor paths</strong><br>Strands: the harness passes the ADBT <code>McpClient</code> into the agent's tools and the model calls ADBT itself. Claude CLI: the CLI reaches ADBT through the MCP config from <code>init-context</code>. Either way the harness reconstructs and hashes what was read.</aside><aside class=\"note success\"><strong>You are done when</strong><br>You can trace each MCP construct from connection through the model's own tool calls, a typed proposal, checks, a verified commit, and the final report.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>A live port stops with exit 3 when ADBT is unavailable; it never continues with unsupported assumptions. Use the recorded ADBT context via the replay fallback above.</aside>"},{"id":"tv","number":"07","nav":"Test remote behavior","time":"20 minutes","title":"Test the flow, not one screenshot","lead":"Trace focus through launch, movement, Select, and Back. A passing build cannot prove this behavior.","objective":"Express TV navigation as observable state transitions instead of visual impressions.","evidence":"tv-focus-result.json records launch, movement, selection, and focus restoration after Back.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Why this step exists</h2><p>TV quality is temporal. A screenshot can show where focus is now, but not whether focus moved correctly, respected boundaries, opened the right screen, or returned to the same card.</p></section><div class=\"remote\" aria-label=\"TV remote direction pad\"><button>↑</button><button>←</button><button class=\"ok\">OK</button><button>→</button><button>↓</button></div><table><thead><tr><th>Action</th><th>Expected result</th></tr></thead><tbody><tr><td>Launch</td><td>Featured action has focus</td></tr><tr><td>Down</td><td>Focus enters the first rail</td></tr><tr><td>Left / right</td><td>Focus stops at list boundaries</td></tr><tr><td>Select</td><td>Details opens for the focused card</td></tr><tr><td>Back</td><td>The same card regains focus</td></tr></tbody></table><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>Which transition is most likely to pass a screenshot review but fail for a real remote user?</p></aside><div class=\"command\"><header><span>Run the executable focus check</span><button class=\"copy\">Copy command</button></header><pre><code>(cd packages/workshop-harness/out/&lt;runId&gt;/app &amp;&amp; \\\n  node --import tsx tests/verify-tv-focus.ts)\ncat packages/workshop-harness/out/&lt;runId&gt;/app/tv-focus-result.json</code></pre></div><h2>Compare passing and failing evidence</h2><ol class=\"tasks\"><li>Replace <code>&lt;runId&gt;</code> with the id from lesson 6.</li><li>Read <code>tv-focus-result.json</code> as a sequence, not a score.</li><li>Open <code>fixtures/focus-failure/README.md</code> and find the failed Back transition.</li><li>Trace the focus state and restoration code in the guarded app.</li></ol><aside class=\"note \"><strong>Where the focus props come from</strong><br><code>hasTVPreferredFocus</code> and the <code>onFocus</code>/<code>onBlur</code> handlers are React Native's TV focus model — see <a href=\"https://github.com/react-native-tvos/react-native-tvos\" target=\"_blank\" rel=\"noopener\">react-native-tvos</a> for the general TV story. This workshop targets Vega, not that fork, but the focus model is the same.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is Back part of the focus contract?</summary><p>Returning to a screen without restoring the user's prior focus loses navigation context. The UI may look correct while the remote interaction is broken.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>The focus check passes the full transition sequence and writes <code>tv-focus-result.json</code>.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Run the same check in <code>checkpoints/vega-buildable/app</code>. No device is required.</aside>"},{"id":"vega","number":"08","nav":"Run the Vega lifecycle","time":"25 minutes","title":"Hand the guarded app to Vega tools","lead":"Run the complete lifecycle against a live Vega SDK and VDA. Recorded replay stays as a fallback when no device is attached.","objective":"Distinguish reproducible lifecycle rehearsal from evidence produced by a real Vega device.","evidence":"Eight lifecycle gates pass, with evidenceMode labeled replay or live.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Two kinds of evidence</h2><p>A live VDA run proves this app built, installed, launched, and produced a screenshot on an attached device. Replay proves you can study the lifecycle and its contracts without one. Keep those claims separate.</p></section><aside class=\"note warning\"><strong>Device screenshot caveat</strong><br>On the current VDA image the screenshot tool segfaults at the capture gate, so a fully live run may fail there even when build, install, and launch pass. The repo records this in the rehearsal note. If you hit it, use the replay fallback to finish the lesson.</aside><div class=\"links\"><a href=\"live-rehearsal.md\">Read the rehearsal record</a></div><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>If the SDK build passes but the device list is empty, which lifecycle gates must the harness refuse to claim?</p></aside><h2>Run the lifecycle on a live VDA</h2><ol class=\"tasks\"><li>Replace <code>&lt;runId&gt;</code> with the id from lesson 6.</li></ol><div class=\"command\"><header><span>Start VDA and keep this terminal open</span><button class=\"copy\">Copy command</button></header><pre><code># Run this in a system terminal and leave it open.\nvega virtual-device start --gui</code></pre></div><div class=\"command\"><header><span>Confirm the SDK and attached device</span><button class=\"copy\">Copy command</button></header><pre><code># Run this in a second system terminal.\nvega --version\nvega virtual-device status\nvega exec vda devices -l</code></pre></div><div class=\"command\"><header><span>Show the Vega plan</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts vega-run &lt;runId&gt; --plan --json</code></pre></div><div class=\"command\"><header><span>Run with Vega SDK and VDA</span><button class=\"copy\">Copy command</button></header><pre><code>npm --prefix packages/workshop-harness/out/&lt;runId&gt;/app/apps/vega install\nyarn --cwd packages/workshop-harness tsx src/index.ts vega-run &lt;runId&gt; --yes --json</code></pre></div><h2>Claim live evidence only when</h2><ol class=\"tasks\"><li>The SDK reports <code>0.22.5875</code>.</li><li>VDA reports <code>running: true</code> and lists an attached device.</li><li>Build, install, launch, logs, capture, and pull all pass.</li><li>The result says <code>evidenceMode: live</code> and the screenshot came from the device.</li></ol><h2>Inspect all eight gates</h2><ol class=\"tasks\"><li>Confirm SDK version and device status were checked before build.</li><li>Find build, install, launch, logs, capture, and pull results.</li><li>Read each gate's exact command, exit code, and output.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What turns lifecycle output into trustworthy evidence?</summary><p>The harness records the exact command, outcome, artifact, and evidence mode for each gate. A successful process exit alone is not enough when no device is attached.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>All eight gates pass on an attached VDA and the result says <code>evidenceMode: live</code> with a device screenshot.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>If no VDA is attached or the screenshot gate segfaults, run the recorded lifecycle instead — same eight gates, labeled evidenceMode: replay:</aside><div class=\"command\"><header><span>Fallback: key-free lifecycle replay</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts vega-run &lt;runId&gt; \\\n  --platform-replay ../../workshop/fixtures/vega-lifecycle.json \\\n  --yes --json</code></pre></div>"},{"id":"bee","number":"09","nav":"Optional Bee context","time":"15 minutes","title":"Import selected context, not a transcript","lead":"Run this only when Bee is configured and participants consent. The synthetic fixture is the normal workshop path.","objective":"Select useful context without turning a private conversation into unreviewed agent memory.","evidence":"A scrubbed snapshot has source ids, dates, a query, a summary, and a stable hash.","body":"<section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Why this step is optional</h2><p><a href=\"https://www.aboutamazon.com/news/devices/bee-amazon-wearable-ai-device-new-features\" target=\"_blank\" rel=\"noopener\">Bee</a> is Amazon's wearable AI device: it listens to your day (when you let it) and turns conversations into searchable summaries, decisions, and to-dos. Conversation search can recover useful decisions, but it also crosses a privacy boundary. Use it only with consent, select the smallest useful excerpt, and store a scrubbed snapshot rather than a transcript.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>Which fields would let a reviewer verify where a summarized fact came from without storing the full conversation?</p></aside><div class=\"command\"><header><span>Search Bee</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts context bee search \\\n  \"Pocket Cinema product decisions\" --json</code></pre></div><div class=\"command\"><header><span>Save one selected snapshot</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts context bee snapshot &lt;conversationId&gt; \\\n  --out candidate-context.json --json</code></pre></div><h2>Review the boundary</h2><ol class=\"tasks\"><li>Check source ids, dates, query, summary, and hash.</li><li>Review the snapshot before proposing memory.</li><li>Never commit a raw private transcript.</li><li>Disconnect Bee and confirm the approved snapshot still works.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why should the snapshot work after Bee is disconnected?</summary><p>The run becomes reproducible and reviewable without a live private-data dependency. The snapshot is the approved input; Bee is only a discovery source.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>Every approved fact has a source and can be reused without Bee.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Use <code>fixtures/bee-context/snapshot.json</code> or skip this optional module.</aside>"},{"id":"finish","number":"10","nav":"Build your own","time":"15 minutes","title":"Design one harness for your work","lead":"Keep the pipeline and replace the TV skill, Vega commands, and D-pad checks with your domain.","objective":"Draft the smallest useful harness for one task in your own engineering domain.","evidence":"A worksheet names the phases, checks, approval point, budget, and evidence the run must retain.","body":"<div class=\"takeaway\"><code>plan → context → run → check → retry → checkpoint → report</code></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Transfer the pattern</h2><p>The reusable idea is a bounded workflow that gives a model strong context, limits its authority, checks each result, and leaves evidence another developer can inspect. TV and Vega are the example, not the point. What you keep is the control and the observability you saw in lesson 6: authority stays in your code, and every run leaves prompts, reads, costs, and commits you can audit.</p></section><h2>Draft your harness</h2><ol class=\"tasks\"><li>Open <code>worksheet.md</code>.</li><li>Name one outcome that can finish in one session.</li><li>Choose the fewest useful phases.</li><li>Give every phase one independent, mechanical check.</li><li>Define the approval point, cost limit, stop conditions, and saved evidence.</li><li>Name your replacement for the TV skill, the ADBT MCP server, the Vega adapter, the D-pad check — and, if you use a different CLI agent, the executor.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the smallest useful first version of your harness?</summary><p>One repeatable task, a short phase sequence, one strong prior, one independent check per phase, a bounded retry, and a report. Add tools only when a proven gap needs them.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>Another developer can follow your worksheet, inspect the evidence, and knows when the harness must stop.</aside><div class=\"links\"><a href=\"worksheet.md\">Open the worksheet</a><a href=\"troubleshooting.md\">Troubleshooting</a><a href=\"instructor-guide.md\">Instructor guide</a></div>"}];
