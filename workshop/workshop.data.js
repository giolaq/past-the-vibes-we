// GENERATED by scripts/build-site.mjs from workshop/lessons/*.md — do not edit by hand.
window.WORKSHOP_MODULES = [{"id":"welcome","number":"00","nav":"Start here","time":"20 minutes","title":"Set up the workshop and learn the harness","lead":"Prepare the workshop workspace. The workspace contains the project packages and dependencies. A model executor connects the harness to a model.","objective":"Explain the function of the harness. Prepare the project, one model connection, Amazon Devices Builder Tools (ADBT), the Vega software development kit (SDK), and the Vega Virtual Device (VDA).","evidence":"A verified workspace and one configured executor.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Set up the workshop and learn the harness</h2><p>Lesson 00 prepares all tools for the workshop.\nYou will install the project dependencies and configure one model connection that sends real requests.\nYou will prepare Amazon Devices Builder Tools, the Vega software development kit, and the Vega Virtual Device.\n\nYou will also learn how the harness works before you run its first phase.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Understand the harness</h2><p>An Agent harness is the software infrastructure around a large language model (LLM).\nThe harness turns model reasoning into actions that software can control and verify.\nTypical components include tools, an isolated execution environment, and context management.\nA tool is a function that the model can request.\nAn isolated execution environment limits what the model can read or change.\nContext management selects the instructions and project information that the model receives.\n\nIn this workshop, the harness is a phase pipeline written in TypeScript.\nThe pipeline contains six ordered phases:\n\n<code>analyze -&gt; plan -&gt; port -&gt; build -&gt; launch -&gt; test</code>\n\nThe <code>analyze</code> phase reads the existing app.\nThe <code>plan</code> phase defines the required TV interaction.\nThe <code>port</code> phase changes the app source.\nThe <code>build</code> phase compiles an installable TV app package.\nThe <code>launch</code> phase starts the package on a TV device.\nThe <code>test</code> phase checks remote-control navigation.\n\nEach phase has a defined goal, input, check, and result.\nA phase can call a model and give the model selected tools.\nThe model can inspect the app and propose changes.\nThe model cannot write files or run shell commands.\nThe harness controls file writes, independent checks, retries, usage limits, Git commits, and reports.\n\nThe harness records evidence that you can inspect after each phase.</p></section><h2>Know the main terms</h2><table><thead><tr><th>Term</th><th>Function in this workshop</th></tr></thead><tbody><tr><td><a href=\"https://strandsagents.com/\" target=\"_blank\" rel=\"noopener\">Strands Agents SDK</a></td><td>An open-source software development kit for AI agents in Python and TypeScript. The harness uses the TypeScript SDK for model access, tool operations, JavaScript Object Notation (JSON) responses with required fields, and Model Context Protocol (MCP) connections.</td></tr><tr><td><a href=\"https://developer.amazon.com/docs/vega/0.22/vega-get-started.html\" target=\"_blank\" rel=\"noopener\">Vega OS</a></td><td>The target TV operating system. The Vega SDK provides tools to build, run, test, and debug Vega apps.</td></tr><tr><td><a href=\"https://developer.amazon.com/docs/vega/0.23/mcp-server.html\" target=\"_blank\" rel=\"noopener\">Amazon Devices Builder Tools (ADBT)</a></td><td>A package that gives AI agents Amazon Devices tools and documents. This workshop connects the model to ADBT through MCP.</td></tr></tbody></table><h2>1. Prepare your tools</h2><p><a href=\"https://nodejs.org/en/download\" target=\"_blank\" rel=\"noopener\">Node.js</a> runs the TypeScript harness. <a href=\"https://git-scm.com/downloads\" target=\"_blank\" rel=\"noopener\">Git</a> records accepted source changes. <a href=\"https://github.com/nodejs/corepack\" target=\"_blank\" rel=\"noopener\">Corepack</a> activates the Yarn package manager used by this repository.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Install the required command-line tools.\nClone the workshop repository.</p></aside><ol class=\"tasks\"><li>Install <a href=\"https://nodejs.org/en/download\" target=\"_blank\" rel=\"noopener\">Node.js</a> 20 or newer.</li><li>Install <a href=\"https://git-scm.com/downloads\" target=\"_blank\" rel=\"noopener\">Git</a>.</li><li>Make sure that <a href=\"https://github.com/nodejs/corepack\" target=\"_blank\" rel=\"noopener\">Corepack</a> is available.</li><li>Clone the repository.</li><li>Open a terminal in the repository root.</li></ol><div class=\"command\"><header><span>Clone the repository</span><button class=\"copy\">Copy command</button></header><pre><code>git clone https://github.com/giolaq/past-the-vibes-we.git\ncd past-the-vibes-we</code></pre></div><p>The workshop uses a sample React Native app named Pocket Cinema. The app source is in:</p><pre><code class=\"language-text\">apps/pocket-cinema</code></pre><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual Android TV capture</div><img src=\"assets/pocket-cinema-android-tv.png\" alt=\"Pocket Cinema home screen on an Android TV emulator\" loading=\"lazy\"><figcaption>This image shows the React Native app before the Vega port. The app has no explicit TV focus behavior.</figcaption></figure><p>The starting app does not contain explicit rules for the TV focus.</p><h2>Know what the harness receives</h2><p>The harness receives five types of input. Each input has a different purpose.</p><table><thead><tr><th>Input type</th><th>Source</th><th>Purpose</th></tr></thead><tbody><tr><td>Existing product</td><td>Source app directory</td><td>Supplies the code, content, design, dependencies, and current behavior</td></tr><tr><td>Port requirements</td><td><code>workshop-brief.md</code></td><td>Defines the required TV flow, constraints, and completion evidence</td></tr><tr><td>Model connection</td><td><code>workshop.config.json</code></td><td>Stores the model settings described in section 4</td></tr><tr><td>Run settings</td><td>Command flags</td><td>Select the phases, run ID, creative seed, and optional usage limits</td></tr><tr><td>Platform knowledge</td><td>ADBT MCP</td><td>Supplies current Vega documents to the model</td></tr></tbody></table><p>The source app and <code>workshop-brief.md</code> define the product work. The run settings control how the harness does that work. ADBT supplies Vega knowledge but does not define the product.</p><p>A run ID is the name of one pipeline run and its directory under <code>out/</code>.</p><p>The Pocket Cinema requirements are in <code>apps/pocket-cinema/workshop-brief.md</code>.</p><h2>2. Install the workspace</h2><p>The workspace is the repository packages and their shared dependencies. Yarn manages this workspace. A dependency is a software package that the project uses.</p><p>Transport Layer Security (TLS) certificate verification protects package downloads. The first command removes an unsafe TLS override if that override exists.</p><p>Run these commands from the repository root:</p><div class=\"command\"><header><span>Install the workshop packages</span><button class=\"copy\">Copy command</button></header><pre><code>unset NODE_TLS_REJECT_UNAUTHORIZED\ncorepack enable\nyarn setup</code></pre></div><p>The <code>corepack enable</code> command makes the repository version of Yarn available. The <code>yarn setup</code> command installs the workspace dependencies.</p><h2>3. Verify the workspace</h2><div class=\"command\"><header><span>Run the local verification</span><button class=\"copy\">Copy command</button></header><pre><code>yarn verify</code></pre></div><p>This command checks TypeScript for type errors. The command also runs automated tests, document checks, and website checks. The command does not call a model or start a Vega device.</p><h3>Verify the starting app</h3><p>The harness package is the <code>packages/workshop-harness</code> directory. Enter the harness package. Keep this terminal in this directory for lessons 1 through 7.</p><div class=\"command\"><header><span>Enter the harness package</span><button class=\"copy\">Copy command</button></header><pre><code>cd packages/workshop-harness</code></pre></div><p>The <code>yarn tsx</code> command runs a TypeScript command-line program. The <code>tv-check</code> command reads the app files and checks for required TV support. The command does not build or start the app.</p><div class=\"command\"><header><span>Run the TV-readiness check</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts tv-check ../../apps/pocket-cinema</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: false\n&quot;failures&quot;: [\n&quot;Focus state module: missing src/tv/focus-state.ts&quot;,\n&quot;App wires shared focus state: src/App.tsx must contain \\&quot;./tv/focus-state\\&quot;&quot;,\n&quot;Initial focus declared: src/App.tsx must contain \\&quot;hasTVPreferredFocus\\&quot;&quot;,\n&quot;Vega package manifest: missing apps/vega/manifest.toml&quot;,\n...\n]</code></pre></div><p>This failure is the expected starting result. The failure list identifies the missing TV support that the workshop will add. Lesson 6 runs the same check on the ported app.</p><h2>4. Select one live executor</h2><p>A live executor sends prompts to an actual model. The executor is the code path that sends the prompt and receives the model response. The provider is the service that hosts the model. The model ID is the provider name for a specific model.</p><p>Use the same executor, provider, and model in all lessons. Do not change providers during the workshop.</p><p>Open <code>../../workshop.config.json</code>. You are in <code>packages/workshop-harness</code>, so <code>../..</code> points to the repository root. Replace the file contents with one configuration from this section. Select any model that the executor and provider support. Use the exact model name or provider model ID.</p><table><thead><tr><th>Path</th><th>Configuration fields</th><th>Credential</th></tr></thead><tbody><tr><td>Claude Code CLI</td><td><code>executor</code>, <code>model</code></td><td>Authenticated Claude Code</td></tr><tr><td>Strands with Bedrock</td><td><code>executor</code>, <code>provider</code>, <code>model</code>, <code>region</code></td><td>AWS credentials and model access</td></tr><tr><td>Strands with OpenAI</td><td><code>executor</code>, <code>provider</code>, <code>model</code></td><td><code>OPENAI_API_KEY</code></td></tr><tr><td>Strands with OpenRouter</td><td><code>executor</code>, <code>provider</code>, <code>model</code></td><td><code>OPENROUTER_API_KEY</code></td></tr></tbody></table><p>CLI means command-line interface. With Claude Code CLI, the harness starts your authenticated <code>claude</code> command. Authenticated means that you signed in to Claude Code before the workshop.</p><p>With Strands, the SDK sends the request to the selected provider.</p><h3>Configure Claude Code</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;claude-cli&quot;,\n  &quot;model&quot;: &quot;claude-sonnet-4-6&quot;\n}</code></pre><p>Use an exact Claude model name. Do not use an alias such as <code>sonnet</code>, <code>opus</code>, or <code>haiku</code>. If your organization manages Claude Code, select a name from <code>availableModels</code> in <code>~/.claude/settings.json</code>.</p><h3>Configure Strands with Bedrock</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;bedrock&quot;,\n  &quot;model&quot;: &quot;anthropic.claude-3-5-sonnet-20241022-v2:0&quot;,\n  &quot;region&quot;: &quot;us-west-2&quot;\n}</code></pre><h3>Configure Strands with OpenAI</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;openai&quot;,\n  &quot;model&quot;: &quot;gpt-4.1&quot;\n}</code></pre><h3>Configure Strands with OpenRouter</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;openrouter&quot;,\n  &quot;model&quot;: &quot;anthropic/claude-sonnet-4&quot;\n}</code></pre><h3>Verify the selected configuration</h3><div class=\"command\"><header><span>Verify the model environment</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts doctor --json</code></pre></div><p>The <code>doctor</code> command checks the selected executor, credentials, and required programs. The <code>--json</code> flag prints the result as machine-readable JSON. The harness automatically loads <code>../../workshop.config.json</code>. Command-line model flags override the file for one command.</p><h2>5. Prepare ADBT</h2><p>The harness uses ADBT through MCP. Strands creates an MCP client for ADBT.</p><h2>6. Prepare Vega</h2><p>Lesson 4 requires Vega SDK version <code>0.23.9221</code>. Use the <a href=\"https://developer.amazon.com/docs/vega/0.23/install-vega-sdk.html\" target=\"_blank\" rel=\"noopener\">Vega installation guide</a> to install this SDK version.</p><p>The Vega Virtual Device (VDA) is a software TV device that runs on your computer. Lesson 5 requires an installed VDA. The harness starts the VDA when no device is attached. Lesson 6 sends remote-control keys and reads the focused control.</p><p>Use your terminal application for the setup commands. Do not run the setup commands inside a model chat. The <code>doctor --adbt-live</code> command checks a live ADBT connection.</p><div class=\"command\"><header><span>Start the Vega environment</span><button class=\"copy\">Copy command</button></header><pre><code>cd packages/workshop-harness\nyarn tsx src/index.ts doctor --adbt-live --json\nvega --version\nvega virtual-device start --gui</code></pre></div><p>The <code>--gui</code> option opens the VDA window. You can start the VDA before Lesson 5. The early VDA start is optional. The launch phase runs the same command when no device is attached.</p><p>In a second terminal, run:</p><div class=\"command\"><header><span>Verify the SDK and device</span><button class=\"copy\">Copy command</button></header><pre><code>vega --version\nvega virtual-device status\nvega exec vda devices -l</code></pre></div><p>The last command lists devices that the Vega CLI can contact. The environment is ready when all these conditions are true:</p><ul><li>The SDK reports version <code>0.23.9221</code>.</li><li>The virtual-device status reports <code>running: true</code>.</li><li>The device list contains an attached device.</li></ul><p>An attached device is a VDA or physical Vega device that the Vega CLI can contact. A successful command with an empty device list does not prove that a device is attached.</p><aside class=\"note success\"><strong>Completion check</strong><br>The workspace passes all local checks.\nOne live executor is ready.\nFor live platform evidence, the Vega SDK and VDA are also ready.</aside>"},{"id":"analyze","number":"01","nav":"Analyze the app","time":"25 minutes","title":"Run one model call, then add controls","lead":"Run one model call without the phase controls. Then identify the claims that the call cannot prove. The guarded copy is the private working copy under out/.","objective":"Explain why a patch that appears correct is not a verified result. Identify the boundary between the model and the harness.","evidence":"An unverified proposal and a verified ANALYSIS.md file in the guarded copy.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Start with the weak process</h2><p>First, ask one model to port the app in one call.\nThe harness saves the proposal but does not apply it.\n\nThen run one controlled phase.\nA schema lists the fields that a response must contain.\nA Git commit records an accepted snapshot of the files.\nThe controlled phase has a schema, a check, and a Git commit.\nCompare the two results.</p></section><h2>Run the one-call example</h2><p>The model receives read-only project tools. A patch is a proposed set of file changes. A typed patch uses a JSON response with the required fields. The model must return a typed patch.</p><p>The call does not receive these controls:</p><ul><li>Phase plan</li><li>ADBT MCP server</li><li>Independent checks</li><li>Retry</li><li>Compiler</li><li>Device</li></ul><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The proposal can contain a manifest, focus code, and a test.\nDoes the proposal prove that the port is correct?\nName one claim that you cannot make.</p></aside><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Run one model call without the controlled phase pipeline.\nInspect its proposal and identify what the call did not prove.</p></aside><div class=\"command\"><header><span>Save one live proposal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts naive ../../apps/pocket-cinema \\\n  --max-tokens 1000000 --run-id naive-demo --yes</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.\nDo not add MCP to this command.\nThe <code>naive</code> command saves a proposal but does not apply the files.\nThe <code>--yes</code> flag confirms that you want to start the live model call.\nThe token limit is a generous safety limit for this comparison.</aside><p><code>naive-proposal.json</code> contains the proposed files. <code>naive-result.json</code> lists the claims that remain unproven.</p><ol class=\"tasks\"><li>Open <code>out/naive-demo/naive-proposal.json</code>.</li><li>Read the proposed files.</li><li>Open <code>out/naive-demo/naive-result.json</code>.</li><li>Find each <code>proven: false</code> value.</li><li>Read the <code>missingProof</code> list.</li><li>Run <code>git status</code> on <code>apps/pocket-cinema</code>.</li><li>Make sure that the source app did not change.</li></ol><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>A good proposal is not evidence</h2><p>The model can propose good code.\nThe proposal is still unverified.\n\nWithout an independent check, an incorrect result can appear correct.\nIndependent checks distinguish correct and incorrect results.</p></section><h2>Know the starting app</h2><p><code>apps/pocket-cinema</code> is a small React Native app. The app has a featured title, two content rails, and a details screen.</p><p>The app has no explicit TV focus behavior. The app has no Vega package. Lesson 00 verified these facts with <code>tv-check</code>.</p><p>The analyze phase will create <code>ANALYSIS.md</code>. This document describes the app structure, dependencies, portable parts, replacement work, and open questions.</p><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual Android TV capture</div><img src=\"assets/pocket-cinema-android-tv.png\" alt=\"Pocket Cinema home screen on an Android TV emulator\" loading=\"lazy\"><figcaption>The app can render on a TV screen. It does not have remote-control focus or a Vega package.</figcaption></figure><h2>Know the six phases</h2><div class=\"flow\"><div><b>Analyze</b><span>Read the app</span></div><i>→</i><div><b>Plan</b><span>Define the TV port</span></div><i>→</i><div><b>Port</b><span>Write the code</span></div><i>→</i><div><b>Build</b><span>Produce the package</span></div><i>→</i><div><b>Launch</b><span>Start the app</span></div><i>→</i><div><b>Test</b><span>Verify remote behavior</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Know the harness boundary</h2><p>The model can list, read, and search the guarded copy.\nThe model has no shell tool.\nThe model has no write tool.\n\nThe model returns a JSON object named <code>{summary, files}</code>.\n<code>summary</code> describes the proposed change.\n<code>files</code> maps each relative file path to its complete new content.\nThe harness validates the response.\nThe harness writes files and runs checks.\nThe harness retries with the exact failure text.\nThe harness commits only a passing result.</p></section><h2>Trace Strands in the analyze phase</h2><p>Open <code>src/port-executor.ts</code>. One phase creates one Strands agent. The optional token and turn limits from Lesson 00 can bound the model call.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-executor.ts (simplified)</figcaption><pre><code>const limits = selectedRunLimits();\nconst agent = new Agent({\n  name: `workshop-${phase}`,\n  model: createModel(config),\n  tools: [\n    ...createProjectReadTools(appDir),\n    ...(options.extraTools ?? []),\n  ],\n  structuredOutputSchema: PortOutputSchema,\n  systemPrompt: &quot;Inspect with read-only tools. Return a complete patch.&quot;,\n  printer: false,\n});\n\nconst result = await consumeStream(\n  agent.stream(prompt, { limits }),\n  event =&gt; transcripts.append(phase, event),\n);</code></pre><p class=\"look\"><strong>Where to look:</strong> <code>extraTools</code> contains the ADBT MCP connection. <code>limits</code> contains only limits selected for the run. Strands supplies the model and tool loop. The harness controls writes, checks, Git, and cumulative usage.</p></figure><table><thead><tr><th>Owner</th><th>Analyze action</th></tr></thead><tbody><tr><td>Strands</td><td>Runs the model and tool loop. Lets the model inspect the guarded copy. Lets the model call ADBT tools.</td></tr><tr><td>ADBT MCP</td><td>Supplies current Vega documents for portability claims.</td></tr><tr><td>Harness</td><td>Permits the MCP server. Validates the typed response. Writes <code>ANALYSIS.md</code>. Runs the check. Commits a passing result.</td></tr><tr><td>Evidence</td><td><code>ANALYSIS.md</code>, <code>model-logs/analyze.jsonl</code>, and the analyze commit</td></tr></tbody></table><aside class=\"note \"><strong>Claude CLI path</strong><br>The snippet runs when <code>workshop.config.json</code> selects <code>strands</code>.\nThe Claude CLI executor receives the same prompt and required response fields.\nThe harness keeps the same write, check, and commit boundary.</aside><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The analyze phase will report which parts can move to Vega.\nWhich part can the phase verify mechanically?\nWhich part remains a model claim?</p></aside><h2>Run the analyze phase</h2><p>Run one command. Use the executor that you selected in Lesson 00. The phase writes its accepted files only to <code>out/workshop/app</code>.</p><div class=\"command\"><header><span>Run the analyze phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases analyze --yes --run-id workshop</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;phasesComplete&quot;:[&quot;analyze&quot;]</code></pre></div><h2>Inspect the evidence</h2><p>The import commit is the first snapshot of the copied source app. The analyze commit is the snapshot accepted after the phase check. A JSON Lines (JSONL) file stores one JSON event on each line.</p><ol class=\"tasks\"><li>Open <code>out/workshop/app/ANALYSIS.md</code>.</li><li>Find the required portability section.</li><li>Run <code>git status</code> on <code>apps/pocket-cinema</code>.</li><li>Make sure that the source app is unchanged.</li><li>Run <code>git log --oneline</code> in <code>out/workshop/app</code>.</li><li>Find the import commit.</li><li>Find the analyze-phase commit.</li><li>Open <code>out/workshop/port-result.json</code>.</li><li>Find the <code>attempts</code> value.</li><li>Open <code>out/workshop/model-logs/analyze.jsonl</code>.</li><li>Find the ADBT document read.</li><li>Record three claims in <code>ANALYSIS.md</code> that no check verified.</li></ol><aside class=\"note \"><strong>Why the harness uses Git</strong><br>The harness resets a failed attempt to the phase-start commit.\nA rejected patch leaves no file changes.\nGit supplies rollback and a record of accepted work.</aside><h2>Compare two executors</h2><p>Work with a person who used a different executor. Do not run the phase again.</p><p>Compare the first and last events in <code>model-logs/analyze.jsonl</code>.</p><table><thead><tr><th>Changes with the executor</th><th>Stays in the harness</th></tr></thead><tbody><tr><td>Provider event names</td><td>Phase name</td></tr><tr><td>Token accounting</td><td>Prompt and response requirements</td></tr><tr><td>Claude Code process or Strands <code>Agent</code></td><td>Required <code>{summary, files}</code> response</td></tr><tr><td>Tool event format</td><td>Write, check, limit, and commit rules</td></tr></tbody></table><p>Changing the executor must not change the pass condition.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The model identified content that can move to Vega</strong></div><dl><dt>Independent check</dt><dd>ANALYSIS.md exists and contains the required portability section</dd><dt>Evidence</dt><dd><code>out/workshop/app/ANALYSIS.md and model-logs/analyze.jsonl</code></dd><dt>Limit</dt><dd>This check does not prove that each portability conclusion is correct</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What did this phase prove?</summary><p>The phase proved that <code>ANALYSIS.md</code> has the required structure.\nThe phase did not prove that the analysis is complete.\nThe phase did not prove that each technical conclusion is correct.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>The one-call example changed no source files.\n<code>ANALYSIS.md</code> exists in the guarded copy.\nThe harness committed the passing phase.\nYou recorded three unverified claims.</aside>"},{"id":"plan","number":"02","nav":"Plan the TV port","time":"30 minutes","title":"Define and approve the TV behavior before you write code","lead":"Supply TV rules and current Vega documents. A structured plan is JSON with required fields. A schema checks those fields. A contract is a set of requirements that later phases must follow. Approve the contract before implementation.","objective":"Identify the knowledge that a phase needs. Review a typed screen and navigation contract before code changes.","evidence":"port-plan.json passes its schema. The human-approved hash is in port-plan-approval.json.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Supply the required knowledge</h2><p>The analyze phase read the app.\nThe plan phase defines the required TV behavior.\n\nThe phase must define initial focus, movement, selection, and Back behavior.\nThe phase must also use current Vega migration information.\nThe model does not have Vega knowledge by default.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Use one Vega knowledge source</h2><p>ADBT is a live MCP server.\nThe model uses ADBT tools to find and read Vega documents.\nThe harness records each document read and its SHA-256 hash.\n\nA skill is a Markdown instruction file that an agent can activate for a task.\nA skill can carry a team-specific instruction.\nThe skill does not replace ADBT for Vega knowledge.</p></section><h2>Inspect the phase definition</h2><p>Open the <code>plan</code> entry in <code>phases()</code> in <code>src/port-pipeline.ts</code>. A consumer is the component that reads and uses a field.</p><table><thead><tr><th>Field</th><th>Function</th><th>Consumer</th></tr></thead><tbody><tr><td><code>goal</code></td><td>Defines the required result</td><td>Model</td></tr><tr><td><code>instruction</code></td><td>Defines a phase rule</td><td>Model</td></tr><tr><td><code>skills</code></td><td>Identifies skill files</td><td>Executor and model</td></tr><tr><td><code>mcp</code></td><td>Identifies permitted MCP servers</td><td>Executor</td></tr><tr><td><code>checks</code></td><td>Defines the pass condition</td><td>Harness</td></tr></tbody></table><p>Keep these fields separate. Do not put verification logic in the prompt.</p><p>The plan phase writes two views of the same decision.</p><ul><li><code>port-plan.json</code> is the machine-checked contract.</li><li><code>VEGA_PORT.md</code> explains the plan to a person.</li></ul><p>The JSON contract contains screens, navigation edges, preserved behavior, deferred behavior, and evidence. A navigation edge describes movement from one screen or focus target to another. Preserved behavior must remain in the port. Deferred behavior is intentionally outside the current port. The JSON schema rejects these errors:</p><ul><li>A missing screen reference.</li><li>Missing Select behavior.</li><li>Missing Back behavior.</li><li>An initial focus target that is not focusable.</li><li>Preserved behavior without evidence.</li></ul><aside class=\"note \"><strong>The harness has no built-in Vega template</strong><br>The default phases use <code>skills: []</code>.\nThe default phases receive ADBT MCP.\nThe model must read a relevant document.\nManifest fields, dependencies, build commands, and runtime APIs come from the ADBT documents.</aside><h2>Trace Strands in the plan phase</h2><p>The plan phase adds the ADBT MCP client to the Strands agent. The default skill list for the plan phase is empty.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-executor.ts</figcaption><pre><code>const agent = new Agent({\n  model: createModel(this.config),\n  tools: [\n    ...createProjectReadTools(this.appDir),\n    ...(options.extraTools ?? []),\n  ],\n  structuredOutputSchema: outputSchema,\n  printer: false,\n});</code></pre><p class=\"look\"><strong>Where to look:</strong> <code>extraTools</code> contains the ADBT MCP connection. The model discovers Vega knowledge through ADBT tools.</p></figure><p>Provenance is a record of where information came from. ADBT provenance contains document names, excerpts, and hashes.</p><table><thead><tr><th>Owner</th><th>Plan action</th></tr></thead><tbody><tr><td>Strands</td><td>Lets the model discover ADBT tools. Lets the model call ADBT tools.</td></tr><tr><td>ADBT MCP</td><td>Supplies current Vega planning, focus, and migration documents.</td></tr><tr><td>Harness</td><td>Selects the permitted MCP server. Validates <code>port-plan.json</code>. Records ADBT provenance. Requires approval.</td></tr><tr><td>Evidence</td><td><code>port-plan.json</code>, <code>port-plan-approval.json</code>, and <code>adbt-port-context.json</code></td></tr></tbody></table><aside class=\"note \"><strong>Claude CLI path</strong><br>Claude Code receives the same exact ADBT server version through <code>--mcp-config</code>.\nThe plan schema and approval requirement do not change.</aside><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>Which ADBT documents define the focus behavior?\nWhat evidence proves that the model read the ADBT documents?</p></aside><h2>Run the plan phase</h2><p>Use the same run ID. The harness reuses the guarded copy and its Git history.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Run the plan phase in the existing <code>workshop</code> run.\nInspect the human-readable plan, the JSON contract, and the ADBT source record.</p></aside><div class=\"command\"><header><span>Run the plan phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases plan --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.\nThe harness supplies ADBT MCP to both live executors.</aside><h2>Inspect the plan</h2><p>An unsupported mapping is a proposed source-to-Vega mapping that has no supporting ADBT document.</p><ol class=\"tasks\"><li>Open <code>out/workshop/app/VEGA_PORT.md</code>.</li><li>Find the <code>## TV Flow</code> section.</li><li>Find the <code>## Focus</code> section.</li><li>Identify the initial focus target.</li><li>Identify the list-boundary behavior.</li><li>Identify the Back behavior.</li><li>Open <code>out/workshop/app/NextSteps.md</code>.</li><li>Find the ADBT sources.</li><li>Find each unsupported mapping.</li><li>Open <code>out/workshop/app/port-plan.json</code>.</li><li>Compare <code>screens</code> with <code>apps/pocket-cinema/workshop-brief.md</code>.</li><li>Verify that <code>navigation</code> contains Select.</li><li>Verify that <code>navigation</code> contains Back.</li><li>Verify that every <code>verification</code> item names a preserved behavior.</li><li>Read <code>deferredBehaviors</code>.</li><li>Read <code>openQuestions</code>.</li><li>Open <code>out/workshop/adbt-port-context.json</code>.</li><li>Find the document names.</li><li>Find the document hashes.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>How does the harness record model-selected documents?</summary><p>The harness reads the ADBT tool history after the phase.\nThe harness records each document name.\nThe harness records each excerpt.\nThe harness records each SHA-256 hash.</p></details></section><h2>Add a team skill</h2><p>Create one skill that contains a rule from your team. Then, add an independent check for the team rule.</p><p>A plugin is an SDK extension. Strands loads skills through its <code>AgentSkills</code> plugin. The <code>contains</code> check passes only when a named file contains the required text.</p><h2>Skill delivery</h2>\n    <table><thead><tr><th>Executor</th><th>Method</th></tr></thead><tbody><tr><td>Claude CLI</td><td><code>injectSkillText()</code> adds the complete skill body to the process prompt.</td></tr><tr><td>Strands</td><td><code>AgentSkills</code> registers each <code>Skill</code> as a plugin. The agent activates instructions with the <code>skills</code> tool.</td></tr></tbody></table>\n    <aside class=\"note \"><strong>Missing skill</strong><br><code>loadSkills()</code> reports and skips a missing skill. The run continues without that knowledge.</aside><ol class=\"tasks\"><li>Create <code>~/.claude/skills/team-open-questions</code>.</li><li>Create <code>SKILL.md</code> in that directory.</li><li>Add this instruction: <code>End each document with a ## Open Questions section.</code></li><li>Add <code>team-open-questions</code> to the plan phase <code>skills</code>.</li><li>Add a <code>contains</code> check for <code>## Open Questions</code>.</li><li>Run the live plan phase again.</li><li>Verify that the new section exists.</li></ol><p>The skill supplies the instruction. The check supplies the pass condition. Keep them in different files.</p><h2>Approve the structured plan</h2><p>Do not approve only because the schema passes. The schema can verify references and required fields. The schema cannot decide if the model selected the correct product flow.</p><ol class=\"tasks\"><li>Compare the screen count with the source app.</li><li>Compare the required home-to-details flow with <code>workshop-brief.md</code>.</li><li>Trace Select from the entry screen.</li><li>Trace Back to the originating focus target.</li><li>Reject invented Vega APIs.</li><li>Reject a verification item that can pass without its behavior.</li><li>Continue only when the plan is correct.</li></ol><div class=\"command\"><header><span>Approve the reviewed plan</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts approve-plan workshop --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;command&quot;: &quot;approve-plan&quot;\n&quot;planSha256&quot;: &quot;sha256:...&quot;</code></pre></div><p>The command writes <code>port-plan-approval.json</code>. The approval contains the exact plan hash and brief hash. The hashes bind the approval to the exact files that you reviewed. A stale approval has hashes that no longer match the plan or brief. The <code>port</code>, <code>build</code>, <code>launch</code>, and <code>test</code> phases refuse a missing approval. The phases also refuse a stale approval.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The plan defines the TV behavior, uses current Vega information, and has human approval</strong></div><dl><dt>Independent check</dt><dd>The typed plan validates. A person reviews the product decisions. The live run records the ADBT documents.</dd><dt>Evidence</dt><dd><code>port-plan.json, port-plan-approval.json, VEGA_PORT.md, NextSteps.md, and adbt-port-context.json</code></dd><dt>Limit</dt><dd>The plan does not prove that the proposed APIs compile</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br><code>port-plan.json</code> contains the screens, navigation, behavior, and evidence.\n<code>port-plan-approval.json</code> contains the plan and brief hashes.\n<code>VEGA_PORT.md</code> contains the focus model and TV flow.\n<code>adbt-port-context.json</code> contains the ADBT document records.\nYour team skill produces a section that an independent check requires.</aside>"},{"id":"port","number":"03","nav":"Write the port","time":"30 minutes","title":"Write the code and verify each requirement","lead":"The port phase changes the app. Nine independent checks control the phase commit.","objective":"Inspect the checks for a code change. Observe how exact failure text causes a useful retry.","evidence":"The guarded copy contains a Vega package and focus module. Git records the passing phase.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Use checks for code changes</h2><p>The first two phases produced documents.\nThe port phase produces code.\n\nThe phase creates a Vega package.\nThe phase creates a shared focus-state module.\nThe phase creates an executable focus test.\nAn executable test is a program that returns pass or fail.\n\nA wrong result can now change application behavior.\nRead the checks before you accept the result.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The verification engine is small</h2><p>The verification engine is the TypeScript code that runs the independent checks.\n<code>src/port-verification.ts</code> defines four check types:\n\n- <code>file_exists</code> checks that a required file exists.\n- <code>contains</code> checks that a file contains required text.\n- <code>json_schema</code> checks that JSON has the required fields and value types.\n- <code>command</code> runs a program and requires a successful exit.\n\n<code>verifyPort()</code> runs each check and collects the failure text.\nThe port phase has nine checks.\nThe checks cover the manifest, build scripts, app configuration, focus module, wiring, and test.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The model can write a Vega manifest that appears correct.\nA static check reads files without running the Vega compiler.\nWhich static checks can accept the manifest even if Vega rejects it?</p></aside><h2>Trace Strands in the port phase</h2><p>Strands returns a typed patch. The harness controls every operation that changes the guarded copy. One attempt is one model proposal followed by the phase checks.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-pipeline.ts (simplified)</figcaption><pre><code>const model = await options.executor.call(\n  phase.name,\n  prompt(phase, options, failures),\n  { mcp: phase.mcp, attempt },\n);\nconst output = parseJsonBlock(model.text, PortOutputSchema, phase.name);\nwriteOutput(options.appDir, output.files, phase.readOnly);\nfailures = await verify(phase, options, deviceMark, true, attempt);\nif (failures.length === 0) break;</code></pre><p class=\"look\"><strong>Where to look:</strong> The executor proposes files. The harness parses, writes, and verifies them.</p></figure><table><thead><tr><th>Owner</th><th>Port action</th></tr></thead><tbody><tr><td>Strands</td><td>Uses read-only project tools. Uses ADBT MCP. Returns <code>{summary, files}</code>.</td></tr><tr><td>ADBT MCP</td><td>Supplies the Vega migration, manifest, dependency, and build guidance selected by the model.</td></tr><tr><td>Harness</td><td>Rejects unsafe paths. Protects the approved plan. Records ADBT provenance. Writes complete files. Runs nine checks. Commits the result.</td></tr><tr><td>Evidence</td><td><code>port-result.json</code>, <code>model-logs/port.jsonl</code>, and the port commit</td></tr></tbody></table><aside class=\"note \"><strong>Claude CLI path</strong><br>The Claude CLI executor implements the same <code>PortExecutor.call()</code> interface.\nThis TypeScript interface requires each executor to support the same call operation.\nThe Claude CLI executor cannot bypass <code>writeOutput()</code> or the independent checks.</aside><h2>Run the port phase</h2><p>Use the same run ID and executor. The port phase checks <code>port-plan-approval.json</code> before it calls the model. The port phase refuses a missing approval. The port phase refuses a plan that changed after approval.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Run the code-writing phase against the approved plan.\nInspect the generated Vega package, shared focus code, and phase commit.</p></aside><div class=\"command\"><header><span>Run the port phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases port --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.\nThe port phase supplies ADBT MCP and requires a document read.</aside><aside class=\"note \"><strong>The plan is read-only</strong><br>The port model can read <code>port-plan.json</code>.\nThe port model cannot change <code>port-plan.json</code> or <code>port-plan-approval.json</code>.\n\nThe approved plan defines success.\nThe implementation cannot change the approved requirements.</aside><h2>Inspect the files and commit</h2><p><code>apps/vega</code> is the target Vega package directory. <code>manifest.toml</code> declares the Vega package. <code>package.json</code> declares JavaScript dependencies and scripts. <code>app.json</code> identifies the React Native app. <code>metro.config.js</code> configures the Metro JavaScript bundler.</p><ol class=\"tasks\"><li>Open <code>out/workshop/app/apps/vega/</code>.</li><li>Find <code>manifest.toml</code>.</li><li>Find <code>package.json</code>.</li><li>Find <code>app.json</code>.</li><li>Find <code>metro.config.js</code>.</li><li>Open <code>src/tv/focus-state.ts</code>.</li><li>Find the focus-state import in <code>src/App.tsx</code>.</li><li>Find the focus-state import in <code>tests/verify-tv-focus.ts</code>.</li><li>Run <code>git log --oneline</code> in the guarded copy.</li><li>Find the port-phase commit.</li></ol><p>The app and test use the same focus module. The shared module supports the Lesson 6 test.</p><h2>Inspect the recorded events</h2><p>The model transcript is the ordered record of requests, responses, tools, checks, and commits. The phase writes <code>out/workshop/model-logs/port.jsonl</code>. Each line contains one complete event. An event has a type, sequence number, and payload. The payload is the event content. The sequence number records event order.</p><div class=\"command\"><header><span>Read the port transcript</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts logs workshop --phase port</code></pre></div><ol class=\"tasks\"><li>Find the <code>request</code> event.</li><li>Read the complete phase prompt.</li><li>Find the model response events.</li><li>Find <code>verification_result</code>.</li><li>Find <code>commit</code>.</li><li>Find <code>phase_complete</code>.</li><li>Verify that the <code>sequence</code> values increase.</li></ol><p>Strands and Claude Code use different native event names. Native means that the event keeps the original executor format. The transcript keeps the native event payloads.</p><p>Use <code>--follow</code> to read a live phase in a second terminal. The option waits for new events and prints them as they arrive.</p><p>The transcript can contain prompts, source text, and tool results. Keep the transcript in the ignored <code>out/</code> directory. Review the transcript before you share the transcript.</p><h2>Know the retry limits</h2><p>When a check fails, the next model request contains the exact failure. The default permits one retry. <code>--max-attempts N</code> sets a different limit. Replace <code>N</code> with a positive number. <code>--until-done</code> removes the attempt limit.</p><p>A configured cumulative token limit still applies. The harness also stops after the same failure occurs twice. The repeated failure is the no-progress stop condition. The model cannot decide to continue the loop.</p><h2>Add one requirement</h2><p>Add a requirement for focus restoration documentation. A mechanical requirement has a check that software can run without human judgment.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Add one mechanical requirement to the verification code.\nProve that the requirement rejects the starting app and accepts the prepared port.</p></aside><ol class=\"tasks\"><li>Open <code>tvReadyChecks()</code> in <code>src/port-verification.ts</code>.</li><li>Add a <code>contains</code> check for <code>TV_VERIFICATION.md</code>.</li><li>Use <code>originating card</code> as the required value.</li><li>Use <code>Focus restoration documented</code> as the label.</li><li>Run <code>tv-check</code> on <code>apps/pocket-cinema</code>.</li><li>Verify that the new check fails.</li><li>Run <code>tv-check</code> on <code>workshop/checkpoints/vega-buildable/app</code>.</li><li>Verify that the new check passes.</li><li>Add one mechanical requirement from your project.</li></ol><div class=\"command\"><header><span>Run the same checks on two apps</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts tv-check ../../apps/pocket-cinema\nyarn tsx src/index.ts tv-check ../../workshop/checkpoints/vega-buildable/app</code></pre></div><p>Use the strongest applicable check type:</p><table><thead><tr><th>Check type</th><th>What it proves</th></tr></thead><tbody><tr><td><code>file_exists</code></td><td>A file exists</td></tr><tr><td><code>contains</code></td><td>A required decision is present</td></tr><tr><td><code>json_schema</code></td><td>Structured data has the required shape</td></tr><tr><td><code>command</code></td><td>A program completed successfully</td></tr></tbody></table><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does the model not verify its own work?</summary><p>A model report is another generated claim.\nAn independent check runs without model agreement.\nThe independent check gives the same result for the same files.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The port code satisfies the declared requirements</strong></div><dl><dt>Independent check</dt><dd>Nine static, schema, and executable checks pass before the commit</dd><dt>Evidence</dt><dd><code>port-result.json, model-logs/port.jsonl, and git log</code></dd><dt>Limit</dt><dd>Static checks can accept a manifest that the Vega compiler rejects</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>The guarded copy contains the Vega package and focus module.\nNine checks pass.\nGit contains the port-phase commit.\nYour added rule fails on the starter app and passes on the ported app.</aside>"},{"id":"build","number":"04","nav":"Build until it compiles","time":"30 minutes","title":"Use the compiler as an independent check","lead":"Run a real Vega build. A compiler diagnostic is the exact error reported during compilation. A .vpkg file is an installable Vega app package.","objective":"Use a produced package as the pass condition. Trace compiler output into the repair request.","evidence":"A .vpkg file and a phase result that records the repaired compiler failure.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Use an executable check</h2><p>The earlier phases can pass when files contain the required text.\nA compiler uses stronger evidence.\nA compiler converts source code into an installable package.\nAn executable check runs a program and uses its success or failure as evidence.\n\nThe build either produces a <code>.vpkg</code> file or it does not.\nIf the build fails, the compiler identifies the cause.\nThe harness sends this exact diagnostic to the model.</p></section><aside class=\"note warning\"><strong>Vega SDK is necessary</strong><br>This lesson requires Vega SDK version <code>0.23.9221</code>.\nLesson 5 requires an installed VDA.\nThe harness starts the VDA when necessary.\n\nIf the SDK is not available, inspect:\n\n<code>workshop/checkpoints/complete/live-build-result.json</code>\n\nThis checkpoint does not prove a build on your computer.</aside><h2>Know the build loop</h2><div class=\"flow\"><div><b>Verify</b><span>Run the build first</span></div><i>→</i><div><b>Fail</b><span>Keep compiler output</span></div><i>→</i><div><b>Prompt</b><span>Add exact diagnostics</span></div><i>→</i><div><b>Patch</b><span>Apply a typed repair</span></div><i>→</i><div><b>Verify</b><span>Build again</span></div></div><p>Before each live build attempt, the harness runs <code>npm install --include=dev</code> inside the guarded <code>apps/vega</code> package. The command installs the declared development dependencies and Vega build command. The command installs dependency changes from a repair. If installation fails, the harness does not start the compiler.</p><p>Dependency installation and the build process each have a 15-minute time limit. The harness limits the amount of build output in the prompt. The harness keeps the start and end of long output.</p><p>The phase runs the build before it calls the model. If the build passes, the phase does not call the model. If the build fails, the repair call receives ADBT MCP as well as the compiler diagnostic. The model must read an ADBT document before proposing a live repair.</p><h2>Trace Strands in the build phase</h2><p>The harness runs the compiler first. Strands receives a prompt only when the compiler reports a failure. A verify-first phase runs its independent check before it considers a model call.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-pipeline.ts (simplified)</figcaption><pre><code>if (phase.verifyFirst) {\n  failures = await verify(phase, options, deviceMark, false, 0);\n}\nif (failures.length) {\n  report(options, `${phase.name} needs a fix`, failures);\n}\nconst model = await options.executor.call(\n  phase.name,\n  prompt(phase, options, failures),\n  { mcp: phase.mcp, attempt },\n);</code></pre><p class=\"look\"><strong>Where to look:</strong> <code>prompt()</code> includes the exact compiler failure. <code>mcp</code> gives the repair access to current ADBT documents. A passing pre-check skips the model call.</p></figure><p>A Hermes bundle is the compiled JavaScript code included in the Vega package. Fresh means that the current build created the file.</p><table><thead><tr><th>Owner</th><th>Build action</th></tr></thead><tbody><tr><td>Strands</td><td>Receives the compiler diagnostic. Reads relevant ADBT guidance. Proposes a typed repair.</td></tr><tr><td>ADBT MCP</td><td>Supplies current Vega build and package guidance to the repair model.</td></tr><tr><td>Harness and Vega CLI</td><td>Install declared dependencies. Run the build. Require a fresh Hermes bundle and <code>.vpkg</code>. Apply the repair. Build again.</td></tr><tr><td>Evidence</td><td>Compiler output, <code>.vpkg</code>, <code>vega-platform-result.json</code>, and the build commit</td></tr></tbody></table><aside class=\"note \"><strong>No model call is a valid result</strong><br>If the build passes before repair, Strands does not run.\nThe Claude CLI path follows the same verify-first rule.</aside><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The compiler reports <code>Type 'number' is not assignable to type 'string'</code>.\nWhat text must the model receive?\nWhy is <code>The build failed</code> insufficient?</p></aside><h2>Add the known build failure</h2><p>The injector is a workshop command that adds one controlled TypeScript error. The injector changes only <code>out/workshop/app</code>. The injector does not change <code>apps/pocket-cinema</code>.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Add the controlled compiler error to the guarded copy.\nRun the build phase and watch the harness send the exact diagnostic to the repair model.</p></aside><div class=\"command\"><header><span>Add the workshop build failure</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts inject-build-failure workshop --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;expectedDiagnostic&quot;:&quot;Type 'number' is not assignable to type 'string'&quot;</code></pre></div><h2>Run the live repair</h2><p>Use the same executor that you used in the earlier lessons.</p><div class=\"command\"><header><span>Run the build phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases build --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.</aside><div class=\"expected\"><strong>Find this evidence</strong><pre><code>build needs a fix:\n  - build failed: react-native build-vega exited with code 2\nsrc/workshop-build-break.ts(2,14): error TS2322: Type 'number' is not assignable to type 'string'.</code></pre></div><p><code>TS2322</code> identifies a TypeScript type-assignment error.</p><h2>Inspect the repair evidence</h2><p>The <code>--porcelain</code> Git option prints a compact working-tree status. Empty output means that no uncommitted file change remains.</p><ol class=\"tasks\"><li>Find the first build failure in the terminal.</li><li>Open <code>out/workshop/model-logs/build.jsonl</code>.</li><li>Find the failed <code>verification_result</code>.</li><li>Find the next model request.</li><li>Verify that the request contains the compiler diagnostic.</li><li>Find the ADBT document read in the model events.</li><li>Open <code>out/workshop/port-result.json</code>.</li><li>Find the recorded failed attempt.</li><li>Find the passing attempt and its token and turn usage.</li><li>Run <code>git -C ../../out/workshop/app status --porcelain</code>.</li><li>Verify that the result is empty.</li><li>Find the <code>.vpkg</code> file in <code>out/workshop/app/apps/vega/build/</code>.</li><li>Find the build-phase commit.</li></ol><aside class=\"note \"><strong>The command creates a repeatable failure</strong><br><code>inject-build-failure</code> adds one invalid TypeScript file.\n<code>inject-build-failure</code> adds one import to <code>src/App.tsx</code>.\nThe command commits these changes only in the guarded copy.\n\nThe build phase requires the file and import to be absent.\nThe normal Vega build must also pass.\nEach live provider receives the same compiler failure.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does the phase verify before the model call?</summary><p>Most passing builds do not require a repair.\nA model call before the build would spend money without a failure.\nThe compiler diagnostic gives the model a specific repair target.</p></details></section><p>For this lesson, <code>evidenceMode: live</code> means that the current Vega compiler produced the evidence.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The Vega app builds</strong></div><dl><dt>Independent check</dt><dd>The Vega build exits successfully and produces a .vpkg file</dd><dt>Evidence</dt><dd><code>vega-platform-result.json and apps/vega/build/*.vpkg</code></dd><dt>Limit</dt><dd>A package can compile and fail after start</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>For a live run, the build directory contains a <code>.vpkg</code> file.\n<code>vega-platform-result.json</code> contains <code>evidenceMode: live</code>.\nThe Git working tree is clean.</aside>"},{"id":"launch","number":"05","nav":"Run it on the device","time":"30 minutes","title":"Start the app and verify that it stays active","lead":"A successful start command proves only that the device accepted the command. A dwell is a fixed wait after start. A crash signature is known failure text in a device log.","objective":"Use two running-state samples and the device log to distinguish an accepted command from a process that stays active.","evidence":"The app reports running after launch and after the dwell. The device log contains no crash signature.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Collect evidence after the start</h2><p><code>launch-app .. success</code> means that the device accepted the command.\nThe success message does not mean that the app stayed active.\n\nAn app can fail during its first render.\n\nThis phase waits five seconds.\nThe phase samples the running state before the wait.\nThe phase samples the running state after the wait.\nThe phase reads the device log.\nThe phase scans the device log.\nThe phase reuses an attached VDA or starts a VDA before installation.</p></section><aside class=\"note warning\"><strong>A VDA installation is necessary</strong><br>The harness checks for an attached VDA.\nIf no VDA is attached, the harness runs <code>vega virtual-device start --gui --timeout 60</code>\nand checks the device list again.\n\nThe VDA in Vega SDK <code>0.23.9221</code> does not support the device screenshot command.\nThe harness does not call the screenshot command.</aside><h2>Know the device sequence</h2><p>The <code>.vpkg</code> file from Lesson 4 is the installable Vega package. The app ID is the unique name that Vega uses to start the installed app. The package ID identifies the app in device logs.</p><div class=\"flow\"><div><b>VDA</b><span>Reuse an attached VDA or start one</span></div><i>→</i><div><b>Install</b><span>Transfer the .vpkg package</span></div><i>→</i><div><b>Start</b><span>Start the app ID</span></div><i>→</i><div><b>State</b><span>Confirm that the app reports running</span></div><i>→</i><div><b>Wait</b><span>Wait five seconds</span></div><i>→</i><div><b>Logs</b><span>Find crash signatures</span></div><i>→</i><div><b>State</b><span>Confirm that the app still reports running</span></div></div><p>The log query reads entries for this package. The log query starts at the app start time.</p><p>The log scan rejects these signatures:</p><ul><li><code>FATAL</code> reports a fatal runtime error.</li><li><code>SIGSEGV</code> reports invalid memory access.</li><li><code>has died</code> reports that the app process stopped.</li><li>ANR means Application Not Responding.</li><li>An unhandled JavaScript exception reports a JavaScript error that the app did not catch.</li></ul><p>Liveness means that the app process continues to run. The second state sample is liveness evidence. An app that stopped during the dwell reports that it is not running.</p><h2>Trace Strands in the launch phase</h2><p>The Vega adapter owns the platform commands. The Vega adapter is the TypeScript component that runs and records Vega CLI commands. A platform command builds, installs, starts, or inspects the app on Vega. The launch phase calls Strands only when this sequence finds a repairable failure. A repairable failure is a source-code problem that a new patch can correct. The repair call receives ADBT MCP. The model must read current Vega guidance. <code>stdout</code> is normal command output. <code>stderr</code> is command error output.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/platform/vega.ts (simplified)</figcaption><pre><code>let devices = await probe(device, &quot;device_status&quot;);\nif (!hasAttachedDevice(devices.stdout)) {\n  await run(device, &quot;vda_start&quot;);\n  devices = await run(device, &quot;device_status&quot;);\n}\nawait run(device, &quot;install&quot;, device.packagePath);\ndevice.launchStartedAt = formatLoggingctlSince(new Date());\nawait run(device, &quot;launch&quot;, device.appId);\nawait recordRunningState(device, &quot;app is running after launch&quot;);\nawait dwell();\nconst logs = await run(device, &quot;logs&quot;, packageId, device.launchStartedAt);\nconst scan = scanDeviceLog(logs.stdout || logs.stderr);\nawait recordRunningState(device, &quot;app remains running after dwell&quot;, logs.stdout);\nif (scan.crashed) {\n  device.blockers.push(`the app crashed after launch: ${scan.matches.join(&quot; | &quot;)}`);\n}</code></pre><p class=\"look\"><strong>Where to look:</strong> The snippet shows harness-controlled Vega CLI operations. The operations are not Strands tools.</p></figure><p>The <code>dwell()</code> operation performs the five-second wait.</p><table><thead><tr><th>Owner</th><th>Launch action</th></tr></thead><tbody><tr><td>Strands</td><td>Receives a failed platform check. Reads relevant ADBT guidance. Proposes a source repair.</td></tr><tr><td>ADBT MCP</td><td>Supplies current Vega device and runtime guidance to the repair model.</td></tr><tr><td>Harness and Vega CLI</td><td>Start or reuse the VDA. Build the app. Install the app. Start the app. Wait five seconds. Read logs. Query the running state twice.</td></tr><tr><td>Evidence</td><td><code>vega-device.log</code>, <code>vega-platform-result.json</code>, and the launch commit</td></tr></tbody></table><aside class=\"note \"><strong>No model call is a valid result</strong><br>If the package starts and stays active, the phase records evidence without\ncalling Strands or Claude Code.\nIf no repair is necessary, the phase does not call ADBT.\nThis verify-first result is valid because the device checks already passed.</aside><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The start command succeeds and the app fails two seconds later.\nWhich evidence identifies the failure?</p></aside><h2>Run the launch phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Start the app on the VDA.\nInspect the two state samples.\nInspect the filtered log.</p></aside><p>Use the same run ID. The phase installs the package from Lesson 4.</p><div class=\"command\"><header><span>Install and start the app</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases launch --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.</aside><h2>Inspect the device evidence</h2><p><code>vega-device.log</code> contains device log entries from this launch. <code>vega-platform-result.json</code> contains every platform step and its pass or fail result.</p><ol class=\"tasks\"><li>Open <code>out/workshop/vega-device.log</code>.</li><li>Find the entries after the app start time.</li><li>Open <code>out/workshop/vega-platform-result.json</code>.</li><li>Find the initial <code>device_status</code> step.</li><li>If the initial <code>device_status</code> found no device, find <code>vda_start</code> and the later <code>device_status</code> steps.</li><li>Find <code>app is running after launch</code>.</li><li>Verify that the check passed.</li><li>Find the log-scan result.</li><li>Verify that no crash signature was found.</li><li>Find <code>app remains running after dwell</code>.</li><li>Verify that the check passed.</li><li>If a repair ran, find the ADBT document read in <code>model-logs/launch.jsonl</code>.</li></ol><aside class=\"note \"><strong>A source repair requires a new build</strong><br>A source change does not change the installed package.\nThe phase rebuilds after a source repair.\nThe phase installs the new package after the build.\n\nThe first verification attempt does not rebuild.\nNo source changed after Lesson 4.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why are two state samples necessary?</summary><p>The first sample proves that the process was active after the start command.\nThe device log and second sample add evidence across the dwell.\nThe samples and log do not prove correct rendering or focus behavior.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The app started and stayed active</strong></div><dl><dt>Independent check</dt><dd>Install and start pass. Both running-state samples pass. The log has no crash signature.</dd><dt>Evidence</dt><dd><code>vega-platform-result.json and vega-device.log</code></dd><dt>Limit</dt><dd>Process and log evidence does not prove visual rendering or correct focus movement</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>For a live run, both running-state samples pass.\nThe device log contains no crash signature.\n<code>vega-platform-result.json</code> contains <code>evidenceMode: live</code>.</aside>"},{"id":"test","number":"06","nav":"Test the remote","time":"25 minutes","title":"Verify the complete focus sequence","lead":"The app is active. A focus transition is the change after one remote key. Lifecycle evidence records that the app started and remained active. Now verify the complete remote sequence.","objective":"Express TV navigation as observable state transitions. Do not use visual impressions as the only evidence.","evidence":"tv-focus-result.json contains all required focus transitions. Lesson 5 records separate lifecycle evidence.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Test behavior over time</h2><p>One observation cannot show the complete focus sequence.\nThe screen can appear correct when focus returns to the wrong card.\n\nThis phase starts the app again.\nThis phase sends remote keys to the VDA.\nThe phase reads the focused element after each key.\nThe phase does not use pixels as focus evidence.</p></section><h2>Know the focus contract</h2><p>The focus contract lists the required focused control after each remote action.</p><div class=\"remote\" aria-label=\"TV remote direction pad\"><button>↑</button><button>←</button><button class=\"ok\">OK</button><button>→</button><button>↓</button></div><table><thead><tr><th>Action</th><th>Required result</th></tr></thead><tbody><tr><td>Start</td><td>The featured action has focus</td></tr><tr><td>Down</td><td>Focus moves to the first rail</td></tr><tr><td>Left or right</td><td>Focus stops at the list boundaries</td></tr><tr><td>Select</td><td>The details screen opens for the focused card</td></tr><tr><td>Back</td><td>Focus returns to the same card</td></tr></tbody></table><div class=\"flow\"><div><b>Start</b><span>Focus the featured action</span></div><i>→</i><div><b>Down</b><span>Enter the first rail</span></div><i>→</i><div><b>Select</b><span>Open the focused details</span></div><i>→</i><div><b>Back</b><span>Restore the same card</span></div></div><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>Which transition can fail when the screen appears correct?</p></aside><p>Automation Toolkit is a VDA service that lets the harness inspect the app interface. JSON-RPC is a structured request-and-response format. The Automation Toolkit JSON-RPC endpoint accepts requests from the harness. <code>getPageSource</code> returns the user-interface hierarchy. The hierarchy is a tree of controls, and each control is a node. A React Native <code>testID</code> is exposed in that hierarchy as <code>test_id</code>.</p><p>D-pad means the directional pad on a TV remote. Key injection means that the harness sends a remote key through software. The <code>inputd-cli</code> command injects keys into the VDA.</p><h2>Follow the live verification loop</h2><p>The approved <code>port-plan.json</code> supplies the expected focus IDs. A focus ID is the stable name of one focusable control. An assertion compares the observed focus ID with the expected focus ID. The harness controls the key sequence and the assertions. The model cannot replace the key sequence or the assertions.</p><div class=\"flow\"><div><b>Plan</b><span>Read the approved focus IDs</span></div><i>→</i><div><b>Launch</b><span>Start from the Home screen</span></div><i>→</i><div><b>Observe</b><span>Read the focused test_id</span></div><i>→</i><div><b>Input</b><span>Send one remote key</span></div><i>→</i><div><b>Assert</b><span>Compare observed and expected focus</span></div></div><table><thead><tr><th>Order</th><th>Harness action</th><th>Live Vega evidence</th></tr></thead><tbody><tr><td>1</td><td>Reuse an attached VDA or start one</td><td>The device remains attached</td></tr><tr><td>2</td><td>Install the package. Start the package again.</td><td>The app is running before and after the dwell</td></tr><tr><td>3</td><td>Enable Automation Toolkit</td><td>The JSON-RPC endpoint accepts <code>getPageSource</code></td></tr><tr><td>4</td><td>Find the node where <code>focused</code> is <code>true</code></td><td>The node exposes a stable React Native <code>testID</code> as <code>test_id</code></td></tr><tr><td>5</td><td>Send <code>KEY_DOWN</code>, <code>KEY_LEFT</code>, <code>KEY_RIGHT</code>, <code>KEY_ENTER</code>, and <code>KEY_BACK</code></td><td><code>inputd-cli</code> accepts each key</td></tr><tr><td>6</td><td>Read focus after every key</td><td>Each observation matches the approved focus ID</td></tr></tbody></table><p>The right-boundary check sends Right until focus stops moving. The harness sends Right one more time. The harness makes sure that focus remains on the final card. After Select opens details, Back must restore that same card.</p><aside class=\"note \"><strong>Automation Toolkit can start after the VDA</strong><br>The VDA can report that it is ready before the Automation Toolkit endpoint\naccepts requests. The harness enables the toolkit.\nThe harness polls <code>getPageSource</code> for a specified time.\nPolling means sending the request again until it succeeds or the time limit ends.</aside><aside class=\"note \"><strong>Fresh device evidence</strong><br>The test phase deletes an earlier <code>tv-focus-result.json</code> before the test starts.\nA stale passing file cannot satisfy a new run.</aside><h2>Trace Strands in the test phase</h2><p>The test phase starts with independent focus and device checks. Strands runs only when the independent checks identify a source repair.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-pipeline.ts (simplified)</figcaption><pre><code>{\n  name: &quot;test&quot;,\n  skills: [],\n  mcp: [ADBT_SERVER],\n  device: [&quot;launch&quot;, &quot;focus&quot;],\n  repairDevice: [&quot;build&quot;, &quot;launch&quot;, &quot;focus&quot;],\n  verifyFirst: true,\n  maxAttempts: 3,\n  checks: [FOCUS_TEST_CHECK, restorationCheck],\n}</code></pre><p class=\"look\"><strong>Where to look:</strong> After a repair, the harness rebuilds the app. The harness starts the app before it accepts the focus result.</p></figure><table><thead><tr><th>Owner</th><th>Test action</th></tr></thead><tbody><tr><td>Strands</td><td>Reads relevant ADBT documents. Proposes a typed repair after failed evidence.</td></tr><tr><td>ADBT MCP</td><td>Supplies current Vega focus guidance.</td></tr><tr><td>Harness and Vega CLI</td><td>Start the app again. Inject D-pad keys. Read focused <code>test_id</code> values. Repeat device checks after a repair.</td></tr><tr><td>Evidence</td><td><code>tv-focus-result.json</code>, <code>TV_VERIFICATION.md</code>, device evidence, and the test commit</td></tr></tbody></table><aside class=\"note \"><strong>No model call is a valid result</strong><br>If all focus and device checks pass, the phase does not call Strands or Claude\nCode.</aside><h2>Run the test phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Run the focus contract.\nInspect the transition sequence.\nInspect the Back restoration.</p></aside><p>Use the same run ID. Host-side means that the check runs on your computer without reading the VDA. A known state means that the app starts from the Home screen before key input. First, the phase runs the host-side focus-state contract. Then, the phase starts the app in a known state. The phase tests the approved plan on the VDA. Run the command from <code>packages/workshop-harness</code>.</p><p>This first contract checks the shared focus-state module.</p><aside class=\"note \"><strong>A final repair must rebuild and start again</strong><br>The first test attempt starts the Lesson 5 package again.\nThe first test attempt does not rebuild the package.\n\nIf the model changes source code after a failed test, the harness does not\naccept only the host-side focus contract.\nThe harness runs this sequence:\n\n<code>build -&gt; install -&gt; launch -&gt; state -&gt; log scan -&gt; state -&gt; keys -&gt; focused test_id</code>\n\nA late focus repair can introduce a compile or startup failure.\nThe test phase passes only after the repaired app compiles and stays active.</aside><div class=\"command\"><header><span>Run the focus test</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases test --yes --run-id workshop</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;phasesComplete&quot;:[&quot;analyze&quot;,&quot;plan&quot;,&quot;port&quot;,&quot;build&quot;,&quot;launch&quot;,&quot;test&quot;]</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.</aside><h2>Inspect the focus evidence</h2><p>From the repository root, open <code>out/workshop/app/tv-focus-result.json</code>. From the harness directory, the same file is <code>../../out/workshop/app/tv-focus-result.json</code>. Read these fields before you inspect the generated code: A transition record contains one key, one expected focus ID, and one observed focus ID. The focus-failure fixture is a prepared example of an incorrect Back transition.</p><table><thead><tr><th>Field</th><th>Evidence meaning</th></tr></thead><tbody><tr><td><code>evidenceMode: &quot;live&quot;</code></td><td>The VDA supplied the observations</td></tr><tr><td><code>passed: true</code></td><td>Every required transition passed in this run</td></tr><tr><td><code>transitions</code></td><td>The VDA observations include the required transition names</td></tr><tr><td><code>observations[].key</code></td><td>The harness sent this remote key</td></tr><tr><td><code>observations[].expected</code></td><td>The focus ID from the approved plan</td></tr><tr><td><code>observations[].observed</code></td><td>The VDA reported this focused <code>test_id</code></td></tr></tbody></table><p>Look for this sequence:</p><figure class=\"snippet\"><figcaption>tv-focus-result.json (abbreviated)</figcaption><pre><code>KEY_DOWN  -&gt; rail-new-card-signal\nKEY_LEFT  -&gt; rail-new-card-signal\nKEY_RIGHT -&gt; each card, then the final card again\nKEY_ENTER -&gt; back-button\nKEY_BACK  -&gt; the card that opened details</code></pre><p class=\"look\"><strong>Where to look:</strong> Expected and observed must match on every observation.</p></figure><ol class=\"tasks\"><li>Open <code>out/workshop/app/tv-focus-result.json</code>.</li><li>Read the transitions in sequence.</li><li>Verify that all six transitions are present.</li><li>Find the <code>back-restore</code> transition.</li><li>Open <code>workshop/fixtures/focus-failure/README.md</code>.</li><li>Read the failed Back example.</li><li>Open the focus-state module in the guarded app.</li><li>Find the focus-restoration code.</li><li>Run <code>git -C ../../out/workshop/app status --porcelain</code>.</li><li>Verify that the result is empty.</li><li>Find the test-phase commit.</li></ol><aside class=\"note \"><strong>Know the focus API</strong><br><code>hasTVPreferredFocus</code> defines the preferred initial focus.\n<code>onFocus</code> and <code>onBlur</code> report focus changes.\n\nThe workshop uses the React Native TV focus model.\nThe target platform is Vega.</aside><aside class=\"note \"><strong>Know the device evidence</strong><br>The harness sends keys with <code>inputd-cli</code>.\nThe harness reads the UI hierarchy through Automation Toolkit <code>getPageSource</code>.\n\nEvery focusable React Native control needs a stable <code>testID</code>.\nThe result proves focus movement and selection, not visual focus styling.</aside><h2>Compare the starting app and ported app</h2><p>Lesson 00 ran <code>tv-check</code> on the starting app. The result was <code>tvReady: false</code>.</p><p>Run the same check on the ported app:</p><div class=\"command\"><header><span>Run the final TV-readiness check</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts tv-check ../../out/workshop/app</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: true\n&quot;failures&quot;: []</code></pre></div><p>The same check now passes. The phase result is the workshop before-and-after evidence.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is Back behavior necessary?</summary><p>Back must restore the user's previous navigation position.\nWithout restoration, the user loses context.\nThe screen can look correct while the interaction is incorrect.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>A remote user can complete the TV flow</strong></div><dl><dt>Independent check</dt><dd>The focus test observes start, movement, selection, boundaries, and Back restoration</dd><dt>Evidence</dt><dd><code>tv-focus-result.json and tv-check with tvReady:true</code></dd><dt>Limit</dt><dd>The UI hierarchy proves focus state, not visual styling</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>The focus test passes all transitions.\n<code>tv-focus-result.json</code> records the sequence.\n<code>tv-check</code> reports <code>tvReady: true</code>.\n\nLive Lesson 5 evidence proves process stability on your device.\nIt does not prove visual rendering.</aside>"},{"id":"finish","number":"07","nav":"Build your own","time":"30 minutes","title":"Control the complete pipeline and design a new harness","lead":"Use the terminal user interface (TUI) to inspect one complete run. The TUI is an interactive dashboard in your terminal. Then design and test a small harness for another engineering task.","objective":"Read a complete run as one system. Design a harness with phases, independent checks, limits, and evidence.","evidence":"A reviewed six-phase run and a team worksheet with an improved check.","body":"<div class=\"takeaway\"><code>plan -> context -> run -> check -> retry -> checkpoint -> report</code></div><section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Use the complete system</h2><p>The earlier lessons presented one control at a time.\nThis lesson presents the complete pipeline.\n\nFirst, use the TUI to inspect all six phases.\nThen, design a harness for another engineering task.\n\nTV and Vega are the example.\nThe controlled pipeline is the reusable result.</p></section><h2>Run the complete pipeline</h2><p>The TUI displays the existing pipeline state. The TUI does not run a second pipeline. The TUI does not replace the JSONL transcripts. An interactive terminal accepts keyboard input while a command runs. The <code>--tui</code> flag opens the dashboard only in an interactive terminal. Run the commands in order. Wait for each command before you continue to the next command.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Run the complete six-phase pipeline with the TUI.\nOpen the messages for each phase and find the strongest evidence.</p></aside><div class=\"command\"><header><span>Prepare and run the complete approved pipeline</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases analyze,plan --seed workshop-v1 \\\n  --max-tokens 1000000 --yes --run-id final-dashboard --tui\nyarn tsx src/index.ts approve-plan final-dashboard --yes\nyarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases port,build,launch,test \\\n  --seed workshop-v1 --max-tokens 1000000 --yes \\\n  --run-id final-dashboard --tui</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.\nDo not change the run ID, seed, token limit, or phase controls.</aside><p>The first dashboard stops after plan. When the dashboard reports that plan approval is required, close the dashboard. Press <code>q</code> to close the dashboard. Review the plan. Approve the plan.</p><p>The second dashboard loads the earlier analyze and plan events. The second dashboard then follows port, build, launch, and test.</p><p>A model call is one request from the harness to a model. The TUI header shows the run ID, executor, provider, model, seed, token usage, turns, and model calls. <code>evidence live</code> means that the current model and Vega device produced the evidence.</p><table><thead><tr><th>Key</th><th>Function</th></tr></thead><tbody><tr><td>Up or Down</td><td>Select a phase</td></tr><tr><td><code>Enter</code></td><td>Open the selected phase's messages</td></tr><tr><td>Up or Down in messages</td><td>Select an event</td></tr><tr><td><code>PageUp</code> or <code>PageDown</code></td><td>Scroll the selected event's content</td></tr><tr><td><code>Tab</code> in messages</td><td>Select checks, model events, tools, or all events</td></tr><tr><td><code>Escape</code></td><td>Return to the phase list</td></tr><tr><td><code>f</code></td><td>Select the active phase</td></tr><tr><td><code>q</code></td><td>Close the completed TUI</td></tr></tbody></table><h2>Inspect the TUI</h2><ol class=\"tasks\"><li>Read the executor in the header.</li><li>Read the provider in the header.</li><li>Read the model in the header.</li><li>Verify that the header reports <code>evidence live</code>.</li><li>Read the seed.</li><li>Read the token limit.</li><li>Read the turns value.</li><li>Read the calls value.</li><li>Select the <code>plan</code> phase.</li><li>Press <code>Enter</code>.</li><li>Read the type of one model event.</li><li>Read the content of the model event.</li><li>Press <code>Tab</code> to select the tools filter.</li><li>Find the ADBT document operations.</li><li>Press <code>Escape</code> to return to the phase list.</li><li>Open the <code>port</code> messages.</li><li>Select the checks filter.</li><li>Find one independent check.</li><li>Find one commit event.</li><li>Return to the phase list.</li><li>Open the <code>build</code> messages.</li><li>Find the compiler evidence.</li><li>Return to the phase list.</li><li>Open the <code>launch</code> messages.</li><li>Find the device evidence.</li><li>Press <code>Escape</code>.</li><li>Press <code>q</code> to close the completed TUI.</li><li>Open <code>out/final-dashboard/model-logs/</code>.</li></ol><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The TUI controls the view</h2><p><code>src/tui.ts</code> reads the existing run state.\nThe TUI opens each phase as a list of events with named types.\nThe JSONL files keep the complete event data.\n\nYou can use filters to reduce the visible detail.\nThe harness does not delete evidence.</p></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does the workshop add the TUI at the end?</summary><p>You must first know the meaning of each signal.\nThe TUI is useful after you understand checks, retries, usage, tools, and device evidence.</p></details></section><h2>Complete the evidence table</h2><table><thead><tr><th>Claim</th><th>Strongest evidence</th></tr></thead><tbody><tr><td>The model inspected the app</td><td>Source inventory and explicit unknowns</td></tr><tr><td>The plan used Vega knowledge</td><td>Live ADBT operations and document hashes</td></tr><tr><td>The code met the requirements</td><td>Independent checks and a phase commit</td></tr><tr><td>The package compiled</td><td>Vega build and <code>.vpkg</code> file</td></tr><tr><td>The app stayed active</td><td>Wait period, device log, and two running-state samples</td></tr><tr><td>The TV flow worked</td><td>Executable focus-transition result</td></tr></tbody></table><p>The evidence becomes stronger near the user behavior. The focus-transition result has a limit. The focus-transition result does not prove visual focus styling.</p><h2>Complete the team exercise</h2><p>Form a team of two or three people. A non-goal is work that the harness must not do. A false positive occurs when a check passes an incorrect result. A prototype demonstrates a design but is not ready for production use. Select one task:</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Design a small harness for one task.\nAsk another team to find a false positive in your checks.</p></aside><div class=\"grid\"><article><h3>Gradle upgrade</h3><p>Upgrade one Android module. Keep a passing debug build.</p></article><article><h3>Accessibility repair</h3><p>Repair labels and focus order on one screen.</p></article><article><h3>API migration</h3><p>Replace one deprecated client API. Do not change server behavior.</p></article><article><h3>Flaky test repair</h3><p>Repair one test. Prove that repeated runs pass.</p></article></div><ol class=\"tasks\"><li>During minutes 0 to 2, write one required result.</li><li>Write three non-goals.</li><li>During minutes 2 to 5, define no more than three phases.</li><li>Give each phase one independent check.</li><li>During minutes 5 to 7, identify the knowledge source.</li><li>Identify the human approval point.</li><li>Set the retry limit.</li><li>Set the no-progress rule.</li><li>Set the cumulative token limit.</li><li>Set the per-call turn limit.</li><li>During minutes 7 to 9, exchange worksheets with another team.</li><li>Find one false positive in the other design.</li><li>During minutes 9 to 10, improve one check.</li><li>Prepare a 30-second report.</li><li>State the claim, evidence, and remaining limit.</li></ol><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The harness pattern applies to a task outside TV development</strong></div><dl><dt>Independent check</dt><dd>Another team can find a false positive and improve the evidence without changing the model</dd><dt>Evidence</dt><dd><code>workshop/worksheet.md</code></dd><dt>Limit</dt><dd>The worksheet defines a prototype. It does not prove production readiness.</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the minimum useful harness?</summary><p>Select one repeatable task.\nUse a short phase sequence.\nSupply one necessary knowledge source.\nAdd one independent check to each phase.\nSet retry, token, and turn limits.\nWrite a report.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>You can explain the complete run from the TUI.\nAnother team can read your worksheet.\nThe team can identify the pass, retry, and stop conditions.</aside><div class=\"links\"><a href=\"worksheet.md\">Open the worksheet</a><a href=\"troubleshooting.md\">Troubleshooting</a><a href=\"editing-guide.md\">Edit the workshop</a><a href=\"instructor-guide.md\">Instructor guide</a><button data-go-module=\"bee\">Challenge: Bee phase</button></div>"},{"id":"bee","number":"A1","nav":"Challenge: Bee phase","time":"40 minutes","title":"Challenge: modify the app from a Bee conversation","lead":"Bee is a wearable personal AI that captures conversations. Use one conversation as private input. BEE_SPEC.md is the request review document. A source ID identifies the conversation. A paraphrase restates a request without its exact words.","objective":"Run phases that read Bee data. Protect private data. Approve each request before the phases change the app.","evidence":"BEE_SPEC.md contains source IDs and paraphrases. The app passes the approved checks and device phases.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Build from a conversation without committing it</h2><p>The six-phase TV port is complete.\nThis challenge adds a source of product requirements.\n\nThe Bee CLI is a command-line tool that searches your Bee data and exposes it to agents through MCP.\nA consented conversation is a conversation that every speaker permits you to use.\nUse the Bee CLI to find a consented conversation about Pocket Cinema.\nThe Bee phases apply approved requests to the app.</p></section><aside class=\"note \"><strong>Consent is a prerequisite</strong><br>Get consent from every speaker before you use a live conversation.\nUse your own Bee account and your own app discussion.\n\nIf you do not have consent or account access, run the recorded fallback.\nA recorded fallback uses prepared model and platform results instead of live services.</aside><h2>Read the challenge brief</h2><p>Record or select one Bee conversation. Make sure that the conversation contains:</p><ul><li>One explicit Pocket Cinema change.</li><li>One detail that makes the change testable.</li><li>One idea that the speakers deliberately defer.</li><li>No production secrets or protected customer data.</li></ul><p>The approved request must change the guarded app. The app checks, build, and launch must pass.</p><div class=\"flow\"><div><b>Bee</b><span>Find the product conversation</span></div><i>→</i><div><b>Spec</b><span>Paraphrase requests and define checks</span></div><i>→</i><div><b>Review</b><span>Approve scope before code changes</span></div><i>→</i><div><b>Apply</b><span>Modify the guarded app</span></div><i>→</i><div><b>Verify</b><span>Run app checks, build, and launch</span></div></div><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>Which information must not become an app requirement?</p></aside><h2>Know the two new phases</h2><p>This challenge adds two phases to the harness plan. The <code>bee_apply</code> phase changes the app. The <code>bee_spec</code> phase produces the approved input for <code>bee_apply</code>.</p><p>A specification is a structured statement of requested changes and checks. Authority is permission to read data, change files, or decide whether work passes. An assertion is a check about a file and its required content. Acceptance criteria are the checks that the completed change must pass. Provenance records where source information came from. An app-owned check is a test maintained with the app, not a check proposed by the conversation.</p><table><thead><tr><th>Phase</th><th>Input</th><th>Authority</th><th>Output</th></tr></thead><tbody><tr><td><code>bee_spec</code></td><td>Bee MCP tools from <code>bee-cli</code></td><td>Reads conversations and proposes a typed specification</td><td><code>bee-spec.json</code></td></tr><tr><td>Human review</td><td>Rendered specification</td><td>Accepts or rejects requests and checks</td><td>Approved <code>BEE_SPEC.md</code></td></tr><tr><td><code>bee_apply</code></td><td>Approved specification and guarded app</td><td>Proposes app files only</td><td>A typed app patch</td></tr><tr><td><code>build</code>, <code>launch</code></td><td>Modified app</td><td>Vega compiler and VDA</td><td>Device evidence</td></tr></tbody></table><p>The harness renders <code>BEE_SPEC.md</code> from validated JSON. The model does not write the review document. The <code>bee_apply</code> phase cannot edit either specification file.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/bee-pipeline.ts (challenge contract)</figcaption><pre><code>bee_spec -&gt; human review -&gt; bee_apply -&gt; build -&gt; launch\n\nbee_spec:\n  MCP: bee\n  output: bee-spec.json\n\nbee_apply:\n  readOnly: [bee-spec.json, BEE_SPEC.md]\n  checks: approved file assertions + app-owned checks</code></pre><p class=\"look\"><strong>Where to look:</strong> The conversation is context. The approved specification is the requirement.</p></figure><h2>Keep the authority narrow</h2><p>Use these rules:</p><table><thead><tr><th>Rule</th><th>Reason</th></tr></thead><tbody><tr><td>Permit only <code>file_exists</code> and <code>contains</code> checks</td><td>A conversation must not specify a shell command</td></tr><tr><td>Keep each check path inside the guarded app</td><td>Approval must not give access outside the app</td></tr><tr><td>Let the harness render <code>BEE_SPEC.md</code></td><td>The reviewed text and the validated JSON must agree</td></tr><tr><td>Make both specification files read-only for <code>bee_apply</code></td><td>The implementation must not change its acceptance criteria</td></tr><tr><td>Store source IDs and hashes in provenance</td><td>The report identifies the source without transcript text</td></tr><tr><td>Keep TypeScript and app tests under harness control</td><td>The model must not grade its own patch</td></tr></tbody></table><aside class=\"note \"><strong>Protect private data</strong><br><code>bee-context.json</code> contains conversation references and hashes.\n<code>bee-context.json</code> does not contain conversation text.\n\n<code>out/bee/model-logs/bee_spec.jsonl</code> can contain conversation text.\nGit ignores the <code>out/</code> directory.\nBefore you share a run directory, remove private data.</aside><h2>Set up bee-cli</h2><p>Read the <a href=\"https://github.com/bee-computer/bee-cli\" target=\"_blank\" rel=\"noopener\">bee-cli repository</a>. Install the current Bee mobile app. Developer Mode is a Bee app setting that permits CLI access. In Settings, tap the app version five times to enable Developer Mode. Keep the Vega SDK and VDA from Lessons 4 and 5 available for the final checks.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Install <code>bee-cli</code>.\nAuthenticate with Bee.\nFind the consented conversation.</p></aside><div class=\"command\"><header><span>Install and authenticate bee-cli</span><button class=\"copy\">Copy command</button></header><pre><code>npm install -g @beeai/cli\nbee version\nbee login\nbee status</code></pre></div><p>Search for the app discussion. Use a query that identifies the conversation. Do not use a person's name in the query.</p><div class=\"command\"><header><span>Find the Pocket Cinema conversation</span><button class=\"copy\">Copy command</button></header><pre><code>bee search --query &quot;Pocket Cinema&quot; \\\n  --filter conversations --limit 5 --json</code></pre></div><p>Do not put the JSON in a source file. Record the selected conversation ID in your private notes.</p><aside class=\"note \"><strong>The harness starts the MCP server</strong><br>Standard input and standard output (stdio) connect two processes through text streams.\nJSON-RPC is a structured request-and-response format.\nThe harness launches <code>bee mcp serve</code> over stdio for the <code>bee_spec</code> phase.\nDo not start the server in a second terminal.\n\nComplete authentication before you start the harness.</aside><h2>Run the specification phase</h2><p>The live Bee path uses Strands. Set <code>&quot;executor&quot;: &quot;strands&quot;</code> and the provider settings in <code>../../workshop.config.json</code>. The <code>--propose</code> option reads Bee data and creates the specification. It does not change app source files.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Run the new specification phase.\nCompare the phase requests with the conversation.\nCompare the phase checks with the conversation.</p></aside><div class=\"command\"><header><span>Create the Bee specification</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --propose</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;event&quot;:&quot;bee_spec_ready&quot;\n&quot;phasesComplete&quot;:[&quot;bee_spec&quot;]</code></pre></div><p>Before you change source code, inspect the phase result:</p><ol class=\"tasks\"><li>Open <code>out/bee/app/BEE_SPEC.md</code>.</li><li>Find the selected conversation ID.</li><li>Confirm that every request is a paraphrase.</li><li>Confirm that the deferred idea is in <code>Deliberately excluded</code>.</li><li>Confirm that unrelated or personal material is excluded.</li><li>Check that every request has a specific file assertion.</li><li>Reject any command check.</li><li>Open <code>out/bee/bee-context.json</code>.</li><li>Confirm that <code>bee-context.json</code> contains references and hashes but no transcript.</li><li>Run <code>git -C ../../out/bee/app diff --name-only HEAD~2 HEAD</code>.</li><li>Confirm that no app source file changed during <code>bee_spec</code>.</li></ol><p>The Git <code>diff --name-only</code> command lists changed file paths without file content.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does the harness store a hash?</summary><p>A hash identifies the source content.\nA hash also detects a later change.\nThe durable report does not need the private conversation text.\n\nThe local model transcript can contain the complete exchange.\nTreat the local model transcript as private data.</p></details></section><h2>Approve or reject the specification</h2><p>Do not approve the specification based only on appearance. Compare the specification with the consented conversation.</p><p>Reject the proposal when:</p><ul><li>A request was only an idea or question.</li><li>A private detail appears in the product scope.</li><li>A check can pass without implementing the behavior.</li><li>A check points outside the app.</li><li>The expected app change is missing.</li></ul><p>The <code>--apply --yes</code> command records your approval. The <code>--apply</code> option applies the approved specification. The <code>--yes</code> option confirms the approval without another prompt.</p><h2>Run the app-modifying phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Apply the approved request.\nTrace the conversation ID to the source patch.\nTrace the conversation ID to the independent checks.</p></aside><div class=\"command\"><header><span>Apply, build, and start</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --apply --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;event&quot;:&quot;bee_apply_complete&quot;\n&quot;phasesComplete&quot;:[&quot;bee_spec&quot;,&quot;bee_apply&quot;,&quot;build&quot;,&quot;launch&quot;]</code></pre></div><p><code>tsc --noEmit</code> checks TypeScript without writing compiled files. A request ID identifies one approved change and its checks.</p><ol class=\"tasks\"><li>Find the first failed approved check in the phase result.</li><li>Open <code>out/bee/bee-result.json</code>.</li><li>Find the request ID on each check.</li><li>Find the source conversation ID.</li><li>Inspect the committed app source change.</li><li>Verify that <code>bee-spec.json</code> did not change.</li><li>Verify that <code>BEE_SPEC.md</code> did not change.</li><li>Verify that <code>tsc --noEmit</code> passes.</li><li>Verify that the app tests pass.</li><li>Verify that the Vega build completes.</li><li>Verify that the modified app remains active on the VDA.</li></ol><h2>Test the phase boundaries</h2><p>A phase boundary defines which input and action a phase permits. The phase boundaries must reject unsafe inputs.</p><ol class=\"tasks\"><li>Run the harness tests for Bee.</li><li>Find the test that rejects a command check.</li><li>Find the test that rejects a path outside the app.</li><li>Find the test that prevents <code>bee_apply</code> from rewriting the specification.</li><li>Find the test that refuses <code>--apply</code> before a specification exists.</li><li>Explain which component rejects each case.</li></ol><div class=\"command\"><header><span>Run the Bee harness tests</span><button class=\"copy\">Copy command</button></header><pre><code>node --import tsx --test tests/bee.test.ts</code></pre></div><h2>Run the challenge without a Bee account</h2><p>The synthetic conversation is a written example, not a live Bee conversation. A fixture is prepared test input stored in the repository. Replay uses stored model and platform results from a fixture. Replay evidence proves the pipeline behavior but does not prove a live Bee connection. The recorded fallback runs the same phase plan with a synthetic conversation. The recorded fallback proves harness behavior. The recorded fallback does not prove a live Bee integration.</p><div class=\"command\"><header><span>Propose from the synthetic conversation</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --replay ../../workshop/fixtures/bee-run/port-recording.json \\\n  --propose --run-id bee</code></pre></div><p>Review <code>out/bee/app/BEE_SPEC.md</code>. Then, run:</p><div class=\"command\"><header><span>Apply the synthetic conversation</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --replay ../../workshop/fixtures/bee-run/port-recording.json \\\n  --platform-replay ../../workshop/fixtures/vega-lifecycle.json \\\n  --apply --yes --run-id bee</code></pre></div><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>A consented Bee conversation can produce a bounded app change</strong></div><dl><dt>Independent check</dt><dd>Human-approved specification checks, app-owned checks, build, and launch all pass</dd><dt>Evidence</dt><dd><code>BEE_SPEC.md, bee-context.json, bee-result.json, and phase commits</code></dd><dt>Limit</dt><dd>The recorded fallback proves only the pipeline. A live claim requires an authenticated Bee read.</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>Git contains the approved <code>BEE_SPEC.md</code>.\nEach request contains a source ID.\nThe excluded list contains private and unapproved content.\n<code>bee-result.json</code> reports passing apply, build, and launch phases.</aside><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>If you do not have consent or Bee access, stop the live appendix.\nInspect the prepared synthetic fixture:\n\n<code>workshop/fixtures/bee-run</code>\n\nDo not make a live Bee integration claim from the synthetic fixture.</aside>"}];
