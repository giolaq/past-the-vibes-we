// GENERATED by scripts/build-site.mjs from workshop/lessons/*.md — do not edit by hand.
window.WORKSHOP_MODULES = [{"id":"welcome","number":"00","nav":"Start here","time":"20 minutes","title":"Set up the workshop and understand the runtime","lead":"Welcome — get this done before we start, and you'll spend the session building instead of installing. If anything live fights you for more than 10 minutes — model account, ADBT, device — stop and use replay instead. Every lesson also runs from recordings, so a broken setup never has to stop you.","objective":"Choose a reliable workshop path and explain where Strands, ADBT, the harness, and Git each fit.","evidence":"A successful replay run, one chosen execution path, and a completed readiness checklist.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Welcome to Past the Vibes</h2><p>Over the next four hours we're going to build a harness together: first a small one you can read end to end, then the real one that ports a React Native app to Vega TV. You'll leave with something you can point at your own codebase. This page is the setup — work through it before we begin, then meet us at lesson 1.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Read this first if you have never built an agent harness</h2><p>You are a React Native developer. You may have never touched an \"agent,\" an \"LLM tool,\" or \"MCP.\" That's fine — nothing here assumes you have. We will port a small RN app to Vega (Amazon's TV OS) without doing it by hand and without just asking an AI to \"please port my app.\" Instead we build a <strong>harness</strong> with AWS Strands Agents SDK: a plain TypeScript program that runs a fixed pipeline, lets an AI model <em>propose</em> code inside tight walls, and keeps every dangerous action — writing files, running checks, committing to Git, spending money, talking to the device — for itself. That structure is why you're here rather than just prompting a coding agent: the harness gives you control (your code owns every write, check, and dollar) and observability (every model turn, document read, cost, and commit is recorded). And it's reusable — swap the skills, the MCP server, or the executor, and the same pipeline works on your own project, driven by the CLI coding agent you already use or directly by Strands + Bedrock.</p></section><aside class=\"note \"><strong>The one sentence to remember</strong><br>The model is a contractor with read-only access. The harness is the foreman: it checks the work and decides what gets in.</aside><h2>Vocabulary, translated for a React Native dev</h2>\n      <p>You already know these ideas under other names. Here's the dictionary; the rest of the workshop uses these words.</p><table><thead><tr><th>Term</th><th>What it actually is</th><th>Your mental model</th></tr></thead><tbody><tr><td><strong>LLM / model</strong></td><td>A program that turns text in into text out. Given a prompt, it returns a guess. Claude is one.</td><td>A well-read intern who writes plausible code but never runs it.</td></tr><tr><td><strong>Prompt</strong></td><td>The text you send the model.</td><td>The Jira ticket plus all the context you paste in.</td></tr><tr><td><strong>Agent</strong></td><td>A model wired to a loop where it can call functions (\"tools\"), see the result, and decide what to do next.</td><td>An intern who can grep your repo before answering, several times.</td></tr><tr><td><strong>Tool</strong></td><td>A named function you expose to the model, with a typed signature. The model <em>requests</em> a call; your code runs it and returns the result.</td><td>A locked-down CLI you hand the intern: <code>read_file</code>, <code>list_files</code>, nothing else.</td></tr><tr><td><strong>Structured output</strong></td><td>Forcing the model to answer as JSON matching a schema, not free prose.</td><td>A required PR template the intern cannot deviate from.</td></tr><tr><td><strong>MCP</strong></td><td><a href=\"https://modelcontextprotocol.io\" target=\"_blank\" rel=\"noopener\">Model Context Protocol</a> — a standard so one program can start another as a \"server\" and call its tools.</td><td>Starting a language server (LSP) and querying it, but for arbitrary tools.</td></tr><tr><td><strong>ADBT</strong></td><td><a href=\"https://www.npmjs.com/package/@amazon-devices/amazon-devices-buildertools-mcp\" target=\"_blank\" rel=\"noopener\">Amazon Devices Builder Tools</a>, exposed here as an MCP server serving Vega migration docs.</td><td>An internal wiki you can query programmatically.</td></tr><tr><td><strong>Skill</strong></td><td>A block of domain instructions (\"how to do TV focus\") kept separate from code.</td><td>A runbook you paste into the ticket for one task.</td></tr><tr><td><strong>Harness</strong></td><td>The deterministic TypeScript program orchestrating all of the above.</td><td>Your CI pipeline, if CI could also call an intern mid-step.</td></tr><tr><td><strong>Replay</strong></td><td>Running the pipeline against <em>recorded</em> model answers instead of a live model.</td><td>Fixtures / VCR cassettes for network calls.</td></tr><tr><td><strong>VDA</strong></td><td>Vega Virtual Device — an emulator for the TV OS.</td><td>Android emulator, but for Vega.</td></tr></tbody></table><aside class=\"note \"><strong>The single most important idea</strong><br>An LLM generates <em>plausible</em> text, and plausible is not the same as correct: <code>plausible &ne; verified</code>. Everything the harness does closes that gap. Every phase ends in a mechanical check, not a vibe.</aside><h2>Who is allowed to do what</h2>\n      <p>This is the security model:</p><div class=\"flow\"><div><b>ADBT MCP</b><span>Approved Vega context</span></div><i>→</i><div><b>Strands</b><span>Read and propose</span></div><i>→</i><div><b>Harness</b><span>Write and check</span></div><i>→</i><div><b>Git</b><span>Commit evidence</span></div></div><figure class=\"snippet\"><figcaption>The boundary, in one diagram</figcaption><pre><code>ADBT (MCP server) &lt;--- model calls list_documents / read_document itself ---+\n                                                                            |\nguarded app copy  &lt;--- read-only tools (list/read/search) -----------------+--&gt; Strands Agent --&gt; typed patch {summary, files}\n                                                                            |\n                                                                            v\n     HARNESS: reconstruct + hash ADBT reads -&gt; validate paths -&gt; write files -&gt; run checks -&gt; retry -&gt; git commit -&gt; report</code></pre><p class=\"look\"><strong>Where to look:</strong> The model (via Strands) can <em>list, read, and search</em> the guarded app, and the harness hands the whole ADBT <code>McpClient</code> to the agent so Strands discovers the ADBT tools dynamically and the model calls them itself. It still has no write tool and no shell. Afterward the harness reconstructs which ADBT docs it read and hashes them. Everything with consequences stays in <code>packages/workshop-harness/src/port-pipeline.ts</code>.</p></figure><p>The strictness has a reason: a model with a write tool or a shell can corrupt your repo on one confident wrong guess. Keep irreversible actions in deterministic code, and the worst a bad answer can do is <em>fail a check and get rejected</em>.</p><aside class=\"note \"><strong>What is Strands Agents SDK?</strong><br><a href=\"https://github.com/strands-agents/harness-sdk\" target=\"_blank\" rel=\"noopener\">Strands</a> is AWS's open-source agent runtime (TypeScript and Python), used here as the live remote path, pinned at 1.10.0. It fits this workshop for two reasons: the plumbing (provider adapters, typed tools, structured output, limits, cancellation, usage metrics) is built in, and it's a library, not a framework — it doesn't try to own writes or orchestration, so the harness boundary stays where we put it.</aside><table><thead><tr><th>Strands supplies</th><th>The harness owns</th></tr></thead><tbody><tr><td>Agent loop and model providers</td><td>Phase order and approval</td></tr><tr><td>Read-only typed tools</td><td>Protected file writes</td></tr><tr><td>Validated patch output</td><td>Checks, retry, and Git commits</td></tr><tr><td>MCP client and metrics</td><td>Cost cap, replay, and report</td></tr></tbody></table><p>The port agent can list, read, and search the guarded app. During <code>analyze</code> and <code>plan</code> the harness also hands it the ADBT <code>McpClient</code>, and Strands discovers ADBT's own tools (<code>list_documents</code>, <code>read_document</code>, <code>search_documentation</code>) dynamically — the harness does not pre-pick which workflows to read; the model does. It still gets no shell or write tool. After the phase, the harness walks the agent's tool calls and records each ADBT read with a SHA-256 hash, so the run stays auditable. Replay uses recorded model turns and recorded ADBT context, so it needs no live model or MCP server.</p><h2>1. Check the basics</h2><ol class=\"tasks\"><li>Install Node.js 20 or newer and Git. <a href=\"https://nodejs.org/api/corepack.html\" target=\"_blank\" rel=\"noopener\">Corepack</a> supplies Yarn 4.12.</li><li>Clone this repository, run <code>cd past-the-vibes-we</code>, and keep the terminal at that root.</li><li>Choose Pocket Cinema unless your own React Native app already runs.</li></ol><div class=\"grid\"><article><h3>Pocket Cinema</h3><p>Recommended. Every exercise, recording, and checkpoint supports this app.</p><code>apps/pocket-cinema</code></article><article><h3>Your app</h3><p>Use one working flow: launch → screen → action → back. Switch to Pocket Cinema if discovery takes more than 10 minutes.</p><code>launch → screen → action → back</code></article></div><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual Android TV capture</div><img src=\"assets/pocket-cinema-android-tv.png\" alt=\"Pocket Cinema home screen running on an Android TV emulator, with a featured title and a horizontal content rail\" loading=\"lazy\"><figcaption>Pocket Cinema is the shared starting app. This is the unported React Native baseline running on the workshop's Android TV AVD; later lessons add explicit focus behavior and a separate Vega package.</figcaption></figure><h3>Bring-your-own-app safety check</h3><div class=\"checklist\"><label><input type=\"checkbox\">The app runs before the workshop</label><label><input type=\"checkbox\">Git status is clean</label><label><input type=\"checkbox\">It contains no production secrets or private data</label><label><input type=\"checkbox\">It contains no protected media</label><label><input type=\"checkbox\">It can be shared with the chosen model provider</label></div><h2>2. Install the workshop workspace</h2><div class=\"command\"><header><span>Install all workshop packages</span><button class=\"copy\">Copy command</button></header><pre><code>unset NODE_TLS_REJECT_UNAUTHORIZED\ncorepack enable\nyarn setup</code></pre></div><h2>3. Run the setup check</h2><div class=\"command\"><header><span>Check the key-free replay path</span><button class=\"copy\">Copy command</button></header><pre><code>yarn doctor</code></pre></div><p>You are ready when the command reports <code>state: ready</code>. Model and Vega checks are optional in replay mode.</p><div class=\"command\"><header><span>Optional: check live ADBT with everything else replayed</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --adbt-live --json</code></pre></div><h3>Prove the starting point is not TV-ready</h3>\n      <p>Don't take our word that Pocket Cinema is touch-only — run the TV-readiness check against it. The same command goes green on the ported copy in lesson 7. If you brought your own app, run it on that too:</p><div class=\"command\"><header><span>Run the TV-readiness check on the starter app</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts tv-check ../../apps/pocket-cinema</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: false\n&quot;failures&quot;: [\n  &quot;Focus state module: missing src/tv/focus-state.ts&quot;,\n  &quot;App wires shared focus state: src/App.tsx must contain \\&quot;./tv/focus-state\\&quot;&quot;,\n  &quot;Initial focus declared: src/App.tsx must contain \\&quot;hasTVPreferredFocus\\&quot;&quot;,\n  &quot;Vega package manifest: missing apps/vega/manifest.toml&quot;,\n  ...\n]</code></pre></div><p>That failure list is the workshop's to-do list: everything on it is produced by the port in lesson 6 and verified in lesson 7.</p><h2>4. Choose one execution path</h2><table><thead><tr><th>Path</th><th>Use it when</th><th>Needs</th></tr></thead><tbody><tr><td>Your CLI coding agent</td><td>You already run <a href=\"https://code.claude.com/docs\" target=\"_blank\" rel=\"noopener\">Claude Code</a>, Codex, or a similar CLI agent. The workshop ships a Claude Code executor; the <code>Executor</code> interface in <code>port-executor.ts</code> is where another CLI plugs in — lesson 4 shows the swap recipe.</td><td>That agent installed and authenticated; ADBT via init-context</td></tr><tr><td>Strands + Bedrock</td><td>Run the model in-process through the SDK the harness is built with.</td><td>AWS credentials and <a href=\"https://docs.aws.amazon.com/bedrock/\" target=\"_blank\" rel=\"noopener\">Bedrock</a> model access</td></tr><tr><td>Replay</td><td>Fallback when a live model, ADBT, or device is unavailable.</td><td>Nothing beyond the installed packages</td></tr></tbody></table><p>Both live paths run the same harness — pick whichever you can authenticate today. The recorded replay path stays available as a fallback in every lesson. Confirm your live path is ready:</p><div class=\"command\"><header><span>Claude Code: check the local executor</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --executor claude-cli --json</code></pre></div><div class=\"command\"><header><span>Strands: check Bedrock credentials</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --executor strands --provider bedrock --json</code></pre></div><div class=\"command\"><header><span>Fallback: check the key-free replay path</span><button class=\"copy\">Copy command</button></header><pre><code>yarn doctor</code></pre></div><aside class=\"note \"><strong>Pick one live executor</strong><br>Choose your CLI agent or Strands + Bedrock as your primary path — you don't need both. If your chosen live path fails mid-workshop, save the error and use the replay fallback shown in that lesson.</aside><h2>5. Optional Vega and VDA setup</h2>\n      <p>Skip this section when you plan to use lifecycle replay. For the live path, install the <a href=\"https://developer.amazon.com/docs/vega/0.22/install-vega-sdk.html\" target=\"_blank\" rel=\"noopener\">Vega SDK</a> <code>0.22.5875</code> and create a Vega Virtual Device.</p>\n      <p>How ADBT reaches the model depends on your executor:</p>\n      <ul>\n        <li><strong>Replay (default)</strong>: recorded context, no <code>init-context</code>, no install.</li>\n        <li><strong>Strands</strong>: the harness owns the ADBT <code>McpClient</code> and hands it to the agent so the model calls ADBT's tools itself. Still run <code>init-context</code> once — lesson 4's live runs load the <code>amazon-devices-vega-*</code> skills it installs.</li>\n        <li><strong>Claude Code CLI</strong>: the CLI has its own MCP client, so the same <code>init-context</code> run registers the ADBT server with it and installs the skills. Run this in a real system terminal, then reopen your agent:</li>\n      </ul><div class=\"command\"><header><span>Set up ADBT MCP for the Claude Code CLI (one time)</span><button class=\"copy\">Copy command</button></header><pre><code># Run in a system terminal (not inside the agent). Sets up the ADBT MCP server\n# and installs the amazon-devices-vega-* skills that lesson 4 loads.\n# --force skips the confirmation prompts.\nnpx -y @amazon-devices/amazon-devices-buildertools-mcp@latest init-context --agent claude-code-cli --force</code></pre></div><div class=\"command\"><header><span>Verify the ADBT MCP setup</span><button class=\"copy\">Copy command</button></header><pre><code>npx -y @amazon-devices/amazon-devices-buildertools-mcp@latest check-status --agent claude-code-cli</code></pre></div><aside class=\"note \"><strong>Where these come from</strong><br>Amazon Devices Builder Tools ships the ADBT MCP server, skills, and steering docs as one pinned npm package: over 400 current Vega documents — migration workflows, knowledge-base pages, prompts — plus ten agent skills, served from a local process. <code>init-context</code> writes the MCP config into your agent (it supports Claude Code, Cursor, Kiro, Cline, and Copilot); <code>check-status</code> confirms it. With it, the model reads the vendor's current guidance instead of guessing Vega APIs from memory. See the <a href=\"https://developer.amazon.com/docs/vega/0.22/mcp-server.html\" target=\"_blank\" rel=\"noopener\">Vega ADBT setup docs</a>.</aside><p>When the harness runs the CLI executor, it invokes it with <code>--allowedTools \"*\"</code> so whatever ADBT tools <code>init-context</code> configured are permitted without stalling on a permission prompt in non-interactive mode. The CLI runs against the guarded copy, and the harness applies only the returned typed patch.</p><div class=\"command\"><header><span>Check ADBT and start VDA in a system terminal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --adbt-live --json\nvega --version\nvega virtual-device start --gui</code></pre></div><p>Keep that terminal open. In a second system terminal, run:</p><div class=\"command\"><header><span>Confirm the SDK and attached device</span><button class=\"copy\">Copy command</button></header><pre><code># Run this in a second system terminal.\nvega --version\nvega virtual-device status\nvega exec vda devices -l</code></pre></div><aside class=\"note success\"><strong>Live Vega is ready only when</strong><br>The SDK prints 0.22.5875, virtual-device status reports running: true, and devices -l lists an attached device.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Try one repair for no more than 10 minutes. Then use replay. Do not spend workshop time repairing a model account or device.</aside><h2>Setup complete</h2><div class=\"checklist\"><label><input type=\"checkbox\">Node 20+, Git, and Corepack are available</label><label><input type=\"checkbox\">All three workspace packages are installed</label><label><input type=\"checkbox\">One replay run completed</label><label><input type=\"checkbox\">I chose replay, Claude Code, or Strands</label><label><input type=\"checkbox\">I chose Pocket Cinema or checked my own app</label><label><input type=\"checkbox\">I understand that Vega device work is optional</label><label><input type=\"checkbox\">I can explain what Strands supplies and what the harness owns</label></div><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the most important boundary in this workshop?</summary><p>The model can inspect and propose, but the harness controls approval, protected writes, verification, retries, cost, commits, and reports. ADBT supplies selected platform knowledge; it does not take over the run.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>The workspace is installed, one replay succeeds, one execution path is chosen, and you know which app you will use.</aside>"},{"id":"single-agent","number":"01","nav":"One model call","time":"15 minutes","title":"Start with one model call","lead":"We start with the smallest thing the harness can do — one model call against your real app — and then find out what it can't prove.","objective":"Locate the model boundary and tell a generated claim apart from a verified one.","evidence":"Three claims in the feasibility verdict that nothing in the run has checked.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>We start where you already are</h2><p>Paste your code into a model, get an answer back, trust it — that's most people's AI workflow today, and it's exactly what this lesson runs. The harness already has a command that does one model call and nothing else: <code>plan</code>. Over the next four hours we'll build everything around it, and each lesson adds one piece. Start by watching what a single call gives you, and what it leaves you holding.</p></section><aside class=\"note \"><strong>One app, one harness, all four hours</strong><br>Everything from here runs <code>packages/workshop-harness</code> against <code>apps/pocket-cinema</code> — the app you checked in lesson 0. The pipeline is <code>analyze → plan → build_test</code>, and today you run none of it: <code>plan</code> stops before the port begins.</aside><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The model is about to judge whether Pocket Cinema can be ported to Vega. Before you run it, name one thing that judgment could get wrong in a way that still reads as confident.</p></aside><h2>Run one call against your app</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Pick the executor you set up in lesson 0 and run it. This calls a real model, with read-only access to your real app, and writes nothing.</p></aside><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI\nyarn --cwd packages/workshop-harness tsx src/index.ts plan ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock\nyarn --cwd packages/workshop-harness tsx src/index.ts plan ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;verdict&quot;: &quot;feasible-with-adapters&quot;\n&quot;phases&quot;: [&quot;analyze&quot;,&quot;plan&quot;,&quot;build_test&quot;]</code></pre></div><h2>Two answers, only one of them checked</h2><p>Look at the output. It holds two kinds of statement, and telling them apart is the whole point of this lesson.</p><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>findings and feasibility are not the same kind of claim</h2><p><code>findings</code> comes from <code>src/portability-audit.ts</code> — plain TypeScript that reads <code>package.json</code> and the project files and reports what it saw. Every entry carries an <code>evidence</code> field naming the file it came from. <code>feasibility</code> comes from <code>src/feasibility.ts</code>, which sends one prompt to a model and gets back a verdict, a per-dependency judgment, and a list of ADBT documents it says it consulted. Both land in the same JSON. One of them was produced by code you can read; the other is a well-written opinion.</p></section><ol class=\"tasks\"><li>Find <code>summary</code> and <code>findings</code>. Note that each finding names its <code>evidence</code>.</li><li>Find <code>feasibility</code>. Read <code>verdict</code>, then read the <code>reasoning</code> on each dependency.</li><li>Find <code>feasibility.sources</code> — the ADBT documents the model says it used.</li><li>Find <code>costUsd</code>. That call had a price.</li><li>Run <code>git status</code>. <code>plan</code> wrote nothing to your app, and that is deliberate.</li></ol><h2>Where the model actually sits</h2><p>Two small files hold the whole boundary, and both are worth opening now — you'll spend the rest of the workshop adding things around them.</p><ol class=\"tasks\"><li>Open <code>packages/workshop-harness/src/port-executor.ts</code> and find the <code>PortExecutor</code> interface. It has one method. Every way of calling a model in this workshop — your CLI agent, Strands, a recording — implements that one method.</li><li>Open <code>packages/workshop-harness/src/port-contract.ts</code>. It is twenty lines: a Zod schema saying an answer must be <code>{summary, files}</code>, and a helper that pulls JSON out of whatever the model wrapped it in.</li><li>In <code>feasibility.ts</code>, find where that schema is swapped for the feasibility one. A phase can demand its own shape of answer.</li></ol><aside class=\"note \"><strong>Why demand a schema instead of asking for JSON</strong><br>\"Return JSON\" is a request. A schema is a contract the code enforces: Strands validates the structured output against it, and the harness refuses an answer that does not match. The model's prose is never parsed by hope.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is this a model call, but not yet a harness?</summary><p>It has a model and a real question, but nothing independent checked the answer, nothing retried it, nothing recorded what changed, and nothing decided what happens if it is wrong.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>You can point at the one method where the model is called, and you have written down three claims in the verdict that nothing in this run has checked.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>If the live model is blocked, run the committed recording instead — same exercise, no account:</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback if the live model is blocked\nyarn --cwd packages/workshop-harness tsx src/index.ts plan ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json</code></pre></div>"},{"id":"verify","number":"02","nav":"Checks you own","time":"25 minutes","title":"Write the check that decides","lead":"Lesson 1 left us holding an unverified opinion. The fix is not a better prompt: it's a check, and a check is just code you own.","objective":"Read the mechanical definition of TV-ready and extend it with your own rule.","evidence":"A check you wrote fails on the starter app and passes on the ported one.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>The smallest fix for lesson 1's problem</h2><p>In lesson 0 you ran <code>tv-check</code> against Pocket Cinema and it came back <code>tvReady: false</code> with a list of failures. Nothing about that list came from a model. It came from a file, and today we open it. By the end of this lesson you will have changed what this workshop means by \"TV-ready\" — and you'll have done it without a model, a device, or a single dollar.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>A check is a value, not a clever function</h2><p><code>src/port-verification.ts</code> is fifty lines. <code>PortCheck</code> is three shapes: a file must exist, a file must contain a string, or a command must exit 0. <code>tvReadyChecks()</code> returns a list of them, and <code>verifyPort()</code> walks the list and collects failure text. That is the whole verification engine. The third shape is the sharp one — it runs a real command, which is how the focus test gets executed rather than described.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The starter app fails every TV-readiness check. Which of the three shapes would you use to prove the ported app's remote navigation actually works, rather than merely exists?</p></aside><h2>Read the file that decided</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Open the checks, then run them against both ends of the workshop — the app we start from and the app we're going to produce.</p></aside><ol class=\"tasks\"><li>Open <code>packages/workshop-harness/src/port-verification.ts</code> and read <code>tvReadyChecks()</code>.</li><li>Match each entry to a line in the failure list you saw in lesson 0.</li><li>Find <code>FOCUS_TEST_CHECK</code>. It is a <code>command</code> check, and <code>build_test</code> uses the same constant — the phase gate and <code>tv-check</code> cannot disagree about how the focus test runs.</li></ol><div class=\"command\"><header><span>Red: the app we start from</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts tv-check ../../apps/pocket-cinema</code></pre></div><div class=\"command\"><header><span>Green: the app we are going to produce</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts tv-check ../../workshop/checkpoints/vega-buildable/app</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: true\n&quot;failures&quot;: []</code></pre></div><h2>Assignment: add a rule of your own</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>A reviewer asks for something the current checks don't cover: the port must document how Back restores focus. Make the harness enforce it.</p></aside><p>The pairing is the point. A skill would tell the model to write that document; a check decides whether it did. They live in different files, and this one is yours.</p><ol class=\"tasks\"><li>In <code>tvReadyChecks()</code>, add a <code>contains</code> check for <code>TV_VERIFICATION.md</code> with the value <code>originating card</code> and the label <code>Focus restoration documented</code>.</li><li>Run <code>tv-check</code> on <code>apps/pocket-cinema</code> again. Your rule appears in the failure list.</li><li>Run it on <code>workshop/checkpoints/vega-buildable/app</code>. Your rule passes, because the ported app documents exactly that.</li><li>Now write one that is yours: a rule your team would actually want. Keep it mechanical — something a string or an exit code can settle.</li></ol><aside class=\"note \"><strong>Choose the shape that proves the most</strong><br><code>file_exists</code> proves a file arrived. <code>contains</code> proves a decision was written down. <code>command</code> proves behavior, because something ran. Reach for the strongest shape the requirement allows — lesson 3 is where a weak check starts costing you.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why keep the check in code instead of asking the model to self-report?</summary><p>A model reporting on its own work is another generated claim. A check runs whether the model is honest, confused, or absent — and it returns the same answer every time.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br><code>tv-check</code> reports your new rule as a failure on <code>apps/pocket-cinema</code> and as a pass on the ported checkpoint, and you can say which of the three shapes you chose and why.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>This lesson needs no model, no credentials, and no device. If everything else in your setup is broken, this one still runs.</aside>"},{"id":"phases","number":"03","nav":"The loop","time":"30 minutes","title":"Close the loop around the model","lead":"Now we let the model change code — inside walls: a guarded copy, a typed patch, a check, one retry carrying the exact failure, and a commit for work that passed.","objective":"Trace a requirement through a failed check, a contextual retry, and a committed result.","evidence":"The failed check appears in your terminal and in port-result.json, and the second attempt is committed.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>The loop is the harness</h2><p>Lesson 1 gave us a model call. Lesson 2 gave us a check. This lesson wires them into a cycle, and that cycle is the thing you take home: propose, write, verify, and on failure retry with the exact reason — never with \"try again\". We run it one phase at a time, so you can watch each part arrive. The model still never writes a file; the harness does, and only after the check agrees.</p></section><div class=\"flow\"><div><b>Propose</b><span>Model returns a typed patch</span></div><i>→</i><div><b>Write</b><span>Harness writes inside the guarded copy</span></div><i>→</i><div><b>Verify</b><span>Checks run and produce exact failures</span></div><i>→</i><div><b>Retry</b><span>The failure text joins the next prompt</span></div><i>→</i><div><b>Commit</b><span>Only passing work reaches Git</span></div></div><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The plan phase must document the remote flow. Predict what the check looks for, and what the harness should send back to the model when the first attempt omits it.</p></aside><h2>Run one phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run the first phase of the real port and nothing else. <code>--phases</code> takes the subset you want, so you can meet the pipeline one piece at a time.</p></aside><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI. One phase, on a guarded copy of your app.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases analyze --yes --run-id lesson3</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2 \\\n  --phases analyze --yes --run-id lesson3</code></pre></div><ol class=\"tasks\"><li>Open <code>packages/workshop-harness/out/lesson3/app</code> — a copy. Run <code>git status</code> on <code>apps/pocket-cinema</code> and confirm your app never moved.</li><li>Run <code>git log --oneline</code> inside that copy. There are two commits: the imported source, and the phase that passed.</li><li>Open <code>ANALYSIS.md</code> in the copy. That file is the model's patch, written by the harness after the check passed.</li></ol><aside class=\"note \"><strong>Why a copy, and why Git</strong><br>The guarded copy is where the model's authority ends. Git inside it is not decoration: a failed attempt is reset to the phase's starting commit, so a rejected patch leaves nothing behind. Rollback and record are the same mechanism.</aside><h2>Watch a check fail and drive a retry</h2><p>A live model often passes on the first try, which is a bad way to learn what happens when it doesn't. The committed recording forces the interesting case: its first plan attempt writes a plan with the remote-flow section missing.</p><div class=\"command\"><header><span>Replay the failed check and the repair</span><button class=\"copy\">Copy command</button></header><pre><code># The recording's first plan attempt omits the required section.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-retry/port-recording.json \\\n  --phases analyze,plan --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>plan attempt 1 failed:\n  - TV flow documented: VEGA_PORT.md must contain &quot;## TV Flow&quot;</code></pre></div><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual replay output</div><img src=\"assets/retry-terminal.png\" alt=\"Terminal output showing the plan check failing, the failure recorded in port-result.json, and the run completing after the second attempt\" loading=\"lazy\"><figcaption>One useful failure. The retry receives evidence — the exact check that failed — not the instruction 'try again'.</figcaption></figure><h2>Follow the evidence yourself</h2><ol class=\"tasks\"><li>Find the failing check in your terminal output. That text came from <code>verifyPort()</code>, the function you read in lesson 2.</li><li>Open <code>out/&lt;runId&gt;/port-result.json</code>. The plan phase records <code>attempts: 2</code> and keeps the failures of the rejected attempt.</li><li>Open <code>prompt()</code> in <code>src/port-pipeline.ts</code> and find where a previous attempt's failures are appended. That is the same text, on its way back to the model.</li><li>Open <code>writeOutput()</code> in the same file. Every path the model proposes must resolve inside the guarded copy, and <code>.git</code>, <code>node_modules</code>, and <code>.env</code> are refused however they are spelled.</li></ol><h2>Stop it, then bring it back</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Long runs die. Rather than hope, stop this one on purpose and continue it — a port that already cost you money should never be repeated from the top.</p></aside><div class=\"command\"><header><span>Resume the same run with the next phase</span><button class=\"copy\">Copy command</button></header><pre><code># Same run id: the guarded copy and its history are reused, not rebuilt.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases plan --yes --run-id lesson3</code></pre></div><ol class=\"tasks\"><li>Run <code>git log --oneline</code> in the copy again. The analyze commit is still there, with plan on top of it.</li><li>Open <code>out/lesson3/status.json</code> and read <code>phasesComplete</code>. It remembers both phases, not just this invocation's.</li><li>Open <code>out/lesson3/report.md</code> and find the line saying the source was not copied again.</li></ol><h2>The loop cannot run away</h2><p>One retry is a teaching default, not a law. <code>--max-attempts N</code> raises it and <code>--until-done</code> removes the cap, and the loop still has three exits — none of which is the model's opinion:</p><ol class=\"tasks\"><li>The checks pass. The verifier decides the phase is done.</li><li>The attempt budget runs out, or the cost cap trips and the run aborts with a clean tree.</li><li>The same failures come back twice in a row. No progress, so more attempts only spend money, and the loop stops.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why pass the exact failure into the retry instead of saying try again?</summary><p>The exact failure narrows the problem, preserves the original requirement, and makes the retry explainable afterwards. A generic retry buys another guess at the same price.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>You can trace one requirement from a failed check, through the retry that carried it, to a commit — and you resumed a run without repeating a phase you already paid for.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Every command here works on the replay path. Swap the executor flags for <code>--replay ../../workshop/fixtures/port-recording.json</code>, or use the retry recording above.</aside>"},{"id":"skills","number":"04","nav":"The parts you swap","time":"30 minutes","title":"Separate knowledge from model access","lead":"We separate the parts that keep getting tangled: skills carry domain knowledge, tools expose narrow capabilities, executors call the model, and the pipeline depends on none of them.","objective":"Separate domain knowledge, model execution, tools, and deterministic pipeline control.","evidence":"A skill you wrote changes what the model produces, and a check you wrote proves it.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>One job per piece</h2><p>The loop from lesson 3 doesn't change again. What changes is what it knows and who it calls — and this is the lesson where those become things you swap rather than things you rewrite. By the end you'll have taught the model one of your team's rules by adding a file, enforced it with a check, and seen where your own CLI agent would plug in.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>Where should a D-pad focus rule live: the executor, a skill, a read tool, or a verification check?</p></aside><h2>Each phase names the knowledge it wants</h2><p>Open <code>phases()</code> in <code>src/port-pipeline.ts</code> and look at one entry. It carries four different things, and keeping them apart is the design:</p><table><thead><tr><th>Field</th><th>What it is</th><th>Who reads it</th></tr></thead><tbody><tr><td><code>goal</code></td><td>What this phase must produce.</td><td>The model, in the prompt.</td></tr><tr><td><code>instruction</code></td><td>The rule this harness writes into the prompt.</td><td>The model, in the prompt.</td></tr><tr><td><code>skills</code></td><td>Names of skill files the executor delivers.</td><td>The model, through its executor.</td></tr><tr><td><code>checks</code></td><td>Code that decides whether the phase passed.</td><td>The harness, never the model.</td></tr></tbody></table><aside class=\"note \"><strong>The skills are Amazon's, not ours</strong><br>ADBT ships ten <code>amazon-devices-vega-*</code> skills — manifest configuration, focus management, navigation, media playback, performance, build-and-run, and more — versioned and updated by Amazon, installed by lesson 0's <code>init-context</code>. The harness names three of them, one per phase, without owning a word of their content: real TV expertise reaches the agent, and swapping a phase's expertise is a one-line change. If they are not installed, <code>src/skills.ts</code> reports each missing skill and the run continues — and <code>workshop/fixtures/adbt-skills.json</code> records their names, hashes, and excerpts so you can still see what they say.</aside><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run the plan phase and watch which skill it asks for. The names come from the phase; the delivery is the executor's problem.</p></aside><div class=\"command\"><header><span>Run the phase that uses the focus skill</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases analyze,plan --yes</code></pre></div><h2>One skill, two delivery paths</h2>\n    <table><thead><tr><th>Executor</th><th>How the selected skill arrives</th></tr></thead><tbody><tr><td>Claude CLI</td><td><code>injectSkillText()</code> appends the complete skill body to the subprocess prompt.</td></tr><tr><td>Strands</td><td><code>Skill</code> objects enter an <code>AgentSkills</code> plugin. Metadata appears first; the agent activates instructions with the <code>skills</code> tool.</td></tr><tr><td>Replay</td><td>No model runs. A recorded response replaces the live executor while the same phase plan remains visible.</td></tr></tbody></table>\n    <aside class=\"note \"><strong>A missing skill never blocks a run</strong><br><code>loadSkills()</code> reports a skill it cannot find and continues. An uninstalled ADBT skill costs the model that knowledge, not the run.</aside><ol class=\"tasks\"><li>Open <code>src/skills.ts</code>. It loads <code>&lt;skills dir&gt;/&lt;name&gt;/SKILL.md</code>, derives a name and description, and returns a body. Point <code>WORKSHOP_SKILLS_DIR</code> at another directory if your agent keeps skills elsewhere.</li><li>In <code>src/port-executor.ts</code>, compare the two delivery paths: <code>injectSkillText()</code> for the CLI subprocess, <code>createSkillsPlugin()</code> for Strands.</li><li>Confirm the pipeline never reads a skill file. It passes names; the executor decides how a model receives them.</li></ol><h2>Assignment: ship a team skill</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Amazon ships its conventions as skills. Now ship one of yours — teach the model a rule your team actually has, then enforce it with a check.</p></aside><p>The pairing is the point: the skill carries the knowledge, the check does the enforcement, and they live in different files on purpose.</p><ol class=\"tasks\"><li>Create the skill: <code>mkdir -p ~/.claude/skills/team-open-questions</code>, then write <code>SKILL.md</code> in it with one instruction — for example, \"End every document you produce with a section titled ## Open Questions listing what you could not verify.\" Keep it to a paragraph.</li><li>In <code>phases()</code>, add <code>team-open-questions</code> to the <code>plan</code> phase's <code>skills</code>.</li><li>In the same phase, add a <code>contains</code> check for <code>VEGA_PORT.md</code> with the value <code>## Open Questions</code>.</li><li>Run the command above again, live. Your skill teaches, your check enforces, and the loop from lesson 3 handles the rest.</li></ol><aside class=\"note success\"><strong>You are done when</strong><br>The run passes, <code>out/&lt;runId&gt;/app/VEGA_PORT.md</code> ends with your <code>## Open Questions</code> section, and you can point at the file that taught the rule and the line that enforced it. Neither one is pipeline code.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>On the replay path your check fails on purpose, and that failure is worth reading. Use the retry recording, which holds two plan attempts:</aside><div class=\"command\"><header><span>Replay variant: the recording cannot satisfy a new requirement</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-retry/port-recording.json \\\n  --phases analyze,plan --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>plan failed after 2 attempts: Open questions recorded: VEGA_PORT.md must contain &quot;## Open Questions&quot;</code></pre></div><p>Both recorded answers were written before your requirement existed, so neither can satisfy it. Replay honors checks, because checks are code — it cannot honor skills, because no model runs to follow an instruction. Write that sentence down; it completes the assignment on the replay path.</p><h2>The tools the model gets</h2><aside class=\"note \"><strong>The read tools are guarded hard</strong><br>In <code>src/port-tools.ts</code> the model gets exactly three capabilities — list, read, search — and they reject absolute paths, <code>..</code> traversal, symlinks, <code>.git</code>, <code>.env</code>, <code>node_modules</code>, binaries, and files over 100&nbsp;KB. There is no write tool and no shell. Even the read side of the model's authority has walls.</aside><h2>The whole model interaction, in one code block</h2>\n      <p>In <code>src/port-executor.ts</code>, the entire live model interaction for one phase is essentially this (the <code>systemPrompt</code> is shortened here):</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-executor.ts (simplified)</figcaption><pre><code>const agent = new Agent({\n  name: `workshop-${phase}`,\n  model: createModel(config),                 // Bedrock / OpenAI / OpenRouter behind one interface\n  tools: createProjectReadTools(appDir),      // list/read/search only — no write, no shell\n  plugins: [createSkillsPlugin(skills)],      // the phase's skills, loaded by the agent on demand\n  structuredOutputSchema: PortOutputSchema,   // must return { summary, files }\n  systemPrompt: &quot;Inspect with read-only tools. Return a complete patch. Never claim a file or API exists without reading evidence.&quot;,\n  printer: false,                             // keep stdout clean for JSON\n});\nconst result = await agent.invoke(prompt, {\n  cancelSignal: AbortSignal.timeout(10 * 60_000),  // 10-min hard stop\n  limits: { turns: 8, totalTokens: 40_000 },       // bounded loop\n});</code></pre><p class=\"look\"><strong>Where to look:</strong> Strands supplies the model-and-tool loop, provider adapters, skill delivery, schema-validated output, turn/token limits, cancellation, and usage metrics. Writing files, verification, Git, cost policy, and ADBT selection stay in the harness.</p></figure><h2>Strands constructs used here</h2>\n    <p>Read this table from setup to result. These are the Strands APIs the workshop uses — and everything in it is SDK-supplied: one dependency provides the loop, providers, validated tools, enforced schemas, limits, and metrics, while writes, checks, retries, cost, and commits stay in workshop code.</p>\n    <table><thead><tr><th>Construct</th><th>What it does here</th><th>Where to find it</th></tr></thead><tbody><tr><td><code>Agent</code></td><td>Runs the model-and-tool loop for one phase. <code>name</code> and <code>description</code> identify its job.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>Model</code>, <code>BedrockModel</code>, <code>OpenAIModel</code></td><td>Hide provider-specific model calls. OpenRouter uses <code>OpenAIModel</code> with an OpenAI-compatible base URL.</td><td><code>model-factory.ts</code></td></tr><tr><td><code>systemPrompt</code></td><td>Sets durable operating rules: inspect first, use read-only evidence, and return a complete patch.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>tool()</code></td><td>Turns a named callback into a model-callable capability. The description tells the model when to use it.</td><td><code>port-tools.ts</code></td></tr><tr><td><code>inputSchema</code></td><td>Uses Zod to validate tool arguments and type the callback input before project code runs.</td><td><code>port-tools.ts</code></td></tr><tr><td><code>tools</code></td><td>Registers only list, read, and literal search. No write or shell capability enters the agent loop.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>Skill</code></td><td>Represents one selected phase skill with a name, description, and full body.</td><td><code>skills.ts</code></td></tr><tr><td><code>AgentSkills</code></td><td>Adds progressive skill disclosure and the model-callable <code>skills</code> activation tool.</td><td><code>skills.ts</code></td></tr><tr><td><code>plugins</code></td><td>Registers <code>AgentSkills</code> on the Strands agent. Claude CLI does not use this field.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>structuredOutputSchema</code></td><td>Requires the final answer to match <code>{ summary, files }</code>. Strands validates it and can feed schema failures back to the model.</td><td><code>port-contract.ts</code></td></tr><tr><td><code>printer: false</code></td><td>Disables Strands' automatic console renderer so the CLI keeps stdout reserved for versioned JSON events.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>agent.invoke()</code></td><td>Starts one bounded run with the assembled phase prompt.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>limits.turns</code> / <code>limits.totalTokens</code></td><td>Bound the loop at 8 turns and 40,000 tokens per phase.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>cancelSignal</code></td><td>Lets a native ten-minute <code>AbortSignal</code> cancel the invocation at Strands cancellation points.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>AgentResult</code></td><td>Carries validated <code>structuredOutput</code>, messages, stop state, and metrics after invocation.</td><td>returned by <code>invoke()</code></td></tr><tr><td><code>StructuredOutputError</code></td><td>Makes a missing structured patch an explicit executor failure.</td><td><code>port-executor.ts</code></td></tr><tr><td><code>metrics.accumulatedUsage</code></td><td>Reports input and output tokens. The harness records them and applies its own cost rates.</td><td><code>port-executor.ts</code></td></tr></tbody></table>\n    <aside class=\"note \"><strong>Keep the boundary visible</strong><br>Zod supplies schemas, native <code>AbortSignal</code> supplies cancellation, and the harness supplies cost policy and verification. Strands consumes those inputs and runs the bounded agent loop.</aside><h2>Why the workshop uses invoke</h2>\n    <p>The workshop uses <code>agent.invoke()</code> so one phase has one visible request, one typed result, and one metrics object. A larger CLI can use <code>agent.stream()</code> and <code>AgentStreamEvent</code> for progress without changing the pipeline boundary.</p>\n    <aside class=\"note \"><strong>Features still outside the design</strong><br>The repository does not use Strands hooks, Graph, Swarm, agent-as-tool, SDK session or memory managers, custom conversation managers, or SDK-provided write and shell tools.</aside><ol class=\"tasks\"><li>Open <code>src/port-tools.ts</code> and match each <code>tool()</code> field to the first table.</li><li>Open <code>port-contract.ts</code> and find the Zod schema passed as <code>structuredOutputSchema</code>.</li><li>Open <code>port-executor.ts</code> and trace <code>new Agent()</code> → <code>invoke()</code> → <code>AgentResult</code>.</li><li>Follow the result into usage accounting and <code>port-recorder.ts</code>.</li><li>Confirm the port agent has no write or shell tool. Its pipeline owns both.</li></ol><h2>Swap in your own CLI agent</h2><p>The executor is the same kind of swap point as a skill, and the recipe fits in three steps. Everything lives in <code>src/port-executor.ts</code>:</p><ol class=\"tasks\"><li>Implement <code>PortExecutor</code> — the one-method interface you met in lesson 1. <code>call(phase, prompt, options)</code> returns <code>{text, costUsd}</code> where <code>text</code> carries the JSON patch. Model it on <code>ClaudeCodePortExecutor</code>: spawn your CLI non-interactively with the prompt on stdin and the guarded app as the working directory, deliver <code>options.skills</code> however your agent expects them, collect the response, and record each turn with <code>PortRecorder</code> so your runs replay like everyone else's.</li><li>Register it: add a <code>kind</code> to <code>ExecutorConfig</code>, a branch in <code>resolveExecutorConfig()</code> for your <code>--executor &lt;name&gt;</code> value, and a branch in <code>createPortExecutor()</code>.</li><li>Keep the contract: the harness applies only the returned typed patch — anything your CLI writes directly to disk is ignored and rolled back — and your agent reaches ADBT through its own MCP config (<code>init-context</code> supports Cursor, Cline, Kiro, Copilot, and <code>other</code>).</li></ol><p>The pipeline, checks, retries, cost cap, and commits never change. That is what \"swap the executor\" means.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why use AgentSkills with Strands but prompt injection with the Claude CLI?</summary><p>Strands can expose skill metadata in-process and let the agent activate the instructions it needs. A CLI subprocess shares no plugin with the harness, so its executor sends the selected instructions directly in the prompt. Same skill file, two delivery paths, one pipeline.</p></details></section><div class=\"links\"><a href=\"strands-constructs.md\">Open the Strands reference</a></div><aside class=\"note success\"><strong>You are done when</strong><br>You can name the file that owns each responsibility — knowledge, tools, model access, and control — and you changed the model's behavior without touching the pipeline.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>If the live model is blocked, replay shows the same module boundaries without credentials:</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback if the live model is blocked\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --phases analyze,plan --yes</code></pre></div>"},{"id":"memory","number":"05","nav":"Project memory","time":"15 minutes","title":"Review facts before saving them","lead":"Here you are the approval gate: proposed context becomes project memory only after you read it and apply it.","objective":"Turn selected source material into small, reviewable project facts with provenance.","evidence":"The saved PROJECT_CONTEXT.md separates decisions, facts, questions, and source ids.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>You are the gate</h2><p>Every run so far started from nothing. Now we give the harness memory — and immediately put a human in front of it. Project context should stay small, and every entry needs a source and a sign-off, because a raw transcript is noisy, private, and easy for an agent to misread as an instruction. You'll review candidate facts before any of them become durable truth.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>What could go wrong if the harness imported every remembered sentence automatically?</p></aside><div class=\"command\"><header><span>Create a disposable input copy</span><button class=\"copy\">Copy command</button></header><pre><code>WORKSHOP_INPUTS=&quot;/tmp/past-the-vibes-pocket-cinema-inputs&quot;\nrm -rf &quot;$WORKSHOP_INPUTS&quot;\ncp -R workshop/fixtures/pocket-cinema-inputs \\\n  &quot;$WORKSHOP_INPUTS&quot;</code></pre></div><div class=\"command\"><header><span>Show the current project memory (empty at first)</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts memory show /tmp/past-the-vibes-pocket-cinema-inputs --json</code></pre></div><div class=\"command\"><header><span>Create a memory proposal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts memory propose /tmp/past-the-vibes-pocket-cinema-inputs \\\n  --from ../../workshop/fixtures/bee-context/snapshot.json \\\n  --json</code></pre></div><h2>Your review, before anything is saved</h2><ol class=\"tasks\"><li>Read every proposed fact.</li><li>Check that each fact names its source.</li><li>Keep open questions separate from decisions.</li><li>Reject anything private, temporary, or ambiguous.</li></ol><div class=\"command\"><header><span>Apply the reviewed proposal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts memory apply /tmp/past-the-vibes-pocket-cinema-inputs \\\n  --from ../../workshop/fixtures/bee-context/snapshot.json \\\n  --yes --json</code></pre></div><h2>Check what you just approved</h2><ol class=\"tasks\"><li>Open <code>/tmp/past-the-vibes-pocket-cinema-inputs/PROJECT_CONTEXT.md</code>.</li><li>Confirm the source ids survived the transformation.</li><li>Confirm the committed fixture in the repository is unchanged.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is the proposal step an approval gate rather than another model phase?</summary><p>The human owns what becomes durable project truth. The model may summarize candidate facts, but it cannot silently promote them into trusted context.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>Every saved fact has a source and the repository fixture is unchanged.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Use the synthetic Bee snapshot — live Bee access isn't required.</aside>"},{"id":"plan","number":"06","nav":"Plan and port","time":"35 minutes","title":"Inspect first, then change a guarded copy","lead":"This is the main event: you approve a plan, then watch the real port run against a guarded copy while your source app stays untouched.","objective":"Follow the complete port boundary from plan approval to a checked, committed app copy.","evidence":"The report links approved ADBT context, a typed patch, check results, cost, and Git commits.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>The one we came here for</h2><p>Until now you have run the harness a piece at a time. This is the whole thing in one command: all three phases, ADBT supplying the platform knowledge, the device gate at the end, and you approving the plan before a dollar is spent. Read the plan carefully when it appears — that approval is the moment you are actually in charge.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The production loop</h2><p>The plan is the human approval boundary. During analyze and plan the model calls ADBT over MCP itself to gather Vega knowledge. Strands proposes a typed patch from read-only project tools. The harness records every ADBT read, applies the patch, checks it, retries once (or loops until green with <code>--max-attempts</code>/<code>--until-done</code>, under the cost cap and a no-progress rule), and commits only verified work.</p></section><div class=\"flow\"><div><b>ADBT MCP</b><span>Model reads workflows</span></div><i>→</i><div><b>Record</b><span>Hash each read</span></div><i>→</i><div><b>Model</b><span>Propose a typed patch</span></div><i>→</i><div><b>Checks</b><span>Write, commit, or retry</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Your source is never touched</h2><p>Before anything runs, the harness copies your app into <code>out/&lt;runId&gt;/app/</code> and runs <code>git init</code> inside that copy. Everything the model proposes is written <em>there</em>. Your real <code>apps/pocket-cinema/</code> is read once and never modified. The <code>&lt;runId&gt;</code> is a fresh directory per run, so runs never clobber each other — and the harness makes a Git commit per passing phase, so if phase 3 explodes, phases 1 and 2 are already committed and safe.</p></section><h2>The three phases of a port</h2>\n      <p>The port is the sequence you have been building since lesson 1: <strong>analyze → plan → build_test</strong>. You ran the first two with <code>--phases</code>; today they run together and <code>build_test</code> joins them. Each is a model phase that ends in a mechanical check; two of them draw on ADBT. Source of truth: <code>phases()</code> in <code>src/port-pipeline.ts</code> — the same function whose skills and checks you edited in lessons 2 and 4.</p><figure class=\"snippet\"><figcaption>The phase order — packages/workshop-harness/src/port-pipeline.ts</figcaption><pre><code>analyze  -&gt;  plan  -&gt;  build_test\n(model +      (model +    (model + executable focus test\n feasibility   live ADBT)  + mandatory device screenshot)\n via ADBT)</code></pre><p class=\"look\"><strong>Where to look:</strong> Open <code>src/port-pipeline.ts</code> and read <code>phases()</code>. Each name below maps to one entry.</p></figure><section class=\"phase\"><h3><span class=\"num\">1</span><code>analyze</code><span class=\"tag model\">model</span><span class=\"tag adbt\">ADBT feasibility</span></h3><dl><dt>Does</dt><dd>Reads the guarded app and writes <code>ANALYSIS.md</code> describing its screens, components, data, and what is portable to Vega TV. Alongside it, during the <code>plan</code> command — before any phase runs — a deterministic dependency inventory (<code>auditSource</code>) plus a bounded model+ADBT step judge whether the port is even possible.</dd><dt>Check</dt><dd><code>ANALYSIS.md</code> must contain <code>## Portable</code>.</dd><dt>Feasibility gate</dt><dd>The model returns a verdict — <code>feasible</code>, <code>feasible-with-adapters</code>, or <code>blocked</code>. A <code>blocked</code> verdict stops the run with exit code 5 <em>before</em> any build budget is spent.</dd><dt>Inspect</dt><dd><code>out/&lt;runId&gt;/app/ANALYSIS.md</code>, <code>feasibility-report.json</code>, and the deterministic inventory in <code>portability-report.json</code>.</dd><dt>Code</dt><dd><code>src/port-pipeline.ts</code>, <code>src/feasibility.ts</code>, <code>src/portability-audit.ts</code></dd></dl></section><section class=\"phase\"><h3><span class=\"num\">2</span><code>plan</code><span class=\"tag model\">model</span><span class=\"tag adbt\">live ADBT</span></h3><dl><dt>Goal</dt><dd>Plan the Vega TV port: preserved product behavior, Vega replacements, and the exact remote flow.</dd><dt>ADBT step</dt><dd>The harness hands the ADBT <code>McpClient</code> to the Strands agent; Strands lists ADBT's tools dynamically and the model calls them itself — <code>list_documents</code> to discover Vega workflows, then <code>read_document</code> to read the ones it judges relevant. The harness pre-picks nothing; after the phase it reconstructs the reads from the agent's messages and hashes them into <code>adbt-port-context.json</code>. (The claude-cli executor reaches ADBT through its own MCP config set up by <code>init-context</code>.)</dd><dt>Skill injected</dt><dd>Use the ADBT tools to discover and read the Vega migration workflows you need. Keep facts and assumptions separate, port one vertical slice, record gaps instead of inventing APIs.</dd><dt>Checks</dt><dd><code>VEGA_PORT.md</code> contains <code>## TV Flow</code>; <code>NextSteps.md</code> contains <code>ADBT</code> (names its sources).</dd><dt>Inspect</dt><dd><code>out/&lt;runId&gt;/app/VEGA_PORT.md</code>, <code>NextSteps.md</code>, <code>adbt-port-context.json</code>, and the commit <code>workshop(plan): ...</code>.</dd></dl></section><section class=\"phase\"><h3><span class=\"num\">3</span><code>build_test</code><span class=\"tag model\">model</span><span class=\"tag \">executable test</span><span class=\"tag adbt\">device screenshot</span></h3><dl><dt>Goal</dt><dd>Build the <code>apps/vega</code> package from the SDK shape, wire the remote-only home→details flow, and prove it.</dd><dt>Build checks</dt><dd>manifest <code>schema-version = 1</code> and <code>[[components.interactive]]</code>; <code>package.json</code> has <code>build-vega</code>; <code>app.json</code> and <code>metro.config.js</code> exist; root <code>package.json</code> has <code>vega:build</code>; <code>src/tv/focus-state.ts</code> exists; <code>src/App.tsx</code> imports <code>./tv/focus-state</code>.</dd><dt>Test checks</dt><dd>A real command runs — <code>node --import tsx tests/verify-tv-focus.ts</code> must exit 0. Back must return focus to the <em>originating card</em>, verified by a script, not a human eyeball. <code>tv-focus-result.json</code> shows <code>&quot;passed&quot;: true</code>; <code>TV_VERIFICATION.md</code> contains <code>originating card</code>.</dd><dt>Device evidence (mandatory)</dt><dd>The phase then runs the Vega device lifecycle: two pre-gates (SDK version, device status), then build → install → launch → capture → pull → logs → capture → pull — the ten gates lesson 8 inspects. <strong>The run fails unless the app is still running after a five-second dwell and both captured frames look like a rendered screen.</strong> Replay uses <code>--platform-replay</code> to stay key-free; live runs against a real VDA.</dd><dt>Inspect</dt><dd><code>out/&lt;runId&gt;/01-launch.png</code>, <code>out/&lt;runId&gt;/02-postlaunch.png</code>, <code>vega-platform-result.json</code>, <code>tv-focus-result.json</code>, and the commit <code>workshop(build_test): ...</code>.</dd><dt>Code</dt><dd><code>src/port-pipeline.ts</code>, <code>src/platform/vega.ts</code></dd></dl></section><aside class=\"note warning\"><strong>Device evidence is now a mandatory gate</strong><br>Running on the device is a required pass criterion of <code>build_test</code>, and \"running\" is checked rather than assumed: the harness waits five seconds after launch, scans the device log for crash signatures, and judges both captured frames as pixels — a 1x1 placeholder, a black screen, or one flat colour fails. Two consequences: a device (or its <code>--platform-replay</code> fixture) is now mandatory for a green run, and on the current VDA image the live screenshot tool segfaults, so <em>live</em> frames can't be produced until that tooling is fixed. The key-free replay path stays green via <code>--platform-replay ../../workshop/fixtures/vega-lifecycle.json</code>.</aside><h2>Every phase is built from the same prompt template</h2>\n      <p>You don't need to guess what the model sees. Every phase prompt is assembled by one function — <code>prompt()</code> in <code>src/port-pipeline.ts</code> — from the same slots. The model is told the exact checks it will be graded against, a failed attempt gets the verbatim failure text fed back in, and the output contract is strict JSON.</p><figure class=\"snippet\"><figcaption>The universal prompt template — src/port-pipeline.ts, prompt()</figcaption><pre><code>You are porting the CURRENT guarded React Native app to Vega SDK 0.22.5875.\nRead existing files before proposing edits. Preserve unrelated work.\n\nPhase: &lt;name&gt;\nGoal: &lt;one sentence&gt;\nSkill: &lt;domain instruction for this phase&gt;\nCreative seed: workshop-v1\n\nApproved context:\n&lt;project memory, or &quot;No approved project context.&quot;&gt;\n\nPortability findings:\n&lt;the JSON from phase 2's audit&gt;\n\n[ ONLY on plan: the ADBT guidance block, with SHA-256 hashes ]\n\nRequired checks:\n&lt;each check listed verbatim, so the model knows the bar it must clear&gt;\n\n[ ONLY on a retry: &quot;Previous attempt failed:\\n&lt;exact failure lines&gt;\\nFix these.&quot; ]\n\nReturn ONLY JSON: {&quot;summary&quot;:&quot;...&quot;,&quot;files&quot;:{&quot;relative/path&quot;:&quot;complete contents&quot;}}.</code></pre><p class=\"look\"><strong>Where to look:</strong> This exact string is built in <code>src/port-pipeline.ts</code>. Every live model turn — the prompt sent and the raw text returned — is recorded to <code>out/&lt;runId&gt;/port-recording.json</code>. That file is your audit trail: <code>request.messages[0].content</code> is the prompt, <code>response[].result</code> is the model's answer.</p></figure><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The model now calls ADBT over MCP itself. What has to be recorded for the run to stay reproducible and auditable?</p></aside><div class=\"command\"><header><span>Plan the Pocket Cinema port</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts plan ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --seed workshop-v1 --max-cost 3 --json</code></pre></div><h2>Read the plan before you approve it</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Generate the plan and actually read it. This is the human approval boundary, so check the scope, the feasibility verdict, the phase list, the seed, and the cost cap before you say yes.</p></aside><ol class=\"tasks\"><li>Confirm the source app and target flow.</li><li>Read the deterministic portability findings and the model's feasibility verdict.</li><li>Confirm the feasibility verdict is not <code>blocked</code> — a blocked verdict stops the run at exit code 5 before any build budget.</li><li>Check that ADBT is assigned to <code>analyze</code> (feasibility) and <code>plan</code>.</li><li>Check the three-phase plan (analyze → plan → build_test), fixed seed, and $3 cap.</li><li>Notice that build_test folds in the device screenshot lifecycle from lesson 8.</li></ol><h2>Now run the port</h2><p>Approve and run with a real model. On the Claude CLI path the model reaches ADBT through the MCP config you set up in lesson 0; on the Strands path the harness hands it the ADBT client. <code>build_test</code> needs an attached VDA to capture the launch screenshot.</p><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI. The model drives ADBT via the CLI's MCP config (init-context).\n# build_test needs an attached VDA to capture the launch screenshot.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --yes --seed workshop-v1 --max-cost 3 --json</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock. The harness hands the ADBT McpClient to the agent.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2 \\\n  --yes --seed workshop-v1 --max-cost 3 --json</code></pre></div><h2>Follow the evidence it left behind</h2><ol class=\"tasks\"><li>Copy the <code>runId</code> from the output.</li><li>Open <code>out/&lt;runId&gt;/adbt-port-context.json</code> and find the workflow names and hashes the model read.</li><li>Open <code>port-result.json</code> and confirm <code>adbt.mode: live</code>.</li><li>Open <code>app/NextSteps.md</code> and find ADBT sources and unsupported mappings.</li><li>Inspect the guarded app, report, and Git log.</li><li>Confirm <code>apps/pocket-cinema</code> is unchanged.</li></ol><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual replay output</div><img src=\"assets/port-terminal.png\" alt=\"Terminal showing a completed three-phase port replay, its cost, and one Git commit for analyze, plan, and build_test\" loading=\"lazy\"><figcaption>A complete key-free run. The important result is not only run_complete: the guarded app has one reviewable commit for every passing model phase.</figcaption></figure><p>This chain is the observability you built the harness for: the prompts, the document reads, the cost, and the commits of the whole run are on disk, inspectable by anyone, without asking the model what it did.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>The model fetches ADBT docs itself now. How does the run stay auditable and reproducible?</summary><p>The harness reconstructs every ADBT read from the agent's messages after the phase — document name, excerpt, and a SHA-256 hash into <code>adbt-port-context.json</code>. So even though the model chose what to read, there is an exact, hashed record of the knowledge it used — and replay can rerun from that recorded context with no live MCP server.</p></details></section><aside class=\"note warning\"><strong>If blocked</strong><br>If a live model, ADBT, or VDA is unavailable, run the fully recorded path — same phases and evidence contract, no credentials:</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback if a live model, ADBT, or VDA is unavailable.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --platform-replay ../../workshop/fixtures/vega-lifecycle.json \\\n  --yes --seed workshop-v1 --max-cost 3 --json</code></pre></div><h2>Worked example: real prompt in, real output out</h2>\n      <p>This is captured from an actual live run (<code>c9fc9e58</code>, real Claude model, live ADBT over MCP). It shows the ADBT-driven planning and the generated Vega package. (This capture predates the model-driven-MCP change: it used harness injection. Today the model calls ADBT's <code>read_document</code> tool itself, but the recorded provenance below — document names and SHA-256 hashes — is exactly what the harness now reconstructs from the model's tool calls.)</p>\n      <h3>What the model read from ADBT</h3>\n      <p>The harness records every document the model fetched, with a hash, into <code>adbt-port-context.json</code>. Recorded provenance from this run:</p><figure class=\"snippet\"><figcaption>adbt-port-context.json (recorded reads, excerpt)</figcaption><pre><code>## ADBT Vega Port Guidance\n\nMode: live\nSources:\n- port_tv_app_to_vega.md (sha256: 5dcf0e6f8a5b6a62d688562c46a9f22f414715c1b792ecdaf92bc0e8016214ea)\n- port_tv_app_to_vega_fos_rn_app.md (sha256: 2f67d9dc1133a52e9873513c3d66a2c0a2ca090a0d90284e1bb54e3f825f5607)\n\n### port_tv_app_to_vega.md\n## Purpose\nEntry point for all FOS-to-Vega app migrations. This workflow determines what the\nuser wants to convert, runs shared prerequisites (SDK check, device detection), then\ndispatches to the appropriate conversion-specific orchestrator.\n...\n\nUse these ADBT sources for Vega-specific decisions. Do not invent Vega APIs.\nWrite unsupported or uncertain mappings to NextSteps.md and name the ADBT documents consulted.</code></pre><p class=\"look\"><strong>Where to look:</strong> Why hashes? So the exact knowledge the model was given is provable and reproducible later. Find it in <code>out/&lt;runId&gt;/adbt-port-context.json</code>.</p></figure><h3>Out of <code>build_test</code> — what came out</h3>\n      <p>The model returned a multi-file patch. Several build_test checks <code>grep</code> the generated manifest — and it contains exactly the strings they look for:</p><figure class=\"snippet\"><figcaption>Generated apps/vega/manifest.toml</figcaption><pre><code>schema-version = 1\n\n[package]\ntitle = &quot;Pocket Cinema&quot;\nid = &quot;com.pocketcinema.app&quot;\n\n[[components.interactive]]\nid = &quot;com.pocketcinema.app.main&quot;\nruntime-module = &quot;@pocket-cinema/rn&quot;\nlaunch-type = &quot;singleton&quot;</code></pre></figure><p>Also worth reading: the <code>NextSteps.md</code> the model wrote back in the <em>plan</em> phase. It didn't have full MCP doc access in that session, and instead of bluffing, it said so:</p><figure class=\"snippet\"><figcaption>Generated NextSteps.md (excerpt) — the model admitting uncertainty</figcaption><pre><code>## Unverified against SDK docs (MCP doc access not granted)\n\nThe buildertools MCP read_document / list_documents calls were denied in this\nsession, so the items below rely on the Vega skill summaries and the ADBT\nworkflows above. They MUST be confirmed against the named KB documents before\nrelying on them — they are not invented APIs presented as fact.\n\n1. Manifest schema — verify field names and the [[components.interactive]] shape.\n2. App icon asset — vega_app_manifest.md requires a 512x512 PNG.\n3. Build CLI — confirm the exact Kepler/Vega build invocation.</code></pre><p class=\"look\"><strong>Where to look:</strong> The skill told the model to record gaps instead of inventing APIs, and this is what that looks like in output. The plan-phase check <code>NextSteps.md contains &quot;ADBT&quot;</code> passed, so the plan committed; the later build_test phase then had to clear all 11 of its own checks before it committed too.</p></figure><aside class=\"note \"><strong>What the worked example shows</strong><br>The model produces large, well-structured artifacts — and it also wraps JSON in prose and admits uncertainty. The harness doesn't read any of that as truth: it extracts the JSON, writes it to the guarded copy, and runs mechanical grep/file_exists checks. Only passing work is committed.</aside><h2>How ADBT connects during the port</h2><div class=\"command\"><header><span>Check the native ADBT MCP path</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --adbt-live --json</code></pre></div><h2>Strands MCP constructs used here</h2>\n    <table><thead><tr><th>Construct</th><th>Role in the ADBT path</th></tr></thead><tbody><tr><td><code>McpClient</code></td><td>Strands client wrapper that connects lazily, exposes MCP tools, calls them, and cleans up the connection.</td></tr><tr><td><code>applicationName</code> / <code>applicationVersion</code></td><td>Identify Past the Vibes Workshop to the MCP server during connection setup.</td></tr><tr><td><code>listTools()</code></td><td>Strands calls this for the passed <code>McpClient</code> to discover ADBT's tools dynamically, then exposes them to the model. The harness does not require or pre-pick tool names.</td></tr><tr><td><code>Agent({ tools: [...projectTools, adbtClient] })</code></td><td>The <code>McpClient</code> is passed into the agent's tools. Strands invokes ADBT's tools for the model during the agent loop; the harness does not call them.</td></tr><tr><td><code>agent.messages</code></td><td>After the phase, the harness walks the tool-use and tool-result blocks to reconstruct which ADBT docs the model read, then hashes them into <code>adbt-port-context.json</code>.</td></tr><tr><td><code>disconnect()</code></td><td>Closes the child server and transport in <code>finally</code>, including failure paths.</td></tr></tbody></table>\n    <aside class=\"note \"><strong>The transport is a separate layer</strong><br><code>StdioClientTransport</code> comes from the official Model Context Protocol SDK, not Strands. It starts pinned ADBT as a child process. The harness passes the <code>McpClient</code> into the agent's tools so the model can call ADBT itself; provenance is reconstructed from the message history afterward.</aside><aside class=\"note \"><strong>Two executor paths</strong><br>Strands: the harness passes the ADBT <code>McpClient</code> into the agent's tools and the model calls ADBT itself. Claude CLI: the CLI reaches ADBT through the MCP config from <code>init-context</code>. Either way the harness reconstructs and hashes what was read.</aside><aside class=\"note success\"><strong>You are done when</strong><br>You can trace each MCP construct from connection through the model's own tool calls, a typed proposal, checks, a verified commit, and the final report.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>A live port stops with exit 3 when ADBT is unavailable; it never continues with unsupported assumptions. Use the recorded ADBT context via the replay fallback above.</aside>"},{"id":"tv","number":"07","nav":"Test remote behavior","time":"20 minutes","title":"Test the flow, not one screenshot","lead":"TV is where a green build stops being enough — we trace focus through launch, movement, Select, and Back, and prove it with a test.","objective":"Express TV navigation as observable state transitions instead of visual impressions.","evidence":"tv-focus-result.json records launch, movement, selection, and focus restoration after Back.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Why TV breaks the usual definition of done</h2><p>On TV, the thing that matters most is the thing a build cannot check. We're going to write down the remote-control contract, then let an executable test decide whether the port honors it — including the transition everyone forgets, where Back has to return focus to the card you came from. TV quality is temporal.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>What a screenshot cannot show</h2><p>TV quality is temporal. A screenshot can show where focus is now, but not whether focus moved correctly, respected boundaries, opened the right screen, or returned to the same card.</p></section><div class=\"remote\" aria-label=\"TV remote direction pad\"><button>↑</button><button>←</button><button class=\"ok\">OK</button><button>→</button><button>↓</button></div><table><thead><tr><th>Action</th><th>Expected result</th></tr></thead><tbody><tr><td>Launch</td><td>Featured action has focus</td></tr><tr><td>Down</td><td>Focus enters the first rail</td></tr><tr><td>Left / right</td><td>Focus stops at list boundaries</td></tr><tr><td>Select</td><td>Details opens for the focused card</td></tr><tr><td>Back</td><td>The same card regains focus</td></tr></tbody></table><div class=\"flow\"><div><b>Launch</b><span>Featured action focused</span></div><i>→</i><div><b>Down</b><span>Enter first rail</span></div><i>→</i><div><b>Select</b><span>Open focused details</span></div><i>→</i><div><b>Back</b><span>Restore the same card</span></div></div><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>Which transition is most likely to pass a screenshot review but fail for a real remote user?</p></aside><div class=\"command\"><header><span>Run the executable focus check</span><button class=\"copy\">Copy command</button></header><pre><code>(cd packages/workshop-harness/out/&lt;runId&gt;/app &amp;&amp; \\\n  node --import tsx tests/verify-tv-focus.ts)\ncat packages/workshop-harness/out/&lt;runId&gt;/app/tv-focus-result.json</code></pre></div><h2>Compare passing and failing evidence</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run the focus check against your ported app. Then do the more interesting half: look at what a failing run reports, and where in the code the contract is kept.</p></aside><ol class=\"tasks\"><li>Replace <code>&lt;runId&gt;</code> with the id from lesson 6.</li><li>Read <code>tv-focus-result.json</code> as a sequence, not a score.</li><li>Open <code>fixtures/focus-failure/README.md</code> and find the failed Back transition.</li><li>Trace the focus state and restoration code in the guarded app.</li></ol><aside class=\"note \"><strong>Where the focus props come from</strong><br><code>hasTVPreferredFocus</code> and the <code>onFocus</code>/<code>onBlur</code> handlers are React Native's TV focus model — see <a href=\"https://github.com/react-native-tvos/react-native-tvos\" target=\"_blank\" rel=\"noopener\">react-native-tvos</a> for the general TV story. This workshop targets Vega, not that fork, but the focus model is the same.</aside><h2>Close the loop you opened in lesson 0</h2><p>In lesson 0 you ran <code>tv-check</code> against the starter app and got <code>tvReady: false</code> with the full failure list. Run the same command against the ported copy:</p><div class=\"command\"><header><span>The before/after: same check, ported app</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts tv-check out/&lt;runId&gt;/app</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: true\n&quot;failures&quot;: []</code></pre></div><p>Every failure from lesson 0 was produced by the port and is now verified mechanically — that pair of outputs is the workshop's before-and-after photo.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is Back part of the focus contract?</summary><p>Returning to a screen without restoring the user's prior focus loses navigation context. The UI may look correct while the remote interaction is broken.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>The focus check passes the full transition sequence, <code>tv-focus-result.json</code> records it, and <code>tv-check</code> reports <code>tvReady: true</code> on the app that failed every check in lesson 0.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Run the same check in <code>checkpoints/vega-buildable/app</code> — no device required.</aside>"},{"id":"vega","number":"08","nav":"Run the Vega lifecycle","time":"25 minutes","title":"Hand the guarded app to Vega tools","lead":"Time to hand the app to real Vega tools — ten gates on a live device, with recorded replay standing by if no device is attached.","objective":"Distinguish reproducible lifecycle rehearsal from evidence produced by a real Vega device.","evidence":"Ten lifecycle gates pass, with evidenceMode labeled replay or live.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Two kinds of evidence, never mixed</h2><p>This lesson is about honesty as much as tooling. The harness will run ten gates against a real device, and it labels what it can actually prove — so by the end you'll be able to say precisely which claims your run earned and which it didn't. A live VDA run proves this app built, installed, launched, stayed running, and rendered a screen on an attached device. Replay proves you can study the lifecycle and its contracts without one. Keep those claims separate.</p></section><aside class=\"note warning\"><strong>Device screenshot caveat</strong><br>On the current VDA image the screenshot tool segfaults at the capture gate, so a fully live run may fail there even when build, install, and launch pass. The repo records this in the rehearsal note. If you hit it, use the replay fallback to finish the lesson.</aside><p>Five gates get the app onto the device:</p><div class=\"flow\"><div><b>SDK</b><span>Require 0.22.5875</span></div><i>→</i><div><b>Device</b><span>Require an attached target</span></div><i>→</i><div><b>Build</b><span>Produce the vpkg</span></div><i>→</i><div><b>Install</b><span>Transfer to the target</span></div><i>→</i><div><b>Launch</b><span>Start the app id</span></div></div><p>Five more prove it is actually running:</p><div class=\"flow\"><div><b>Capture</b><span>Create a device image</span></div><i>→</i><div><b>Pull</b><span>Save the launch frame</span></div><i>→</i><div><b>Logs</b><span>Read after a 5s dwell</span></div><i>→</i><div><b>Capture</b><span>Create a second image</span></div><i>→</i><div><b>Pull</b><span>Save the post-launch frame</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Launch accepted is not launch survived</h2><p><code>launch-app .. success</code> means the device accepted the command. An app that throws in its first render still prints it, and the screenshot that follows is a black rectangle — a file, so the old gate passed. Three checks close that hole. The harness waits five seconds, then reads the device log for crash signatures (<code>FATAL</code>, <code>SIGSEGV</code>, <code>has died</code>, ANR, an unhandled JS exception), and it judges both frames as pixels: a screen smaller than 640x360, all one flat colour, or pinned black or white is not a rendered app. An app that died on startup cannot produce the second frame.</p></section><div class=\"links\"><a href=\"live-rehearsal.md\">Read the rehearsal record</a></div><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>If the SDK build passes but the device list is empty, which lifecycle gates must the harness refuse to claim?</p></aside><h2>Run the lifecycle on a live VDA</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Start the virtual device, show the plan, then run the lifecycle. Keep an eye on which gates pass — that list is your evidence.</p></aside><ol class=\"tasks\"><li>Replace <code>&lt;runId&gt;</code> with the id from lesson 6.</li></ol><div class=\"command\"><header><span>Start VDA and keep this terminal open</span><button class=\"copy\">Copy command</button></header><pre><code># Run this in a system terminal and leave it open.\nvega virtual-device start --gui</code></pre></div><div class=\"command\"><header><span>Confirm the SDK and attached device</span><button class=\"copy\">Copy command</button></header><pre><code># Run this in a second system terminal.\nvega --version\nvega virtual-device status\nvega exec vda devices -l</code></pre></div><div class=\"command\"><header><span>Show the Vega plan</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts vega-run &lt;runId&gt; --plan --json</code></pre></div><div class=\"command\"><header><span>Run with Vega SDK and VDA</span><button class=\"copy\">Copy command</button></header><pre><code>npm --prefix packages/workshop-harness/out/&lt;runId&gt;/app/apps/vega install\nyarn --cwd packages/workshop-harness tsx src/index.ts vega-run &lt;runId&gt; --yes --json</code></pre></div><h2>You can claim live evidence only when</h2><ol class=\"tasks\"><li>The SDK reports <code>0.22.5875</code>.</li><li>VDA reports <code>running: true</code> and lists an attached device.</li><li>Build, install, launch, logs, and both capture/pull pairs all pass.</li><li>The device log holds no crash signature after the dwell.</li><li>Both frames pass the pixel gate.</li><li>The result says <code>evidenceMode: live</code> and the screenshots came from the device.</li></ol><h2>Inspect all ten gates yourself</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Open the result and read it as a sequence. The interesting part is not that it says complete — it is which named check earned that word.</p></aside><ol class=\"tasks\"><li>Confirm SDK version and device status were checked before build.</li><li>Find build, install, launch, both capture/pull pairs, and the log read between them.</li><li>Read each gate's exact command, exit code, and output.</li><li>In <code>checks</code>, read the four device claims: <code>launch screenshot renders content</code>, <code>device log free of crash signatures</code>, <code>post-launch screenshot renders content</code>, <code>focus transition suite</code>. Each carries its own evidence — frame size, colour count, and mean luminance for a screenshot; the crash line itself when the log fails.</li><li>Open <code>01-launch.png</code> and <code>02-postlaunch.png</code>. The second frame is the one that proves the app was still alive after the dwell.</li></ol><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual replay output</div><img src=\"assets/vega-lifecycle-terminal.png\" alt=\"Terminal summary showing all Vega lifecycle gates passing in replay mode\" loading=\"lazy\"><figcaption>The replay exercises the complete evidence contract. It is deliberately labeled evidenceMode: replay and must not be presented as proof of a live VDA session.</figcaption></figure><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What turns lifecycle output into trustworthy evidence?</summary><p>The harness records the exact command, outcome, artifact, and evidence mode for each gate, and it judges the artifact rather than trusting that it exists. A successful process exit alone is not enough — neither is a screenshot file.</p></details></section><aside class=\"note warning\"><strong>Try breaking it</strong><br>A gate you have never seen fail is a gate you are trusting on faith. Copy <code>fixtures/vega-lifecycle.json</code>, delete its <code>screenshot</code> line, and run the lifecycle against your copy: the replay falls back to a 1x1 pixel and the run fails with <code>frame is 1x1, smaller than the 640x360 minimum for a device screen</code>. Then put the line back and add <code>FATAL EXCEPTION: main</code> to the <code>logs</code> turn — the run fails naming that line. Both end at exit code 2.</aside><aside class=\"note success\"><strong>You are done when</strong><br>All ten gates pass on an attached VDA, the result says <code>evidenceMode: live</code>, and both device frames pass the pixel gate.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>If no VDA is attached or the screenshot gate segfaults, run the recorded lifecycle instead — same ten gates, labeled evidenceMode: replay:</aside><div class=\"command\"><header><span>Fallback: key-free lifecycle replay</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts vega-run &lt;runId&gt; \\\n  --platform-replay ../../workshop/fixtures/vega-lifecycle.json \\\n  --yes --json</code></pre></div><h2>Optional: ask a model what the screen shows</h2><p>The pixel gate is deterministic and cheap, and it stops at what pixels can prove. It cannot tell a rendered home screen from a rendered error dialog — both are colourful. If you have a multimodal model configured, add one bounded call that looks at the post-launch frame:</p><div class=\"command\"><header><span>Add the model review to the lifecycle</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts vega-run &lt;runId&gt; \\\n  --executor strands --provider bedrock \\\n  --evaluate-screenshot --yes --json</code></pre></div><p>The verdict lands in <code>checks</code> as <code>screenshot review</code>, and only a clear <code>not-app</code> blocks the run. Note where the authority sits: the model reports what it sees, your harness decides what that means. The deterministic gate runs either way, so a missing key costs you the opinion, not the check.</p>"},{"id":"bee","number":"09","nav":"Optional Bee context","time":"15 minutes","title":"Import selected context, not a transcript","lead":"An optional last layer: if Bee is configured and everyone has consented, we feed real conversations into the harness. The synthetic fixture is the normal path.","objective":"Select useful context without turning a private conversation into unreviewed agent memory.","evidence":"A scrubbed snapshot has source ids, dates, a query, a summary, and a stable hash.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Optional, and here is why</h2><p>This is the one lesson we may skip together, and the reason is worth stating out loud: it touches private conversations. Run it only with consent, and know that the synthetic fixture gives you the same mechanics with none of the risk. <a href=\"https://www.aboutamazon.com/news/devices/bee-amazon-wearable-ai-device-new-features\" target=\"_blank\" rel=\"noopener\">Bee</a> is Amazon's wearable AI device: it listens to your day (when you let it) and turns conversations into searchable summaries, decisions, and to-dos. Conversation search can recover useful decisions, but it also crosses a privacy boundary. Use it only with consent, select the smallest useful excerpt, and store a scrubbed snapshot rather than a transcript.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>Which fields would let a reviewer verify where a summarized fact came from without storing the full conversation?</p></aside><div class=\"command\"><header><span>Search Bee</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts context bee search \\\n  &quot;Pocket Cinema product decisions&quot; --json</code></pre></div><div class=\"command\"><header><span>Save one selected snapshot</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts context bee snapshot &lt;conversationId&gt; \\\n  --out candidate-context.json --json</code></pre></div><h2>Review the boundary</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>You are the same approval gate you were in lesson 5, with higher stakes: this material came from someone's conversation.</p></aside><ol class=\"tasks\"><li>Check source ids, dates, query, summary, and hash.</li><li>Review the snapshot before proposing memory.</li><li>Never commit a raw private transcript.</li><li>Disconnect Bee and confirm the approved snapshot still works.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why should the snapshot work after Bee is disconnected?</summary><p>The run becomes reproducible and reviewable without a live private-data dependency. The snapshot is the approved input; Bee is only a discovery source.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>Every approved fact has a source and can be reused without Bee.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Use <code>fixtures/bee-context/snapshot.json</code> or skip this optional module.</aside>"},{"id":"finish","number":"10","nav":"Build your own","time":"15 minutes","title":"Design one harness for your work","lead":"Last stretch, and it's yours: keep the pipeline, swap the TV skill, Vega commands, and D-pad checks for your own domain.","objective":"Draft the smallest useful harness for one task in your own engineering domain.","evidence":"A worksheet names the phases, checks, approval point, budget, and evidence the run must retain.","body":"<div class=\"takeaway\"><code>plan → context → run → check → retry → checkpoint → report</code></div><section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Now take it home</h2><p>You've built the loop and watched it hold. The last thing to do is point it at work you actually care about, so this lesson is a worksheet rather than a command — you design the harness and we'll help you check it. The reusable idea is a bounded workflow that gives a model strong context, limits its authority, checks each result, and leaves evidence another developer can inspect. TV and Vega are the example, not the point. What you keep is the control and the observability you saw in lesson 6: authority stays in your code, and every run leaves prompts, reads, costs, and commits you can audit.</p></section><h2>Draft your harness</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>This one has no command to copy. Design your own harness on the worksheet, and we'll come round to help you check it.</p></aside><ol class=\"tasks\"><li>Open <code>worksheet.md</code>.</li><li>Name one outcome that can finish in one session.</li><li>Choose the fewest useful phases.</li><li>Give every phase one independent, mechanical check.</li><li>Define the approval point, cost limit, stop conditions, and saved evidence.</li><li>Name your replacement for the TV skill, the ADBT MCP server, the Vega adapter, the D-pad check — and, if you use a different CLI agent, the executor.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the smallest useful first version of your harness?</summary><p>One repeatable task, a short phase sequence, one strong prior, one independent check per phase, a bounded retry, and a report. Add tools only when a proven gap needs them.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>Another developer can follow your worksheet, inspect the evidence, and knows when the harness must stop.</aside><div class=\"links\"><a href=\"worksheet.md\">Open the worksheet</a><a href=\"troubleshooting.md\">Troubleshooting</a><a href=\"instructor-guide.md\">Instructor guide</a></div>"}];
