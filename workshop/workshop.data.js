// GENERATED by scripts/build-site.mjs from workshop/lessons/*.md — do not edit by hand.
window.WORKSHOP_MODULES = [{"id":"welcome","number":"00","nav":"Start here","time":"20 minutes","title":"Set up the workshop and understand the runtime","lead":"Welcome — get this done before we start, and you'll spend the session building instead of installing. Choose one live model path; recordings are the fallback when external tooling blocks you.","objective":"Prepare one live executor and explain where Strands, ADBT, the harness, and Git each fit.","evidence":"A working executor/provider/model combination and a completed readiness checklist.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Welcome to Past the Vibes</h2><p>Over the next four hours we build a harness that ports a React Native app to Vega TV, one phase per lesson: analyze the app, plan the TV port, write it, build it, run it on a device, and prove the remote works. You'll leave with the harness itself, pointed at your own codebase. This page is the setup — work through it before we begin, then meet us at lesson 1.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Read this first if you have never built an agent harness</h2><p>You are a React Native developer. You may have never touched an \"agent,\" an \"LLM tool,\" or \"MCP.\" That's fine — nothing here assumes you have. We will port a small RN app to Vega (Amazon's TV OS) without doing it by hand and without just asking an AI to \"please port my app.\" Instead we build a <strong>harness</strong> with AWS Strands Agents SDK: a plain TypeScript program that runs a fixed pipeline, lets an AI model <em>propose</em> code inside tight walls, and keeps every dangerous action — writing files, running checks, committing to Git, spending money, talking to the device — for itself. That structure is why you're here rather than just prompting a coding agent: the harness gives you control (your code owns every write, check, and dollar) and observability (every model turn, document read, cost, and commit is recorded). And it's reusable — swap the skills, the MCP server, or the executor, and the same pipeline works on your own project, driven by the CLI coding agent you already use or directly by Strands + Bedrock.</p></section><aside class=\"note \"><strong>The one sentence to remember</strong><br>The model is a contractor with read-only access. The harness is the foreman: it checks the work and decides what gets in.</aside><h2>Vocabulary, translated for a React Native dev</h2>\n      <p>You already know these ideas under other names. Here's the dictionary; the rest of the workshop uses these words.</p><table><thead><tr><th>Term</th><th>What it actually is</th><th>Your mental model</th></tr></thead><tbody><tr><td><strong>LLM / model</strong></td><td>A program that turns text in into text out. Given a prompt, it returns a guess. Claude is one.</td><td>A well-read intern who writes plausible code but never runs it.</td></tr><tr><td><strong>Prompt</strong></td><td>The text you send the model.</td><td>The Jira ticket plus all the context you paste in.</td></tr><tr><td><strong>Agent</strong></td><td>A model wired to a loop where it can call functions (\"tools\"), see the result, and decide what to do next.</td><td>An intern who can grep your repo before answering, several times.</td></tr><tr><td><strong>Tool</strong></td><td>A named function you expose to the model, with a typed signature. The model <em>requests</em> a call; your code runs it and returns the result.</td><td>A locked-down CLI you hand the intern: <code>read_file</code>, <code>list_files</code>, nothing else.</td></tr><tr><td><strong>Structured output</strong></td><td>Forcing the model to answer as JSON matching a schema, not free prose.</td><td>A required PR template the intern cannot deviate from.</td></tr><tr><td><strong>MCP</strong></td><td><a href=\"https://modelcontextprotocol.io\" target=\"_blank\" rel=\"noopener\">Model Context Protocol</a> — a standard so one program can start another as a \"server\" and call its tools.</td><td>Starting a language server (LSP) and querying it, but for arbitrary tools.</td></tr><tr><td><strong>ADBT</strong></td><td><a href=\"https://www.npmjs.com/package/@amazon-devices/amazon-devices-buildertools-mcp\" target=\"_blank\" rel=\"noopener\">Amazon Devices Builder Tools</a>, exposed here as an MCP server serving Vega migration docs.</td><td>An internal wiki you can query programmatically.</td></tr><tr><td><strong>Skill</strong></td><td>A block of domain instructions (\"how to do TV focus\") kept separate from code.</td><td>A runbook you paste into the ticket for one task.</td></tr><tr><td><strong>Harness</strong></td><td>The deterministic TypeScript program orchestrating all of the above.</td><td>Your CI pipeline, if CI could also call an intern mid-step.</td></tr><tr><td><strong>Replay</strong></td><td>Running the pipeline against <em>recorded</em> model answers instead of a live model.</td><td>Fixtures / VCR cassettes for network calls.</td></tr><tr><td><strong>VDA</strong></td><td>Vega Virtual Device — an emulator for the TV OS.</td><td>Android emulator, but for Vega.</td></tr></tbody></table><aside class=\"note \"><strong>The single most important idea</strong><br>An LLM generates <em>plausible</em> text, and plausible is not the same as correct: <code>plausible &ne; verified</code>. Everything the harness does closes that gap. Every phase ends in a mechanical check, not a vibe.</aside><h2>The board we will complete together</h2><p>Every lesson adds one row to the same argument. The left column is something an agent can say. The middle column is what can prove it without trusting the agent.</p><table><thead><tr><th>Agent claim</th><th>Independent gate</th><th>Evidence you keep</th></tr></thead><tbody><tr><td>“I understand the app”</td><td>Source inventory and explicit unknowns</td><td><code>ANALYSIS.md</code>, transcript</td></tr><tr><td>“This is the right TV plan”</td><td>Required flow/focus sections plus live ADBT provenance</td><td><code>VEGA_PORT.md</code>, document hashes</td></tr><tr><td>“The code is ready”</td><td>Static, schema, and executable checks</td><td>Check results, Git commit</td></tr><tr><td>“It builds”</td><td>Vega compiler produces a package</td><td><code>.vpkg</code>, build output</td></tr><tr><td>“It runs”</td><td>Dwell, crash scan, and two real frames</td><td>Device log, screenshots</td></tr><tr><td>“The remote works”</td><td>Focus transition contract</td><td><code>tv-focus-result.json</code></td></tr></tbody></table><aside class=\"note \"><strong>Keep asking one question</strong><br>When the model says something is done, ask: <strong>what observed it independently?</strong> The\nclaim-versus-proof card at the end of each lesson answers that question and names the artifact.</aside><h2>Who is allowed to do what</h2>\n      <p>This is the security model:</p><div class=\"flow\"><div><b>ADBT MCP</b><span>Approved Vega context</span></div><i>→</i><div><b>Strands or Claude</b><span>Read and propose</span></div><i>→</i><div><b>Harness</b><span>Write and check</span></div><i>→</i><div><b>Git</b><span>Commit evidence</span></div></div><figure class=\"snippet\"><figcaption>The boundary, in one diagram</figcaption><pre><code>ADBT (MCP server) &lt;--- model calls list_documents / read_document itself ---+\n                                                                            |\nguarded app copy  &lt;--- read-only tools (list/read/search) -----------------+--&gt; selected agent --&gt; typed patch {summary, files}\n                                                                            |\n                                                                            v\n     HARNESS: reconstruct + hash ADBT reads -&gt; validate paths -&gt; write files -&gt; run checks -&gt; retry -&gt; git commit -&gt; report</code></pre><p class=\"look\"><strong>Where to look:</strong> Both live executors receive the same pinned ADBT stdio MCP server. Strands gets a native <code>McpClient</code>; Claude Code gets an explicit <code>--mcp-config</code>. Both can read and search the guarded app and call ADBT, but neither gets a shell or write tool. The harness reconstructs and hashes the ADBT reads, then applies only the validated patch. Everything with consequences stays in <code>packages/workshop-harness/src/port-pipeline.ts</code>.</p></figure><p>The strictness has a reason: a model with a write tool or a shell can corrupt your repo on one confident wrong guess. Keep irreversible actions in deterministic code, and the worst a bad answer can do is <em>fail a check and get rejected</em>. From lesson 4 on, those checks stop being file assertions and become a compiler and a device.</p><aside class=\"note \"><strong>What is Strands Agents SDK?</strong><br><a href=\"https://github.com/strands-agents/harness-sdk\" target=\"_blank\" rel=\"noopener\">Strands</a> is AWS's open-source agent runtime (TypeScript and Python), used here as the live remote path, pinned at 1.10.0. It fits this workshop for two reasons: the plumbing (provider adapters, typed tools, structured output, limits, cancellation, usage metrics) is built in, and it's a library, not a framework — it doesn't try to own writes or orchestration, so the harness boundary stays where we put it.</aside><table><thead><tr><th>Strands supplies</th><th>The harness owns</th></tr></thead><tbody><tr><td>Agent loop and model providers</td><td>Phase order and approval</td></tr><tr><td>Read-only typed tools</td><td>Protected file writes</td></tr><tr><td>Validated patch output</td><td>Checks, retry, and Git commits</td></tr><tr><td>MCP client and metrics</td><td>Cost cap, transcripts, and report</td></tr></tbody></table><p>The port agent can list, read, and search the guarded app. The feasibility audit and plan phase also receive ADBT's own tools (<code>list_documents</code>, <code>read_document</code>, <code>search_documentation</code>) through MCP. The harness does not pre-pick what the model reads. After each live call, it walks the tool history and records every ADBT read with a SHA-256 hash. A live phase fails if it claims ADBT guidance without reading a document. Recorded turns are available only as a fallback and for the deliberate retry case study.</p><h2>1. Check the basics</h2><ol class=\"tasks\"><li>Install Node.js 20 or newer and Git. <a href=\"https://nodejs.org/api/corepack.html\" target=\"_blank\" rel=\"noopener\">Corepack</a> supplies Yarn 4.12.</li><li>Clone this repository, run <code>cd past-the-vibes-we</code>, and keep the terminal at that root.</li><li>Choose Pocket Cinema unless your own React Native app already runs.</li></ol><div class=\"grid\"><article><h3>Pocket Cinema</h3><p>Recommended. Every exercise, recording, and checkpoint supports this app.</p><code>apps/pocket-cinema</code></article><article><h3>Your app</h3><p>Use one working flow: launch → screen → action → back. Switch to Pocket Cinema if discovery takes more than 10 minutes.</p><code>launch → screen → action → back</code></article></div><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual Android TV capture</div><img src=\"assets/pocket-cinema-android-tv.png\" alt=\"Pocket Cinema home screen running on an Android TV emulator, with a featured title and a horizontal content rail\" loading=\"lazy\"><figcaption>Pocket Cinema is the shared starting app. This is the unported React Native baseline running on the workshop's Android TV AVD; later lessons add explicit focus behavior and a separate Vega package.</figcaption></figure><h3>Bring-your-own-app safety check</h3><div class=\"checklist\"><label><input type=\"checkbox\">The app runs before the workshop</label><label><input type=\"checkbox\">Git status is clean</label><label><input type=\"checkbox\">It contains no production secrets or private data</label><label><input type=\"checkbox\">It contains no protected media</label><label><input type=\"checkbox\">It can be shared with the chosen model provider</label></div><h2>2. Install the workshop workspace</h2><div class=\"command\"><header><span>Install all workshop packages</span><button class=\"copy\">Copy command</button></header><pre><code>unset NODE_TLS_REJECT_UNAUTHORIZED\ncorepack enable\nyarn setup</code></pre></div><h2>3. Verify the workspace</h2><div class=\"command\"><header><span>Run the local checks</span><button class=\"copy\">Copy command</button></header><pre><code>yarn verify</code></pre></div><p>This proves the repository, fixtures, app tests, harness tests, and workshop website are internally consistent. It does not prove that your model account or Vega device works; you check those next.</p><h3>Prove the starting point is not TV-ready</h3>\n      <p>Don't take our word that Pocket Cinema is touch-only — run the TV-readiness check against it. The same command goes green on the ported copy in lesson 6. If you brought your own app, run it on that too:</p><div class=\"command\"><header><span>Run the TV-readiness check on the starter app</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts tv-check ../../apps/pocket-cinema</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: false\n&quot;failures&quot;: [\n  &quot;Focus state module: missing src/tv/focus-state.ts&quot;,\n  &quot;App wires shared focus state: src/App.tsx must contain \\&quot;./tv/focus-state\\&quot;&quot;,\n  &quot;Initial focus declared: src/App.tsx must contain \\&quot;hasTVPreferredFocus\\&quot;&quot;,\n  &quot;Vega package manifest: missing apps/vega/manifest.toml&quot;,\n  ...\n]</code></pre></div><p>That failure list is the workshop's to-do list: everything on it is produced by the port in lesson 3 and verified mechanically in lesson 6.</p><h2>4. Choose your live executor</h2><p>Choose either Claude Code CLI or Strands, then keep the same provider and model through the workshop. The lesson commands use Claude Code as the readable default; Strands users replace only the three executor/provider/model flags shown below. Replay is the fallback, not the main workshop path.</p><table><thead><tr><th>Path</th><th>Executor flags</th><th>Credential</th></tr></thead><tbody><tr><td>Claude Code CLI</td><td><code>--executor claude-cli --model sonnet</code></td><td><a href=\"https://code.claude.com/docs\" target=\"_blank\" rel=\"noopener\">Claude Code</a> installed and already authenticated</td></tr><tr><td>Strands + Bedrock</td><td><code>--executor strands --provider bedrock --model &lt;Bedrock model id&gt; --region &lt;region&gt;</code></td><td><code>AWS_PROFILE</code> or <code>AWS_ACCESS_KEY_ID</code>, plus model access</td></tr><tr><td>Strands + OpenAI</td><td><code>--executor strands --provider openai --model &lt;OpenAI model id&gt;</code></td><td><code>OPENAI_API_KEY</code></td></tr><tr><td>Strands + OpenRouter</td><td><code>--executor strands --provider openrouter --model &lt;OpenRouter model id&gt;</code></td><td><code>OPENROUTER_API_KEY</code></td></tr><tr><td>Recorded fallback</td><td><code>--replay &lt;recording.json&gt;</code></td><td>None. Use only when a live dependency blocks the exercise.</td></tr></tbody></table><p><code>--executor</code> selects how the harness talks to a model. <code>--provider</code> is used only by the Strands executor. <code>--model</code> is the exact model id understood by Claude Code or that provider. ADBT is still supplied by the harness through MCP in both live executor paths.</p><p>With Claude Code, authenticate once and then run the harness command — do not open a separate interactive Claude session. The harness starts <code>claude</code>, sends the phase prompt through stdin, and reads stream JSON back. With Strands, no model CLI is involved; the SDK calls the selected provider inside the harness process.</p><h3>Use the same choice in every lesson</h3><p>Live commands in the lessons show the Claude Code flags <code>--executor claude-cli --model sonnet</code>.</p><p>If you chose Strands, replace only that line with one of these. Keep the app path, inputs, phase, run id, seed, confirmation, and budget unchanged:</p><figure class=\"snippet\"><figcaption>Strands executor replacements</figcaption><pre><code># Bedrock\n--executor strands --provider bedrock \\\n--model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2\n\n# OpenAI\n--executor strands --provider openai --model gpt-4.1\n\n# OpenRouter\n--executor strands --provider openrouter --model anthropic/claude-sonnet-4</code></pre><p class=\"look\"><strong>Where to look:</strong> Choose one pair of provider and model flags. Do not combine them.</p></figure><p>Those are the defaults currently encoded in <code>src/model-factory.ts</code>; you can replace the model id with another tool-capable model available to your account. The optional screenshot review also needs image input. For a model absent from the harness pricing table, add both <code>--input-rate</code> and <code>--output-rate</code> in USD per million tokens. Read the rates from your provider. Do not guess them.</p><aside class=\"note \"><strong>Keep credentials out of the repository</strong><br>Configure the required credential in your terminal or normal credential manager. Never put an\nAPI key in the app, an input fixture, a lesson command, or a committed <code>.env</code> file.</aside><h3>Check the exact selection</h3><p>Run <code>doctor</code> with the same flags you will use for the lessons:</p><div class=\"command\"><header><span>Claude Code: check the local executor</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor \\\n  --executor claude-cli --model sonnet --json</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 \\\n  --region us-west-2 --json</code></pre></div><div class=\"command\"><header><span>Strands + OpenAI</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor \\\n  --executor strands --provider openai --model gpt-4.1 --json</code></pre></div><div class=\"command\"><header><span>Strands + OpenRouter</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor \\\n  --executor strands --provider openrouter \\\n  --model anthropic/claude-sonnet-4 --json</code></pre></div><div class=\"command\"><header><span>Fallback only: verify the recorded path</span><button class=\"copy\">Copy command</button></header><pre><code>yarn replay</code></pre></div><aside class=\"note \"><strong>Pick one live executor</strong><br>You need one live path, not every path. <code>doctor</code> checks that the command or credential exists; the\nfirst <code>analyze</code> call confirms that your account can use the selected model. If one repair does not\nfix that call, save the error and use the recorded fallback for that exercise. Do not spend the\nworkshop switching providers.</aside><h2>5. Prepare Vega SDK and VDA for real evidence</h2>\n      <p>Lesson 4 needs Vega SDK <code>0.22.5875</code> to produce a real <code>.vpkg</code>. Lesson 5 needs an attached Vega Virtual Device to install, launch, filter the app's logs from the launch time, and pull two frames. Lesson 6 runs the host-side focus contract and reuses those frames; it does not press a device button. A recorded fallback can keep you moving, but it demonstrates control flow only. It cannot prove a build or device result.</p>\n      <p>How ADBT reaches the model depends on your executor:</p>\n      <ul>\n        <li><strong>Strands</strong>: the harness creates an ADBT <code>McpClient</code> and hands it to the agent.</li>\n        <li><strong>Claude Code CLI</strong>: the harness passes the same pinned server in <code>--mcp-config</code> with <code>--strict-mcp-config</code>. It does not rely on a user's global MCP settings.</li>\n        <li><strong>Recorded fallback</strong>: stored ADBT context, so no live MCP call is made.</li>\n      </ul><p>The harness starts MCP itself for both live executor paths. Run <code>init-context</code> once only to install the <code>amazon-devices-vega-*</code> skills:</p><div class=\"command\"><header><span>Install the pinned ADBT skills (one time)</span><button class=\"copy\">Copy command</button></header><pre><code># Run in a system terminal (not inside the agent).\n# --force skips the confirmation prompts.\nnpx -y @amazon-devices/amazon-devices-buildertools-mcp@1.0.5 init-context --agent claude-code-cli --force</code></pre></div><div class=\"command\"><header><span>Verify the ADBT MCP setup</span><button class=\"copy\">Copy command</button></header><pre><code>npx -y @amazon-devices/amazon-devices-buildertools-mcp@1.0.5 check-status --agent claude-code-cli</code></pre></div><aside class=\"note \"><strong>Where these come from</strong><br>Amazon Devices Builder Tools ships the ADBT MCP server, skills, and steering docs as one pinned npm package: over 400 current Vega documents — migration workflows, knowledge-base pages, prompts — plus ten agent skills, served from a local process. In this workshop the harness starts that process itself, so live runs do not depend on your agent's global MCP config. <code>init-context</code> installs the skills used for phase guidance; <code>check-status</code> confirms them. With ADBT MCP, the model reads the vendor's current guidance instead of guessing Vega APIs from memory. See the <a href=\"https://developer.amazon.com/docs/vega/0.22/mcp-server.html\" target=\"_blank\" rel=\"noopener\">Vega ADBT setup docs</a>.</aside><p>The Claude executor uses <code>--tools Read,Grep,Glob</code>, explicitly denies shell and write tools, and permits only the named ADBT MCP tools. It fingerprints the guarded copy before and after the subprocess call; any direct change fails and is rolled back. The only accepted write is the typed patch that passes the harness path checks.</p><div class=\"command\"><header><span>Check ADBT and start VDA in a system terminal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts doctor --adbt-live --json\nvega --version\nvega virtual-device start --gui</code></pre></div><p>Keep that terminal open. In a second system terminal, run:</p><div class=\"command\"><header><span>Confirm the SDK and attached device</span><button class=\"copy\">Copy command</button></header><pre><code># Run this in a second system terminal.\nvega --version\nvega virtual-device status\nvega exec vda devices -l</code></pre></div><aside class=\"note success\"><strong>Live Vega is ready only when</strong><br>The SDK prints 0.22.5875, virtual-device status reports running: true, and devices -l lists an attached device.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Try one repair for no more than 10 minutes. Then use the recorded fallback for the blocked exercise. Do not spend workshop time repairing a model account or device.</aside><h2>Setup complete</h2><div class=\"checklist\"><label><input type=\"checkbox\">Node 20+, Git, and Corepack are available</label><label><input type=\"checkbox\">The workspace packages are installed and <code>yarn verify</code> passes</label><label><input type=\"checkbox\">I wrote down one executor/provider/model combination and its doctor check passed</label><label><input type=\"checkbox\">I chose Pocket Cinema or checked my own app</label><label><input type=\"checkbox\">For real platform evidence: Vega SDK is ready for lesson 4 and VDA is ready for lesson 5</label><label><input type=\"checkbox\">I know replay is a fallback, not evidence from my model or device</label><label><input type=\"checkbox\">I can explain what Strands supplies and what the harness owns</label></div><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the most important boundary in this workshop?</summary><p>The model can inspect and propose, but the harness controls approval, protected writes, verification, retries, cost, commits, and reports. ADBT supplies selected platform knowledge; it does not take over the run.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>The workspace passes verification, one live executor is ready, and you know which app you will use. For real platform evidence, the SDK and VDA checks also pass.</aside>"},{"id":"analyze","number":"01","nav":"Analyze the app","time":"25 minutes","title":"First ask once, then build the boundary","lead":"Start with the tempting one-shot port. It may look excellent. Then inspect the five things it cannot prove and add the first controlled phase.","objective":"Explain why a plausible patch is not a verified result, then locate the boundary between what the model proposes and what your code does.","evidence":"A saved one-shot proposal that changed nothing, followed by a checked ANALYSIS.md in the guarded copy.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Begin with the shortcut</h2><p>The fastest way to understand a harness is to remove it. First ask one model to port the whole app in one call. We save its patch but apply nothing. Then we put the first boundary back: one phase, one structured result, one check, one commit. The difference between those two runs is the workshop.</p></section><h2>The tempting one-shot version</h2><p>This call still gets read-only project tools and a typed patch because we refuse to give a live model direct write access. It gets no phase plan, TV skill, ADBT MCP server, verification, retry, compiler, or device. That is close to how “just ask the agent” feels in practice.</p><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>If the proposal contains a Vega manifest, focus code, and a test, has the port succeeded? Name the\nfirst claim you would still refuse to make.</p></aside><div class=\"command\"><header><span>Save one live proposal; apply nothing</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts naive ../../apps/pocket-cinema \\\n  --executor claude-cli --model sonnet \\\n  --max-cost 1 --run-id naive-demo --yes</code></pre></div><aside class=\"note \"><strong>Using Strands</strong><br>Replace <code>--executor claude-cli --model sonnet</code> with the provider and model flags you chose in\nlesson 0. This probe works through either executor and deliberately supplies neither skills nor MCP.</aside><ol class=\"tasks\"><li>Open <code>out/naive-demo/naive-proposal.json</code>. It can be detailed, coherent, and still unproved.</li><li>Open <code>out/naive-demo/naive-result.json</code>. Every coverage row says <code>proven: false</code>, including rows the model proposed.</li><li>Read <code>missingProof</code>: no ADBT source, no applied patch, no compiler, no device, no remote behavior.</li><li>Run <code>git status</code> on <code>apps/pocket-cinema</code>. The anti-demo changed nothing outside its guarded copy.</li><li>Keep the proposal open. At the end of lesson 6, compare its claims with the evidence the full harness retained.</li></ol><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The defect is the process, not necessarily the code</h2><p>The model may produce good code. That does not repair the missing contract around it. Without an\nindependent observer, “correct” and “convincing” look identical. A harness does not assume the\nmodel will fail; it makes success distinguishable from confidence.</p></section><h2>The app</h2><p><code>apps/pocket-cinema</code> is a small React Native app: a featured title, two rails of cards, and a details screen. It runs on a phone. It is not a TV app, and in lesson 0 you proved that mechanically — <code>tv-check</code> reported <code>tvReady: false</code> with a list of failures. That list is the workshop's to-do list.</p><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual Android TV capture</div><img src=\"assets/pocket-cinema-android-tv.png\" alt=\"Pocket Cinema home screen running on an Android TV emulator, with a featured title and a horizontal content rail\" loading=\"lazy\"><figcaption>The starting point. It renders on a TV screen, but nothing in it answers a remote control — no focus, no Vega package, no way to build.</figcaption></figure><h2>The harness, in one diagram</h2><p>Six phases, each one a model call bounded by code that checks the result:</p><div class=\"flow\"><div><b>Analyze</b><span>Read the app</span></div><i>→</i><div><b>Plan</b><span>Decide the TV port</span></div><i>→</i><div><b>Port</b><span>Write the code</span></div><i>→</i><div><b>Build</b><span>Make it compile</span></div><i>→</i><div><b>Launch</b><span>Make it run</span></div><i>→</i><div><b>Test</b><span>Prove the remote works</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The boundary that makes this a harness</h2><p>The model can list, read, and search the guarded copy of your app. It has no write tool and no shell. It answers in a fixed shape — <code>{summary, files}</code> — and your code decides what happens next: write the files, run the checks, retry with the exact failure, commit what passed, stop when the money runs out. Everything with consequences lives in <code>src/port-pipeline.ts</code>. That split is the reason to build a harness instead of prompting a coding agent: you own every write, every check, and every dollar, and every one of them leaves a record.</p></section><h2>How the agent is built</h2><p>One phase is one bounded agent. This is the whole live model interaction, from <code>src/port-executor.ts</code>:</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-executor.ts (simplified)</figcaption><pre><code>const agent = new Agent({\n  name: `workshop-${phase}`,\n  model: createModel(config),                 // Bedrock / OpenAI / OpenRouter behind one interface\n  tools: createProjectReadTools(appDir),      // list/read/search only — no write, no shell\n  plugins: [createSkillsPlugin(skills)],      // the phase's skills, loaded by the agent on demand\n  structuredOutputSchema: PortOutputSchema,   // must return { summary, files }\n  systemPrompt: &quot;Inspect with read-only tools. Return a complete patch. Never claim a file or API exists without reading evidence.&quot;,\n  printer: false,                             // keep stdout clean for JSON\n});\nconst result = await consumeStream(\n  agent.stream(prompt, {\n    cancelSignal: AbortSignal.timeout(10 * 60_000),  // 10-min hard stop\n    limits: { turns: 8, totalTokens: 40_000 },       // bounded loop\n  }),\n  event =&gt; {\n    const payload = serializable(event);\n    transcripts.append(phase, {\n      attempt, executor: &quot;strands&quot;,\n      direction: strandsDirection(payload.type, payload),\n      kind: payload.type, payload,\n    });\n  },\n);</code></pre><p class=\"look\"><strong>Where to look:</strong> Strands supplies the model-and-tool loop, provider adapters, skill delivery, schema-validated output, turn and token limits, cancellation, and usage metrics. Writing files, verification, Git, cost policy, and ADBT selection stay in the harness.</p></figure><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The controlled agent is about to report what is portable. Which part of its answer can the first\nphase check mechanically, and which part remains only a claim?</p></aside><h2>Run the first phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run phase 1 and nothing else. <code>--phases</code> takes the subset you want, so you meet the pipeline one piece at a time.</p></aside><aside class=\"note \"><strong>Use the model you selected in setup</strong><br>Run one live block, not both. For Strands with OpenAI or OpenRouter, keep the Strands block and\nreplace its provider and model flags with the pair from lesson 0.</aside><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># Claude Code CLI. One phase, on a guarded copy of your app.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases analyze --yes --run-id workshop</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># Strands + Bedrock\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2 \\\n  --phases analyze --yes --run-id workshop</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;phasesComplete&quot;:[&quot;analyze&quot;]</code></pre></div><h2>Look at what it produced</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/ANALYSIS.md</code> — the model's answer, written by the harness after the check passed.</li><li>Run <code>git status</code> on <code>apps/pocket-cinema</code>. Your app never moved: the harness copied it first, and everything happens in the copy.</li><li>Run <code>git log --oneline</code> inside <code>out/workshop/app</code>. Two commits: the imported source, and the phase that passed.</li><li>Open <code>out/workshop/port-result.json</code> and find the phase's <code>attempts</code> and the checks it cleared.</li><li>Write down three claims in <code>ANALYSIS.md</code> that nothing in this run has verified.</li></ol><aside class=\"note \"><strong>Why a copy, and why Git</strong><br>The guarded copy is where the model's authority ends. Git inside it is not decoration: a failed attempt is reset to the phase's starting commit, so a rejected patch leaves nothing behind. Rollback and record are the same mechanism, and you will watch it work in lesson 3.</aside><h2>Same harness, different executor</h2><p>Pair with someone who chose a different live path. Do not rerun the phase. Compare the first and last events in your two <code>model-logs/analyze.jsonl</code> files.</p><table><thead><tr><th>Changes with the executor</th><th>Stays owned by the harness</th></tr></thead><tbody><tr><td>Provider event names and token accounting</td><td>Phase name and prompt contract</td></tr><tr><td>Claude subprocess versus Strands <code>Agent</code></td><td><code>{summary, files}</code> schema</td></tr><tr><td>Skill delivery mechanism</td><td>Guarded write, check, budget, commit</td></tr></tbody></table><p>The executor is an adapter. If changing providers also changes what “passed” means, the boundary is in the wrong place.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → proof</span><strong>I understand what can move to Vega</strong></div><dl><dt>Independent gate</dt><dd>ANALYSIS.md exists and contains the required portability section</dd><dt>Evidence</dt><dd><code>out/workshop/app/ANALYSIS.md + model-logs/analyze.jsonl</code></dd><dt>Still not proved</dt><dd>The first gate proves structure, not that every portability conclusion is correct</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What has this phase actually proved?</summary><p>That a file exists and contains a required section. Nothing has checked whether the analysis is complete, whether its portability calls are right, or whether a later phase will invent an API. Every phase that follows closes one of those gaps.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>The one-shot proposal changed nothing, <code>ANALYSIS.md</code> exists in the guarded copy, the source app is untouched, and you have three unverified claims written down.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>If the live model is blocked, run the committed recording instead — same exercise, no account:</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback if the live model is blocked\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --phases analyze --yes --run-id workshop</code></pre></div>"},{"id":"plan","number":"02","nav":"Plan the TV port","time":"30 minutes","title":"Decide how this becomes a TV app","lead":"Porting to TV is a design decision before it is a build problem, so this phase gets two kinds of knowledge: a 10-foot interface skill, and Vega's own migration workflows over MCP.","objective":"Separate the knowledge a phase needs from the code that runs it, and see where each kind comes from.","evidence":"VEGA_PORT.md documents a focus model and a remote flow, and NextSteps.md names the ADBT documents the model read.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Knowledge is a thing you supply</h2><p>Phase 1 read the app. Phase 2 decides what it should become — and that decision needs expertise neither you nor the model has by default. A phone app becomes a TV app by answering questions about a remote control: where does focus start, what can it reach, what does Back do. And it becomes a *Vega* app by following Amazon's actual migration guidance rather than a model's memory of it. This lesson is about handing a phase both, and noticing that neither one is code you wrote.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Two kinds of knowledge, two delivery mechanisms</h2><p>A <strong>skill</strong> is a Markdown file of domain instructions the executor delivers to the model — here, <code>amazon-devices-vega-focus-management</code>, one of ten <code>amazon-devices-vega-*</code> skills ADBT installs. <strong>ADBT over MCP</strong> is a live tool the model calls itself: it lists and reads Vega migration documents, and the harness records every read with a SHA-256 hash. One is knowledge you push; the other is knowledge the model pulls. This phase uses both, and the pipeline itself contains neither.</p></section><h2>What a phase carries</h2><p>Open <code>phases()</code> in <code>src/port-pipeline.ts</code> and read the <code>plan</code> entry. Four different things, kept apart on purpose:</p><table><thead><tr><th>Field</th><th>What it is</th><th>Who reads it</th></tr></thead><tbody><tr><td><code>goal</code></td><td>What this phase must produce.</td><td>The model, in the prompt.</td></tr><tr><td><code>instruction</code></td><td>The rule this harness writes into the prompt.</td><td>The model, in the prompt.</td></tr><tr><td><code>skills</code></td><td>Names of skill files the executor delivers.</td><td>The model, through its executor.</td></tr><tr><td><code>mcp</code></td><td>Named MCP servers allowed for this phase.</td><td>The selected executor.</td></tr><tr><td><code>checks</code></td><td>Code that decides whether the phase passed.</td><td>The harness, never the model.</td></tr></tbody></table><aside class=\"note \"><strong>The skills are Amazon's, not ours</strong><br>ADBT ships ten <code>amazon-devices-vega-*</code> skills — manifest configuration, focus management, navigation, media playback, performance, build-and-run, and more — versioned and updated by Amazon, installed by lesson 0's <code>init-context</code>. The harness names them, one or two per phase, without owning a word of their content. Swapping a phase's expertise is a one-line change. If they are not installed, <code>src/skills.ts</code> reports each missing skill and the run continues — and <code>workshop/fixtures/adbt-skills.json</code> records their names, hashes, and excerpts so you can still read what they say.</aside><h2>One skill, two delivery paths</h2>\n    <table><thead><tr><th>Executor</th><th>How the selected skill arrives</th></tr></thead><tbody><tr><td>Claude CLI</td><td><code>injectSkillText()</code> appends the complete skill body to the subprocess prompt.</td></tr><tr><td>Strands</td><td><code>Skill</code> objects enter an <code>AgentSkills</code> plugin. Metadata appears first; the agent activates instructions with the <code>skills</code> tool.</td></tr><tr><td>Replay</td><td>No model runs. A recorded response replaces the live executor while the same phase plan remains visible.</td></tr></tbody></table>\n    <aside class=\"note \"><strong>A missing skill never blocks a run</strong><br><code>loadSkills()</code> reports a skill it cannot find and continues. An uninstalled ADBT skill costs the model that knowledge, not the run.</aside><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The model is about to decide where focus starts and what Back does. Which of the two knowledge sources should answer that, and which should answer \"which Vega API replaces this\"?</p></aside><h2>Run the plan phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run phase 2 onto the same run id. The guarded copy and its history are reused, so this builds on the analysis you already have.</p></aside><aside class=\"note \"><strong>Keep your executor choice</strong><br>Run one live block. If you selected Strands with OpenAI or OpenRouter, use the provider and model\nflags from lesson 0. The ADBT MCP tools are still supplied by the harness.</aside><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code># The harness passes the pinned ADBT server through --mcp-config for this call.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases plan --yes --run-id workshop</code></pre></div><div class=\"command\"><header><span>Strands + Bedrock</span><button class=\"copy\">Copy command</button></header><pre><code># The harness hands the ADBT McpClient to the agent, which calls its tools itself.\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2 \\\n  --phases plan --yes --run-id workshop</code></pre></div><h2>Read the plan, and where it came from</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/VEGA_PORT.md</code>. It must document a <code>## TV Flow</code> and a <code>## Focus</code> model — the two checks this phase is graded against.</li><li>Read the focus section as a contract: where focus starts, what the boundaries do, what Back restores. Lesson 6 turns exactly this into an executable test.</li><li>Open <code>out/workshop/app/NextSteps.md</code> for the ADBT sources and anything the model could not map.</li><li>Open <code>out/workshop/adbt-port-context.json</code>. Every document the model read is recorded with a hash — the model chose what to read, and you can still audit it afterwards.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>The model picked its own ADBT documents. How does the run stay reproducible?</summary><p>The harness walks the agent's tool calls after the phase and records each document name, excerpt, and SHA-256 into <code>adbt-port-context.json</code>. Replay then reruns from that recorded context with no live MCP server. The model's freedom to choose costs you nothing in auditability.</p></details></section><h2>Assignment: ship a skill of your own</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Amazon ships its conventions as skills. Now ship one of yours — teach the model a rule your team actually has, then enforce it with a check.</p></aside><p>The pairing is the point: the skill carries the knowledge, the check does the enforcement, and they live in different files.</p><ol class=\"tasks\"><li>Create the skill: <code>mkdir -p ~/.claude/skills/team-open-questions</code>, then write <code>SKILL.md</code> in it with one instruction — for example, \"End every document you produce with a section titled ## Open Questions listing what you could not verify.\" Keep it to a paragraph.</li><li>In <code>phases()</code>, add <code>team-open-questions</code> to the <code>plan</code> phase's <code>skills</code>.</li><li>In the same phase, add a <code>contains</code> check for <code>VEGA_PORT.md</code> with the value <code>## Open Questions</code>.</li><li>Run the phase again, live.</li></ol><section class=\"proof\"><div class=\"proof-title\"><span>Claim → proof</span><strong>This is the right TV and Vega plan</strong></div><dl><dt>Independent gate</dt><dd>The plan names the TV flow and focus model, and a live run retains the ADBT documents the model read</dd><dt>Evidence</dt><dd><code>VEGA_PORT.md + NextSteps.md + adbt-port-context.json</code></dd><dt>Still not proved</dt><dd>A checked plan still does not prove that its proposed APIs compile</dd></dl></section><aside class=\"note success\"><strong>You are done when</strong><br><code>VEGA_PORT.md</code> documents a focus model and a TV flow, <code>adbt-port-context.json</code> names hashed ADBT documents, and — for the assignment — your own section is there because your skill asked for it and your check insisted on it. Neither is pipeline code.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>On the replay path your added check fails on purpose, and that failure is worth reading. The recording was made before your requirement existed, so no recorded answer can satisfy it. Replay honors checks, because checks are code — it cannot honor skills, because no model runs to follow an instruction.</aside><div class=\"command\"><header><span>Fallback: replay</span><button class=\"copy\">Copy command</button></header><pre><code># Fallback if the live model or ADBT is blocked\nyarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --phases plan --yes --run-id workshop</code></pre></div>"},{"id":"port","number":"03","nav":"Write the port","time":"30 minutes","title":"Let it write the code, then decide whether to keep it","lead":"This is the phase that changes the app — so it is also the phase where checks stop being a formality: nine of them, and a retry that carries the exact failure.","objective":"Read the checks that gate a real code change, and watch a failed one drive a useful retry.","evidence":"The guarded copy has a Vega package and a shared focus module, committed only after nine checks passed.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Where a check earns its keep</h2><p>The first two phases produced documents. This one produces code: a Vega package, a focus-state module both the app and the test import, and the test itself. A wrong answer here is expensive, so this is where you should look hard at what decides whether an answer is accepted. That decision is fifty lines of TypeScript, and by the end of this lesson you will have read all of it.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>A check is a value, not a clever function</h2><p><code>src/port-verification.ts</code> defines <code>PortCheck</code> as four shapes: a file must exist, a file must contain a string, JSON must match a schema, or a command must exit 0. <code>verifyPort()</code> walks the list and collects failure text. That is the whole verification engine. The <code>port</code> phase carries nine of them — the Vega manifest's schema line and interactive component, the build script, the app and Metro config, the root build script, the focus module, the App wiring, and the focus test itself.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The model is about to write a Vega package from scratch. Which of the nine checks would still pass if it invented a plausible manifest that Vega cannot actually load?</p></aside><h2>Run the port phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run phase 3 onto the same run id, then read what it wrote before you read whether it passed.</p></aside><aside class=\"note \"><strong>Keep your executor choice</strong><br>The command shows Claude Code. If you selected Strands, replace only\n<code>--executor claude-cli --model sonnet</code> with your provider and model flags from lesson 0.</aside><div class=\"command\"><header><span>Claude Code CLI</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases port --yes --run-id workshop</code></pre></div><ol class=\"tasks\"><li>Open <code>out/workshop/app/apps/vega/</code> — a new package: <code>manifest.toml</code>, <code>package.json</code>, <code>app.json</code>, <code>metro.config.js</code>.</li><li>Open <code>src/tv/focus-state.ts</code>. One module holds the focus rules, and both <code>src/App.tsx</code> and <code>tests/verify-tv-focus.ts</code> import it. That shared module is what makes lesson 6's test meaningful.</li><li>Run <code>git log --oneline</code> in the copy. Three phases, three commits, in order.</li></ol><h2>Read the whole model exchange</h2><p>The phase also writes <code>out/workshop/model-logs/port.jsonl</code>. This is the live audit trail, not a summary written after the fact. Each line is one complete event with its phase, attempt, executor, direction, kind, and native payload.</p><div class=\"command\"><header><span>Read the port transcript</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts logs workshop --phase port</code></pre></div><ol class=\"tasks\"><li>Find <code>kind: &quot;request&quot;</code> or <code>kind: &quot;replay_request&quot;</code>. Its payload contains the complete prompt the phase assembled.</li><li>Find the model response. Strands uses native events such as <code>modelMessageEvent</code> and <code>toolResultEvent</code>; Claude uses its native <code>assistant</code>, <code>stream_event</code>, and <code>result</code> records.</li><li>Find <code>verification_result</code>, then <code>phase_complete</code>. The model conversation and the independent decision about it are in one ordered file.</li><li>Check <code>sequence</code>. A resumed phase appends to the same file; it does not erase the earlier attempt.</li></ol><p>For a live run, add <code>--follow</code> in a second terminal while the phase is active. You can also use <code>tail -f out/workshop/model-logs/port.jsonl | jq .</code>. The payload is intentionally complete, so it can contain prompts, source excerpts, and tool results. Keep it under the gitignored <code>out/</code> directory and review it before sharing.</p><h2>Watch a check fail and drive a retry</h2><p>A live model often passes on the first try, which is a bad way to learn what happens when it doesn't. The committed recording forces the interesting case: its first plan attempt omits a required section.</p><div class=\"command\"><header><span>Replay the failed check and the repair</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-retry/port-recording.json \\\n  --phases analyze,plan --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>plan attempt 1 failed:\n  - TV flow documented: VEGA_PORT.md must contain &quot;## TV Flow&quot;\n  - Focus model documented: VEGA_PORT.md must contain &quot;## Focus&quot;</code></pre></div><figure class=\"lesson-visual\"><div class=\"visual-label\">Captured replay output</div><img src=\"assets/retry-terminal.png\" alt=\"Terminal output showing a check failing, the failure recorded in port-result.json, and the run completing after the second attempt\" loading=\"lazy\"><figcaption>This image is rendered from the real key-free command output committed in workshop/assets/retry-terminal.txt. Two useful failures become the retry context, not the instruction 'try again'.</figcaption></figure><ol class=\"tasks\"><li>Find both failed checks in your terminal. That text came from <code>verifyPort()</code>.</li><li>Open <code>out/&lt;runId&gt;/port-result.json</code>. The phase records <code>attempts: 2</code> and keeps the failures of the rejected attempt.</li><li>Open <code>out/&lt;runId&gt;/model-logs/plan.jsonl</code>. Find attempt 1's <code>verification_result</code>, then attempt 2's request. The same failure text is visible in both places.</li><li>Open <code>prompt()</code> in <code>src/port-pipeline.ts</code> and find where a previous attempt's failures are appended.</li><li>Open <code>writeOutput()</code> in the same file. Every path must resolve inside the guarded copy; <code>.git</code>, <code>node_modules</code>, <code>.env</code>, <code>..</code>, absolute paths, and symlink traversal are refused.</li></ol><aside class=\"note \"><strong>The loop cannot run away</strong><br>One retry is a default, not a law — <code>--max-attempts N</code> raises it and <code>--until-done</code> removes the cap. The loop still has three exits, and none of them is the model's opinion: the checks pass, the attempt or cost budget runs out, or the same failure comes back twice in a row and the harness stops rather than paying for another identical guess.</aside><h2>Assignment: add a rule of your own</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>A reviewer asks for something the current checks don't cover: the port must document how Back restores focus. Make the harness enforce it.</p></aside><ol class=\"tasks\"><li>In <code>tvReadyChecks()</code> in <code>src/port-verification.ts</code>, add a <code>contains</code> check for <code>TV_VERIFICATION.md</code> with the value <code>originating card</code> and the label <code>Focus restoration documented</code>.</li><li>Run <code>tv-check</code> on <code>apps/pocket-cinema</code> — your rule appears in the failure list.</li><li>Run it on <code>workshop/checkpoints/vega-buildable/app</code> — your rule passes, because the ported app documents exactly that.</li><li>Now write one that is yours. Keep it mechanical: something a string or an exit code can settle.</li></ol><div class=\"command\"><header><span>The same checks, run standalone</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts tv-check ../../apps/pocket-cinema\nyarn --cwd packages/workshop-harness tsx src/index.ts tv-check ../../workshop/checkpoints/vega-buildable/app</code></pre></div><aside class=\"note \"><strong>Choose the shape that proves the most</strong><br><code>file_exists</code> proves a file arrived. <code>contains</code> proves a decision was written down. <code>json_schema</code> proves machine-readable structure. <code>command</code> proves behavior, because something ran. Reach for the strongest shape the requirement allows — the next lesson is where a weak check would cost you.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why keep the check in code instead of asking the model to confirm its own work?</summary><p>A model reporting on its own work is another generated claim. A check runs whether the model is honest, confused, or absent, and it returns the same answer every time.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → proof</span><strong>The port code is ready</strong></div><dl><dt>Independent gate</dt><dd>Nine static, schema, and executable checks pass before the phase commit is created</dd><dt>Evidence</dt><dd><code>port-result.json + model-logs/port.jsonl + git log</code></dd><dt>Still not proved</dt><dd>These checks can still accept a plausible manifest that the Vega compiler rejects</dd></dl></section><aside class=\"note success\"><strong>You are done when</strong><br>The guarded copy holds a Vega package and a shared focus module, committed after nine checks passed, and <code>tv-check</code> reports your added rule as a failure on the starter app and a pass on the ported one.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Every command here works on the replay path — swap the executor flags for <code>--replay ../../workshop/fixtures/port-recording.json</code>.</aside>"},{"id":"build","number":"04","nav":"Build until it compiles","time":"30 minutes","title":"Let the compiler be the judge","lead":"Now the check stops being a grep and becomes a real build — and the failure the model gets back is the compiler's own diagnostics.","objective":"Run a loop whose pass condition is a produced artifact, and trace real build output into the next prompt.","evidence":"A .vpkg exists, and port-result.json records the build failures that were repaired to get it.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>The first check that can't be talked around</h2><p>Everything up to here could be satisfied by a file containing the right words. A build cannot. It either produces a <code>.vpkg</code> or it does not, and when it does not it says exactly why, in a language the model can act on. This lesson is the same loop you already know — propose, verify, retry with the exact failure — with the strongest possible verifier plugged into it.</p></section><aside class=\"note warning\"><strong>This lesson needs the Vega SDK</strong><br>Phases 4, 5, and 6 run real Vega tooling: SDK <code>0.22.5875</code> for this one, and an attached virtual device for the next two. If your setup is incomplete, the recorded fallback at the bottom of this page runs the identical control flow with recorded device results — and says so.</aside><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>What changes when the check executes</h2><p>Three things the earlier phases didn't need. The check runs a process with a 15-minute ceiling instead of reading a file. Its output is bounded before it reaches a prompt — a failing build can print megabytes, and the agent has a 40,000-token budget, so <code>runProcess</code> keeps the head and the tail and elides the middle. And the phase checks <strong>before</strong> it prompts: a build that already passes never reaches the model, so a green phase costs nothing.</p></section><div class=\"flow\"><div><b>Verify</b><span>Run the build first</span></div><i>→</i><div><b>Fail</b><span>Keep the compiler's output</span></div><i>→</i><div><b>Prompt</b><span>Send that exact text</span></div><i>→</i><div><b>Patch</b><span>Write, rebuild, judge again</span></div></div><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The build fails with <code>Type 'number' is not assignable to type 'string'</code>. What exact information\nshould reach the model, and why is “the build failed, try again” not enough?</p></aside><h2>Create the failure, then repair it live</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Inject one known TypeScript fault into the guarded copy, then run the normal build phase. The fault\ncommand refuses to touch your source app. The compiler, not a recording or a lucky model mistake,\nstarts the retry loop.</p></aside><aside class=\"note \"><strong>Keep your executor choice</strong><br>The command shows Claude Code. If you selected Strands, replace only\n<code>--executor claude-cli --model sonnet</code> with your provider and model flags from lesson 0.</aside><div class=\"command\"><header><span>Inject the workshop fault into the guarded app</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts inject-build-failure workshop --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;expectedDiagnostic&quot;:&quot;Type 'number' is not assignable to type 'string'&quot;</code></pre></div><div class=\"command\"><header><span>Run the live compiler-repair loop</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases build --yes --run-id workshop</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>build needs a fix:\n  - build failed: react-native build-vega exited with code 2\nsrc/workshop-build-break.ts(2,14): error TS2322: Type 'number' is not assignable to type 'string'.</code></pre></div><ol class=\"tasks\"><li>Watch the build fail before the first model call. The phase verifies first, so the error is the reason the model runs.</li><li>Open <code>out/workshop/model-logs/build.jsonl</code>. Find the failed <code>verification_result</code>, then the model request containing the same compiler text.</li><li>Open <code>out/workshop/port-result.json</code>. The rejected failure remains beside the passing attempt and its cost.</li><li>Run <code>git -C out/workshop/app status --porcelain</code>: empty. The phase removed the injected file and import, built successfully, and committed the verified repair.</li><li>Find the <code>.vpkg</code> under <code>out/workshop/app/apps/vega/build/</code>. That artifact, not the model response, is the pass condition.</li></ol><aside class=\"note \"><strong>Why this demo is deterministic</strong><br><code>inject-build-failure</code> adds a tiny invalid TypeScript module and commits it only inside\n<code>out/workshop/app</code>. The build phase also checks that the teaching fault and its import are gone.\nEvery live provider therefore receives a real compiler failure, while the acceptance bar remains\nthe normal Vega build plus a clean tree.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does this phase check before it calls the model?</summary><p>Because most of the time there is nothing to fix. Prompting first would spend a model call to be told the build is fine. Checking first means the loop only pays when something actually failed — and it makes the failure, not the schedule, the reason a model runs.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → proof</span><strong>The Vega app builds</strong></div><dl><dt>Independent gate</dt><dd>The real Vega build exits successfully and produces a .vpkg</dd><dt>Evidence</dt><dd><code>vega-platform-result.json + apps/vega/build/*.vpkg</code></dd><dt>Still not proved</dt><dd>A package can compile and still crash immediately after launch</dd></dl></section><aside class=\"note success\"><strong>You are done when</strong><br>Live: <code>out/&lt;runId&gt;/app/apps/vega/build/</code> contains a <code>.vpkg</code>, and <code>vega-platform-result.json</code> says <code>evidenceMode: live</code>. Replay: the recorded build gate completes and preserves any failure text, but no local <code>.vpkg</code> is expected and you make no compile claim.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Without the Vega SDK, use the deterministic recorded repair in a separate run. It demonstrates\nfailure context and retry control, but it does not produce a local package:</aside><div class=\"command\"><header><span>Fallback: recorded compiler repair</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/build-retry/port-recording.json \\\n  --platform-replay ../../workshop/fixtures/build-retry/vega-lifecycle.json \\\n  --run-id build-fallback --yes</code></pre></div>"},{"id":"launch","number":"05","nav":"Run it on the device","time":"30 minutes","title":"Make it run, and prove it stayed running","lead":"A launch command that exits 0 tells you the device accepted it. This phase finds out what happened next.","objective":"Distinguish a launch that was accepted from an app that is actually running, using the log and the frames.","evidence":"The device log holds no crash signature after the dwell, and two captured frames both render content.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Accepted is not survived</h2><p><code>launch-app .. success</code> means the device took the command. An app that throws in its first render prints exactly the same line, and the screenshot that follows is a black rectangle — a file, which a weaker gate would happily accept. This phase waits, reads the device log, and looks at the pixels. It is the clearest example in the workshop of the difference between an exit code and evidence.</p></section><aside class=\"note warning\"><strong>This lesson needs an attached device</strong><br>Start the Vega Virtual Device before you begin, and leave that terminal open. On the current VDA image the screenshot tool can segfault at the capture step; if that happens, finish on the recorded fallback and say which claim you earned.</aside><div class=\"command\"><header><span>Start VDA and confirm it is attached</span><button class=\"copy\">Copy command</button></header><pre><code># Run these in a system terminal and leave it open.\nvega virtual-device start --gui\nvega exec vda devices -l</code></pre></div><h2>What the phase actually does</h2><div class=\"flow\"><div><b>Install</b><span>Transfer the vpkg</span></div><i>→</i><div><b>Launch</b><span>Start the app id</span></div><i>→</i><div><b>Capture</b><span>First frame</span></div><i>→</i><div><b>Dwell</b><span>Wait 5 seconds</span></div><i>→</i><div><b>Logs</b><span>Scan for crash signatures</span></div><i>→</i><div><b>Capture</b><span>Second frame</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Three ways this phase catches a dead app</h2><p>The <strong>log query</strong> asks <code>loggingctl</code> only for this package and only for entries since the harness launched it. The <strong>log scan</strong> (<code>src/platform/device-log.ts</code>) then refuses <code>FATAL</code>, <code>SIGSEGV</code>, <code>has died</code>, ANR, and unhandled JS exceptions, reporting the matching line rather than the word \"crashed\". The <strong>pixel gate</strong> (<code>src/platform/screenshot.ts</code>) decodes each pulled PNG and refuses a frame under 640x360, one flat colour, or pinned black or white. The <strong>second frame</strong> is the liveness proof: an app that died on startup cannot produce it.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The app launches, renders one frame, and then throws in a rail component two seconds later. Which of the three catches it?</p></aside><h2>Run the launch phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run phase 5 onto the same run id. It installs the package phase 4 built — and if your fix touches source, it rebuilds first so what runs is what you wrote.</p></aside><aside class=\"note \"><strong>Keep your executor choice</strong><br>The command shows Claude Code. If you selected Strands, replace only\n<code>--executor claude-cli --model sonnet</code> with your provider and model flags from lesson 0.</aside><div class=\"command\"><header><span>Install, launch, and prove it is alive</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases launch --yes --run-id workshop</code></pre></div><ol class=\"tasks\"><li>Open <code>out/workshop/01-launch.png</code> and <code>02-postlaunch.png</code>. Both must look like a rendered screen; the second is the one that proves the app was still alive after the dwell.</li><li>Open <code>out/workshop/vega-device.log</code> and read what the app said about itself.</li><li>In <code>vega-platform-result.json</code>, read the named checks and their evidence — frame size, colour count, and mean luminance for a screenshot; the crash line itself when the log fails.</li></ol><aside class=\"note \"><strong>Rebuilding is part of this check</strong><br>A crash fix usually touches source, and source that is not rebuilt never reaches the device. So a retry in this phase rebuilds before it reinstalls. The first check does not rebuild, because nothing has changed since phase 4 — that is the difference between one build and two on every attempt.</aside><h2>Try breaking it</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>A gate you have never seen fail is a gate you are trusting on faith. Break each one on purpose and read what comes back.</p></aside><ol class=\"tasks\"><li>Copy <code>workshop/fixtures/vega-lifecycle.json</code> into that same directory, so its <code>screenshot</code> path still resolves. In your copy, add <code>FATAL EXCEPTION: main</code> to the <code>logs</code> turn's stdout. Run with <code>--platform-replay</code> pointed at the copy: the run fails naming that exact line.</li><li>In another copy, delete the <code>screenshot</code> line. The replay falls back to a 1x1 pixel and the run fails with <code>frame is 1x1, smaller than the 640x360 minimum for a device screen</code>.</li><li>Both end at exit code 2. Neither reaches the test phase. The line after the gate says the recording ran out of turns — a live run would rebuild and retry here, and the recording has nothing left to give.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is a screenshot alone not enough here?</summary><p>A screenshot proves something rendered at the moment it was taken. It cannot prove the app survived the next second, and it cannot tell a rendered home screen from a rendered error dialog. The log covers the first gap; the next lesson covers the second.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → proof</span><strong>The app launched and stayed alive</strong></div><dl><dt>Independent gate</dt><dd>Install and launch pass, the post-launch log has no crash signature, and two real frames contain rendered pixels</dd><dt>Evidence</dt><dd><code>vega-platform-result.json + vega-device.log + 01-launch.png + 02-postlaunch.png</code></dd><dt>Still not proved</dt><dd>Rendered frames do not prove that focus moved correctly</dd></dl></section><aside class=\"note success\"><strong>You are done when</strong><br>Live: both frames pass the pixel gate, the package-and-launch-time-filtered log has no crash signature, and <code>vega-platform-result.json</code> says <code>evidenceMode: live</code>. Replay: the same gates and failure paths execute with recorded evidence, and the result stays labeled <code>evidenceMode: replay</code>.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Without an attached device, run the recorded lifecycle. Same gates, same failures available to provoke, labeled <code>evidenceMode: replay</code> — which proves the control flow and nothing about a device:</aside><div class=\"command\"><header><span>Fallback: recorded device results</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --platform-replay ../../workshop/fixtures/vega-lifecycle.json \\\n  --phases launch --yes --run-id workshop</code></pre></div>"},{"id":"test","number":"06","nav":"Test the remote","time":"25 minutes","title":"Test the flow, not one screenshot","lead":"The app runs. The last phase asks the question a running app cannot answer by itself — does the remote control actually work.","objective":"Express TV navigation as observable state transitions instead of visual impressions.","evidence":"tv-focus-result.json records launch, movement, selection, and focus restoration after Back, alongside the device frames.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>The bug a screenshot cannot see</h2><p>Focus landing on the wrong card looks exactly like focus landing on the right one, in a still image. So the final phase does two things that prove different claims: an executable test walks the focus contract transition by transition, and the device frames show the app was rendering while it did. Neither is sufficient alone, which is the whole point of this lesson.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>What a screenshot cannot show</h2><p>TV quality is temporal. A screenshot can show where focus is now, but not whether focus moved correctly, respected boundaries, opened the right screen, or returned to the same card.</p></section><div class=\"remote\" aria-label=\"TV remote direction pad\"><button>↑</button><button>←</button><button class=\"ok\">OK</button><button>→</button><button>↓</button></div><table><thead><tr><th>Action</th><th>Expected result</th></tr></thead><tbody><tr><td>Launch</td><td>Featured action has focus</td></tr><tr><td>Down</td><td>Focus enters the first rail</td></tr><tr><td>Left / right</td><td>Focus stops at list boundaries</td></tr><tr><td>Select</td><td>Details opens for the focused card</td></tr><tr><td>Back</td><td>The same card regains focus</td></tr></tbody></table><div class=\"flow\"><div><b>Launch</b><span>Featured action focused</span></div><i>→</i><div><b>Down</b><span>Enter first rail</span></div><i>→</i><div><b>Select</b><span>Open focused details</span></div><i>→</i><div><b>Back</b><span>Restore the same card</span></div></div><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>Which transition is most likely to pass a screenshot review but fail for a real remote user?</p></aside><h2>Run the test phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run phase 6 onto the same run id. It executes the focus test the port phase wrote, and reads the frames the launch phase captured.</p></aside><aside class=\"note \"><strong>Keep your executor choice</strong><br>The command shows Claude Code. If you selected Strands, replace only\n<code>--executor claude-cli --model sonnet</code> with your provider and model flags from lesson 0.</aside><div class=\"command\"><header><span>Prove the remote contract</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --phases test --yes --run-id workshop</code></pre></div><ol class=\"tasks\"><li>Open <code>out/workshop/app/tv-focus-result.json</code> and read it as a sequence, not a score. Six transitions must be present, including <code>back-restore</code>.</li><li>Open <code>workshop/fixtures/focus-failure/README.md</code> and find the failed Back transition — what a broken contract looks like.</li><li>Trace the focus state and restoration code in the guarded app. One module answers both the app and the test.</li><li>Run <code>git -C out/workshop/app status --porcelain</code>. It is empty: the harness committed the deterministic focus evidence even though this green phase needed no model call.</li></ol><aside class=\"note \"><strong>Where the focus props come from</strong><br><code>hasTVPreferredFocus</code> and the <code>onFocus</code>/<code>onBlur</code> handlers are React Native's TV focus model — see <a href=\"https://github.com/react-native-tvos/react-native-tvos\" target=\"_blank\" rel=\"noopener\">react-native-tvos</a> for the general TV story. This workshop targets Vega, not that fork, but the focus model is the same.</aside><aside class=\"note \"><strong>The honest limit of this phase</strong><br>The test drives the focus module, not the device's input system. It proves the contract the app implements; it does not press a physical button. Injecting real remote input needs a device input capability this workshop does not ship — if your image supports it, a new <code>VegaCapability</code> is where it plugs in, and the frames you already capture become per-step evidence.</aside><h2>Optional: ask a model what the screen shows</h2><p>The pixel gate is deterministic and cheap, and it stops at what pixels can prove. It cannot tell a rendered home screen from a rendered error dialog. If you have a multimodal model configured, add one bounded call:</p><div class=\"command\"><header><span>Add the model review</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor strands --provider bedrock \\\n  --evaluate-screenshot --phases test --yes --run-id workshop</code></pre></div><p>The verdict lands in <code>vega-platform-result.json</code> as <code>screenshot review</code>, and only a clear <code>not-app</code> blocks the run. Note where authority sits: the model reports what it sees, your harness decides what that means. The deterministic gate runs either way.</p><h2>Close the loop you opened in lesson 0</h2><p>In lesson 0 you ran <code>tv-check</code> against the starter app and got <code>tvReady: false</code> with the full failure list. Run the same command against the ported copy:</p><div class=\"command\"><header><span>The before/after: same check, ported app</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts tv-check out/workshop/app</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: true\n&quot;failures&quot;: []</code></pre></div><p>Every failure from lesson 0 was produced by the six phases and is now verified mechanically. That pair of outputs is the workshop's before-and-after photo.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is Back part of the focus contract?</summary><p>Returning to a screen without restoring the user's prior focus loses navigation context. The UI may look correct while the remote interaction is broken.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → proof</span><strong>A remote user can complete the TV flow</strong></div><dl><dt>Independent gate</dt><dd>The executable focus contract observes launch, movement, selection, boundaries, and Back restoration</dd><dt>Evidence</dt><dd><code>tv-focus-result.json + tv-check tvReady:true</code></dd><dt>Still not proved</dt><dd>This host-side contract proves the shared focus model; only a separate device-input test can prove physical key delivery</dd></dl></section><aside class=\"note success\"><strong>You are done when</strong><br>On either path, the focus check passes the full transition sequence, <code>tv-focus-result.json</code> records it, and <code>tv-check</code> reports <code>tvReady: true</code>. Only a prior lesson 5 result marked <code>evidenceMode: live</code> lets you add that the two frames came from your device; replay frames prove the gate and report flow only.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Run the same check in <code>checkpoints/vega-buildable/app</code> — no device required.</aside>"},{"id":"finish","number":"07","nav":"Build your own","time":"30 minutes","title":"Control the whole pipeline, then design one as a team","lead":"You learned each signal separately. Now operate the full live pipeline from one dashboard, complete the trust board, and defend a harness design for another domain.","objective":"Read one complete run as a system, then design and challenge the smallest useful harness for one task in your own engineering domain.","evidence":"A reviewed six-phase live run and a team worksheet naming phases, independent checks, approval, budget, and retained evidence.","body":"<div class=\"takeaway\"><code>plan → context → run → check → retry → checkpoint → report</code></div><section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Now take it home</h2><p>You've built the loop and inspected each part separately. This final lesson changes scale: first use one dashboard to see the preflight and all six phases as a single controlled run, then point the same design at work you actually care about. The reusable idea is a bounded workflow that gives a model strong context, limits its authority, checks each result, and leaves evidence another developer can inspect. TV and Vega are the example, not the point.</p></section><h2>See the complete harness</h2><p>The earlier lessons use plain output on purpose. You were learning one check, retry, or device gate at a time. Now add <code>--tui</code> to open the operator view. The TUI does not run a second pipeline and it does not replace the logs. It renders the same phase callbacks, cost tally, and append-only transcripts you already inspected.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run the complete pipeline with the live executor you used in the earlier lessons. When it finishes, keep the dashboard open and inspect at\nleast one model phase and one device phase before you close it.</p></aside><div class=\"command\"><header><span>Run all phases in the final dashboard</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --executor claude-cli --model sonnet \\\n  --seed workshop-v1 --max-cost 3 --yes \\\n  --run-id final-dashboard --tui</code></pre></div><aside class=\"note \"><strong>Using Strands</strong><br>Replace <code>--executor claude-cli --model sonnet</code> with the provider and model flags you selected in\nlesson 0. Do not change the run id, seed, budget, or phase controls.</aside><table><thead><tr><th>Key</th><th>What it changes</th></tr></thead><tbody><tr><td><code>↑</code> / <code>↓</code></td><td>Select a phase. The run keeps going.</td></tr><tr><td><code>Tab</code></td><td>Cycle the visible activity: checks, model, tools, or all.</td></tr><tr><td><code>f</code></td><td>Return to the phase that is currently running.</td></tr><tr><td><code>q</code> or <code>Enter</code></td><td>Close the dashboard after the run completes.</td></tr></tbody></table><ol class=\"tasks\"><li>Read the header: exact executor/provider/model, <code>evidence live</code>, seed, elapsed time, and cost against the cap.</li><li>Start in <code>checks</code>. Watch acceptance stay separate from model output and find the commit event created only after a phase passes.</li><li>Select <code>plan</code>, press <code>Tab</code> until <code>model</code> appears, and find its request and response.</li><li>Select <code>plan</code> and switch to <code>tools</code>. Find the ADBT document list/read calls made through MCP. This is context the model chose at runtime, not text hidden in the prompt.</li><li>Select <code>build</code>, <code>launch</code>, or <code>test</code>. These phases are decided by the platform adapter and checks, not by a model saying the app works.</li><li>Press <code>q</code> or <code>Enter</code>. Open <code>out/final-dashboard/model-logs/</code> if you need the complete payload; the dashboard controls visibility, not evidence retention.</li></ol><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Control the view, not the evidence</h2><p><code>src/tui.ts</code> is a small adapter over existing run state. It keeps at most a short activity preview\non screen and filters that preview by concern. The canonical JSONL files remain complete. This\nis the safe observability pattern: a human gets a calm operator view without losing the detailed\nrecord needed for debugging or audit.</p></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why introduce the TUI only after the phase lessons?</summary><p>An overview is useful after you know what each signal means. Earlier it would hide the checks,\nretry context, and device evidence behind a convenient screen. Here it helps you control a system\nyou already understand.</p></details></section><aside class=\"note warning\"><strong>If blocked</strong><br>If a live model, SDK, or VDA blocks this final run, use the command below to learn the dashboard.\nIt shows the same phases and checks, but its model and platform evidence is recorded:\n\n<code>yarn --cwd packages/workshop-harness tsx src/index.ts run ../../apps/pocket-cinema --inputs ../../workshop/fixtures/pocket-cinema-inputs --replay ../../workshop/fixtures/port-recording.json --platform-replay ../../workshop/fixtures/vega-lifecycle.json --seed workshop-v1 --max-cost 3 --yes --run-id final-dashboard --tui</code>\n\nIn that fallback, the <code>plan</code> tools view is empty because recorded ADBT context replaces live MCP calls. Say that explicitly when you report what you inspected.</aside><h2>Complete the trust board</h2><table><thead><tr><th>Claim</th><th>Strongest proof you used</th></tr></thead><tbody><tr><td>The model understood the app</td><td>Source inventory plus explicit unknowns</td></tr><tr><td>The plan used current Vega knowledge</td><td>Live ADBT tool calls and document hashes</td></tr><tr><td>The code met the declared contract</td><td>Independent checks and a phase commit</td></tr><tr><td>The package compiled</td><td>Vega build and the <code>.vpkg</code> artifact</td></tr><tr><td>The app stayed alive</td><td>Dwell, filtered device log, and two frames</td></tr><tr><td>The TV flow worked</td><td>Executable focus-transition result</td></tr></tbody></table><p>The strength increases down the table because the observer gets closer to user behavior. The final row is still honest about its boundary: it tests the shared focus model, not physical key delivery.</p><h2>Ten-minute team challenge</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Form a team of two or three. Design the smallest useful harness for one task, then give the design\nto another team to attack. You are not pitching features; you are defending the proof.</p></aside><div class=\"grid\"><article><h3>Gradle upgrade</h3><p>Upgrade one Android module and retain a passing debug build.</p></article><article><h3>Accessibility repair</h3><p>Find and fix one screen's labeled controls and focus order.</p></article><article><h3>API migration</h3><p>Replace one deprecated client API without changing server behavior.</p></article><article><h3>Flaky test repair</h3><p>Diagnose one test, change the smallest cause, and prove repeatability.</p></article></div><ol class=\"tasks\"><li><strong>Minute 0–2:</strong> choose one domain card or one real task. Write one outcome and three explicit non-goals.</li><li><strong>Minute 2–5:</strong> choose at most three phases. Give each phase one observer that is not the model.</li><li><strong>Minute 5–7:</strong> name the knowledge source, approval point, retry limit, no-progress rule, and cost cap.</li><li><strong>Minute 7–9:</strong> swap worksheets. The other team finds one false positive: how could all checks pass while the outcome is still wrong?</li><li><strong>Minute 9–10:</strong> strengthen one check, then prepare a 30-second explanation: claim, proof, remaining limit.</li><li>Two teams share. The room votes only on whether the proof matches the claim.</li></ol><section class=\"proof\"><div class=\"proof-title\"><span>Claim → proof</span><strong>This harness pattern transfers beyond TV</strong></div><dl><dt>Independent gate</dt><dd>Another team can identify the phases, attack a false positive, and strengthen the evidence without changing the model</dd><dt>Evidence</dt><dd><code>workshop/worksheet.md</code></dd><dt>Still not proved</dt><dd>A paper design is ready for a prototype, not a production claim</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the smallest useful first version of your harness?</summary><p>One repeatable task, a short phase sequence, one strong prior, one independent check per phase, a bounded retry, and a report. Add tools only when a proven gap needs them.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br>You can explain the complete TV port from the dashboard, and another team can read your worksheet,\nchallenge its checks, and tell exactly when your harness passes, retries, or stops.</aside><div class=\"links\"><a href=\"worksheet.md\">Open the worksheet</a><a href=\"troubleshooting.md\">Troubleshooting</a><a href=\"instructor-guide.md\">Instructor guide</a><button data-go-module=\"bee\">Appendix: Bee pipeline</button></div>"},{"id":"bee","number":"A1","nav":"Appendix: Bee pipeline","time":"25 minutes","title":"Appendix: a conversation becomes code, with a gate in the middle","lead":"An optional second pipeline on the same engine: a conversation about the app becomes a reviewed spec, then working code, then a running app.","objective":"Turn an approved conversation into shipped change without committing a transcript or letting a model grade its own work.","evidence":"BEE_SPEC.md carries a paraphrase and a source id per request, and the applied change clears those checks plus the app's own tests on the device.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Welcome</p><h2>Optional, and here is why</h2><p>This is the one lesson we may skip together, and the reason is worth stating out loud: it touches private conversations. Run the live path only with explicit consent and your own Bee account. If either is missing, skip the exercise; the instructor can use the synthetic recording to demonstrate the gate without handling anyone's data. <a href=\"https://www.aboutamazon.com/news/devices/bee-amazon-wearable-ai-device-new-features\" target=\"_blank\" rel=\"noopener\">Bee</a> is Amazon's wearable AI device: it listens to your day, when you let it, and turns conversations into searchable summaries. Somewhere in a week of those conversations is the decision about what the app should do next. This lesson gets that decision into the app.</p></section><aside class=\"note \"><strong>What this needs, and what it does not</strong><br><code>bee login</code> and <code>bee mcp serve</code> happen outside the harness, in your own terminal, with your own account. The synthetic recording is demonstration material, not a substitute for claiming a live integration. The device half uses the VDA from lessons 4 and 5.</aside><aside class=\"note \"><strong>Privacy boundary</strong><br>The durable <code>bee-context.json</code> records only conversation ids and hashes. The complete local\nmodel transcript at <code>out/bee/model-logs/bee_spec.jsonl</code> is different: it records what the model\nactually received, including conversation text returned by Bee. <code>out/</code> is gitignored. Use this\nlesson only with consent, and delete or scrub that transcript before sharing the run directory.</aside><h2>Two halves, and a human between them</h2><div class=\"flow\"><div><b>Propose</b><span>Bee to a reviewable spec</span></div><i>→</i><div><b>Approve</b><span>You read it</span></div><i>→</i><div><b>Apply</b><span>Spec to code</span></div><i>→</i><div><b>Build</b><span>The vpkg</span></div><i>→</i><div><b>Launch</b><span>On the device</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The spec carries its own acceptance criteria</h2><p>Each request in <code>bee-spec.json</code> arrives with the file assertion that will prove it — <code>src/catalog.ts</code> must contain <code>Continue Watching</code>, and so on. You approve the requests and the criteria together, before any code exists. So when the apply phase passes, it passed a bar that was set before it started, by a document you signed off. A model that writes code and then decides whether the code is good is grading its own work; this is the arrangement that avoids it.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Two things the spec is not</h2><p>It is <strong>not a transcript</strong>. <code>request</code> is the harness's paraphrase plus a source id, and <code>BEE_SPEC.md</code> is rendered by the harness from the validated JSON — so the prose you approve cannot disagree with what gets built. It is <strong>not a command line</strong>. Spec checks are <code>file_exists</code> and <code>contains</code> only; a model-authored assertion and a model-authored command are different kinds of authority, and your approval does not close that gap.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Pause before running</p><strong>Make a prediction</strong><p>The conversation also mentions a flight time, a family visit, and an idea for search that nobody agreed on. Where should each of those end up?</p></aside><h2>Propose: read the conversation, write the spec</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>Run the first half. It writes no code — check that yourself rather than taking it on trust.</p></aside><div class=\"command\"><header><span>Extract a spec from the conversation</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2 \\\n  --propose</code></pre></div><aside class=\"note \"><strong>Why this optional lesson uses Strands</strong><br>The live Bee MCP client is handed directly to a Strands agent. You may use Bedrock, OpenAI, or\nOpenRouter, but you must keep the same Strands provider and model for propose and apply. Claude\nCode remains supported by the main TV-port pipeline; it is not the live Bee executor in this lesson.</aside><ol class=\"tasks\"><li>Read <code>out/bee/app/BEE_SPEC.md</code>. Every request has a source conversation, a reason, and the check that will prove it.</li><li>Read the <strong>Deliberately excluded</strong> section hardest. The travel and family material is there, and so is search, because nobody agreed what it searches.</li><li>Run <code>git -C out/bee/app diff --name-only HEAD~2 HEAD</code>. Two files: the spec and its rendering. No source changed.</li><li>Open <code>out/bee/bee-context.json</code>. It records the tool, the conversation id, and a hash — and not a word of what was said.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why hash the conversation instead of storing it?</summary><p>The hash gives the durable report enough evidence to identify the source and detect edits without\ncopying the conversation into <code>bee-context.json</code> or the app repository. The local phase\ntranscript still contains the complete model exchange for debugging and must be treated as\nprivate. The recorded fixture is hash-verified on load for the same reason: change a line and\nthe run stops instead of quietly working from edited context.</p></details></section><h2>Approve: you are the gate</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Over to you</p><strong>Now it's your turn</strong><p>This is the moment the lesson is built around. Read the spec as the requirement it is about to become.</p></aside><ol class=\"tasks\"><li>Does each request match something that was actually decided, or has a suggestion been promoted to a requirement?</li><li>Would you accept each check as proof? A check you could satisfy without doing the work is a check that proves nothing.</li><li>Is anything in the requests that belongs in the excluded list instead?</li><li>Only then run the second half. <code>--apply</code> refuses to start without a spec on disk that matches the schema.</li></ol><h2>Apply: the spec becomes code, and the code reaches the device</h2><div class=\"command\"><header><span>Implement the approved spec, then build and launch</span><button class=\"copy\">Copy command</button></header><pre><code>yarn --cwd packages/workshop-harness tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --executor strands --provider bedrock \\\n  --model anthropic.claude-3-5-sonnet-20241022-v2:0 --region us-west-2 \\\n  --apply --yes</code></pre></div><ol class=\"tasks\"><li>Watch the first thing it prints: the spec's checks failing. The phase verifies before it prompts, so the failure that provoked the work is recorded as evidence.</li><li>Read the check names in <code>out/bee/bee-result.json</code>. Each one carries its request id and the conversation it came from, so a failure points back at a decision.</li><li><code>bee_apply</code> also has to clear the app's own gates — <code>tsc --noEmit</code> and the catalog tests — which the spec does not control and the model cannot loosen.</li><li><code>build</code> and <code>launch</code> are the phases from lessons 4 and 5, reused unchanged. The conversation's request reaches a device through machinery that knows nothing about Bee.</li></ol><aside class=\"note \"><strong>The requirement is not editable by the thing being measured</strong><br><code>bee_apply</code> declares <code>bee-spec.json</code> and <code>BEE_SPEC.md</code> read-only. A patch that touches either is refused before it is written. Without that, a model one attempt away from passing could pass by rewriting the requirement.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is this a separate command instead of a seventh phase?</summary><p>The port pipeline answers one question — does this app run on Vega — and its phases build on each other. This answers a different one, from a different source, and it can run any time after the app exists. Making them separate is also what proved the engine is general: <code>runPortPipeline</code> takes a plan, and this is the second one.</p></details></section><aside class=\"note success\"><strong>You are done when</strong><br><code>BEE_SPEC.md</code> is committed with a source id per request and an excluded list, <code>bee-result.json</code> shows <code>bee_apply</code>, <code>build</code>, and <code>launch</code> complete, and the change the conversation asked for is in the app's catalog and its details screen.</aside><aside class=\"note warning\"><strong>If blocked</strong><br>Without Bee consent or account access, skip the live exercise. For an instructor-led case study,\nreplace the executor/provider/model flags in both commands with\n<code>--replay ../../workshop/fixtures/bee-run/port-recording.json</code>; add\n<code>--platform-replay ../../workshop/fixtures/vega-lifecycle.json</code> to the apply command when no VDA\nis attached. The conversation is synthetic, and the result is recorded evidence only.</aside>"}];
