// GENERATED by scripts/build-site.mjs from workshop/lessons/*.md — do not edit by hand.
window.WORKSHOP_MODULES = [{"id":"welcome","number":"00","nav":"Start here","time":"20 minutes","title":"Set up the workshop and learn the harness","lead":"Prepare the workshop workspace. The workspace contains the project packages and dependencies. A model executor connects the harness to a model.","objective":"Explain the function of the harness. Prepare the project, one model connection, Amazon Devices Builder Tools (ADBT), the Vega software development kit (SDK), and the Vega Virtual Device (VDA).","evidence":"A verified workspace and one configured executor.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Set up the workshop and learn the harness</h2><p>Lesson 00 prepares all tools for the workshop.\nYou will install the project dependencies and configure one model connection that sends real requests.\nYou will prepare Amazon Devices Builder Tools, the Vega software development kit, and the Vega Virtual Device.\n\nYou will also learn how the harness works before you run its first phase.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Understand the harness</h2><p>An Agent harness is the software infrastructure around a large language model (LLM).\nThe harness turns model reasoning into actions that software can control and verify.\nTypical components include tools, an isolated execution environment, and context management.\nA tool is a function that the model can request.\nAn isolated execution environment limits what the model can read or change.\nContext management selects the instructions and project information that the model receives.\n\nIn this workshop, the harness is a phase pipeline written in TypeScript.\nThe pipeline contains six ordered phases:\n\n<code>analyze -&gt; plan -&gt; port -&gt; build -&gt; launch -&gt; test</code>\n\nThe <code>analyze</code> phase reads the existing app.\nThe <code>plan</code> phase defines the required TV interaction.\nThe <code>port</code> phase changes the app source.\nThe <code>build</code> phase compiles an installable TV app package.\nThe <code>launch</code> phase starts the package on a TV device.\nThe <code>test</code> phase checks remote-control navigation.\n\nEach phase has a defined goal, input, check, and result.\nA phase can call a model and give the model selected tools.\nThe model can inspect the app and propose changes.\nThe model cannot write files or run shell commands.\nThe harness controls file writes, independent checks, retries, usage limits, Git commits, and reports.\n\nThe harness records evidence that you can inspect after each phase.</p></section><h2>Know the main terms</h2><table><thead><tr><th>Term</th><th>Function in this workshop</th></tr></thead><tbody><tr><td><a href=\"https://strandsagents.com/\" target=\"_blank\" rel=\"noopener\">Strands Agents SDK</a></td><td>An open-source software development kit for AI agents in Python and TypeScript. The harness uses the TypeScript SDK for model access, tool operations, JavaScript Object Notation (JSON) responses with required fields, and Model Context Protocol (MCP) connections.</td></tr><tr><td><a href=\"https://developer.amazon.com/docs/vega/0.22/vega-get-started.html\" target=\"_blank\" rel=\"noopener\">Vega OS</a></td><td>The target TV operating system. The Vega SDK provides tools to build, run, test, and debug Vega apps.</td></tr><tr><td><a href=\"https://developer.amazon.com/docs/vega/0.23/mcp-server.html\" target=\"_blank\" rel=\"noopener\">Amazon Devices Builder Tools (ADBT)</a></td><td>A package that gives AI agents Amazon Devices tools and documents. This workshop connects the model to ADBT through MCP.</td></tr></tbody></table><h2>1. Prepare your tools</h2><p><a href=\"https://nodejs.org/en/download\" target=\"_blank\" rel=\"noopener\">Node.js</a> runs the TypeScript harness. <a href=\"https://git-scm.com/downloads\" target=\"_blank\" rel=\"noopener\">Git</a> records accepted source changes. <a href=\"https://github.com/nodejs/corepack\" target=\"_blank\" rel=\"noopener\">Corepack</a> activates the Yarn package manager used by this repository.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Install the required command-line tools.\nClone the workshop repository.</p></aside><ol class=\"tasks\"><li>Install <a href=\"https://nodejs.org/en/download\" target=\"_blank\" rel=\"noopener\">Node.js</a> 20 or newer.</li><li>Install <a href=\"https://git-scm.com/downloads\" target=\"_blank\" rel=\"noopener\">Git</a>.</li><li>Make sure that <a href=\"https://github.com/nodejs/corepack\" target=\"_blank\" rel=\"noopener\">Corepack</a> is available.</li><li>Clone the repository.</li><li>Open a terminal in the repository root.</li></ol><div class=\"command\"><header><span>Clone the repository</span><button class=\"copy\">Copy command</button></header><pre><code>git clone https://github.com/giolaq/past-the-vibes-we.git\ncd past-the-vibes-we</code></pre></div><p>The workshop uses a sample React Native app named Pocket Cinema. The app source is in:</p><pre><code class=\"language-text\">apps/pocket-cinema</code></pre><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual Android TV capture</div><img src=\"assets/pocket-cinema-android-tv.png\" alt=\"Pocket Cinema home screen on an Android TV emulator\" loading=\"lazy\"><figcaption>This image shows the React Native app before the Vega port. The app has no explicit TV focus behavior.</figcaption></figure><p>The starting app does not contain explicit rules for the TV focus.</p><h2>Know what the harness receives</h2><p>The harness receives five types of input. Each input has a different purpose.</p><table><thead><tr><th>Input type</th><th>Source</th><th>Purpose</th></tr></thead><tbody><tr><td>Existing product</td><td>Source app directory</td><td>Supplies the code, content, design, dependencies, and current behavior</td></tr><tr><td>Port requirements</td><td><code>workshop-brief.md</code></td><td>Defines the required TV flow, constraints, and completion evidence</td></tr><tr><td>Model connection</td><td><code>workshop.config.json</code></td><td>Stores the model settings described in section 4</td></tr><tr><td>Run settings</td><td>Command flags</td><td>Select the phases, run ID, creative seed, and optional usage limits</td></tr><tr><td>Platform knowledge</td><td>ADBT MCP</td><td>Supplies current Vega documents to the model</td></tr></tbody></table><p>The source app and <code>workshop-brief.md</code> define the product work. The run settings control how the harness does that work. ADBT supplies Vega knowledge but does not define the product.</p><p>A run ID is the name of one pipeline run and its directory under <code>out/</code>.</p><p>The Pocket Cinema requirements are in <code>apps/pocket-cinema/workshop-brief.md</code>.</p><h2>2. Install the workspace</h2><p>The workspace is the repository packages and their shared dependencies. Yarn manages this workspace. A dependency is a software package that the project uses.</p><p>Transport Layer Security (TLS) certificate verification protects package downloads. The first command removes an unsafe TLS override if that override exists.</p><p>Run these commands from the repository root:</p><div class=\"command\"><header><span>Install the workshop packages</span><button class=\"copy\">Copy command</button></header><pre><code>unset NODE_TLS_REJECT_UNAUTHORIZED\ncorepack enable\nyarn setup</code></pre></div><p>The <code>corepack enable</code> command makes the repository version of Yarn available. The <code>yarn setup</code> command installs the workspace dependencies.</p><h2>3. Verify the workspace</h2><div class=\"command\"><header><span>Run the local verification</span><button class=\"copy\">Copy command</button></header><pre><code>yarn verify</code></pre></div><p>This command checks TypeScript for type errors. The command also runs automated tests, document checks, and website checks. The command does not call a model or start a Vega device.</p><h3>Verify the starting app</h3><p>The harness package is the <code>packages/workshop-harness</code> directory. Enter the harness package. Keep this terminal in this directory for lessons 1 through 7.</p><div class=\"command\"><header><span>Enter the harness package</span><button class=\"copy\">Copy command</button></header><pre><code>cd packages/workshop-harness</code></pre></div><p>The <code>yarn tsx</code> command runs a TypeScript command-line program. The <code>tv-check</code> command reads the app files and checks for required TV support. The command does not build or start the app.</p><div class=\"command\"><header><span>Run the TV-readiness check</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts tv-check ../../apps/pocket-cinema</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: false\n&quot;failures&quot;: [\n&quot;Focus state module: missing src/tv/focus-state.ts&quot;,\n&quot;App wires shared focus state: src/App.tsx must contain \\&quot;./tv/focus-state\\&quot;&quot;,\n&quot;Initial focus declared: src/App.tsx must contain \\&quot;hasTVPreferredFocus\\&quot;&quot;,\n&quot;Vega package manifest: missing apps/vega/manifest.toml&quot;,\n...\n]</code></pre></div><p>This failure is the expected starting result. The failure list identifies the missing TV support that the workshop will add. Lesson 6 runs the same check on the ported app.</p><h2>4. Select one live executor</h2><p>A live executor sends prompts to an actual model. The executor is the code path that sends the prompt and receives the model response. The provider is the service that hosts the model. The model ID is the provider name for a specific model.</p><p>Use the same executor, provider, and model in all lessons. Do not change providers during the workshop.</p><p>Open <code>../../workshop.config.json</code>. You are in <code>packages/workshop-harness</code>, so <code>../..</code> points to the repository root. Replace the file contents with one configuration from this section. Select any model that the executor and provider support. Use the exact model name or provider model ID.</p><table><thead><tr><th>Path</th><th>Configuration fields</th><th>Credential</th></tr></thead><tbody><tr><td>Claude Code CLI</td><td><code>executor</code>, <code>model</code></td><td>Authenticated Claude Code</td></tr><tr><td>Strands with Bedrock</td><td><code>executor</code>, <code>provider</code>, <code>model</code>, <code>region</code></td><td>AWS credentials and model access</td></tr><tr><td>Strands with OpenAI</td><td><code>executor</code>, <code>provider</code>, <code>model</code></td><td><code>OPENAI_API_KEY</code></td></tr><tr><td>Strands with OpenRouter</td><td><code>executor</code>, <code>provider</code>, <code>model</code></td><td><code>OPENROUTER_API_KEY</code></td></tr></tbody></table><p>CLI means command-line interface. With Claude Code CLI, the harness starts your authenticated <code>claude</code> command. Authenticated means that you signed in to Claude Code before the workshop.</p><p>With Strands, the SDK sends the request to the selected provider.</p><h3>Configure Claude Code</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;claude-cli&quot;,\n  &quot;model&quot;: &quot;claude-sonnet-4-6&quot;\n}</code></pre><p>Use an exact Claude model name. Do not use an alias such as <code>sonnet</code>, <code>opus</code>, or <code>haiku</code>. If your organization manages Claude Code, select a name from <code>availableModels</code> in <code>~/.claude/settings.json</code>.</p><h3>Configure Strands with Bedrock</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;bedrock&quot;,\n  &quot;model&quot;: &quot;anthropic.claude-3-5-sonnet-20241022-v2:0&quot;,\n  &quot;region&quot;: &quot;us-west-2&quot;\n}</code></pre><h3>Configure Strands with OpenAI</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;openai&quot;,\n  &quot;model&quot;: &quot;gpt-4.1&quot;\n}</code></pre><h3>Configure Strands with OpenRouter</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;openrouter&quot;,\n  &quot;model&quot;: &quot;anthropic/claude-sonnet-4&quot;\n}</code></pre><h3>Verify the selected configuration</h3><div class=\"command\"><header><span>Verify the model environment</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts doctor --json</code></pre></div><p>The <code>doctor</code> command checks the selected executor, credentials, and required programs. The <code>--json</code> flag prints the result as machine-readable JSON. The harness automatically loads <code>../../workshop.config.json</code>. Command-line model flags override the file for one command.</p><h2>5. Prepare ADBT</h2><p>The harness uses ADBT through MCP. Strands creates an MCP client for ADBT.</p><h2>6. Prepare Vega</h2><p>Lesson 4 requires Vega SDK version <code>0.23.9221</code>. Use the <a href=\"https://developer.amazon.com/docs/vega/0.23/install-vega-sdk.html\" target=\"_blank\" rel=\"noopener\">Vega installation guide</a> to install this SDK version.</p><p>The Vega Virtual Device (VDA) is a software TV device that runs on your computer. Lesson 5 requires an installed VDA. The harness starts the VDA when no device is attached. Lesson 6 sends remote-control keys and reads the focused control.</p><p>Use your terminal application for the setup commands. Do not run the setup commands inside a model chat. The <code>doctor --adbt-live</code> command checks a live ADBT connection.</p><div class=\"command\"><header><span>Start the Vega environment</span><button class=\"copy\">Copy command</button></header><pre><code>cd packages/workshop-harness\nyarn tsx src/index.ts doctor --adbt-live --json\nvega --version\nvega virtual-device start --gui</code></pre></div><p>The <code>--gui</code> option opens the VDA window. You can start the VDA before Lesson 5. The early VDA start is optional. The launch phase runs the same command when no device is attached.</p><p>In a second terminal, run:</p><div class=\"command\"><header><span>Verify the SDK and device</span><button class=\"copy\">Copy command</button></header><pre><code>vega --version\nvega virtual-device status\nvega exec vda devices -l</code></pre></div><p>The last command lists devices that the Vega CLI can contact. The environment is ready when all these conditions are true:</p><ul><li>The SDK reports version <code>0.23.9221</code>.</li><li>The virtual-device status reports <code>running: true</code>.</li><li>The device list contains an attached device.</li></ul><p>An attached device is a VDA or physical Vega device that the Vega CLI can contact. A successful command with an empty device list does not prove that a device is attached.</p><aside class=\"note success\"><strong>Completion check</strong><br>The workspace passes all local checks.\nOne live executor is ready.\nFor live platform evidence, the Vega SDK and VDA are also ready.</aside>"},{"id":"analyze","number":"01","nav":"Analyze the app","time":"25 minutes","title":"Analyze the app","lead":"The analyze phase works in a copy under out/ and records the Vega documents that the model reads.","objective":"Run the analyze phase and inspect the output.","evidence":"An verified ANALYSIS.md file in the out/ directory.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Start with one model call</h2><p>First, run one model call without the phase controls.\nThen, run the controlled analyze phase.\n\nBoth commands ask a model to inspect the same app.\nOnly the controlled phase uses ADBT, an independent check, and a Git commit.</p></section><h2>Compare the two paths</h2><p>The <code>naive</code> command asks for one proposed port. The command saves the model response but does not apply the proposed files.</p><p>The analyze phase has a smaller goal. It reads the app and writes <code>ANALYSIS.md</code>. It does not change app code.</p><table><thead><tr><th>Path</th><th>Model task</th><th>Harness control</th></tr></thead><tbody><tr><td><code>naive</code></td><td>Propose a complete port</td><td>Save the proposal</td></tr><tr><td><code>analyze</code></td><td>Describe the app and portability work</td><td>Use ADBT, check the document, and commit it</td></tr></tbody></table><h2>Run the one-call example</h2><p>Run the command from <code>packages/workshop-harness</code>.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Save one proposed port.\nInspect the missing proof.\nConfirm that the source app did not change.</p></aside><div class=\"command\"><header><span>Save one live proposal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts naive ../../apps/pocket-cinema \\\n --run-id naive-demo --yes</code></pre></div><p>The command flags have these functions:</p><table><thead><tr><th>Flag or value</th><th>Function</th></tr></thead><tbody><tr><td><code>../../apps/pocket-cinema</code></td><td>Selects the source app</td></tr><tr><td><code>--run-id naive-demo</code></td><td>Writes this run under <code>out/naive-demo</code></td></tr><tr><td><code>--yes</code></td><td>Confirms the live model call</td></tr></tbody></table><p>Expect the command to write two files:</p><ul><li><code>out/naive-demo/naive-proposal.json</code> contains the proposed files.</li><li><code>out/naive-demo/naive-result.json</code> lists five claims without proof.</li></ul><ol class=\"tasks\"><li>Open <code>out/naive-demo/naive-proposal.json</code>.</li><li>Find one proposed Vega file.</li><li>Open <code>out/naive-demo/naive-result.json</code>.</li><li>Find the <code>missingProof</code> list.</li><li>Run <code>git -C ../../apps/pocket-cinema status --short</code>.</li><li>Confirm that the command prints no source changes.</li></ol><p>The proposal can contain useful code. The proposal is still a model claim. No compiler or device checked it.</p><h2>Important harness code</h2><p>The analyze phase is a TypeScript object in <code>src/port-pipeline.ts</code>.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-pipeline.ts (simplified)</figcaption><pre><code>{\nname: &quot;analyze&quot;,\ngoal: &quot;Read the guarded React Native app and write ANALYSIS.md.&quot;,\ninstruction: &quot;Read ADBT before making Vega portability claims.&quot;,\nmcp: [ADBT_SERVER],\nchecks: [{\ntype: &quot;contains&quot;,\npath: &quot;ANALYSIS.md&quot;,\nvalue: &quot;## Portable&quot;,\nlabel: &quot;Portability analysis documented&quot;,\n}],\n}\n\n&gt; look: The phase gives ADBT tools to the model. The independent check requires one section in ANALYSIS.md.\n&gt; :::\n\nThe model can list, read, and search the guarded copy.\nThe model has no shell tool and no file write tool.\nThe model returns proposed files in JSON.\n\nThe harness validates the JSON and writes the files.\nThe harness then runs the `contains` check.\nThe harness commits the files only when the check passes.\n\n## Run the analyze phase\n\nUse run ID `workshop` for the main workshop flow.\nLater lessons continue the same run.\n\n:::yourturn\nRun the analyze phase.\nWait for the model to inspect the app and read an ADBT document.\nDo not start a second analyze command while the first command runs.</code></pre></figure><div class=\"command\"><header><span>Run the analyze phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n --phases analyze --yes --run-id workshop</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;phasesComplete&quot;:[&quot;analyze&quot;]</code></pre></div><p>The command can take several minutes. The final JSON reports the model, token use, turns, and completed phases.</p><p>To watch the phase from a second terminal, run:</p><pre><code class=\"language-sh\">cd packages/workshop-harness\nyarn tsx src/index.ts logs workshop --phase analyze --follow</code></pre><p>The live transcript shows model requests, ADBT tool operations, checks, and the phase commit.</p><h2>Inspect the result</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/ANALYSIS.md</code>.</li><li>Find the screen and component inventory.</li><li>Find the <code>## Portable</code> section.</li><li>Find the replacement work.</li><li>Open <code>out/workshop/adbt-port-context.json</code>.</li><li>Find the ADBT document names and hashes.</li><li>Open <code>out/workshop/port-result.json</code>.</li><li>Find the analyze attempt and usage values.</li><li>Run <code>git -C ../../out/workshop/app log --oneline</code>.</li><li>Find the analyze-phase commit.</li><li>Record three technical claims that the <code>contains</code> check did not verify.</li></ol><p>The ADBT context file proves that the model read current Vega documents. It does not prove that every model conclusion is correct.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The model analyzed the app and identified portable work</strong></div><dl><dt>Independent check</dt><dd>ANALYSIS.md contains the required portability section</dd><dt>Evidence</dt><dd><code>out/workshop/app/ANALYSIS.md, adbt-port-context.json, and the analyze commit</code></dd><dt>Limit</dt><dd>The check verifies document structure, not every technical conclusion</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What happened?</summary><p>The model inspected a guarded copy of Pocket Cinema.\nThe model used ADBT for Vega information.\nThe harness wrote and checked <code>ANALYSIS.md</code>.\nThe source app remained unchanged.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>The one-call example changed no source files.\n<code>ANALYSIS.md</code> exists in the guarded copy.\nThe harness committed the passing phase.\nYou recorded three unverified claims.</aside>"},{"id":"plan","number":"02","nav":"Plan the TV port","time":"30 minutes","title":"Define and approve the Plan for TV porting","lead":"Convert the app analysis into a structured TV plan. Review focus, Select, and Back behavior before the harness permits source changes.","objective":"Run the plan phase, inspect its ADBT sources, and approve the exact plan that later phases must follow.","evidence":"port-plan.json passes its schema. The human-approved plan is in port-plan-approval.json.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Plan before code changes</h2><p>The analyze phase described the current app.\nThe plan phase defines the TV version of that app.\n\nThe model proposes the plan.\nThe harness checks the plan structure.\nA person approves the product decisions.</p></section><h2>Know the plan input and output</h2><p>The plan phase receives:</p><ul><li>The guarded app.</li><li><code>ANALYSIS.md</code>.</li><li><code>workshop-brief.md</code>.</li><li>Current Vega documents from ADBT.</li></ul><p>The phase writes:</p><table><thead><tr><th>File</th><th>Function</th></tr></thead><tbody><tr><td><code>port-plan.json</code></td><td>Machine-checked screens, navigation, behavior, and evidence</td></tr><tr><td><code>VEGA_PORT.md</code></td><td>Human-readable TV flow and focus model</td></tr><tr><td><code>NextSteps.md</code></td><td>ADBT sources, unsupported mappings, and open work</td></tr><tr><td><code>adbt-port-context.json</code></td><td>Names, excerpts, and hashes of ADBT documents</td></tr></tbody></table><p>A navigation edge describes movement between screens or focus targets. Preserved behavior must remain in the port. Deferred behavior is outside the current port.</p><h2>Important harness code</h2><p>The plan phase declares its files and checks in <code>src/port-pipeline.ts</code>.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-pipeline.ts (simplified)</figcaption><pre><code>{\nname: &quot;plan&quot;,\nmcp: [ADBT_SERVER],\nchecks: [\n{ type: &quot;json_schema&quot;, path: &quot;port-plan.json&quot;,\nschema: PortPlanSchema, label: &quot;Structured port plan&quot; },\n{ type: &quot;contains&quot;, path: &quot;VEGA_PORT.md&quot;,\nvalue: &quot;## TV Flow&quot;, label: &quot;TV flow documented&quot; },\n{ type: &quot;contains&quot;, path: &quot;VEGA_PORT.md&quot;,\nvalue: &quot;## Focus&quot;, label: &quot;Focus model documented&quot; },\n{ type: &quot;contains&quot;, path: &quot;NextSteps.md&quot;,\nvalue: &quot;ADBT&quot;, label: &quot;ADBT gaps and sources&quot; },\n],\n}\n\n&gt; look: The schema checks references and required behavior. The text checks require the human explanation and ADBT source record.\n&gt; :::\n\nThe schema rejects a missing screen reference.\nIt also rejects missing Select, Back, initial focus, or evidence mappings.\nThe schema cannot decide if the selected product flow is correct.\n\n## Run the plan phase\n\nUse the same `workshop` run ID from Lesson 01.\n\n:::yourturn\nRun the plan phase.\nWait for the model to read ADBT and write both plan files.</code></pre></figure><div class=\"command\"><header><span>Run the plan phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n --phases plan --yes --run-id workshop</code></pre></div><p>The final result has this state:</p><pre><code class=\"language-json\">{\n  &quot;state&quot;: &quot;awaiting_approval&quot;,\n  &quot;phasesComplete&quot;: [&quot;analyze&quot;, &quot;plan&quot;]\n}</code></pre><p><code>awaiting_approval</code> is the correct result. The plan phase is complete, but code changes are not permitted yet.</p><h2>Inspect the proposed plan</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/port-plan.json</code>.</li><li>Compare <code>screens</code> with <code>apps/pocket-cinema/workshop-brief.md</code>.</li><li>Find the initial focus ID.</li><li>Trace the Select action from Home to Details.</li><li>Trace the Back action from Details to the originating card.</li><li>Read <code>preservedBehaviors</code>.</li><li>Read <code>deferredBehaviors</code>.</li><li>Read <code>verification</code>.</li><li>Open <code>out/workshop/app/VEGA_PORT.md</code>.</li><li>Find <code>## TV Flow</code>.</li><li>Find <code>## Focus</code>.</li><li>Open <code>out/workshop/adbt-port-context.json</code>.</li><li>Find the ADBT document hashes.</li></ol><p>Reject the plan if it invents a screen, loses Back behavior, or names a Vega API without an ADBT source.</p><h2>Test the approval boundary</h2><p>Run the port phase before approval:</p><pre><code class=\"language-sh\">yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --phases port --yes --run-id workshop</code></pre><p>Expect <code>plan_approval_required</code>. The harness stops before it calls the port model.</p><p>The check is in the command setup:</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/index.ts (simplified)</figcaption><pre><code>beforePhase: (phase) =&gt; {\nif ([&quot;port&quot;, &quot;build&quot;, &quot;launch&quot;, &quot;test&quot;].includes(phase.name)) {\nassertPortPlanApproved(appDir);\n}\n},\n\n&gt; look: Every code or device phase checks the approval before the phase starts.\n&gt; :::\n\n## Approve the plan\n\nApprove only after you complete the review.\n\n:::command Approve the reviewed plan\nyarn tsx src/index.ts approve-plan workshop --yes</code></pre></figure><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;command&quot;: &quot;approve-plan&quot;\n&quot;planSha256&quot;: &quot;sha256:...&quot;</code></pre></div><p>The command writes <code>out/workshop/app/port-plan-approval.json</code>. The file contains the plan hash and workshop brief hash.</p><p>If either input changes, the approval becomes stale. The harness then requires a new review and approval.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The TV plan has valid structure, ADBT sources, and human approval</strong></div><dl><dt>Independent check</dt><dd>The schema passes and the approval hashes match the reviewed plan and brief</dd><dt>Evidence</dt><dd><code>port-plan.json, port-plan-approval.json, VEGA_PORT.md, and adbt-port-context.json</code></dd><dt>Limit</dt><dd>The plan does not prove that the proposed Vega code will compile</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What happened?</summary><p>ADBT supplied current Vega information.\nThe model converted the app requirements into a structured plan.\nThe harness checked the plan references.\nYou approved the product decisions.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br><code>port-plan.json</code> contains the screens, navigation, behavior, and evidence.\n<code>port-plan-approval.json</code> contains the plan and brief hashes.\n<code>VEGA_PORT.md</code> contains the focus model and TV flow.\n<code>adbt-port-context.json</code> contains the ADBT document records.</aside>"},{"id":"port","number":"03","nav":"The Porting","time":"30 minutes","title":"Run the porting and inspect the result","lead":"Apply the approved plan. The model proposes files, and the harness controls writes, checks, retries, and the phase commit.","objective":"Run the port phase. Inspect the generated Vega package, focus code, check result, model transcript, and Git commit.","evidence":"The guarded copy contains a Vega package and focus module.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Change code inside the harness boundary</h2><p>The first two phases produced documents.\nThe port phase produces application code.\n\nThe harness applies the model response and checks the result.</p></section><h2>Know what the phase creates</h2><p>The port phase follows the approved <code>port-plan.json</code>. It creates these main parts:</p><table><thead><tr><th>Path</th><th>Function</th></tr></thead><tbody><tr><td><code>apps/vega/</code></td><td>Vega package, manifest, build configuration, and dependencies</td></tr><tr><td><code>src/tv/focus-state.ts</code></td><td>Shared focus state for the app and tests</td></tr><tr><td><code>src/App.tsx</code></td><td>TV focus IDs and remote behavior</td></tr><tr><td><code>tests/verify-tv-focus.ts</code></td><td>Host-side focus contract</td></tr><tr><td><code>TV_VERIFICATION.md</code></td><td>Required focus and Back behavior</td></tr></tbody></table><p>Every focusable control needs a stable React Native <code>testID</code>. Lesson 06 reads these IDs from the VDA.</p><h2>Important harness code</h2><p>The port loop is in <code>src/port-pipeline.ts</code>.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-pipeline.ts (simplified)</figcaption><pre><code>const model = await executor.call(\nphase.name,\nprompt(phase, failures),\n{ mcp: phase.mcp, attempt },\n);\n\nconst output = parseJsonBlock(model.text, PortOutputSchema, phase.name);\nwriteOutput(appDir, output.files, phase.readOnly);\nfailures = await verify(phase);\n\nif (failures.length === 0) {\ncommitAll(appDir, `workshop(${phase.name}): ${output.summary}`);\n}\n\n&gt; look: The model proposes complete files. The harness parses, writes, checks, and commits them.\n&gt; :::\n\nThe port phase runs ten checks.\n\n| Check group  | What it checks                                                                     |\n| ------------ | ---------------------------------------------------------------------------------- |\n| Vega package | Manifest schema, interactive component, app registration, Metro, and build scripts |\n| TV code      | Shared focus module, app wiring, and stable `testID` values                        |\n| Test code    | Executable host-side focus check                                                   |\n\nIf a check fails, the next model request contains the exact failure text.\nThe harness resets a failed attempt before it applies the next proposal.\n\n## Run the port phase\n\nUse the approved `workshop` run.\n\n:::yourturn\nRun the code-writing phase.\nWatch for a failed check and retry.\nWait for the final `run_complete` event.</code></pre></figure><div class=\"command\"><header><span>Run the port phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n --phases port --yes --run-id workshop</code></pre></div><p>The phase can take several minutes. The model reads the approved plan, project files, and ADBT documents.</p><p>A passing final result contains:</p><pre><code class=\"language-json\">{\n  &quot;state&quot;: &quot;complete&quot;,\n  &quot;phasesComplete&quot;: [&quot;analyze&quot;, &quot;plan&quot;, &quot;port&quot;]\n}</code></pre><p>If a check fails, the terminal prints:</p><pre><code class=\"language-text\">port attempt 1 failed:\n  - &lt;exact check failure&gt;</code></pre><p>This message means that the harness rejected the first proposal. The next attempt receives the same failure text.</p><h2>Inspect the generated files</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/apps/vega/manifest.toml</code>.</li><li>Find <code>schema-version = 1</code>.</li><li>Find <code>[[components.interactive]]</code>.</li><li>Open <code>out/workshop/app/apps/vega/package.json</code>.</li><li>Find the Vega build command.</li><li>Open <code>out/workshop/app/src/tv/focus-state.ts</code>.</li><li>Open <code>out/workshop/app/src/App.tsx</code>.</li><li>Find the focus-state import.</li><li>Find the stable <code>testID</code> values.</li><li>Open <code>out/workshop/app/tests/verify-tv-focus.ts</code>.</li></ol><h2>Inspect the checks and commit</h2><p>Open the phase result:</p><pre><code class=\"language-sh\">node -e 'const r=require(&quot;../../out/workshop/port-result.json&quot;); console.log({phase:r.phases.find(p=&gt;p.name===&quot;port&quot;),usage:r.usage})'</code></pre><p>Read the <code>attempts</code>, <code>failures</code>, <code>checks</code>, and usage values.</p><p>Then, inspect the guarded Git history:</p><pre><code class=\"language-sh\">git -C ../../out/workshop/app log --oneline\ngit -C ../../out/workshop/app status --short</code></pre><p>The log must contain a <code>workshop(port)</code> commit. The status command must print no uncommitted files.</p><h2>Inspect the model messages</h2><p>Read the port transcript:</p><div class=\"command\"><header><span>Read the port transcript</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts logs workshop --phase port</code></pre></div><p>Find these event types:</p><ol class=\"tasks\"><li><code>request</code></li><li>ADBT tool operations</li><li>Model response events</li><li><code>verification_result</code></li><li><code>commit</code></li><li><code>phase_complete</code></li></ol><p>The transcript is the complete phase record. The TUI in Lesson 07 provides a shorter view of the same events.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The port files satisfy the declared source checks</strong></div><dl><dt>Independent check</dt><dd>Ten file, text, schema, and command checks pass before the commit</dd><dt>Evidence</dt><dd><code>port-result.json, model-logs/port.jsonl, generated files, and the port commit</code></dd><dt>Limit</dt><dd>Source checks do not prove that the Vega compiler accepts the package</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What happened?</summary><p>The model proposed a Vega package and TV focus code.\nThe harness protected the approved plan.\nThe harness applied only safe relative paths.\nThe harness committed the files after all port checks passed.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>The guarded copy contains the Vega package and focus module.\nTen checks pass.\nGit contains the port-phase commit.\nThe guarded Git working tree is clean.</aside>"},{"id":"build","number":"04","nav":"Build the ported app","time":"30 minutes","title":"Build the Vega package and repair one compiler failure","lead":"Run a real Vega build against the guarded copy. The harness gives exact compiler diagnostics to the model only when the first build fails.","objective":"Observe the verify-first build loop. Confirm that the repaired build produces a fresh Hermes bundle and an installable .vpkg package.","evidence":"A .vpkg file and a phase result that records the repaired compiler failure.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Let the compiler decide</h2><p>The port checks verified files and required text.\nThey did not run the Vega compiler.\n\nThis lesson adds one controlled TypeScript error.\nThe build phase finds the error, sends the exact diagnostic to the model, and\nruns the build again.</p></section><aside class=\"note warning\"><strong>Prepare the Vega SDK</strong><br>This lesson requires Vega SDK version <code>0.23.9221</code>.\nRun <code>vega --version</code> before you continue.\nStop if the version is different.</aside><h2>Know the build sequence</h2><p>The phase performs this sequence:</p><div class=\"flow\"><div><b>Install</b><span>Install declared development dependencies</span></div><i>→</i><div><b>Build</b><span>Run npm run build:debug</span></div><i>→</i><div><b>Check</b><span>Require a fresh Hermes bundle and .vpkg file</span></div><i>→</i><div><b>Repair</b><span>Call the model only after a failed build</span></div><i>→</i><div><b>Build</b><span>Run the complete build again</span></div></div><p>A Hermes bundle contains the compiled JavaScript for the Vega package. A <code>.vpkg</code> file is the installable Vega application package.</p><p>The dependency install and build each have a 15-minute time limit. The model call has no elapsed-time limit. The command can appear quiet while the model works.</p><p>Use this command in a second terminal to watch the phase:</p><pre><code class=\"language-sh\">cd packages/workshop-harness\nyarn tsx src/index.ts logs workshop --phase build --follow</code></pre><h2>Important harness code</h2><p>The build phase uses <code>verifyFirst</code>.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-pipeline.ts (simplified)</figcaption><pre><code>{\nname: &quot;build&quot;,\nmcp: [ADBT_SERVER],\ndevice: [&quot;build&quot;],\nverifyFirst: true,\nmaxAttempts: 5,\n}\n\nif (phase.verifyFirst) {\nfailures = await verify(phase);\n}\n\nif (failures.length &gt; 0) {\nconst model = await executor.call(\nphase.name,\nprompt(phase, failures),\n{ mcp: phase.mcp, attempt },\n);\n}\n\n&gt; look: The compiler runs before the model. A passing build uses no model call.\n&gt; :::\n\nThe Vega adapter owns the actual commands:\n\n:::snippet packages/workshop-harness/src/platform/vega.ts (simplified)\ndependencies: [&quot;npm&quot;, &quot;install&quot;, &quot;--include=dev&quot;],\nbuild: [&quot;npm&quot;, &quot;run&quot;, &quot;build:debug&quot;],\n\nif (build.code === 0) {\npackagePath = findVpkg(appDir);\n}\nif (!hasGeneratedBundle(appDir)) {\nblockers.push(&quot;build produced no non-empty index.hermes.bundle&quot;);\n}\n\n&gt; look: A successful command is not enough. The harness also requires the package and JavaScript bundle.\n&gt; :::\n\n## Add the controlled failure\n\nThe injector changes only `out/workshop/app`.\nIt adds one invalid TypeScript file and one import.\nIt does not change `apps/pocket-cinema`.\n\n:::yourturn\nAdd the controlled error.\nConfirm that the injector reports the expected TypeScript diagnostic.</code></pre></figure><div class=\"command\"><header><span>Add the workshop build failure</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts inject-build-failure workshop --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;expectedDiagnostic&quot;:&quot;Type 'number' is not assignable to type 'string'&quot;</code></pre></div><h2>Run the build phase</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Run the build.\nFind the first compiler failure.\nWait for the model repair and the second build.</p></aside><div class=\"command\"><header><span>Run the build phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n --phases build --yes --run-id workshop</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>build needs a fix:\n\n- build failed: react-native build-vega exited with code 2\n  src/workshop-build-break.ts(2,14): error TS2322: Type 'number' is not assignable to type 'string'.</code></pre></div><p>The failure is expected. <code>TS2322</code> identifies the controlled TypeScript error.</p><p>After this message, the harness performs these operations:</p><ol class=\"tasks\"><li>Starts the configured model.</li><li>Supplies the exact compiler text.</li><li>Supplies ADBT MCP.</li><li>Applies the proposed repair.</li><li>Runs dependency installation and the build again.</li><li>Checks the Hermes bundle and <code>.vpkg</code> file.</li><li>Commits the passing repair.</li></ol><p>The final JSON must include <code>build</code> in <code>phasesComplete</code>.</p><h2>Inspect the build evidence</h2><ol class=\"tasks\"><li>Open <code>out/workshop/model-logs/build.jsonl</code>.</li><li>Find the failed <code>verification_result</code>.</li><li>Find the next model request.</li><li>Confirm that the request contains <code>TS2322</code>.</li><li>Find the ADBT document operation.</li><li>Open <code>out/workshop/vega-platform-result.json</code>.</li><li>Find the <code>dependencies</code> step.</li><li>Find the failed and passing <code>build</code> steps.</li><li>Confirm that <code>evidenceMode</code> is <code>live</code>.</li><li>Open <code>out/workshop/port-result.json</code>.</li><li>Find the build attempt count and earlier failure.</li><li>Find the <code>.vpkg</code> file under <code>out/workshop/app/apps/vega/build/</code>.</li><li>Find <code>index.hermes.bundle</code> under the same build directory.</li><li>Run <code>git -C ../../out/workshop/app status --short</code>.</li><li>Confirm that the Git status is empty.</li></ol><p>The first failure remains in the phase result. The passing repair does not erase the failed evidence.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The guarded Vega app builds</strong></div><dl><dt>Independent check</dt><dd>The Vega build passes and creates a fresh Hermes bundle and .vpkg file</dd><dt>Evidence</dt><dd><code>vega-platform-result.json, the build files, model-logs/build.jsonl, and the build commit</code></dd><dt>Limit</dt><dd>A package can compile and still fail when it starts</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What happened?</summary><p>The harness ran the compiler before it called the model.\nThe compiler supplied a specific repair target.\nThe model used ADBT and proposed a repair.\nThe harness accepted the repair only after the complete build passed.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>For a live run, the build directory contains a <code>.vpkg</code> file.\n<code>vega-platform-result.json</code> contains <code>evidenceMode: live</code>.\nThe Git working tree is clean.</aside>"},{"id":"launch","number":"05","nav":"Run it on the VDA","time":"30 minutes","title":"Install the app and prove that runs correctly","lead":"Start or reuse a Vega Virtual Device. Install the package, start the app, wait five seconds, scan the device log, and confirm that the process is still running.","objective":"A Succesful start command of the ported app.","evidence":"The app reports running after launch and the device log contains no crash signature.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Check the process after start</h2><p>A successful start command proves that the device accepted the command.\nIt does not prove that the app stayed active.\n\nThe launch phase checks the process immediately after start.\nIt waits five seconds, reads the device log, and checks the process again.</p></section><h2>Prepare the VDA</h2><p>The Vega Virtual Device (VDA) must be installed. You can start it before the phase:</p><pre><code class=\"language-sh\">vega virtual-device start --gui</code></pre><p>If no device is attached, the harness starts the VDA. The harness then waits for two stable device checks. This wait can take up to 60 seconds.</p><p>The app can appear in the VDA before the phase command returns. This is normal. The harness still has to complete the dwell, log scan, and final state check.</p><h2>Know the launch sequence</h2><div class=\"flow\"><div><b>SDK</b><span>Require Vega SDK 0.23.9221</span></div><i>→</i><div><b>VDA</b><span>Reuse an attached device or start one</span></div><i>→</i><div><b>Install</b><span>Install the .vpkg from Lesson 04</span></div><i>→</i><div><b>Start</b><span>Start the app ID</span></div><i>→</i><div><b>State</b><span>Confirm that the process is running</span></div><i>→</i><div><b>Wait</b><span>Wait five seconds</span></div><i>→</i><div><b>Logs</b><span>Reject crash signatures</span></div><i>→</i><div><b>State</b><span>Confirm that the process is still running</span></div></div><p>The log scan rejects fatal errors, segmentation faults, dead processes, Application Not Responding errors, and unhandled JavaScript exceptions.</p><p>The phase does not use screenshots. Process and log checks are the launch evidence.</p><h2>Important harness code</h2><p>The Vega adapter defines each platform command.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/platform/vega.ts (simplified)</figcaption><pre><code>device_status: [&quot;vega&quot;, &quot;exec&quot;, &quot;vda&quot;, &quot;devices&quot;, &quot;-l&quot;],\nvda_start: [&quot;vega&quot;, &quot;virtual-device&quot;, &quot;start&quot;, &quot;--gui&quot;, &quot;--timeout&quot;, &quot;60&quot;],\ninstall: [&quot;vega&quot;, &quot;device&quot;, &quot;install-app&quot;, &quot;--packagePath&quot;, packagePath],\nlaunch: [&quot;vega&quot;, &quot;device&quot;, &quot;launch-app&quot;, &quot;--appName&quot;, appId],\napp_status: [&quot;vega&quot;, &quot;device&quot;, &quot;is-app-running&quot;, &quot;--appName&quot;, appId],\n\n&gt; look: The harness owns these commands. They are not model tools.\n&gt; :::\n\nAfter start, the harness records two state samples:\n\n:::snippet packages/workshop-harness/src/platform/vega.ts (simplified)\nawait recordRunningState(device, &quot;app is running after launch&quot;);\nawait wait(5_000);\n\nconst logs = await readDeviceLogs();\nconst scan = scanDeviceLog(logs);\n\nawait recordRunningState(\ndevice,\n&quot;app remains running after dwell&quot;,\nlogs,\n);\n\n&gt; look: The second sample can find an app that started and then stopped.\n&gt; :::\n\nThe launch phase also uses `verifyFirst`.\nIt calls the model only when the device checks find a repairable source\nfailure.\n\nIf the model changes source code, the harness rebuilds the package, installs\nit, and runs all launch checks again.\n\n## Run the launch phase\n\nUse the package from Lesson 04 and the same run ID.\n\n:::yourturn\nStart the launch phase.\nWatch the VDA.\nWait for the final state and log checks after the app appears.</code></pre></figure><div class=\"command\"><header><span>Install and start the app</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n --phases launch --yes --run-id workshop</code></pre></div><p>Expect the final JSON to contain:</p><pre><code class=\"language-json\">{\n  &quot;state&quot;: &quot;complete&quot;,\n  &quot;phasesComplete&quot;: [&quot;analyze&quot;, &quot;plan&quot;, &quot;port&quot;, &quot;build&quot;, &quot;launch&quot;]\n}</code></pre><p>If all device checks pass, the phase records <code>attempts: 0</code>. This value means that the phase did not call a model.</p><p>If the app crashes, the terminal prints <code>launch needs a fix</code>. The repair model receives the log and the failed state check.</p><h2>Inspect the device evidence</h2><ol class=\"tasks\"><li>Open <code>out/workshop/vega-platform-result.json</code>.</li><li>Confirm that <code>evidenceMode</code> is <code>live</code>.</li><li>Find the first <code>device_status</code> step.</li><li>If the device was not attached, find <code>vda_start</code>.</li><li>Find the passing <code>install</code> step.</li><li>Find the passing <code>launch</code> step.</li><li>Find <code>app is running after launch</code>.</li><li>Confirm that the check passed.</li><li>Find <code>device log free of crash signatures</code>.</li><li>Confirm that the check passed.</li><li>Find <code>app remains running after dwell</code>.</li><li>Confirm that the check passed.</li><li>Open <code>out/workshop/vega-device.log</code>.</li><li>Confirm that the log contains no fatal error for the app.</li><li>Open <code>out/workshop/port-result.json</code>.</li><li>Find the launch <code>attempts</code> value.</li></ol><p><code>vega-platform-result.json</code> also records each command, exit code, standard output, and standard error.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The app started and stayed active</strong></div><dl><dt>Independent check</dt><dd>Install and start pass, both state samples pass, and the log has no crash signature</dd><dt>Evidence</dt><dd><code>vega-platform-result.json and vega-device.log</code></dd><dt>Limit</dt><dd>Process and log evidence does not prove visual rendering or focus behavior</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What happened?</summary><p>The harness reused or started the VDA.\nThe harness installed and started the app.\nThe harness checked the process before and after a five-second wait.\nThe model did not run when all device checks passed.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>For a live run, both running-state samples pass.\nThe device log contains no crash signature.\n<code>vega-platform-result.json</code> contains <code>evidenceMode: live</code>.</aside>"},{"id":"test","number":"06","nav":"Test the remote control","time":"25 minutes","title":"Inject remote keys and verify focus","lead":"Relaunch the app, send remote keys to the VDA, and read the focused test ID after each key. Compare each observed ID with the approved plan.","objective":"Run the complete remote-control sequence and inspect the evidence for movement, selection, list boundaries, and Back restoration.","evidence":"tv-focus-result.json contains all required focus transitions. Lesson 5 records separate lifecycle evidence.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Test behavior</h2><p>The test phase sends remote keys through software.\nAfter each key, it reads the focused control from the VDA user-interface\nhierarchy.</p></section><h2>Know the focus contract</h2><p>The approved <code>port-plan.json</code> supplies the expected focus IDs. A focus ID is the stable <code>testID</code> of one React Native control.</p><table><thead><tr><th>Action</th><th>Required result</th></tr></thead><tbody><tr><td>Start</td><td>The featured action has focus</td></tr><tr><td>Down</td><td>Focus moves to the first rail</td></tr><tr><td>Left</td><td>Focus stays at the left boundary</td></tr><tr><td>Right</td><td>Focus moves across the rail and stops at the right boundary</td></tr><tr><td>Select</td><td>The Details screen opens</td></tr><tr><td>Back</td><td>Focus returns to the card that opened Details</td></tr></tbody></table><p>The model cannot change the key sequence or expected IDs during the test.</p><h2>How the harness reads focus</h2><p>Automation Toolkit is a VDA service. Its <code>getPageSource</code> operation returns the user-interface hierarchy.</p><p>A focused React Native control appears with:</p><pre><code class=\"language-json\">{\n  &quot;focused&quot;: true,\n  &quot;test_id&quot;: &quot;rail-new-card-signal&quot;\n}</code></pre><p>The harness sends keys with <code>inputd-cli</code>. It polls Automation Toolkit until the expected focus appears or the time limit ends.</p><h2>Important harness code</h2><p>The focus contract comes from the approved plan:</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/platform/focus.ts (simplified)</figcaption><pre><code>const plan = PortPlanSchema.parse(readFile(&quot;port-plan.json&quot;));\nconst home = plan.screens.find(\nscreen =&gt; screen.id === plan.entryScreenId,\n);\n\nreturn {\ninitialFocusId: home.initialFocusId,\nfirstRailFocusId: home.focusableIds.find(\nid =&gt; id !== home.initialFocusId,\n),\ndetailFocusId: details.initialFocusId,\nhomeFocusableIds: home.focusableIds,\n};\n\n&gt; look: The approved plan supplies expected values. The model does not supply them during verification.\n&gt; :::\n\nThe test phase defines the device work:\n\n:::snippet packages/workshop-harness/src/port-pipeline.ts (simplified)\n{\nname: &quot;test&quot;,\nmcp: [ADBT_SERVER],\ndevice: [&quot;launch&quot;, &quot;focus&quot;],\nrepairDevice: [&quot;build&quot;, &quot;launch&quot;, &quot;focus&quot;],\nverifyFirst: true,\nmaxAttempts: 3,\nchecks: [FOCUS_TEST_CHECK, restorationCheck],\n}\n\n&gt; look: A source repair must build, start, and pass the complete focus sequence.\n&gt; :::\n\n## Know the test sequence\n\nThe phase performs these operations:\n\n1. Runs the host-side focus-state test.\n2. Deletes an earlier `tv-focus-result.json`.\n3. Starts the app from Home.\n4. Enables Automation Toolkit.\n5. Reads the initial focused `test_id`.\n6. Sends one remote key.\n7. Reads focus again.\n8. Repeats the operation for every required transition.\n9. Writes a new `tv-focus-result.json`.\n\nThe first attempt does not rebuild the package.\nIf a failed focus check causes a source repair, the harness rebuilds and\nrelaunches before it tests focus again.\n\n## Run the test phase\n\n:::yourturn\nRun the focus sequence.\nWatch the VDA respond to the injected keys.\nWait for the final focus result.</code></pre></figure><div class=\"command\"><header><span>Run the focus test</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n --phases test --yes --run-id workshop</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;phasesComplete&quot;:[&quot;analyze&quot;,&quot;plan&quot;,&quot;port&quot;,&quot;build&quot;,&quot;launch&quot;,&quot;test&quot;]</code></pre></div><p>A passing verify-first test can report <code>attempts: 0</code>. The phase calls a model only when the independent checks identify a source repair.</p><h2>Inspect the focus evidence</h2><p>Open <code>out/workshop/app/tv-focus-result.json</code>.</p><table><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>evidenceMode: &quot;live&quot;</code></td><td>The VDA supplied the observations</td></tr><tr><td><code>passed: true</code></td><td>Every required transition passed</td></tr><tr><td><code>transitions</code></td><td>Names of passing transitions</td></tr><tr><td><code>observations[].key</code></td><td>Key sent by the harness</td></tr><tr><td><code>observations[].expected</code></td><td>Focus ID from the approved plan</td></tr><tr><td><code>observations[].observed</code></td><td>Focused <code>test_id</code> from Automation Toolkit</td></tr><tr><td><code>observations[].passed</code></td><td>Comparison result</td></tr></tbody></table><p>Find these transition names:</p><pre><code class=\"language-text\">launch-hero\ndown-to-first-rail\nleft-boundary\nright-boundary\nopen-details\nback-restore</code></pre><ol class=\"tasks\"><li>Confirm that <code>evidenceMode</code> is <code>live</code>.</li><li>Confirm that <code>passed</code> is <code>true</code>.</li><li>Read each observation in order.</li><li>Compare <code>expected</code> and <code>observed</code>.</li><li>Find the <code>right-boundary</code> observation.</li><li>Confirm that focus stays on the final card.</li><li>Find the <code>open-details</code> observation.</li><li>Confirm that the Details screen receives focus.</li><li>Find the <code>back-restore</code> observation.</li><li>Confirm that focus returns to the originating card.</li><li>Open <code>out/workshop/vega-platform-result.json</code>.</li><li>Find the Automation Toolkit and key press steps.</li></ol><h2>Compare before and after</h2><p>Lesson 00 ran <code>tv-check</code> on the starting app. That command reported <code>tvReady: false</code>.</p><p>Run the same check on the guarded port:</p><div class=\"command\"><header><span>Run the final TV-readiness check</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts tv-check ../../out/workshop/app</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: true\n&quot;failures&quot;: []</code></pre></div><p>The same source check now passes. The live focus result adds device observations that <code>tv-check</code> cannot supply.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>A remote user can complete the required TV flow</strong></div><dl><dt>Independent check</dt><dd>The VDA reports the expected focus after start, movement, selection, boundaries, and Back</dd><dt>Evidence</dt><dd><code>tv-focus-result.json and tv-check with tvReady:true</code></dd><dt>Limit</dt><dd>The UI hierarchy proves focus state, not visual focus styling</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What happened?</summary><p>The harness took expected focus IDs from the approved plan.\nThe harness injected keys through the VDA.\nAutomation Toolkit reported the focused <code>test_id</code>.\nThe harness compared expected and observed focus without model judgment.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>The focus test passes all transitions.\n<code>tv-focus-result.json</code> records the sequence.\n<code>tv-check</code> reports <code>tvReady: true</code>.\n\nLive Lesson 5 evidence proves process stability on your device.\nIt does not prove visual rendering.</aside>"},{"id":"finish","number":"07","nav":"A complete run","time":"30 minutes","title":"Inspect every phase in the terminal user interface","lead":"Run the complete pipeline with the terminal user interface. Open each phase, read its messages, and connect the summary to the saved evidence.","objective":"Use the terminal user interface to inspect phase state, model events, tools, checks, commits, usage, and device evidence.","evidence":"A reviewed six-phase run.","body":"<div class=\"takeaway\"><code>plan -> context -> run -> check -> retry -> report</code></div><section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Read the complete pipeline</h2><p>The earlier lessons ran one phase at a time.\nThis lesson runs the same six phases as one complete workflow.\n\nThe terminal user interface (TUI) shows phase state and recent events.\nThe JSONL transcripts remain the complete record.</p></section><h2>Know when the TUI appears</h2><p>The TUI opens only when all these conditions are true:</p><ul><li>The command includes <code>--tui</code>.</li><li>The command runs in an interactive terminal.</li><li>Standard output and standard error are attached to that terminal.</li><li>The command does not include <code>--json</code> or <code>--detach</code>.</li></ul><p>The <code>approve-plan</code> command does not open the TUI. The first run stops after plan so that you can review and approve it.</p><h2>Important harness code</h2><p><code>src/tui.ts</code> checks the terminal before it opens the dashboard.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/tui.ts (simplified)</figcaption><pre><code>return stdoutIsTty === true &amp;&amp;\nstderrIsTty === true &amp;&amp;\nargs.includes(&quot;--tui&quot;) &amp;&amp;\n!args.includes(&quot;--json&quot;) &amp;&amp;\n!args.includes(&quot;--detach&quot;);\n\n&gt; look: A redirected or non-interactive command prints normal JSON instead of the TUI.\n&gt; :::\n\nThe TUI also controls the two views:\n\n:::snippet packages/workshop-harness/src/tui.ts (simplified)\nif (key.name === &quot;return&quot; &amp;&amp; state.view === &quot;phases&quot;) {\nopenMessages();\n}\n\nif (key.name === &quot;escape&quot; &amp;&amp; state.view === &quot;messages&quot;) {\nstate.view = &quot;phases&quot;;\n}\n\n&gt; look: Enter opens the selected phase. Escape returns to the phase list.\n&gt; :::\n\n## Run analyze and plan\n\nUse a new run ID so that you can watch every phase.\n\n:::yourturn\nStart the first dashboard.\nWatch analyze and plan.\nClose the dashboard when it requests plan approval.</code></pre></figure><div class=\"command\"><header><span>Run analyze and plan with the TUI</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n --phases analyze,plan --seed workshop-v1 \\\n --yes --run-id final-dashboard --tui</code></pre></div><p>The dashboard shows a <code>feasibility</code> preflight before the six port phases. The preflight checks whether the source app and its dependencies can continue. The dashboard then shows <code>analyze</code> and <code>plan</code> as passed. The later phases remain pending. The run ends in <code>awaiting_approval</code>. The completed dashboard remains open for review.</p><p>Press <code>q</code> to close it. Then, open <code>out/final-dashboard/app/port-plan.json</code>. Review the plan as you did in Lesson 02.</p><p>Approve the plan:</p><pre><code class=\"language-sh\">yarn tsx src/index.ts approve-plan final-dashboard --yes</code></pre><h2>Run the remaining phases</h2><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Start the second dashboard.\nConfirm that it loads analyze and plan from the earlier run.\nWatch port, build, launch, and test.</p></aside><div class=\"command\"><header><span>Run port through test with the TUI</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n --phases port,build,launch,test \\\n --seed workshop-v1 --yes \\\n --run-id final-dashboard --tui</code></pre></div><p>The dashboard can remain on one phase for several minutes. This does not mean that the process stopped. The selected phase can be waiting for a model, compiler, VDA, dwell, or focus poll.</p><p>The header shows:</p><ul><li>Run ID</li><li>Executor and model</li><li>Evidence mode</li><li>Seed</li><li>Token usage</li><li>Turns</li><li>Model calls</li><li>Provider cost when the provider reports it</li></ul><h2>Use the TUI controls</h2><table><thead><tr><th>Key</th><th>Function</th></tr></thead><tbody><tr><td>Up or Down</td><td>Select a phase or event</td></tr><tr><td><code>Enter</code></td><td>Open messages for the selected phase</td></tr><tr><td><code>Escape</code></td><td>Return to the phase list</td></tr><tr><td><code>Tab</code></td><td>Select checks, model events, tools, or all events</td></tr><tr><td><code>PageUp</code> or <code>PageDown</code></td><td>Scroll the selected event</td></tr><tr><td><code>f</code></td><td>Follow the active phase</td></tr><tr><td><code>q</code></td><td>Close a completed dashboard</td></tr></tbody></table><ol class=\"tasks\"><li>Select <code>feasibility</code>.</li><li>Press <code>Enter</code>.</li><li>Find the dependency result.</li><li>Press <code>Escape</code>.</li><li>Select <code>analyze</code>.</li><li>Press <code>Enter</code>.</li><li>Find the ADBT document operation.</li><li>Press <code>Escape</code>.</li><li>Select <code>plan</code>.</li><li>Press <code>Enter</code>.</li><li>Find the plan checks.</li><li>Press <code>Escape</code>.</li><li>Select <code>port</code>.</li><li>Press <code>Enter</code>.</li><li>Press <code>Tab</code> until the checks filter is active.</li><li>Find the port commit.</li><li>Press <code>Escape</code>.</li><li>Select <code>build</code>.</li><li>Open its messages.</li><li>Find the compiler evidence.</li><li>Return to the phase list.</li><li>Open <code>launch</code>.</li><li>Find the two running-state checks.</li><li>Open <code>test</code>.</li><li>Find the focus transition result.</li><li>Press <code>q</code> after the complete run passes.</li></ol><h2>Connect the TUI to saved evidence</h2><p>The TUI is a view. It does not replace the evidence files.</p><table><thead><tr><th>Claim</th><th>Saved evidence</th></tr></thead><tbody><tr><td>The model inspected the app</td><td><code>app/ANALYSIS.md</code> and analyze transcript</td></tr><tr><td>The plan used Vega documents</td><td><code>adbt-port-context.json</code></td></tr><tr><td>The code passed source checks</td><td><code>port-result.json</code> and phase commit</td></tr><tr><td>The package compiled</td><td><code>.vpkg</code> and <code>vega-platform-result.json</code></td></tr><tr><td>The app stayed active</td><td><code>vega-device.log</code> and state checks</td></tr><tr><td>The remote flow worked</td><td><code>app/tv-focus-result.json</code></td></tr></tbody></table><p>Open <code>out/final-dashboard/model-logs/</code>. Each phase has one JSONL transcript. The TUI reads these events and keeps the full files unchanged.</p><h2>Design a small harness</h2><p>The TV port is one use of the harness pattern. Use <code>workshop/worksheet.md</code> to define a smaller engineering workflow.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Select one task.\nDefine no more than three phases.\nGive each phase one independent check.</p></aside><p>Select one task:</p><ul><li>Upgrade one Android dependency.</li><li>Repair accessibility labels and focus order.</li><li>Replace one deprecated API.</li><li>Repair one intermittent test.</li></ul><ol class=\"tasks\"><li>Write one required result.</li><li>Write three non-goals.</li><li>Define no more than three ordered phases.</li><li>Name the input for each phase.</li><li>Give each phase one independent check.</li><li>Identify the human approval point.</li><li>Set an attempt limit.</li><li>Set a no-progress rule.</li><li>Ask another person to find one false positive.</li><li>Improve the weak check.</li></ol><p>A false positive occurs when a check accepts an incorrect result.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The harness pattern applies outside the Vega port</strong></div><dl><dt>Independent check</dt><dd>Another person can find a false positive and improve one independent check</dd><dt>Evidence</dt><dd><code>workshop/worksheet.md</code></dd><dt>Limit</dt><dd>The worksheet is a design exercise, not a production implementation</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What happened?</summary><p>The TUI combined phase state and transcript events in one terminal view.\nThe saved files remained the evidence source.\nThe team exercise applied the same control pattern to another task.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>You can explain the complete run from the TUI.\nAnother team can read your worksheet.\nThe team can identify the pass, retry, and stop conditions.</aside><div class=\"links\"><a href=\"worksheet.md\">Open the worksheet</a><a href=\"troubleshooting.md\">Troubleshooting</a><a href=\"editing-guide.md\">Edit the workshop</a><a href=\"instructor-guide.md\">Instructor guide</a><button data-go-module=\"bee\">Challenge: Bee phase</button></div>"},{"id":"bee","number":"A1","nav":"Challenge: Bee phase","time":"40 minutes","title":"Challenge: modify the app from a Bee conversation","lead":"Use one consented Bee conversation as product input. Create and review a specification before a second phase can change the app.","objective":"Run the Bee specification and apply phases. Protect private conversation text and keep approval separate from implementation.","evidence":"BEE_SPEC.md contains source IDs and paraphrases. The app passes the approved checks and device phases.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Add a conversation-driven phase</h2><p>The main TV port is complete.\nThis challenge uses the same harness engine with a different phase plan.\n\nBee is a wearable personal AI that captures conversations.\nThe Bee CLI lets the harness search and read your Bee data through MCP.</p></section><aside class=\"note warning\"><strong>Consent is required</strong><br>Use a live conversation only when every speaker permits this use.\nDo not use production secrets, protected customer data, or unrelated personal\ninformation.</aside><h2>Know the challenge flow</h2><p>The conversation is private context. It is not the approved requirement.</p><div class=\"flow\"><div><b>Search</b><span>Find the consented conversation</span></div><i>→</i><div><b>Specify</b><span>Paraphrase requests and define file checks</span></div><i>→</i><div><b>Review</b><span>Check scope, sources, exclusions, and evidence</span></div><i>→</i><div><b>Apply</b><span>Change the guarded app</span></div><i>→</i><div><b>Verify</b><span>Run approved checks, app tests, build, and launch</span></div></div><p>The flow adds two phases:</p><table><thead><tr><th>Phase</th><th>Input</th><th>Output</th></tr></thead><tbody><tr><td><code>bee_spec</code></td><td>Bee MCP and guarded app</td><td><code>bee-spec.json</code> and rendered <code>BEE_SPEC.md</code></td></tr><tr><td><code>bee_apply</code></td><td>Approved specification and guarded app</td><td>App source changes</td></tr></tbody></table><p>The harness then reuses the normal <code>build</code> and <code>launch</code> phases.</p><h2>Important harness code</h2><p>The Bee pipeline uses the same <code>runPortPipeline</code> engine as the TV port.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/bee-pipeline.ts (simplified)</figcaption><pre><code>export function beePhases(spec, appDir) {\nconst port = portPhases();\nconst device = [&quot;build&quot;, &quot;launch&quot;].map(\nname =&gt; port.find(phase =&gt; phase.name === name),\n);\n\nreturn spec\n? [beeSpecPhase(), beeApplyPhase(spec, appDir), ...device]\n: [beeSpecPhase()];\n}\n\n&gt; look: The Bee plan changes. The phase engine, build phase, and launch phase stay the same.\n&gt; :::\n\nThe apply phase protects the reviewed specification:\n\n:::snippet packages/workshop-harness/src/bee-pipeline.ts (simplified)\n{\nname: &quot;bee_apply&quot;,\nverifyFirst: true,\nmaxAttempts: 3,\nreadOnly: [&quot;bee-spec.json&quot;, &quot;BEE_SPEC.md&quot;],\nchecks: [\n...approvedRequestChecks,\ntypecheck,\nappTests,\n],\n}\n\n&gt; look: The implementation cannot edit the requirement or its acceptance checks.\n&gt; :::\n\nThe specification can define only `file_exists` and `contains` checks.\nIt cannot define a shell command.\nEvery check path must stay inside the guarded app.\n\n## Prepare Bee CLI\n\nRead the [Bee CLI repository](https://github.com/bee-computer/bee-cli).\nInstall the current Bee mobile app.\n\nEnable Developer Mode in the Bee app:\n\n1. Open Settings.\n2. Find the app version.\n3. Tap the app version five times.\n\nInstall and authenticate the CLI:\n\n:::command Install and authenticate Bee CLI\nnpm install -g @beeai/cli\nbee version\nbee login\nbee status</code></pre></figure><p>Search for the Pocket Cinema discussion:</p><div class=\"command\"><header><span>Find the Pocket Cinema conversation</span><button class=\"copy\">Copy command</button></header><pre><code>bee search --query &quot;Pocket Cinema&quot; \\\n --filter conversations --limit 5 --json</code></pre></div><p>Record the selected conversation ID in private notes. Do not add the search JSON to the repository.</p><p>The harness starts <code>bee mcp serve</code> for the specification phase. Do not start a second MCP server.</p><h2>Configure the live model</h2><p>The live Bee path uses Strands. Set <code>&quot;executor&quot;: &quot;strands&quot;</code> in <code>workshop.config.json</code>. Configure Bedrock, OpenAI, or OpenRouter as described in Lesson 00.</p><p>The Vega SDK and VDA must remain available. The apply command runs the normal build and launch checks.</p><h2>Create the specification</h2><p><code>--propose</code> runs only <code>bee_spec</code>. The model searches Bee, reads the selected conversation, and returns <code>bee-spec.json</code>.</p><p>The harness validates the JSON. The harness renders <code>BEE_SPEC.md</code>. The model does not write the Markdown review document.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Run the specification phase.\nReview the paraphrases, sources, exclusions, and file checks.</p></aside><div class=\"command\"><header><span>Create the Bee specification</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n --propose --run-id bee</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;event&quot;:&quot;bee_spec_ready&quot;\n&quot;phasesComplete&quot;:[&quot;bee_spec&quot;]</code></pre></div><h2>Inspect and approve the specification</h2><p>Open <code>out/bee/app/BEE_SPEC.md</code>.</p><ol class=\"tasks\"><li>Find each requested app change.</li><li>Confirm that each request is a paraphrase.</li><li>Find the source conversation ID.</li><li>Confirm that the request has one specific file check.</li><li>Read <code>Deliberately excluded</code>.</li><li>Confirm that personal and unrelated information is excluded.</li><li>Open <code>out/bee/bee-context.json</code>.</li><li>Confirm that it contains IDs and hashes.</li><li>Confirm that it contains no conversation text.</li><li>Run <code>git -C ../../out/bee/app diff --name-only HEAD~2 HEAD</code>.</li><li>Confirm that the result contains only specification files.</li></ol><p>Reject the specification when a request was only an idea, a check can pass without the change, or a path points outside the app.</p><p>The model transcript can contain conversation text. Treat <code>out/bee/model-logs/bee_spec.jsonl</code> as private data.</p><h2>Apply the approved request</h2><p><code>--apply --yes</code> confirms your review and runs:</p><pre><code class=\"language-text\">bee_apply -&gt; build -&gt; launch</code></pre><p>The approved file checks fail before implementation. The failure text tells the apply model what it must change. The phase also requires <code>tsc --noEmit</code> and the app tests.</p><aside class=\"yourturn\"><p class=\"eyebrow\">Exercise</p><strong>Do this task</strong><p>Apply the reviewed request.\nWait for the app checks, Vega build, and VDA launch.</p></aside><div class=\"command\"><header><span>Apply, build, and start</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n --apply --yes --run-id bee</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;event&quot;:&quot;bee_apply_complete&quot;\n&quot;phasesComplete&quot;:[&quot;bee_spec&quot;,&quot;bee_apply&quot;,&quot;build&quot;,&quot;launch&quot;]</code></pre></div><h2>Inspect the result</h2><ol class=\"tasks\"><li>Open <code>out/bee/bee-result.json</code>.</li><li>Find the <code>bee_apply</code> phase.</li><li>Find each request ID in the check labels.</li><li>Find the source conversation ID.</li><li>Inspect the committed app source change.</li><li>Confirm that <code>bee-spec.json</code> did not change.</li><li>Confirm that <code>BEE_SPEC.md</code> did not change.</li><li>Confirm that the type check passed.</li><li>Confirm that the app tests passed.</li><li>Confirm that the Vega build passed.</li><li>Confirm that the launch state checks passed.</li></ol><h2>Use the prepared conversation when necessary</h2><p>If you do not have Bee access or consent, use the prepared conversation. This path proves the phase controls. It does not prove a live Bee connection.</p><p>Create the prepared specification:</p><pre><code class=\"language-sh\">yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --replay ../../workshop/fixtures/bee-run/port-recording.json \\\n  --propose --run-id bee</code></pre><p>Review <code>out/bee/app/BEE_SPEC.md</code>. Then, apply the prepared request:</p><pre><code class=\"language-sh\">yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --replay ../../workshop/fixtures/bee-run/port-recording.json \\\n  --platform-replay ../../workshop/fixtures/vega-lifecycle.json \\\n  --apply --yes --run-id bee</code></pre><p><code>out/bee/vega-platform-result.json</code> has <code>evidenceMode: replay</code>. Do not report it as a live Bee or live device result.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>A consented Bee conversation can produce a bounded app change</strong></div><dl><dt>Independent check</dt><dd>The approved request checks, app checks, build, and launch pass</dd><dt>Evidence</dt><dd><code>BEE_SPEC.md, bee-context.json, bee-result.json, and phase commits</code></dd><dt>Limit</dt><dd>A live Bee claim requires an authenticated read of a consented conversation</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What happened?</summary><p>The Bee conversation supplied private context.\nThe harness produced a review document without transcript text.\nYou approved the request before the model changed the app.\nThe apply phase could not change its specification or checks.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>Git contains the approved <code>BEE_SPEC.md</code>.\nEach request contains a source ID.\nThe excluded list contains private and unapproved content.\n<code>bee-result.json</code> reports passing apply, build, and launch phases.</aside>"}];
