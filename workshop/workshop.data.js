// GENERATED by scripts/build-site.mjs from workshop/lessons/*.md — do not edit by hand.
window.WORKSHOP_MODULES = [{"id":"welcome","number":"00","nav":"Start here","time":"20 minutes","title":"Set up the workshop and understand the runtime","lead":"Complete this setup before the workshop. Select one live model path. Use recorded data only if a live tool fails.","objective":"Prepare one live executor. Explain the functions of Strands, ADBT, the harness, and Git.","evidence":"A working executor, a selected provider and model, and a completed readiness list.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Welcome to Past the Vibes</h2><p>This workshop is four hours.\nYou will build a harness that ports a React Native app to Vega TV.\nYou will add one phase in each lesson.\nComplete this setup before Lesson 1.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Know the system</h2><p>A harness is a TypeScript program.\nIt runs a fixed sequence of phases.\nThe model can inspect the app and propose files.\nThe model cannot write files or run shell commands.\n\nThe harness controls file writes, checks, retries, costs, commits, and reports.\nThe model makes a proposal.\nThe harness accepts or rejects the proposal.</p></section><aside class=\"note \"><strong>Important rule</strong><br>A plausible result is not necessarily a correct result.\nEach phase must use an independent check.</aside><h2>Know the main terms</h2><table><thead><tr><th>Term</th><th>Meaning</th></tr></thead><tbody><tr><td>Model</td><td>Software that produces a response from a prompt</td></tr><tr><td>Prompt</td><td>Instructions and context for the model</td></tr><tr><td>Agent</td><td>A model that can request tool operations</td></tr><tr><td>Tool</td><td>A function that the agent can request</td></tr><tr><td>Structured output</td><td>JSON data that must agree with a schema</td></tr><tr><td>MCP</td><td>A protocol that connects a model to external tools</td></tr><tr><td>ADBT</td><td>Amazon tools and documents for Vega development</td></tr><tr><td>Skill</td><td>Instructions for one technical task</td></tr><tr><td>Harness</td><td>The program that controls phases, checks, and writes</td></tr><tr><td>Executor</td><td>The component that sends prompts to a model</td></tr><tr><td>VDA</td><td>The Vega Virtual Device</td></tr><tr><td>Recorded fallback</td><td>Stored model and platform data for use after a live failure</td></tr></tbody></table><h2>Use the claim and evidence table</h2><p>Use this table during the workshop.</p><table><thead><tr><th>Model claim</th><th>Independent check</th><th>Evidence</th></tr></thead><tbody><tr><td>The model understands the app</td><td>Source inventory and recorded unknowns</td><td><code>ANALYSIS.md</code></td></tr><tr><td>The plan is correct</td><td>Required sections and ADBT document records</td><td><code>VEGA_PORT.md</code></td></tr><tr><td>The code is ready</td><td>Schema, file, and executable checks</td><td>Check results and Git commit</td></tr><tr><td>The app builds</td><td>Vega compiler</td><td><code>.vpkg</code> file and build output</td></tr><tr><td>The app runs</td><td>Device log and two screenshots</td><td>Log file and image files</td></tr><tr><td>The remote works</td><td>Focus-transition test</td><td><code>tv-focus-result.json</code></td></tr></tbody></table><aside class=\"note \"><strong>Ask this question</strong><br>When the model reports success, ask: <strong>What independent component verified the result?</strong></aside><h2>Know the security boundary</h2><div class=\"flow\"><div><b>ADBT MCP</b><span>Supply Vega documents and tools</span></div><i>→</i><div><b>Model</b><span>Read the guarded copy and propose changes</span></div><i>→</i><div><b>Harness</b><span>Write files and run checks</span></div><i>→</i><div><b>Git</b><span>Record accepted changes</span></div></div><p>The harness creates a guarded copy of the app. The model receives read-only access to this copy. The model does not receive a shell tool. The model does not receive a file-write tool.</p><p>The harness validates all proposed file paths. The harness writes only a valid structured patch. If a check fails, the harness rejects the change.</p><figure class=\"snippet\"><figcaption>The system boundary</figcaption><pre><code>ADBT MCP ---&gt; selected model ---&gt; typed patch\n    ^              |\n    |              v\nVega documents   guarded app copy (read-only)\n                       |\n                       v\nHARNESS: validate paths -&gt; write files -&gt; run checks -&gt; retry -&gt; commit -&gt; report</code></pre><p class=\"look\"><strong>Where to look:</strong> Strands receives a native McpClient. Claude Code receives an explicit --mcp-config. The harness controls all operations that change files or devices.</p></figure><h2>Know how Strands is used</h2><p><a href=\"https://github.com/strands-agents/harness-sdk\" target=\"_blank\" rel=\"noopener\">Strands Agents SDK</a> is an agent runtime. This workshop uses TypeScript SDK version <code>1.10.0</code>.</p><table><thead><tr><th>Strands supplies</th><th>The harness supplies</th></tr></thead><tbody><tr><td>Model provider adapters</td><td>Phase order</td></tr><tr><td>Agent loop</td><td>Protected file writes</td></tr><tr><td>Typed tools</td><td>Independent checks</td></tr><tr><td>Structured output</td><td>Retry control</td></tr><tr><td>MCP support</td><td>Cost limits</td></tr><tr><td>Token limits and cancellation</td><td>Git commits and reports</td></tr></tbody></table><p>Strands does not control the complete workflow. The harness controls the workflow.</p><h2>1. Prepare your computer</h2><ol class=\"tasks\"><li>Install Node.js 20 or newer.</li><li>Install Git.</li><li>Make sure that Corepack is available.</li><li>Clone the repository.</li><li>Open a terminal in the repository root.</li></ol><div class=\"command\"><header><span>Clone the repository</span><button class=\"copy\">Copy command</button></header><pre><code>git clone https://github.com/giolaq/past-the-vibes-we.git\ncd past-the-vibes-we</code></pre></div><p>Select Pocket Cinema for the workshop:</p><pre><code class=\"language-text\">apps/pocket-cinema</code></pre><p>Use your app only if it already runs. Your app must have one working flow:</p><pre><code class=\"language-text\">launch -&gt; screen -&gt; action -&gt; back</code></pre><div class=\"grid\"><article><h3>Pocket Cinema</h3><p>Use this app for the recommended path. All exercises and recovery files support it.</p><code>apps/pocket-cinema</code></article><article><h3>Your app</h3><p>Use one working flow. Use Pocket Cinema if source discovery takes more than 10 minutes.</p><code>launch -&gt; screen -&gt; action -&gt; back</code></article></div><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual Android TV capture</div><img src=\"assets/pocket-cinema-android-tv.png\" alt=\"Pocket Cinema home screen on an Android TV emulator\" loading=\"lazy\"><figcaption>This image shows the React Native app before the Vega port. The app has no explicit TV focus behavior.</figcaption></figure><h3>Do a safety check of your app</h3><div class=\"checklist\"><label><input type=\"checkbox\">The app runs before the workshop</label><label><input type=\"checkbox\">The Git working tree is clean</label><label><input type=\"checkbox\">The app contains no production secrets</label><label><input type=\"checkbox\">The app contains no private data</label><label><input type=\"checkbox\">The app contains no protected media</label><label><input type=\"checkbox\">The selected model provider can receive the app content</label></div><h2>2. Install the workspace</h2><p>Run these commands from the repository root:</p><div class=\"command\"><header><span>Install the workshop packages</span><button class=\"copy\">Copy command</button></header><pre><code>unset NODE_TLS_REJECT_UNAUTHORIZED\ncorepack enable\nyarn setup</code></pre></div><p>Stop if the installation fails. Do not disable TLS certificate verification.</p><h2>3. Verify the workspace</h2><div class=\"command\"><header><span>Run the local verification</span><button class=\"copy\">Copy command</button></header><pre><code>yarn verify</code></pre></div><p>This command verifies the harness, tests, documents, and website data. It does not verify your model account. It does not verify the Vega SDK or VDA.</p><h3>Verify the starting app</h3><p>Enter the harness package. Keep this terminal in this directory for lessons 1 through 7.</p><div class=\"command\"><header><span>Enter the harness package</span><button class=\"copy\">Copy command</button></header><pre><code>cd packages/workshop-harness</code></pre></div><div class=\"command\"><header><span>Run the TV-readiness check</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts tv-check ../../apps/pocket-cinema</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: false\n&quot;failures&quot;: [\n  &quot;Focus state module: missing src/tv/focus-state.ts&quot;,\n  &quot;App wires shared focus state: src/App.tsx must contain \\&quot;./tv/focus-state\\&quot;&quot;,\n  &quot;Initial focus declared: src/App.tsx must contain \\&quot;hasTVPreferredFocus\\&quot;&quot;,\n  &quot;Vega package manifest: missing apps/vega/manifest.toml&quot;,\n  ...\n]</code></pre></div><p>The failure list identifies the work for the workshop. Lesson 6 runs the same check on the ported app.</p><h2>4. Select one live executor</h2><p>Use the same executor, provider, and model in all lessons. Do not change providers during the workshop.</p><p>Open <code>../../workshop.config.json</code>. Replace its contents with one configuration from this section.</p><table><thead><tr><th>Path</th><th>Configuration fields</th><th>Credential</th></tr></thead><tbody><tr><td>Claude Code CLI</td><td><code>executor</code>, <code>model</code></td><td>Authenticated Claude Code</td></tr><tr><td>Strands with Bedrock</td><td><code>executor</code>, <code>provider</code>, <code>model</code>, <code>region</code></td><td>AWS credentials and model access</td></tr><tr><td>Strands with OpenAI</td><td><code>executor</code>, <code>provider</code>, <code>model</code></td><td><code>OPENAI_API_KEY</code></td></tr><tr><td>Strands with OpenRouter</td><td><code>executor</code>, <code>provider</code>, <code>model</code></td><td><code>OPENROUTER_API_KEY</code></td></tr><tr><td>Recorded fallback</td><td><code>--replay &lt;recording.json&gt;</code></td><td>No credential</td></tr></tbody></table><p>With Claude Code, the harness starts the <code>claude</code> command. The harness sends the prompt through standard input. Claude Code returns stream JSON.</p><p>With Strands, the SDK calls the selected provider. No model CLI is necessary.</p><h3>Configure Claude Code</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;claude-cli&quot;,\n  &quot;model&quot;: &quot;sonnet&quot;\n}</code></pre><h3>Configure Strands with Bedrock</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;bedrock&quot;,\n  &quot;model&quot;: &quot;anthropic.claude-3-5-sonnet-20241022-v2:0&quot;,\n  &quot;region&quot;: &quot;us-west-2&quot;\n}</code></pre><h3>Configure Strands with OpenAI</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;openai&quot;,\n  &quot;model&quot;: &quot;gpt-4.1&quot;\n}</code></pre><h3>Configure Strands with OpenRouter</h3><pre><code class=\"language-json\">{\n  &quot;executor&quot;: &quot;strands&quot;,\n  &quot;provider&quot;: &quot;openrouter&quot;,\n  &quot;model&quot;: &quot;anthropic/claude-sonnet-4&quot;\n}</code></pre><h3>Verify the selected configuration</h3><div class=\"command\"><header><span>Verify the model environment</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts doctor --json</code></pre></div><p>The harness automatically loads <code>../../workshop.config.json</code>. Command-line model flags override the file for one command.</p><aside class=\"note \"><strong>Protect credentials</strong><br>Keep credentials in your terminal or credential manager.\nDo not put credentials in the repository.\nDo not commit credentials in an <code>.env</code> file.</aside><aside class=\"note \"><strong>Select one live path</strong><br>You do not need all providers.\nSave one configuration.\nRun the <code>doctor</code> command.\nIf one repair fails, use the recorded fallback for that exercise.</aside><h2>5. Prepare ADBT</h2><p>Both live executors use ADBT through MCP. Strands receives an ADBT <code>McpClient</code>. Claude Code receives an explicit MCP configuration.</p><p>The harness starts the pinned ADBT server. Install the ADBT skills one time:</p><div class=\"command\"><header><span>Install the pinned ADBT skills</span><button class=\"copy\">Copy command</button></header><pre><code>npx -y @amazon-devices/amazon-devices-buildertools-mcp@1.0.5 \\\n  init-context --agent claude-code-cli --force</code></pre></div><div class=\"command\"><header><span>Verify the ADBT skill installation</span><button class=\"copy\">Copy command</button></header><pre><code>npx -y @amazon-devices/amazon-devices-buildertools-mcp@1.0.5 \\\n  check-status --agent claude-code-cli</code></pre></div><p>During a live phase, the model selects the ADBT documents that it needs. The harness records the document names and SHA-256 hashes. The phase fails if it requires ADBT but reads no ADBT document.</p><h2>6. Prepare Vega</h2><p>Lesson 4 requires Vega SDK version <code>0.22.5875</code>. Lesson 5 requires an attached VDA. Lesson 6 uses the focus test and device images.</p><p>In a system terminal, run:</p><div class=\"command\"><header><span>Start the Vega environment</span><button class=\"copy\">Copy command</button></header><pre><code>cd packages/workshop-harness\nyarn tsx src/index.ts doctor --adbt-live --json\nvega --version\nvega virtual-device start --gui</code></pre></div><p>Keep this terminal open.</p><p>In a second terminal, run:</p><div class=\"command\"><header><span>Verify the SDK and device</span><button class=\"copy\">Copy command</button></header><pre><code>vega --version\nvega virtual-device status\nvega exec vda devices -l</code></pre></div><p>The environment is ready when all these conditions are true:</p><ul><li>The SDK reports version <code>0.22.5875</code>.</li><li>The virtual-device status reports <code>running: true</code>.</li><li>The device list contains a device.</li></ul><p>A successful command with an empty device list is not sufficient.</p><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>Try one repair if a live service fails.\nDo not use more than 10 minutes for the repair.\nThen use the recorded fallback for the blocked exercise.\n\nA recorded fallback verifies the harness control flow.\nIt does not verify your model, compiler, or device.</aside><h2>Setup checklist</h2><div class=\"checklist\"><label><input type=\"checkbox\">Node.js 20 or newer is available</label><label><input type=\"checkbox\">Git and Corepack are available</label><label><input type=\"checkbox\"><code>yarn setup</code> is complete</label><label><input type=\"checkbox\"><code>yarn verify</code> passes</label><label><input type=\"checkbox\">I selected one executor, provider, and model</label><label><input type=\"checkbox\">The matching <code>doctor</code> command passes</label><label><input type=\"checkbox\">I selected Pocket Cinema or verified my app</label><label><input type=\"checkbox\">The Vega SDK is ready for Lesson 4</label><label><input type=\"checkbox\">The VDA is ready for Lesson 5</label><label><input type=\"checkbox\">I know that recorded data is only a fallback</label><label><input type=\"checkbox\">I can explain what Strands supplies</label><label><input type=\"checkbox\">I can explain what the harness controls</label></div><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the most important boundary?</summary><p>The model can inspect the app and propose changes.\nThe harness controls writes, checks, retries, costs, commits, and reports.\nADBT supplies Vega knowledge through MCP.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>The workspace passes all local checks.\nOne live executor is ready.\nYou know which app you will use.\nFor live platform evidence, the Vega SDK and VDA are also ready.</aside>"},{"id":"analyze","number":"01","nav":"Analyze the app","time":"25 minutes","title":"Run one model call, then add controls","lead":"Run one model call without the phase controls. Then identify the claims that the call cannot prove.","objective":"Explain why a plausible patch is not a verified result. Identify the boundary between the model and the harness.","evidence":"An unverified proposal and a verified ANALYSIS.md file in the guarded copy.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Start with the weak process</h2><p>First, ask one model to port the app in one call.\nThe harness saves the proposal but does not apply it.\n\nThen run one controlled phase.\nThe controlled phase has a schema, a check, and a Git commit.\nCompare the two results.</p></section><h2>Run the one-call example</h2><p>The model receives read-only project tools. The model must return a typed patch.</p><p>The call does not receive these controls:</p><ul><li>Phase plan</li><li>TV skill</li><li>ADBT MCP server</li><li>Independent checks</li><li>Retry</li><li>Compiler</li><li>Device</li></ul><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The proposal can contain a manifest, focus code, and a test.\nDoes this prove that the port is correct?\nName one claim that you cannot make.</p></aside><div class=\"command\"><header><span>Save one live proposal</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts naive ../../apps/pocket-cinema \\\n  --max-cost 1 --run-id naive-demo --yes</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.\nDo not add skills or MCP to this command.</aside><ol class=\"tasks\"><li>Open <code>out/naive-demo/naive-proposal.json</code>.</li><li>Read the proposed files.</li><li>Open <code>out/naive-demo/naive-result.json</code>.</li><li>Find each <code>proven: false</code> value.</li><li>Read the <code>missingProof</code> list.</li><li>Run <code>git status</code> on <code>apps/pocket-cinema</code>.</li><li>Make sure that the source app did not change.</li></ol><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>A good proposal is not evidence</h2><p>The model can propose good code.\nThe proposal is still unverified.\n\nWithout an independent check, a correct result and a convincing result look the same.\nThe harness makes these results different.</p></section><h2>Know the starting app</h2><p><code>apps/pocket-cinema</code> is a small React Native app. It has a featured title, two content rails, and a details screen.</p><p>The app has no explicit TV focus behavior. It has no Vega package. Lesson 00 verified these facts with <code>tv-check</code>.</p><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual Android TV capture</div><img src=\"assets/pocket-cinema-android-tv.png\" alt=\"Pocket Cinema home screen on an Android TV emulator\" loading=\"lazy\"><figcaption>The app can render on a TV screen. It does not have remote-control focus or a Vega package.</figcaption></figure><h2>Know the six phases</h2><div class=\"flow\"><div><b>Analyze</b><span>Read the app</span></div><i>→</i><div><b>Plan</b><span>Define the TV port</span></div><i>→</i><div><b>Port</b><span>Write the code</span></div><i>→</i><div><b>Build</b><span>Produce the package</span></div><i>→</i><div><b>Launch</b><span>Start the app</span></div><i>→</i><div><b>Test</b><span>Verify remote behavior</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Know the harness boundary</h2><p>The model can list, read, and search the guarded copy.\nIt has no shell tool.\nIt has no write tool.\n\nThe model returns <code>{summary, files}</code>.\nThe harness validates the response.\nThe harness writes files and runs checks.\nThe harness retries with the exact failure text.\nThe harness commits only a passing result.</p></section><h2>Inspect the Strands agent</h2><p>Open <code>src/port-executor.ts</code>. One phase creates one bounded Strands agent.</p><figure class=\"snippet\"><figcaption>packages/workshop-harness/src/port-executor.ts (simplified)</figcaption><pre><code>const agent = new Agent({\n  name: `workshop-${phase}`,\n  model: createModel(config),\n  tools: createProjectReadTools(appDir),\n  plugins: [createSkillsPlugin(skills)],\n  structuredOutputSchema: PortOutputSchema,\n  systemPrompt: &quot;Inspect with read-only tools. Return a complete patch.&quot;,\n  printer: false,\n});\n\nconst result = await consumeStream(\n  agent.stream(prompt, {\n    cancelSignal: AbortSignal.timeout(10 * 60_000),\n    limits: { turns: 8, totalTokens: 40_000 },\n  }),\n  event =&gt; transcripts.append(phase, event),\n);</code></pre><p class=\"look\"><strong>Where to look:</strong> Strands supplies the agent loop, providers, tools, skills, schema, limits, cancellation, and metrics. The harness controls writes, checks, Git, and costs.</p></figure><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The analyze phase will report which parts can move to Vega.\nWhich part can the phase verify mechanically?\nWhich part remains a model claim?</p></aside><h2>Run the analyze phase</h2><p>Run one command. Use the executor that you selected in Lesson 00.</p><div class=\"command\"><header><span>Run the analyze phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --phases analyze --yes --run-id workshop</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;phasesComplete&quot;:[&quot;analyze&quot;]</code></pre></div><h2>Inspect the evidence</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/ANALYSIS.md</code>.</li><li>Find the required portability section.</li><li>Run <code>git status</code> on <code>apps/pocket-cinema</code>.</li><li>Make sure that the source app is unchanged.</li><li>Run <code>git log --oneline</code> in <code>out/workshop/app</code>.</li><li>Find the import commit.</li><li>Find the analyze-phase commit.</li><li>Open <code>out/workshop/port-result.json</code>.</li><li>Find the <code>attempts</code> value.</li><li>Record three claims in <code>ANALYSIS.md</code> that no check verified.</li></ol><aside class=\"note \"><strong>Why the harness uses Git</strong><br>The harness resets a failed attempt to the phase-start commit.\nA rejected patch leaves no file changes.\nGit supplies rollback and a record of accepted work.</aside><h2>Compare two executors</h2><p>Work with a person who used a different executor. Do not run the phase again.</p><p>Compare the first and last events in <code>model-logs/analyze.jsonl</code>.</p><table><thead><tr><th>Changes with the executor</th><th>Stays in the harness</th></tr></thead><tbody><tr><td>Provider event names</td><td>Phase name</td></tr><tr><td>Token accounting</td><td>Prompt contract</td></tr><tr><td>Claude subprocess or Strands <code>Agent</code></td><td><code>{summary, files}</code> schema</td></tr><tr><td>Skill delivery</td><td>Write, check, budget, and commit</td></tr></tbody></table><p>Changing the executor must not change the pass condition.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The model identified content that can move to Vega</strong></div><dl><dt>Independent check</dt><dd>ANALYSIS.md exists and contains the required portability section</dd><dt>Evidence</dt><dd><code>out/workshop/app/ANALYSIS.md and model-logs/analyze.jsonl</code></dd><dt>Limit</dt><dd>This check does not prove that each portability conclusion is correct</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What did this phase prove?</summary><p>The phase proved that <code>ANALYSIS.md</code> has the required structure.\nIt did not prove that the analysis is complete.\nIt did not prove that each technical conclusion is correct.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>The one-call example changed no source files.\n<code>ANALYSIS.md</code> exists in the guarded copy.\nThe harness committed the passing phase.\nYou recorded three unverified claims.</aside><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>If the live model fails, use the recorded fallback:</aside><div class=\"command\"><header><span>Recorded fallback</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --phases analyze --yes --run-id workshop</code></pre></div>"},{"id":"plan","number":"02","nav":"Plan the TV port","time":"30 minutes","title":"Define the TV behavior before you write code","lead":"Supply TV design rules and current Vega documents. Keep these knowledge sources separate from pipeline code.","objective":"Identify the knowledge that a phase needs. Identify how the executor supplies each knowledge source.","evidence":"VEGA_PORT.md contains the focus model and TV flow. NextSteps.md identifies the ADBT documents.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Supply the required knowledge</h2><p>The analyze phase read the app.\nThe plan phase defines the required TV behavior.\n\nThe phase must define initial focus, movement, selection, and Back behavior.\nThe phase must also use current Vega migration information.\nThe model does not have this knowledge by default.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Use two knowledge sources</h2><p>A skill is a Markdown file with domain instructions.\nThe executor supplies selected skills to the model.\n\nADBT is a live MCP server.\nThe model uses ADBT tools to find and read Vega documents.\nThe harness records each document read and its SHA-256 hash.</p></section><h2>Inspect the phase definition</h2><p>Open the <code>plan</code> entry in <code>phases()</code> in <code>src/port-pipeline.ts</code>.</p><table><thead><tr><th>Field</th><th>Function</th><th>Consumer</th></tr></thead><tbody><tr><td><code>goal</code></td><td>Defines the required result</td><td>Model</td></tr><tr><td><code>instruction</code></td><td>Defines a phase rule</td><td>Model</td></tr><tr><td><code>skills</code></td><td>Identifies skill files</td><td>Executor and model</td></tr><tr><td><code>mcp</code></td><td>Identifies permitted MCP servers</td><td>Executor</td></tr><tr><td><code>checks</code></td><td>Defines the pass condition</td><td>Harness</td></tr></tbody></table><p>Keep these fields separate. Do not put verification logic in the prompt.</p><aside class=\"note \"><strong>ADBT supplies the Vega skills</strong><br>ADBT supplies ten <code>amazon-devices-vega-*</code> skills.\nThe skills include focus, navigation, manifest, media, performance, and build instructions.\n\nLesson 00 installed these skills.\nThe harness selects the skills for each phase.\nThe harness does not own the skill content.</aside><h2>Skill delivery</h2>\n    <table><thead><tr><th>Executor</th><th>Method</th></tr></thead><tbody><tr><td>Claude CLI</td><td><code>injectSkillText()</code> adds the complete skill body to the process prompt.</td></tr><tr><td>Strands</td><td><code>AgentSkills</code> registers each <code>Skill</code> as a plugin. The agent activates instructions with the <code>skills</code> tool.</td></tr><tr><td>Recorded fallback</td><td>No model runs. A recorded response replaces the live call.</td></tr></tbody></table>\n    <aside class=\"note \"><strong>Missing skill</strong><br><code>loadSkills()</code> reports and skips a missing skill. The run continues without that knowledge.</aside><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>Which knowledge source defines the focus behavior?\nWhich knowledge source identifies the correct Vega API?</p></aside><h2>Run the plan phase</h2><p>Use the same run ID. The harness reuses the guarded copy and its Git history.</p><div class=\"command\"><header><span>Run the plan phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --phases plan --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.\nThe harness supplies ADBT MCP to both live executors.</aside><h2>Inspect the plan</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/VEGA_PORT.md</code>.</li><li>Find the <code>## TV Flow</code> section.</li><li>Find the <code>## Focus</code> section.</li><li>Identify the initial focus target.</li><li>Identify the list-boundary behavior.</li><li>Identify the Back behavior.</li><li>Open <code>out/workshop/app/NextSteps.md</code>.</li><li>Find the ADBT sources.</li><li>Find each unsupported mapping.</li><li>Open <code>out/workshop/adbt-port-context.json</code>.</li><li>Find the document names and hashes.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>How does the harness record model-selected documents?</summary><p>The harness reads the ADBT tool history after the phase.\nIt records each document name, excerpt, and SHA-256 hash.\n\nThe recorded fallback uses this stored context.\nIt does not start a live MCP server.</p></details></section><h2>Add a team skill</h2><p>Create one skill that contains a rule from your team. Then add an independent check for that rule.</p><ol class=\"tasks\"><li>Create <code>~/.claude/skills/team-open-questions</code>.</li><li>Create <code>SKILL.md</code> in that directory.</li><li>Add this instruction: <code>End each document with a ## Open Questions section.</code></li><li>Add <code>team-open-questions</code> to the plan phase <code>skills</code>.</li><li>Add a <code>contains</code> check for <code>## Open Questions</code>.</li><li>Run the live plan phase again.</li><li>Verify that the new section exists.</li></ol><p>The skill supplies the instruction. The check supplies the pass condition. Keep them in different files.</p><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The plan defines the TV behavior and uses current Vega information</strong></div><dl><dt>Independent check</dt><dd>The plan contains the TV flow and focus model. The live run records the ADBT documents.</dd><dt>Evidence</dt><dd><code>VEGA_PORT.md, NextSteps.md, and adbt-port-context.json</code></dd><dt>Limit</dt><dd>The plan does not prove that the proposed APIs compile</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br><code>VEGA_PORT.md</code> contains the focus model and TV flow.\n<code>adbt-port-context.json</code> contains the ADBT document records.\nYour team skill produces a section that an independent check requires.</aside><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>The recorded response cannot follow a skill that you added after the recording.\nYour new check will fail on the recorded response.\nThis failure is correct.\nChecks run during a recorded fallback, but no model runs.</aside><div class=\"command\"><header><span>Recorded fallback</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --phases plan --yes --run-id workshop</code></pre></div>"},{"id":"port","number":"03","nav":"Write the port","time":"30 minutes","title":"Write the code and verify each requirement","lead":"The port phase changes the app. Nine independent checks control the phase commit.","objective":"Inspect the checks for a code change. Observe how exact failure text causes a useful retry.","evidence":"The guarded copy contains a Vega package and focus module. Git records the passing phase.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Use checks for code changes</h2><p>The first two phases produced documents.\nThe port phase produces code.\n\nThe phase creates a Vega package.\nIt creates a shared focus-state module.\nIt also creates an executable focus test.\n\nA wrong result can now change application behavior.\nRead the checks before you accept the result.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The verification engine is small</h2><p><code>src/port-verification.ts</code> defines four check types:\n\n- <code>file_exists</code>\n- <code>contains</code>\n- <code>json_schema</code>\n- <code>command</code>\n\n<code>verifyPort()</code> runs each check and collects the failure text.\nThe port phase has nine checks.\nThe checks cover the manifest, build scripts, app configuration, focus module, wiring, and test.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The model can write a plausible Vega manifest.\nWhich static checks can accept the manifest even if Vega rejects it?</p></aside><h2>Run the port phase</h2><p>Use the same run ID and executor.</p><div class=\"command\"><header><span>Run the port phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --phases port --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.</aside><h2>Inspect the files and commit</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/apps/vega/</code>.</li><li>Find <code>manifest.toml</code>.</li><li>Find <code>package.json</code>.</li><li>Find <code>app.json</code>.</li><li>Find <code>metro.config.js</code>.</li><li>Open <code>src/tv/focus-state.ts</code>.</li><li>Find its import in <code>src/App.tsx</code>.</li><li>Find its import in <code>tests/verify-tv-focus.ts</code>.</li><li>Run <code>git log --oneline</code> in the guarded copy.</li><li>Find the port-phase commit.</li></ol><p>The app and test use the same focus module. This shared module makes the Lesson 6 test useful.</p><h2>Inspect the model transcript</h2><p>The phase writes <code>out/workshop/model-logs/port.jsonl</code>. Each line contains one complete event.</p><div class=\"command\"><header><span>Read the port transcript</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts logs workshop --phase port</code></pre></div><ol class=\"tasks\"><li>Find the <code>request</code> event.</li><li>Read the complete phase prompt.</li><li>Find the model response events.</li><li>Find <code>verification_result</code>.</li><li>Find <code>commit</code>.</li><li>Find <code>phase_complete</code>.</li><li>Verify that the <code>sequence</code> values increase.</li></ol><p>Strands and Claude Code use different native event names. The transcript keeps the native event payloads.</p><p>Use <code>--follow</code> to read a live phase in a second terminal. You can also run:</p><pre><code class=\"language-sh\">tail -f ../../out/workshop/model-logs/port.jsonl | jq .</code></pre><p>The transcript can contain prompts, source text, and tool results. Keep the transcript in the ignored <code>out/</code> directory. Review it before you share it.</p><h2>Observe a failed check and retry</h2><p>The committed recording contains one failed plan attempt. Use it to inspect exact retry context.</p><div class=\"command\"><header><span>Run the retry example</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-retry/port-recording.json \\\n  --phases analyze,plan --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>plan attempt 1 failed:\n  - TV flow documented: VEGA_PORT.md must contain &quot;## TV Flow&quot;\n  - Focus model documented: VEGA_PORT.md must contain &quot;## Focus&quot;</code></pre></div><figure class=\"lesson-visual\"><div class=\"visual-label\">Actual recorded command output</div><img src=\"assets/retry-terminal.png\" alt=\"Terminal output with a failed check and a passing retry\" loading=\"lazy\"><figcaption>The harness adds two exact failures to the second request. It does not use a general retry instruction.</figcaption></figure><ol class=\"tasks\"><li>Find the two failed checks in the terminal.</li><li>Open <code>out/&lt;runId&gt;/port-result.json</code>.</li><li>Find <code>attempts: 2</code>.</li><li>Find the rejected failures.</li><li>Open <code>out/&lt;runId&gt;/model-logs/plan.jsonl</code>.</li><li>Find the first <code>verification_result</code>.</li><li>Find the second model request.</li><li>Verify that both events contain the same failure text.</li><li>Open <code>prompt()</code> in <code>src/port-pipeline.ts</code>.</li><li>Find the code that adds the previous failures.</li><li>Open <code>writeOutput()</code>.</li><li>Find the protected-path rules.</li></ol><aside class=\"note \"><strong>The retry loop has limits</strong><br>The default permits one retry.\n<code>--max-attempts N</code> sets a different limit.\n<code>--until-done</code> removes the attempt limit.\n\nThe cost limit still applies.\nThe harness also stops after the same failure occurs twice.\nThe model cannot decide to continue the loop.</aside><h2>Add one requirement</h2><p>Add a requirement for focus restoration documentation.</p><ol class=\"tasks\"><li>Open <code>tvReadyChecks()</code> in <code>src/port-verification.ts</code>.</li><li>Add a <code>contains</code> check for <code>TV_VERIFICATION.md</code>.</li><li>Use <code>originating card</code> as the required value.</li><li>Use <code>Focus restoration documented</code> as the label.</li><li>Run <code>tv-check</code> on <code>apps/pocket-cinema</code>.</li><li>Verify that the new check fails.</li><li>Run <code>tv-check</code> on <code>workshop/checkpoints/vega-buildable/app</code>.</li><li>Verify that the new check passes.</li><li>Add one mechanical requirement from your project.</li></ol><div class=\"command\"><header><span>Run the same checks on two apps</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts tv-check ../../apps/pocket-cinema\nyarn tsx src/index.ts tv-check ../../workshop/checkpoints/vega-buildable/app</code></pre></div><p>Use the strongest applicable check type:</p><table><thead><tr><th>Check type</th><th>What it proves</th></tr></thead><tbody><tr><td><code>file_exists</code></td><td>A file exists</td></tr><tr><td><code>contains</code></td><td>A required decision is present</td></tr><tr><td><code>json_schema</code></td><td>Structured data has the required shape</td></tr><tr><td><code>command</code></td><td>A program completed successfully</td></tr></tbody></table><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does the model not verify its own work?</summary><p>A model report is another generated claim.\nAn independent check runs without model agreement.\nIt gives the same result for the same files.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The port code satisfies the declared requirements</strong></div><dl><dt>Independent check</dt><dd>Nine static, schema, and executable checks pass before the commit</dd><dt>Evidence</dt><dd><code>port-result.json, model-logs/port.jsonl, and git log</code></dd><dt>Limit</dt><dd>Static checks can accept a manifest that the Vega compiler rejects</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>The guarded copy contains the Vega package and focus module.\nNine checks pass.\nGit contains the port-phase commit.\nYour added rule fails on the starter app and passes on the ported app.</aside><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>Add this recorded model option:\n\n<code>--replay ../../workshop/fixtures/port-recording.json</code></aside>"},{"id":"build","number":"04","nav":"Build until it compiles","time":"30 minutes","title":"Use the compiler as an independent check","lead":"Run a real Vega build. Send the exact compiler diagnostics to the next model attempt.","objective":"Use a produced package as the pass condition. Trace compiler output into the repair request.","evidence":"A .vpkg file and a phase result that records the repaired compiler failure.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Use an executable check</h2><p>The earlier phases can pass when files contain the required text.\nA compiler uses stronger evidence.\n\nThe build either produces a <code>.vpkg</code> file or it does not.\nIf the build fails, the compiler identifies the cause.\nThe harness sends this exact diagnostic to the model.</p></section><aside class=\"note warning\"><strong>Vega SDK is necessary</strong><br>This lesson requires Vega SDK version <code>0.22.5875</code>.\nLessons 5 and 6 also require an attached VDA.\n\nIf the SDK is not available, use the recorded fallback.\nThe fallback verifies control flow only.\nIt does not produce a local package.</aside><h2>Know the build loop</h2><div class=\"flow\"><div><b>Verify</b><span>Run the build first</span></div><i>→</i><div><b>Fail</b><span>Keep compiler output</span></div><i>→</i><div><b>Prompt</b><span>Add exact diagnostics</span></div><i>→</i><div><b>Patch</b><span>Apply a typed repair</span></div><i>→</i><div><b>Verify</b><span>Build again</span></div></div><p>The build process has a 15-minute time limit. The harness limits the amount of build output in the prompt. It keeps the start and end of long output.</p><p>The phase runs the build before it calls the model. If the build passes, the phase does not call the model.</p><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The compiler reports <code>Type 'number' is not assignable to type 'string'</code>.\nWhat text must the model receive?\nWhy is <code>The build failed</code> insufficient?</p></aside><h2>Add the known build failure</h2><p>The injector changes only <code>out/workshop/app</code>. It does not change <code>apps/pocket-cinema</code>.</p><div class=\"command\"><header><span>Add the workshop build failure</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts inject-build-failure workshop --yes</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;expectedDiagnostic&quot;:&quot;Type 'number' is not assignable to type 'string'&quot;</code></pre></div><h2>Run the live repair</h2><p>Use the same executor that you used in the earlier lessons.</p><div class=\"command\"><header><span>Run the build phase</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --phases build --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.</aside><div class=\"expected\"><strong>Find this evidence</strong><pre><code>build needs a fix:\n  - build failed: react-native build-vega exited with code 2\nsrc/workshop-build-break.ts(2,14): error TS2322: Type 'number' is not assignable to type 'string'.</code></pre></div><h2>Inspect the repair evidence</h2><ol class=\"tasks\"><li>Find the first build failure in the terminal.</li><li>Open <code>out/workshop/model-logs/build.jsonl</code>.</li><li>Find the failed <code>verification_result</code>.</li><li>Find the next model request.</li><li>Verify that the request contains the compiler diagnostic.</li><li>Open <code>out/workshop/port-result.json</code>.</li><li>Find the rejected failure.</li><li>Find the passing attempt and its cost.</li><li>Run <code>git -C ../../out/workshop/app status --porcelain</code>.</li><li>Verify that the result is empty.</li><li>Find the <code>.vpkg</code> file in <code>out/workshop/app/apps/vega/build/</code>.</li><li>Find the build-phase commit.</li></ol><aside class=\"note \"><strong>The failure is deterministic</strong><br><code>inject-build-failure</code> adds one invalid TypeScript file.\nIt also adds one import to <code>src/App.tsx</code>.\nThe command commits these changes only in the guarded copy.\n\nThe build phase requires the file and import to be absent.\nThe normal Vega build must also pass.\nEach live provider receives the same compiler failure.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does the phase verify before the model call?</summary><p>Most passing builds do not require a repair.\nA model call before the build would spend money without a failure.\nThe compiler diagnostic gives the model a specific repair target.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The Vega app builds</strong></div><dl><dt>Independent check</dt><dd>The Vega build exits successfully and produces a .vpkg file</dd><dt>Evidence</dt><dd><code>vega-platform-result.json and apps/vega/build/*.vpkg</code></dd><dt>Limit</dt><dd>A package can compile and fail after start</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>For a live run, the build directory contains a <code>.vpkg</code> file.\n<code>vega-platform-result.json</code> contains <code>evidenceMode: live</code>.\nThe Git working tree is clean.\n\nFor a recorded fallback, do not make a local-build claim.</aside><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>If the Vega SDK is not available, run the recorded repair in a new run:</aside><div class=\"command\"><header><span>Recorded compiler repair</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/build-retry/port-recording.json \\\n  --platform-replay ../../workshop/fixtures/build-retry/vega-lifecycle.json \\\n  --run-id build-fallback --yes</code></pre></div>"},{"id":"launch","number":"05","nav":"Run it on the device","time":"30 minutes","title":"Start the app and verify that it stays active","lead":"A successful start command proves only that the device accepted the command. Collect evidence after the start.","objective":"Use the device log and two frames to distinguish an accepted command from an active app.","evidence":"The device log contains no crash signature. Two device frames contain rendered content.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Collect evidence after the start</h2><p><code>launch-app .. success</code> means that the device accepted the command.\nIt does not mean that the app stayed active.\n\nAn app can fail during its first render.\nThe first frame can also be black.\n\nThis phase waits five seconds.\nIt reads the device log.\nIt analyzes two screenshots.</p></section><aside class=\"note warning\"><strong>An attached device is necessary</strong><br>Start the VDA before this lesson.\nKeep the VDA terminal open.\n\nThe screenshot tool can fail on some VDA images.\nIf it fails, use the recorded fallback.\nState which evidence came from recorded data.</aside><div class=\"command\"><header><span>Start VDA and list devices</span><button class=\"copy\">Copy command</button></header><pre><code>vega virtual-device start --gui\nvega exec vda devices -l</code></pre></div><h2>Know the device sequence</h2><div class=\"flow\"><div><b>Install</b><span>Transfer the vpkg</span></div><i>→</i><div><b>Start</b><span>Start the app id</span></div><i>→</i><div><b>Capture</b><span>Save the first frame</span></div><i>→</i><div><b>Wait</b><span>Wait five seconds</span></div><i>→</i><div><b>Logs</b><span>Find crash signatures</span></div><i>→</i><div><b>Capture</b><span>Save the second frame</span></div></div><p>The log query reads entries for this package. It reads entries from the app start time.</p><p>The log scan rejects these signatures:</p><ul><li><code>FATAL</code></li><li><code>SIGSEGV</code></li><li><code>has died</code></li><li>ANR</li><li>Unhandled JavaScript exception</li></ul><p>The pixel check rejects these frames:</p><ul><li>Frame smaller than 640x360</li><li>One flat color</li><li>Black frame</li><li>White frame</li></ul><p>The second frame is liveness evidence. An app that stopped cannot produce a valid second frame.</p><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The app renders one frame and fails two seconds later.\nWhich evidence identifies the failure?</p></aside><h2>Run the launch phase</h2><p>Use the same run ID. The phase installs the package from Lesson 4.</p><div class=\"command\"><header><span>Install and start the app</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --phases launch --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.</aside><h2>Inspect the device evidence</h2><ol class=\"tasks\"><li>Open <code>out/workshop/01-launch.png</code>.</li><li>Verify that it shows rendered content.</li><li>Open <code>out/workshop/02-postlaunch.png</code>.</li><li>Verify that it shows rendered content.</li><li>Open <code>out/workshop/vega-device.log</code>.</li><li>Find the entries after the app start time.</li><li>Open <code>out/workshop/vega-platform-result.json</code>.</li><li>Find the frame dimensions.</li><li>Find the color count.</li><li>Find the mean luminance.</li><li>Find the log-scan result.</li></ol><aside class=\"note \"><strong>A source repair requires a new build</strong><br>A source change does not change the installed package.\nThe phase rebuilds after a source repair.\nIt then installs the new package.\n\nThe first verification attempt does not rebuild.\nNo source changed after Lesson 4.</aside><h2>Cause two controlled failures</h2><p>Use copies of the lifecycle fixture. Keep the copies in the same directory as the source fixture.</p><ol class=\"tasks\"><li>Copy <code>workshop/fixtures/vega-lifecycle.json</code>.</li><li>Add <code>FATAL EXCEPTION: main</code> to the copied <code>logs</code> output.</li><li>Run the launch phase with <code>--platform-replay</code> and the copied file.</li><li>Verify that the failure includes the exact crash line.</li><li>Make a second copy of the lifecycle fixture.</li><li>Delete the <code>screenshot</code> line from the second copy.</li><li>Run the launch phase with the second copy.</li><li>Verify that the result reports a 1x1 frame.</li><li>Verify that both commands exit with code 2.</li><li>Delete the temporary fixture copies.</li></ol><p>The recorded file has no additional repair response. A live run can call the model and try the repair.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why are two screenshots necessary?</summary><p>The first screenshot proves that content rendered at one time.\nIt does not prove that the app stayed active.\nThe device log and second frame add liveness evidence.\nThey do not prove correct focus behavior.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The app started and stayed active</strong></div><dl><dt>Independent check</dt><dd>Install and start pass. The log has no crash signature. Two frames contain rendered pixels.</dd><dt>Evidence</dt><dd><code>vega-platform-result.json, vega-device.log, 01-launch.png, and 02-postlaunch.png</code></dd><dt>Limit</dt><dd>Rendered frames do not prove correct focus movement</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>For a live run, both frames pass the pixel check.\nThe device log contains no crash signature.\n<code>vega-platform-result.json</code> contains <code>evidenceMode: live</code>.\n\nFor a recorded fallback, the result contains <code>evidenceMode: replay</code>.</aside><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>If no device is attached, run the recorded device sequence:</aside><div class=\"command\"><header><span>Recorded device sequence</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --replay ../../workshop/fixtures/port-recording.json \\\n  --platform-replay ../../workshop/fixtures/vega-lifecycle.json \\\n  --phases launch --yes --run-id workshop</code></pre></div>"},{"id":"test","number":"06","nav":"Test the remote","time":"25 minutes","title":"Verify the complete focus sequence","lead":"The app is active. Now verify movement, selection, boundaries, and Back behavior.","objective":"Express TV navigation as observable state transitions. Do not use visual impressions as the only evidence.","evidence":"tv-focus-result.json contains all focus transitions. The device frames show that the app rendered.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Test behavior over time</h2><p>A screenshot shows one moment.\nIt cannot show the complete focus sequence.\n\nFocus can return to the wrong card.\nThe screenshot can still look correct.\n\nThis phase runs an executable focus test.\nThe device frames supply separate render evidence.</p></section><h2>Know the focus contract</h2><div class=\"remote\" aria-label=\"TV remote direction pad\"><button>↑</button><button>←</button><button class=\"ok\">OK</button><button>→</button><button>↓</button></div><table><thead><tr><th>Action</th><th>Required result</th></tr></thead><tbody><tr><td>Start</td><td>The featured action has focus</td></tr><tr><td>Down</td><td>Focus moves to the first rail</td></tr><tr><td>Left or right</td><td>Focus stops at the list boundaries</td></tr><tr><td>Select</td><td>The details screen opens for the focused card</td></tr><tr><td>Back</td><td>Focus returns to the same card</td></tr></tbody></table><div class=\"flow\"><div><b>Start</b><span>Focus the featured action</span></div><i>→</i><div><b>Down</b><span>Enter the first rail</span></div><i>→</i><div><b>Select</b><span>Open the focused details</span></div><i>→</i><div><b>Back</b><span>Restore the same card</span></div></div><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>Which transition can look correct in a screenshot but fail for a remote user?</p></aside><h2>Run the test phase</h2><p>Use the same run ID. The phase runs the focus test from the port phase. It also reads the frames from the launch phase.</p><div class=\"command\"><header><span>Run the focus test</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --phases test --yes --run-id workshop</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.</aside><h2>Inspect the focus evidence</h2><ol class=\"tasks\"><li>Open <code>out/workshop/app/tv-focus-result.json</code>.</li><li>Read the transitions in sequence.</li><li>Verify that all six transitions are present.</li><li>Find the <code>back-restore</code> transition.</li><li>Open <code>workshop/fixtures/focus-failure/README.md</code>.</li><li>Read the failed Back example.</li><li>Open the focus-state module in the guarded app.</li><li>Find the focus-restoration code.</li><li>Run <code>git -C ../../out/workshop/app status --porcelain</code>.</li><li>Verify that the result is empty.</li><li>Find the test-phase commit.</li></ol><aside class=\"note \"><strong>Know the focus API</strong><br><code>hasTVPreferredFocus</code> defines the preferred initial focus.\n<code>onFocus</code> and <code>onBlur</code> report focus changes.\n\nThe workshop uses the React Native TV focus model.\nThe target platform is Vega.</aside><aside class=\"note \"><strong>Know the limit</strong><br>The test calls the shared focus module.\nIt does not send physical key events to the device.\n\nA device-input test requires an additional Vega device capability.\nThe workshop does not supply this capability.</aside><h2>Optional screenshot review</h2><p>The deterministic pixel check verifies image properties. It cannot identify every incorrect application screen.</p><p>If your configured Strands model supports image input, add one bounded model review:</p><div class=\"command\"><header><span>Add the optional screenshot review</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --evaluate-screenshot --phases test --yes --run-id workshop</code></pre></div><p>The result appears in <code>vega-platform-result.json</code>. Only a clear <code>not-app</code> result blocks the run. The deterministic pixel check always runs.</p><h2>Compare the starting app and ported app</h2><p>Lesson 00 ran <code>tv-check</code> on the starting app. The result was <code>tvReady: false</code>.</p><p>Run the same check on the ported app:</p><div class=\"command\"><header><span>Run the final TV-readiness check</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts tv-check ../../out/workshop/app</code></pre></div><div class=\"expected\"><strong>Find this evidence</strong><pre><code>&quot;tvReady&quot;: true\n&quot;failures&quot;: []</code></pre></div><p>The same check now passes. This result is the workshop before-and-after evidence.</p><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is Back behavior necessary?</summary><p>Back must restore the user's previous navigation position.\nWithout restoration, the user loses context.\nThe screen can look correct while the interaction is incorrect.</p></details></section><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>A remote user can complete the TV flow</strong></div><dl><dt>Independent check</dt><dd>The focus test observes start, movement, selection, boundaries, and Back restoration</dd><dt>Evidence</dt><dd><code>tv-focus-result.json and tv-check with tvReady:true</code></dd><dt>Limit</dt><dd>The host-side test does not prove physical key delivery</dd></dl></section><aside class=\"note success\"><strong>Completion check</strong><br>The focus test passes all transitions.\n<code>tv-focus-result.json</code> records the sequence.\n<code>tv-check</code> reports <code>tvReady: true</code>.\n\nOnly live Lesson 5 evidence proves that the screenshots came from your device.</aside><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>If no device is available, run the same focus check in:\n\n<code>workshop/checkpoints/vega-buildable/app</code></aside>"},{"id":"finish","number":"07","nav":"Build your own","time":"30 minutes","title":"Control the complete pipeline and design a new harness","lead":"Use the TUI to inspect one complete run. Then design and test a small harness for another engineering task.","objective":"Read a complete run as one system. Design a harness with phases, independent checks, limits, and evidence.","evidence":"A reviewed six-phase run and a team worksheet with an improved check.","body":"<div class=\"takeaway\"><code>plan -> context -> run -> check -> retry -> checkpoint -> report</code></div><section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Use the complete system</h2><p>The earlier lessons presented one control at a time.\nThis lesson presents the complete pipeline.\n\nFirst, use the TUI to inspect all six phases.\nThen design a harness for another engineering task.\n\nTV and Vega are the example.\nThe controlled pipeline is the reusable result.</p></section><h2>Run the complete pipeline</h2><p>The TUI displays the existing pipeline state. It does not run a second pipeline. It does not replace the JSONL transcripts.</p><div class=\"command\"><header><span>Run all phases with the TUI</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts run ../../apps/pocket-cinema \\\n  --inputs ../../workshop/fixtures/pocket-cinema-inputs \\\n  --seed workshop-v1 --max-cost 3 --yes \\\n  --run-id final-dashboard --tui</code></pre></div><aside class=\"note \"><strong>Use your workshop configuration</strong><br>The command reads the model settings from <code>../../workshop.config.json</code>.\nDo not change the run ID, seed, cost limit, or phase controls.</aside><table><thead><tr><th>Key</th><th>Function</th></tr></thead><tbody><tr><td>Up or Down</td><td>Select a phase</td></tr><tr><td><code>Tab</code></td><td>Select checks, model events, tools, or all events</td></tr><tr><td><code>f</code></td><td>Select the active phase</td></tr><tr><td><code>q</code> or <code>Enter</code></td><td>Close the completed TUI</td></tr></tbody></table><h2>Inspect the TUI</h2><ol class=\"tasks\"><li>Read the executor, provider, and model in the header.</li><li>Verify that the header reports <code>evidence live</code>.</li><li>Read the seed and cost limit.</li><li>Select the <code>checks</code> view.</li><li>Find one independent check.</li><li>Find one commit event.</li><li>Select the <code>plan</code> phase.</li><li>Select the <code>model</code> view.</li><li>Find the model request and response.</li><li>Select the <code>tools</code> view.</li><li>Find the ADBT document operations.</li><li>Select the <code>build</code> phase.</li><li>Find the compiler evidence.</li><li>Select the <code>launch</code> phase.</li><li>Find the device evidence.</li><li>Close the TUI.</li><li>Open <code>out/final-dashboard/model-logs/</code>.</li></ol><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>The TUI controls the view</h2><p><code>src/tui.ts</code> reads the existing run state.\nIt shows a short activity list.\nThe JSONL files keep the complete event data.\n\nThe operator can reduce visible detail.\nThe harness does not delete evidence.</p></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does the workshop add the TUI at the end?</summary><p>You must first know the meaning of each signal.\nThe TUI is useful after you understand checks, retries, costs, tools, and device evidence.</p></details></section><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>If a live dependency fails, run:\n\n<code>yarn tsx src/index.ts run ../../apps/pocket-cinema --inputs ../../workshop/fixtures/pocket-cinema-inputs --replay ../../workshop/fixtures/port-recording.json --platform-replay ../../workshop/fixtures/vega-lifecycle.json --seed workshop-v1 --max-cost 3 --yes --run-id final-dashboard --tui</code>\n\nThe recorded TUI has no live ADBT tool events.\nReport <code>evidence recorded</code>.</aside><h2>Complete the evidence table</h2><table><thead><tr><th>Claim</th><th>Strongest evidence</th></tr></thead><tbody><tr><td>The model inspected the app</td><td>Source inventory and explicit unknowns</td></tr><tr><td>The plan used Vega knowledge</td><td>Live ADBT operations and document hashes</td></tr><tr><td>The code met the requirements</td><td>Independent checks and a phase commit</td></tr><tr><td>The package compiled</td><td>Vega build and <code>.vpkg</code> file</td></tr><tr><td>The app stayed active</td><td>Wait period, device log, and two frames</td></tr><tr><td>The TV flow worked</td><td>Executable focus-transition result</td></tr></tbody></table><p>The evidence becomes stronger near the user behavior. The last row still has a limit. It does not prove physical key delivery.</p><h2>Complete the team exercise</h2><p>Form a team of two or three people. Select one task:</p><div class=\"grid\"><article><h3>Gradle upgrade</h3><p>Upgrade one Android module. Keep a passing debug build.</p></article><article><h3>Accessibility repair</h3><p>Repair labels and focus order on one screen.</p></article><article><h3>API migration</h3><p>Replace one deprecated client API. Do not change server behavior.</p></article><article><h3>Flaky test repair</h3><p>Repair one test. Prove that repeated runs pass.</p></article></div><ol class=\"tasks\"><li>During minutes 0 to 2, write one required result.</li><li>Write three non-goals.</li><li>During minutes 2 to 5, define no more than three phases.</li><li>Give each phase one independent check.</li><li>During minutes 5 to 7, identify the knowledge source.</li><li>Identify the human approval point.</li><li>Set the retry limit.</li><li>Set the no-progress rule.</li><li>Set the cost limit.</li><li>During minutes 7 to 9, exchange worksheets with another team.</li><li>Find one false positive in the other design.</li><li>During minutes 9 to 10, improve one check.</li><li>Prepare a 30-second report.</li><li>State the claim, evidence, and remaining limit.</li></ol><section class=\"proof\"><div class=\"proof-title\"><span>Claim → evidence</span><strong>The harness pattern applies to a task outside TV development</strong></div><dl><dt>Independent check</dt><dd>Another team can find a false positive and improve the evidence without changing the model</dd><dt>Evidence</dt><dd><code>workshop/worksheet.md</code></dd><dt>Limit</dt><dd>The worksheet defines a prototype. It does not prove production readiness.</dd></dl></section><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>What is the minimum useful harness?</summary><p>Select one repeatable task.\nUse a short phase sequence.\nSupply one necessary knowledge source.\nAdd one independent check to each phase.\nSet a retry limit and cost limit.\nWrite a report.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>You can explain the complete run from the TUI.\nAnother team can read your worksheet.\nThe team can identify the pass, retry, and stop conditions.</aside><div class=\"links\"><a href=\"worksheet.md\">Open the worksheet</a><a href=\"troubleshooting.md\">Troubleshooting</a><a href=\"instructor-guide.md\">Instructor guide</a><button data-go-module=\"bee\">Appendix: Bee pipeline</button></div>"},{"id":"bee","number":"A1","nav":"Appendix: Bee pipeline","time":"25 minutes","title":"Appendix: convert an approved conversation into code","lead":"Use a second pipeline. Convert a conversation into a reviewed specification before you write code.","objective":"Apply an approved request without committing private conversation text. Do not let the model change its acceptance criteria.","evidence":"BEE_SPEC.md contains source IDs and paraphrases. The app passes the approved checks and device phases.","body":"<section class=\"welcome\"><p class=\"eyebrow\">Lesson goal</p><h2>Use this appendix only with consent</h2><p>This appendix uses private conversation data.\nUse the live path only with explicit consent.\nUse your own Bee account.\n\nIf you do not have consent or access, do not run the live exercise.\nUse the synthetic instructor example.</p></section><aside class=\"note \"><strong>Required external setup</strong><br>Run <code>bee login</code> and <code>bee mcp serve</code> in your terminal.\nThese commands are outside the harness.\n\nThe synthetic recording does not prove a live Bee integration.\nThe device phases use the VDA from Lessons 4 and 5.</aside><aside class=\"note \"><strong>Protect private data</strong><br><code>bee-context.json</code> contains conversation IDs and hashes.\nIt does not contain conversation text.\n\n<code>out/bee/model-logs/bee_spec.jsonl</code> can contain conversation text.\nThe <code>out/</code> directory is ignored by Git.\nDelete or remove private data before you share the run directory.</aside><h2>Know the two-stage approval</h2><div class=\"flow\"><div><b>Propose</b><span>Convert Bee data to a specification</span></div><i>→</i><div><b>Approve</b><span>Review the specification</span></div><i>→</i><div><b>Apply</b><span>Convert the specification to code</span></div><i>→</i><div><b>Build</b><span>Produce the package</span></div><i>→</i><div><b>Launch</b><span>Start the app</span></div></div><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Approve the acceptance criteria first</h2><p>Each request in <code>bee-spec.json</code> contains an independent check.\nReview the request and its check together.\nDo this before the model writes code.\n\nThe apply phase uses the approved check.\nThe model cannot define success after it writes the code.</p></section><section class=\"concept\"><p class=\"eyebrow\">Concept</p><h2>Know what the specification excludes</h2><p>The specification is not a transcript.\nIt contains a paraphrase and source ID.\n\nThe specification cannot contain command checks.\nIt can contain only <code>file_exists</code> and <code>contains</code> checks.\nHuman approval does not make a model-written shell command safe.</p></section><aside class=\"predict\"><p class=\"eyebrow\">Before you run the command</p><strong>Predict the result</strong><p>The conversation contains a flight time and family information.\nIt also contains an unapproved search idea.\nWhere must this information go?</p></aside><h2>Create the proposed specification</h2><p>The live Bee path requires Strands. Set <code>&quot;executor&quot;: &quot;strands&quot;</code> and the provider settings in <code>../../workshop.config.json</code> before you continue.</p><div class=\"command\"><header><span>Create the Bee specification</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --propose</code></pre></div><aside class=\"note \"><strong>Use Strands for the live Bee path</strong><br>The harness supplies the Bee MCP client directly to a Strands agent.\nYou can use Bedrock, OpenAI, or OpenRouter.\nUse the same provider and model for propose and apply.\n\nThe main TV pipeline supports Claude Code.\nThe live Bee path does not.</aside><ol class=\"tasks\"><li>Open <code>out/bee/app/BEE_SPEC.md</code>.</li><li>Read each request.</li><li>Find the source ID.</li><li>Find the reason.</li><li>Find the independent check.</li><li>Read the <code>Deliberately excluded</code> section.</li><li>Verify that travel and family information is excluded.</li><li>Verify that the unapproved search idea is excluded.</li><li>Run <code>git -C ../../out/bee/app diff --name-only HEAD~2 HEAD</code>.</li><li>Verify that only the specification files changed.</li><li>Open <code>out/bee/bee-context.json</code>.</li><li>Verify that it contains IDs and hashes only.</li></ol><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why does the harness store a hash?</summary><p>The hash identifies the source and detects a later change.\nThe durable report does not need the private conversation text.\n\nThe local transcript still contains the complete model exchange.\nTreat the transcript as private data.</p></details></section><h2>Approve the specification</h2><p>Do not run the apply command until you approve the specification.</p><ol class=\"tasks\"><li>Compare each request with the actual decision.</li><li>Move unapproved suggestions to the excluded list.</li><li>Decide if each check proves the request.</li><li>Reject a check that can pass without the required change.</li><li>Run the apply command only after approval.</li></ol><h2>Apply the approved specification</h2><div class=\"command\"><header><span>Apply, build, and start</span><button class=\"copy\">Copy command</button></header><pre><code>yarn tsx src/index.ts bee-run ../../apps/pocket-cinema \\\n  --apply --yes</code></pre></div><ol class=\"tasks\"><li>Find the initial failed specification checks.</li><li>Open <code>out/bee/bee-result.json</code>.</li><li>Find the request ID on each check.</li><li>Find the source conversation ID.</li><li>Verify that <code>tsc --noEmit</code> passes.</li><li>Verify that the catalog tests pass.</li><li>Verify that the build phase completes.</li><li>Verify that the launch phase completes.</li></ol><aside class=\"note \"><strong>Protect the approved requirement</strong><br>The model cannot change <code>bee-spec.json</code>.\nThe model cannot change <code>BEE_SPEC.md</code>.\nThe harness rejects a patch that changes either file.</aside><section class=\"knowledge\"><p class=\"eyebrow\">Check your understanding</p><details><summary>Why is Bee a separate command?</summary><p>The TV port and Bee pipeline answer different questions.\nThe TV phases depend on each other.\nThe Bee pipeline can run after the app exists.\n\nBoth pipelines use <code>runPortPipeline</code>.\nThis shows that the engine can run different phase plans.</p></details></section><aside class=\"note success\"><strong>Completion check</strong><br>Git contains the approved <code>BEE_SPEC.md</code>.\nEach request contains a source ID.\nThe excluded list contains private and unapproved content.\n<code>bee-result.json</code> reports passing apply, build, and launch phases.</aside><aside class=\"note warning\"><strong>If the live path is blocked</strong><br>If you do not have consent or Bee access, use the synthetic recording.\nAdd this recorded model option:\n\n<code>--replay ../../workshop/fixtures/bee-run/port-recording.json</code>\n\nIf no VDA is attached, also add:\n\n<code>--platform-replay ../../workshop/fixtures/vega-lifecycle.json</code>\n\nThe result is recorded evidence only.</aside>"}];
